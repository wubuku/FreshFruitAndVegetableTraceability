aggregates:
  TelecomNumber:
    inheritsFrom: ContactMech
    discriminatorValue: "TELECOM_NUMBER"
    # ---------------------------------
    id:
      name: ContactMechId
      columnName: CONTACT_MECH_ID
      type: id
      
    properties:
      CountryCode:
        type: very-short
      AreaCode:
        type: very-short
      ContactNumber:
        type: short-varchar
      # AskForName:
      #   # 请求接听人姓名
      #   type: name
