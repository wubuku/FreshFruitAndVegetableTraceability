// <autogenerated>
//   This file was generated by dddappp code generator.
//   Any changes made to this file manually will be lost next time the file is regenerated.
// </autogenerated>

package org.dddml.ffvtraceability.restful.resource;

import java.util.*;
import java.util.stream.*;
import jakarta.servlet.http.*;
import jakarta.validation.constraints.*;
import org.springframework.http.MediaType;
import org.springframework.http.HttpStatus;
import org.springframework.web.bind.annotation.*;
import org.springframework.transaction.annotation.Transactional;
import org.dddml.support.criterion.*;
import java.time.OffsetDateTime;
import org.dddml.ffvtraceability.domain.*;
import org.dddml.ffvtraceability.specialization.*;
import org.dddml.ffvtraceability.domain.facilitytype.*;
import static org.dddml.ffvtraceability.domain.meta.M.*;

import com.fasterxml.jackson.databind.ObjectMapper;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.security.access.prepost.*;
import org.dddml.support.criterion.TypeConverter;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

@RequestMapping(path = "FacilityTypes", produces = MediaType.APPLICATION_JSON_VALUE)
@RestController
public class FacilityTypeResource {
    private Logger logger = LoggerFactory.getLogger(this.getClass());


    @Autowired
    private FacilityTypeApplicationService facilityTypeApplicationService;


    /**
     * Retrieve.
     * Retrieve FacilityTypes
     */
    @GetMapping
    @Transactional(readOnly = true)
    public FacilityTypeStateDto[] getAll( HttpServletRequest request,
                    @RequestParam(value = "sort", required = false) String sort,
                    @RequestParam(value = "fields", required = false) String fields,
                    @RequestParam(value = "firstResult", defaultValue = "0") Integer firstResult,
                    @RequestParam(value = "maxResults", defaultValue = "2147483647") Integer maxResults,
                    @RequestParam(value = "filter", required = false) String filter) {
        try {
        if (firstResult < 0) { firstResult = 0; }
        if (maxResults == null || maxResults < 1) { maxResults = Integer.MAX_VALUE; }

            Iterable<FacilityTypeState> states = null; 
            CriterionDto criterion = null;
            if (!StringHelper.isNullOrEmpty(filter)) {
                criterion = new ObjectMapper().readValue(filter, CriterionDto.class);
            } else {
                criterion = QueryParamUtils.getQueryCriterionDto(request.getParameterMap().entrySet().stream()
                    .filter(kv -> FacilityTypeResourceUtils.getFilterPropertyName(kv.getKey()) != null)
                    .collect(Collectors.toMap(kv -> kv.getKey(), kv -> kv.getValue())));
            }
            Criterion c = CriterionDto.toSubclass(criterion, getCriterionTypeConverter(), getPropertyTypeResolver(), 
                n -> (FacilityTypeMetadata.aliasMap.containsKey(n) ? FacilityTypeMetadata.aliasMap.get(n) : n));
            states = facilityTypeApplicationService.get(
                c,
                FacilityTypeResourceUtils.getQuerySorts(request.getParameterMap()),
                firstResult, maxResults);

            FacilityTypeStateDto.DtoConverter dtoConverter = new FacilityTypeStateDto.DtoConverter();
            if (StringHelper.isNullOrEmpty(fields)) {
                dtoConverter.setAllFieldsReturned(true);
            } else {
                dtoConverter.setReturnedFieldsString(fields);
            }
            return dtoConverter.toFacilityTypeStateDtoArray(states);

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }

    /**
     * Retrieve in pages.
     * Retrieve FacilityTypes in pages.
     */
    @GetMapping("_page")
    @Transactional(readOnly = true)
    public Page<FacilityTypeStateDto> getPage( HttpServletRequest request,
                    @RequestParam(value = "fields", required = false) String fields,
                    @RequestParam(value = "page", defaultValue = "0") Integer page,
                    @RequestParam(value = "size", defaultValue = "20") Integer size,
                    @RequestParam(value = "filter", required = false) String filter) {
        try {
            Integer firstResult = (page == null ? 0 : page) * (size == null ? 20 : size);
            Integer maxResults = (size == null ? 20 : size);
            Iterable<FacilityTypeState> states = null; 
            CriterionDto criterion = null;
            if (!StringHelper.isNullOrEmpty(filter)) {
                criterion = new ObjectMapper().readValue(filter, CriterionDto.class);
            } else {
                criterion = QueryParamUtils.getQueryCriterionDto(request.getParameterMap().entrySet().stream()
                    .filter(kv -> FacilityTypeResourceUtils.getFilterPropertyName(kv.getKey()) != null)
                    .collect(Collectors.toMap(kv -> kv.getKey(), kv -> kv.getValue())));
            }
            Criterion c = CriterionDto.toSubclass(criterion, getCriterionTypeConverter(), getPropertyTypeResolver(), 
                n -> (FacilityTypeMetadata.aliasMap.containsKey(n) ? FacilityTypeMetadata.aliasMap.get(n) : n));
            states = facilityTypeApplicationService.get(
                c,
                FacilityTypeResourceUtils.getQuerySorts(request.getParameterMap()),
                firstResult, maxResults);
            long count = facilityTypeApplicationService.getCount(c);

            FacilityTypeStateDto.DtoConverter dtoConverter = new FacilityTypeStateDto.DtoConverter();
            if (StringHelper.isNullOrEmpty(fields)) {
                dtoConverter.setAllFieldsReturned(true);
            } else {
                dtoConverter.setReturnedFieldsString(fields);
            }
            Page.PageImpl<FacilityTypeStateDto> statePage =  new Page.PageImpl<>(dtoConverter.toFacilityTypeStateDtoList(states), count);
            statePage.setSize(size);
            statePage.setNumber(page);
            return statePage;

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }

    /**
     * Retrieve.
     * Retrieves FacilityType with the specified ID.
     */
    @GetMapping("{facilityTypeId}")
    @Transactional(readOnly = true)
    public FacilityTypeStateDto get(@PathVariable("facilityTypeId") String facilityTypeId, @RequestParam(value = "fields", required = false) String fields) {
        try {
            String idObj = facilityTypeId;
            FacilityTypeState state = facilityTypeApplicationService.get(idObj);
            if (state == null) { return null; }

            FacilityTypeStateDto.DtoConverter dtoConverter = new FacilityTypeStateDto.DtoConverter();
            if (StringHelper.isNullOrEmpty(fields)) {
                dtoConverter.setAllFieldsReturned(true);
            } else {
                dtoConverter.setReturnedFieldsString(fields);
            }
            return dtoConverter.toFacilityTypeStateDto(state);

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }

    @GetMapping("_count")
    @Transactional(readOnly = true)
    public long getCount( HttpServletRequest request,
                         @RequestParam(value = "filter", required = false) String filter) {
        try {
            long count = 0;
            CriterionDto criterion = null;
            if (!StringHelper.isNullOrEmpty(filter)) {
                criterion = new ObjectMapper().readValue(filter, CriterionDto.class);
            } else {
                criterion = QueryParamUtils.getQueryCriterionDto(request.getParameterMap());
            }
            Criterion c = CriterionDto.toSubclass(criterion,
                getCriterionTypeConverter(), 
                getPropertyTypeResolver(), 
                n -> (FacilityTypeMetadata.aliasMap.containsKey(n) ? FacilityTypeMetadata.aliasMap.get(n) : n));
            count = facilityTypeApplicationService.getCount(c);
            return count;

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }


    /**
     * Create.
     * Create FacilityType
     */
    @PostMapping @ResponseBody @ResponseStatus(HttpStatus.CREATED)
    public String post(@RequestBody CreateOrMergePatchFacilityTypeDto.CreateFacilityTypeDto value,  HttpServletResponse response) {
        try {
            FacilityTypeCommand.CreateFacilityType cmd = value;//.toCreateFacilityType();
            if (cmd.getFacilityTypeId() == null) {
                throw DomainError.named("nullId", "Aggregate Id in cmd is null, aggregate name: %1$s.", "FacilityType");
            }
            String idObj = cmd.getFacilityTypeId();
            cmd.setRequesterId(SecurityContextUtil.getRequesterId());
            facilityTypeApplicationService.when(cmd);

            return idObj;
        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }


    /**
     * Create or update.
     * Create or update FacilityType
     */
    @PutMapping("{facilityTypeId}")
    public void put(@PathVariable("facilityTypeId") String facilityTypeId, @RequestBody CreateOrMergePatchFacilityTypeDto value) {
        try {
            if (value.getVersion() != null) {
                value.setCommandType(Command.COMMAND_TYPE_MERGE_PATCH);
                FacilityTypeCommand.MergePatchFacilityType cmd = (FacilityTypeCommand.MergePatchFacilityType) value.toSubclass();
                FacilityTypeResourceUtils.setNullIdOrThrowOnInconsistentIds(facilityTypeId, cmd);
                cmd.setRequesterId(SecurityContextUtil.getRequesterId());
                facilityTypeApplicationService.when(cmd);
                return;
            }

            value.setCommandType(Command.COMMAND_TYPE_CREATE);
            FacilityTypeCommand.CreateFacilityType cmd = (FacilityTypeCommand.CreateFacilityType) value.toSubclass();
            FacilityTypeResourceUtils.setNullIdOrThrowOnInconsistentIds(facilityTypeId, cmd);
            cmd.setRequesterId(SecurityContextUtil.getRequesterId());
            facilityTypeApplicationService.when(cmd);

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }


    /**
     * Patch.
     * Patch FacilityType
     */
    @PatchMapping("{facilityTypeId}")
    public void patch(@PathVariable("facilityTypeId") String facilityTypeId, @RequestBody CreateOrMergePatchFacilityTypeDto.MergePatchFacilityTypeDto value) {
        try {

            FacilityTypeCommand.MergePatchFacilityType cmd = value;//.toMergePatchFacilityType();
            FacilityTypeResourceUtils.setNullIdOrThrowOnInconsistentIds(facilityTypeId, cmd);
            cmd.setRequesterId(SecurityContextUtil.getRequesterId());
            facilityTypeApplicationService.when(cmd);

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }

    /**
     * Delete.
     * Delete FacilityType
     */
    @DeleteMapping("{facilityTypeId}")
    public void delete(@PathVariable("facilityTypeId") String facilityTypeId,
                       @NotNull @RequestParam(value = "commandId", required = false) String commandId,
                       @NotNull @RequestParam(value = "version", required = false) @Min(value = -1) Long version,
                       @RequestParam(value = "requesterId", required = false) String requesterId) {
        try {

            FacilityTypeCommand.DeleteFacilityType deleteCmd = new DeleteFacilityTypeDto();//.toDeleteFacilityType();;

            deleteCmd.setCommandId(commandId);
            deleteCmd.setRequesterId(requesterId);
            deleteCmd.setVersion(version);
            FacilityTypeResourceUtils.setNullIdOrThrowOnInconsistentIds(facilityTypeId, deleteCmd);
            deleteCmd.setRequesterId(SecurityContextUtil.getRequesterId());
            facilityTypeApplicationService.when(deleteCmd);

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }

    @GetMapping("_metadata/filteringFields")
    public List<PropertyMetadataDto> getMetadataFilteringFields() {
        try {

            List<PropertyMetadataDto> filtering = new ArrayList<>();
            FacilityTypeMetadata.propertyTypeMap.forEach((key, value) -> {
                filtering.add(new PropertyMetadataDto(key, value, true));
            });
            return filtering;

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }


    protected TypeConverter getCriterionTypeConverter() {
        return new DefaultTypeConverter();
    }

    protected PropertyTypeResolver getPropertyTypeResolver() {
        return new PropertyTypeResolver() {
            @Override
            public Class resolveTypeByPropertyName(String propertyName) {
                return FacilityTypeResourceUtils.getFilterPropertyType(propertyName);
            }
        };
    }

 
    public static class FacilityTypeResourceUtils {

        public static void setNullIdOrThrowOnInconsistentIds(String facilityTypeId, org.dddml.ffvtraceability.domain.facilitytype.FacilityTypeCommand value) {
            String idObj = facilityTypeId;
            if (value.getFacilityTypeId() == null) {
                value.setFacilityTypeId(idObj);
            } else if (!value.getFacilityTypeId().equals(idObj)) {
                throw DomainError.named("inconsistentId", "Argument Id %1$s NOT equals body Id %2$s", facilityTypeId, value.getFacilityTypeId());
            }
        }
    
        public static List<String> getQueryOrders(String str, String separator) {
            return QueryParamUtils.getQueryOrders(str, separator, FacilityTypeMetadata.aliasMap);
        }

        public static List<String> getQuerySorts(Map<String, String[]> queryNameValuePairs) {
            String[] values = queryNameValuePairs.get("sort");
            return QueryParamUtils.getQuerySorts(values, FacilityTypeMetadata.aliasMap);
        }

        public static String getFilterPropertyName(String fieldName) {
            if ("sort".equalsIgnoreCase(fieldName)
                    || "firstResult".equalsIgnoreCase(fieldName)
                    || "maxResults".equalsIgnoreCase(fieldName)
                    || "fields".equalsIgnoreCase(fieldName)) {
                return null;
            }
            if (FacilityTypeMetadata.aliasMap.containsKey(fieldName)) {
                return FacilityTypeMetadata.aliasMap.get(fieldName);
            }
            return null;
        }

        public static Class getFilterPropertyType(String propertyName) {
            if (FacilityTypeMetadata.propertyTypeMap.containsKey(propertyName)) {
                String propertyType = FacilityTypeMetadata.propertyTypeMap.get(propertyName);
                if (!StringHelper.isNullOrEmpty(propertyType)) {
                    if (BoundedContextMetadata.CLASS_MAP.containsKey(propertyType)) {
                        return BoundedContextMetadata.CLASS_MAP.get(propertyType);
                    }
                }
            }
            return String.class;
        }

        public static Iterable<Map.Entry<String, Object>> getQueryFilterMap(Map<String, String[]> queryNameValuePairs) {
            Map<String, Object> filter = new HashMap<>();
            queryNameValuePairs.forEach((key, values) -> {
                if (values.length > 0) {
                    String pName = getFilterPropertyName(key);
                    if (!StringHelper.isNullOrEmpty(pName)) {
                        Class pClass = getFilterPropertyType(pName);
                        filter.put(pName, ApplicationContext.current.getTypeConverter().convertFromString(pClass, values[0]));
                    }
                }
            });
            return filter.entrySet();
        }

        public static FacilityTypeStateDto[] toFacilityTypeStateDtoArray(Iterable<String> ids) {
            List<FacilityTypeStateDto> states = new ArrayList<>();
            ids.forEach(i -> {
                FacilityTypeStateDto dto = new FacilityTypeStateDto();
                dto.setFacilityTypeId(i);
                states.add(dto);
            });
            return states.toArray(new FacilityTypeStateDto[0]);
        }

    }

}

