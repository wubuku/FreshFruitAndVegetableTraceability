// <autogenerated>
//   This file was generated by dddappp code generator.
//   Any changes made to this file manually will be lost next time the file is regenerated.
// </autogenerated>

package org.dddml.ffvtraceability.restful.resource;

import java.util.*;
import java.util.stream.*;
import jakarta.servlet.http.*;
import jakarta.validation.constraints.*;
import org.springframework.http.MediaType;
import org.springframework.http.HttpStatus;
import org.springframework.web.bind.annotation.*;
import org.springframework.transaction.annotation.Transactional;
import org.dddml.support.criterion.*;
import org.dddml.ffvtraceability.domain.*;
import java.time.OffsetDateTime;
import org.dddml.ffvtraceability.specialization.*;
import org.dddml.ffvtraceability.domain.gs1applicationidentifier.*;
import static org.dddml.ffvtraceability.domain.meta.M.*;

import com.fasterxml.jackson.databind.ObjectMapper;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.security.access.prepost.*;
import org.dddml.support.criterion.TypeConverter;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

@RequestMapping(path = "Gs1ApplicationIdentifiers", produces = MediaType.APPLICATION_JSON_VALUE)
@RestController
public class Gs1ApplicationIdentifierResource {
    private Logger logger = LoggerFactory.getLogger(this.getClass());


    @Autowired
    private Gs1ApplicationIdentifierApplicationService gs1ApplicationIdentifierApplicationService;


    /**
     * Retrieve.
     * Retrieve Gs1ApplicationIdentifiers
     */
    @GetMapping
    @Transactional(readOnly = true)
    public Gs1ApplicationIdentifierStateDto[] getAll( HttpServletRequest request,
                    @RequestParam(value = "sort", required = false) String sort,
                    @RequestParam(value = "fields", required = false) String fields,
                    @RequestParam(value = "firstResult", defaultValue = "0") Integer firstResult,
                    @RequestParam(value = "maxResults", defaultValue = "2147483647") Integer maxResults,
                    @RequestParam(value = "filter", required = false) String filter) {
        try {
        if (firstResult < 0) { firstResult = 0; }
        if (maxResults == null || maxResults < 1) { maxResults = Integer.MAX_VALUE; }

            Iterable<Gs1ApplicationIdentifierState> states = null; 
            CriterionDto criterion = null;
            if (!StringHelper.isNullOrEmpty(filter)) {
                criterion = new ObjectMapper().readValue(filter, CriterionDto.class);
            } else {
                criterion = QueryParamUtils.getQueryCriterionDto(request.getParameterMap().entrySet().stream()
                    .filter(kv -> Gs1ApplicationIdentifierResourceUtils.getFilterPropertyName(kv.getKey()) != null)
                    .collect(Collectors.toMap(kv -> kv.getKey(), kv -> kv.getValue())));
            }
            Criterion c = CriterionDto.toSubclass(criterion, getCriterionTypeConverter(), getPropertyTypeResolver(), 
                n -> (Gs1ApplicationIdentifierMetadata.aliasMap.containsKey(n) ? Gs1ApplicationIdentifierMetadata.aliasMap.get(n) : n));
            states = gs1ApplicationIdentifierApplicationService.get(
                c,
                Gs1ApplicationIdentifierResourceUtils.getQuerySorts(request.getParameterMap()),
                firstResult, maxResults);

            Gs1ApplicationIdentifierStateDto.DtoConverter dtoConverter = new Gs1ApplicationIdentifierStateDto.DtoConverter();
            if (StringHelper.isNullOrEmpty(fields)) {
                dtoConverter.setAllFieldsReturned(true);
            } else {
                dtoConverter.setReturnedFieldsString(fields);
            }
            return dtoConverter.toGs1ApplicationIdentifierStateDtoArray(states);

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }

    /**
     * Retrieve in pages.
     * Retrieve Gs1ApplicationIdentifiers in pages.
     */
    @GetMapping("_page")
    @Transactional(readOnly = true)
    public Page<Gs1ApplicationIdentifierStateDto> getPage( HttpServletRequest request,
                    @RequestParam(value = "fields", required = false) String fields,
                    @RequestParam(value = "page", defaultValue = "0") Integer page,
                    @RequestParam(value = "size", defaultValue = "20") Integer size,
                    @RequestParam(value = "filter", required = false) String filter) {
        try {
            Integer firstResult = (page == null ? 0 : page) * (size == null ? 20 : size);
            Integer maxResults = (size == null ? 20 : size);
            Iterable<Gs1ApplicationIdentifierState> states = null; 
            CriterionDto criterion = null;
            if (!StringHelper.isNullOrEmpty(filter)) {
                criterion = new ObjectMapper().readValue(filter, CriterionDto.class);
            } else {
                criterion = QueryParamUtils.getQueryCriterionDto(request.getParameterMap().entrySet().stream()
                    .filter(kv -> Gs1ApplicationIdentifierResourceUtils.getFilterPropertyName(kv.getKey()) != null)
                    .collect(Collectors.toMap(kv -> kv.getKey(), kv -> kv.getValue())));
            }
            Criterion c = CriterionDto.toSubclass(criterion, getCriterionTypeConverter(), getPropertyTypeResolver(), 
                n -> (Gs1ApplicationIdentifierMetadata.aliasMap.containsKey(n) ? Gs1ApplicationIdentifierMetadata.aliasMap.get(n) : n));
            states = gs1ApplicationIdentifierApplicationService.get(
                c,
                Gs1ApplicationIdentifierResourceUtils.getQuerySorts(request.getParameterMap()),
                firstResult, maxResults);
            long count = gs1ApplicationIdentifierApplicationService.getCount(c);

            Gs1ApplicationIdentifierStateDto.DtoConverter dtoConverter = new Gs1ApplicationIdentifierStateDto.DtoConverter();
            if (StringHelper.isNullOrEmpty(fields)) {
                dtoConverter.setAllFieldsReturned(true);
            } else {
                dtoConverter.setReturnedFieldsString(fields);
            }
            Page.PageImpl<Gs1ApplicationIdentifierStateDto> statePage =  new Page.PageImpl<>(dtoConverter.toGs1ApplicationIdentifierStateDtoList(states), count);
            statePage.setSize(size);
            statePage.setNumber(page);
            return statePage;

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }

    /**
     * Retrieve.
     * Retrieves Gs1ApplicationIdentifier with the specified ID.
     */
    @GetMapping("{applicationIdentifier}")
    @Transactional(readOnly = true)
    public Gs1ApplicationIdentifierStateDto get(@PathVariable("applicationIdentifier") String applicationIdentifier, @RequestParam(value = "fields", required = false) String fields) {
        try {
            String idObj = applicationIdentifier;
            Gs1ApplicationIdentifierState state = gs1ApplicationIdentifierApplicationService.get(idObj);
            if (state == null) { return null; }

            Gs1ApplicationIdentifierStateDto.DtoConverter dtoConverter = new Gs1ApplicationIdentifierStateDto.DtoConverter();
            if (StringHelper.isNullOrEmpty(fields)) {
                dtoConverter.setAllFieldsReturned(true);
            } else {
                dtoConverter.setReturnedFieldsString(fields);
            }
            return dtoConverter.toGs1ApplicationIdentifierStateDto(state);

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }

    @GetMapping("_count")
    @Transactional(readOnly = true)
    public long getCount( HttpServletRequest request,
                         @RequestParam(value = "filter", required = false) String filter) {
        try {
            long count = 0;
            CriterionDto criterion = null;
            if (!StringHelper.isNullOrEmpty(filter)) {
                criterion = new ObjectMapper().readValue(filter, CriterionDto.class);
            } else {
                criterion = QueryParamUtils.getQueryCriterionDto(request.getParameterMap());
            }
            Criterion c = CriterionDto.toSubclass(criterion,
                getCriterionTypeConverter(), 
                getPropertyTypeResolver(), 
                n -> (Gs1ApplicationIdentifierMetadata.aliasMap.containsKey(n) ? Gs1ApplicationIdentifierMetadata.aliasMap.get(n) : n));
            count = gs1ApplicationIdentifierApplicationService.getCount(c);
            return count;

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }


    /**
     * Create.
     * Create Gs1ApplicationIdentifier
     */
    @PostMapping @ResponseBody @ResponseStatus(HttpStatus.CREATED)
    public String post(@RequestBody CreateOrMergePatchGs1ApplicationIdentifierDto.CreateGs1ApplicationIdentifierDto value,  HttpServletResponse response) {
        try {
            Gs1ApplicationIdentifierCommand.CreateGs1ApplicationIdentifier cmd = value;//.toCreateGs1ApplicationIdentifier();
            if (cmd.getApplicationIdentifier() == null) {
                throw DomainError.named("nullId", "Aggregate Id in cmd is null, aggregate name: %1$s.", "Gs1ApplicationIdentifier");
            }
            String idObj = cmd.getApplicationIdentifier();
            cmd.setRequesterId(SecurityContextUtil.getRequesterId());
            gs1ApplicationIdentifierApplicationService.when(cmd);

            return idObj;
        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }


    /**
     * Create or update.
     * Create or update Gs1ApplicationIdentifier
     */
    @PutMapping("{applicationIdentifier}")
    public void put(@PathVariable("applicationIdentifier") String applicationIdentifier, @RequestBody CreateOrMergePatchGs1ApplicationIdentifierDto value) {
        try {
            if (value.getVersion() != null) {
                value.setCommandType(Command.COMMAND_TYPE_MERGE_PATCH);
                Gs1ApplicationIdentifierCommand.MergePatchGs1ApplicationIdentifier cmd = (Gs1ApplicationIdentifierCommand.MergePatchGs1ApplicationIdentifier) value.toSubclass();
                Gs1ApplicationIdentifierResourceUtils.setNullIdOrThrowOnInconsistentIds(applicationIdentifier, cmd);
                cmd.setRequesterId(SecurityContextUtil.getRequesterId());
                gs1ApplicationIdentifierApplicationService.when(cmd);
                return;
            }

            value.setCommandType(Command.COMMAND_TYPE_CREATE);
            Gs1ApplicationIdentifierCommand.CreateGs1ApplicationIdentifier cmd = (Gs1ApplicationIdentifierCommand.CreateGs1ApplicationIdentifier) value.toSubclass();
            Gs1ApplicationIdentifierResourceUtils.setNullIdOrThrowOnInconsistentIds(applicationIdentifier, cmd);
            cmd.setRequesterId(SecurityContextUtil.getRequesterId());
            gs1ApplicationIdentifierApplicationService.when(cmd);

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }


    /**
     * Patch.
     * Patch Gs1ApplicationIdentifier
     */
    @PatchMapping("{applicationIdentifier}")
    public void patch(@PathVariable("applicationIdentifier") String applicationIdentifier, @RequestBody CreateOrMergePatchGs1ApplicationIdentifierDto.MergePatchGs1ApplicationIdentifierDto value) {
        try {

            Gs1ApplicationIdentifierCommand.MergePatchGs1ApplicationIdentifier cmd = value;//.toMergePatchGs1ApplicationIdentifier();
            Gs1ApplicationIdentifierResourceUtils.setNullIdOrThrowOnInconsistentIds(applicationIdentifier, cmd);
            cmd.setRequesterId(SecurityContextUtil.getRequesterId());
            gs1ApplicationIdentifierApplicationService.when(cmd);

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }

    @GetMapping("_metadata/filteringFields")
    public List<PropertyMetadataDto> getMetadataFilteringFields() {
        try {

            List<PropertyMetadataDto> filtering = new ArrayList<>();
            Gs1ApplicationIdentifierMetadata.propertyTypeMap.forEach((key, value) -> {
                filtering.add(new PropertyMetadataDto(key, value, true));
            });
            return filtering;

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }


    protected TypeConverter getCriterionTypeConverter() {
        return new DefaultTypeConverter();
    }

    protected PropertyTypeResolver getPropertyTypeResolver() {
        return new PropertyTypeResolver() {
            @Override
            public Class resolveTypeByPropertyName(String propertyName) {
                return Gs1ApplicationIdentifierResourceUtils.getFilterPropertyType(propertyName);
            }
        };
    }

 
    public static class Gs1ApplicationIdentifierResourceUtils {

        public static void setNullIdOrThrowOnInconsistentIds(String applicationIdentifier, org.dddml.ffvtraceability.domain.gs1applicationidentifier.Gs1ApplicationIdentifierCommand value) {
            String idObj = applicationIdentifier;
            if (value.getApplicationIdentifier() == null) {
                value.setApplicationIdentifier(idObj);
            } else if (!value.getApplicationIdentifier().equals(idObj)) {
                throw DomainError.named("inconsistentId", "Argument Id %1$s NOT equals body Id %2$s", applicationIdentifier, value.getApplicationIdentifier());
            }
        }
    
        public static List<String> getQueryOrders(String str, String separator) {
            return QueryParamUtils.getQueryOrders(str, separator, Gs1ApplicationIdentifierMetadata.aliasMap);
        }

        public static List<String> getQuerySorts(Map<String, String[]> queryNameValuePairs) {
            String[] values = queryNameValuePairs.get("sort");
            return QueryParamUtils.getQuerySorts(values, Gs1ApplicationIdentifierMetadata.aliasMap);
        }

        public static String getFilterPropertyName(String fieldName) {
            if ("sort".equalsIgnoreCase(fieldName)
                    || "firstResult".equalsIgnoreCase(fieldName)
                    || "maxResults".equalsIgnoreCase(fieldName)
                    || "fields".equalsIgnoreCase(fieldName)) {
                return null;
            }
            if (Gs1ApplicationIdentifierMetadata.aliasMap.containsKey(fieldName)) {
                return Gs1ApplicationIdentifierMetadata.aliasMap.get(fieldName);
            }
            return null;
        }

        public static Class getFilterPropertyType(String propertyName) {
            if (Gs1ApplicationIdentifierMetadata.propertyTypeMap.containsKey(propertyName)) {
                String propertyType = Gs1ApplicationIdentifierMetadata.propertyTypeMap.get(propertyName);
                if (!StringHelper.isNullOrEmpty(propertyType)) {
                    if (BoundedContextMetadata.CLASS_MAP.containsKey(propertyType)) {
                        return BoundedContextMetadata.CLASS_MAP.get(propertyType);
                    }
                }
            }
            return String.class;
        }

        public static Iterable<Map.Entry<String, Object>> getQueryFilterMap(Map<String, String[]> queryNameValuePairs) {
            Map<String, Object> filter = new HashMap<>();
            queryNameValuePairs.forEach((key, values) -> {
                if (values.length > 0) {
                    String pName = getFilterPropertyName(key);
                    if (!StringHelper.isNullOrEmpty(pName)) {
                        Class pClass = getFilterPropertyType(pName);
                        filter.put(pName, ApplicationContext.current.getTypeConverter().convertFromString(pClass, values[0]));
                    }
                }
            });
            return filter.entrySet();
        }

        public static Gs1ApplicationIdentifierStateDto[] toGs1ApplicationIdentifierStateDtoArray(Iterable<String> ids) {
            List<Gs1ApplicationIdentifierStateDto> states = new ArrayList<>();
            ids.forEach(i -> {
                Gs1ApplicationIdentifierStateDto dto = new Gs1ApplicationIdentifierStateDto();
                dto.setApplicationIdentifier(i);
                states.add(dto);
            });
            return states.toArray(new Gs1ApplicationIdentifierStateDto[0]);
        }

    }

}

