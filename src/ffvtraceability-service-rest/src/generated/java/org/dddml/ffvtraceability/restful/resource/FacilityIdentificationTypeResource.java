// <autogenerated>
//   This file was generated by dddappp code generator.
//   Any changes made to this file manually will be lost next time the file is regenerated.
// </autogenerated>

package org.dddml.ffvtraceability.restful.resource;

import java.util.*;
import java.util.stream.*;
import jakarta.servlet.http.*;
import jakarta.validation.constraints.*;
import org.springframework.http.MediaType;
import org.springframework.http.HttpStatus;
import org.springframework.web.bind.annotation.*;
import org.springframework.transaction.annotation.Transactional;
import org.dddml.support.criterion.*;
import java.time.OffsetDateTime;
import org.dddml.ffvtraceability.domain.*;
import org.dddml.ffvtraceability.specialization.*;
import org.dddml.ffvtraceability.domain.facilityidentificationtype.*;
import static org.dddml.ffvtraceability.domain.meta.M.*;

import com.fasterxml.jackson.databind.ObjectMapper;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.security.access.prepost.*;
import org.dddml.support.criterion.TypeConverter;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

@RequestMapping(path = "FacilityIdentificationTypes", produces = MediaType.APPLICATION_JSON_VALUE)
@RestController
public class FacilityIdentificationTypeResource {
    private Logger logger = LoggerFactory.getLogger(this.getClass());


    @Autowired
    private FacilityIdentificationTypeApplicationService facilityIdentificationTypeApplicationService;


    /**
     * Retrieve.
     * Retrieve FacilityIdentificationTypes
     */
    @GetMapping
    @Transactional(readOnly = true)
    public FacilityIdentificationTypeStateDto[] getAll( HttpServletRequest request,
                    @RequestParam(value = "sort", required = false) String sort,
                    @RequestParam(value = "fields", required = false) String fields,
                    @RequestParam(value = "firstResult", defaultValue = "0") Integer firstResult,
                    @RequestParam(value = "maxResults", defaultValue = "2147483647") Integer maxResults,
                    @RequestParam(value = "filter", required = false) String filter) {
        try {
        if (firstResult < 0) { firstResult = 0; }
        if (maxResults == null || maxResults < 1) { maxResults = Integer.MAX_VALUE; }

            Iterable<FacilityIdentificationTypeState> states = null; 
            CriterionDto criterion = null;
            if (!StringHelper.isNullOrEmpty(filter)) {
                criterion = new ObjectMapper().readValue(filter, CriterionDto.class);
            } else {
                criterion = QueryParamUtils.getQueryCriterionDto(request.getParameterMap().entrySet().stream()
                    .filter(kv -> FacilityIdentificationTypeResourceUtils.getFilterPropertyName(kv.getKey()) != null)
                    .collect(Collectors.toMap(kv -> kv.getKey(), kv -> kv.getValue())));
            }
            Criterion c = CriterionDto.toSubclass(criterion, getCriterionTypeConverter(), getPropertyTypeResolver(), 
                n -> (FacilityIdentificationTypeMetadata.aliasMap.containsKey(n) ? FacilityIdentificationTypeMetadata.aliasMap.get(n) : n));
            states = facilityIdentificationTypeApplicationService.get(
                c,
                FacilityIdentificationTypeResourceUtils.getQuerySorts(request.getParameterMap()),
                firstResult, maxResults);

            FacilityIdentificationTypeStateDto.DtoConverter dtoConverter = new FacilityIdentificationTypeStateDto.DtoConverter();
            if (StringHelper.isNullOrEmpty(fields)) {
                dtoConverter.setAllFieldsReturned(true);
            } else {
                dtoConverter.setReturnedFieldsString(fields);
            }
            return dtoConverter.toFacilityIdentificationTypeStateDtoArray(states);

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }

    /**
     * Retrieve in pages.
     * Retrieve FacilityIdentificationTypes in pages.
     */
    @GetMapping("_page")
    @Transactional(readOnly = true)
    public Page<FacilityIdentificationTypeStateDto> getPage( HttpServletRequest request,
                    @RequestParam(value = "fields", required = false) String fields,
                    @RequestParam(value = "page", defaultValue = "0") Integer page,
                    @RequestParam(value = "size", defaultValue = "20") Integer size,
                    @RequestParam(value = "filter", required = false) String filter) {
        try {
            Integer firstResult = (page == null ? 0 : page) * (size == null ? 20 : size);
            Integer maxResults = (size == null ? 20 : size);
            Iterable<FacilityIdentificationTypeState> states = null; 
            CriterionDto criterion = null;
            if (!StringHelper.isNullOrEmpty(filter)) {
                criterion = new ObjectMapper().readValue(filter, CriterionDto.class);
            } else {
                criterion = QueryParamUtils.getQueryCriterionDto(request.getParameterMap().entrySet().stream()
                    .filter(kv -> FacilityIdentificationTypeResourceUtils.getFilterPropertyName(kv.getKey()) != null)
                    .collect(Collectors.toMap(kv -> kv.getKey(), kv -> kv.getValue())));
            }
            Criterion c = CriterionDto.toSubclass(criterion, getCriterionTypeConverter(), getPropertyTypeResolver(), 
                n -> (FacilityIdentificationTypeMetadata.aliasMap.containsKey(n) ? FacilityIdentificationTypeMetadata.aliasMap.get(n) : n));
            states = facilityIdentificationTypeApplicationService.get(
                c,
                FacilityIdentificationTypeResourceUtils.getQuerySorts(request.getParameterMap()),
                firstResult, maxResults);
            long count = facilityIdentificationTypeApplicationService.getCount(c);

            FacilityIdentificationTypeStateDto.DtoConverter dtoConverter = new FacilityIdentificationTypeStateDto.DtoConverter();
            if (StringHelper.isNullOrEmpty(fields)) {
                dtoConverter.setAllFieldsReturned(true);
            } else {
                dtoConverter.setReturnedFieldsString(fields);
            }
            Page.PageImpl<FacilityIdentificationTypeStateDto> statePage =  new Page.PageImpl<>(dtoConverter.toFacilityIdentificationTypeStateDtoList(states), count);
            statePage.setSize(size);
            statePage.setNumber(page);
            return statePage;

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }

    /**
     * Retrieve.
     * Retrieves FacilityIdentificationType with the specified ID.
     */
    @GetMapping("{facilityIdentificationTypeId}")
    @Transactional(readOnly = true)
    public FacilityIdentificationTypeStateDto get(@PathVariable("facilityIdentificationTypeId") String facilityIdentificationTypeId, @RequestParam(value = "fields", required = false) String fields) {
        try {
            String idObj = facilityIdentificationTypeId;
            FacilityIdentificationTypeState state = facilityIdentificationTypeApplicationService.get(idObj);
            if (state == null) { return null; }

            FacilityIdentificationTypeStateDto.DtoConverter dtoConverter = new FacilityIdentificationTypeStateDto.DtoConverter();
            if (StringHelper.isNullOrEmpty(fields)) {
                dtoConverter.setAllFieldsReturned(true);
            } else {
                dtoConverter.setReturnedFieldsString(fields);
            }
            return dtoConverter.toFacilityIdentificationTypeStateDto(state);

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }

    @GetMapping("_count")
    @Transactional(readOnly = true)
    public long getCount( HttpServletRequest request,
                         @RequestParam(value = "filter", required = false) String filter) {
        try {
            long count = 0;
            CriterionDto criterion = null;
            if (!StringHelper.isNullOrEmpty(filter)) {
                criterion = new ObjectMapper().readValue(filter, CriterionDto.class);
            } else {
                criterion = QueryParamUtils.getQueryCriterionDto(request.getParameterMap());
            }
            Criterion c = CriterionDto.toSubclass(criterion,
                getCriterionTypeConverter(), 
                getPropertyTypeResolver(), 
                n -> (FacilityIdentificationTypeMetadata.aliasMap.containsKey(n) ? FacilityIdentificationTypeMetadata.aliasMap.get(n) : n));
            count = facilityIdentificationTypeApplicationService.getCount(c);
            return count;

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }


    /**
     * Create.
     * Create FacilityIdentificationType
     */
    @PostMapping @ResponseBody @ResponseStatus(HttpStatus.CREATED)
    public String post(@RequestBody CreateOrMergePatchFacilityIdentificationTypeDto.CreateFacilityIdentificationTypeDto value,  HttpServletResponse response) {
        try {
            FacilityIdentificationTypeCommand.CreateFacilityIdentificationType cmd = value;//.toCreateFacilityIdentificationType();
            if (cmd.getFacilityIdentificationTypeId() == null) {
                throw DomainError.named("nullId", "Aggregate Id in cmd is null, aggregate name: %1$s.", "FacilityIdentificationType");
            }
            String idObj = cmd.getFacilityIdentificationTypeId();
            cmd.setRequesterId(SecurityContextUtil.getRequesterId());
            facilityIdentificationTypeApplicationService.when(cmd);

            return idObj;
        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }


    /**
     * Create or update.
     * Create or update FacilityIdentificationType
     */
    @PutMapping("{facilityIdentificationTypeId}")
    public void put(@PathVariable("facilityIdentificationTypeId") String facilityIdentificationTypeId, @RequestBody CreateOrMergePatchFacilityIdentificationTypeDto value) {
        try {
            if (value.getVersion() != null) {
                value.setCommandType(Command.COMMAND_TYPE_MERGE_PATCH);
                FacilityIdentificationTypeCommand.MergePatchFacilityIdentificationType cmd = (FacilityIdentificationTypeCommand.MergePatchFacilityIdentificationType) value.toSubclass();
                FacilityIdentificationTypeResourceUtils.setNullIdOrThrowOnInconsistentIds(facilityIdentificationTypeId, cmd);
                cmd.setRequesterId(SecurityContextUtil.getRequesterId());
                facilityIdentificationTypeApplicationService.when(cmd);
                return;
            }

            value.setCommandType(Command.COMMAND_TYPE_CREATE);
            FacilityIdentificationTypeCommand.CreateFacilityIdentificationType cmd = (FacilityIdentificationTypeCommand.CreateFacilityIdentificationType) value.toSubclass();
            FacilityIdentificationTypeResourceUtils.setNullIdOrThrowOnInconsistentIds(facilityIdentificationTypeId, cmd);
            cmd.setRequesterId(SecurityContextUtil.getRequesterId());
            facilityIdentificationTypeApplicationService.when(cmd);

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }


    /**
     * Patch.
     * Patch FacilityIdentificationType
     */
    @PatchMapping("{facilityIdentificationTypeId}")
    public void patch(@PathVariable("facilityIdentificationTypeId") String facilityIdentificationTypeId, @RequestBody CreateOrMergePatchFacilityIdentificationTypeDto.MergePatchFacilityIdentificationTypeDto value) {
        try {

            FacilityIdentificationTypeCommand.MergePatchFacilityIdentificationType cmd = value;//.toMergePatchFacilityIdentificationType();
            FacilityIdentificationTypeResourceUtils.setNullIdOrThrowOnInconsistentIds(facilityIdentificationTypeId, cmd);
            cmd.setRequesterId(SecurityContextUtil.getRequesterId());
            facilityIdentificationTypeApplicationService.when(cmd);

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }

    /**
     * Delete.
     * Delete FacilityIdentificationType
     */
    @DeleteMapping("{facilityIdentificationTypeId}")
    public void delete(@PathVariable("facilityIdentificationTypeId") String facilityIdentificationTypeId,
                       @NotNull @RequestParam(value = "commandId", required = false) String commandId,
                       @NotNull @RequestParam(value = "version", required = false) @Min(value = -1) Long version,
                       @RequestParam(value = "requesterId", required = false) String requesterId) {
        try {

            FacilityIdentificationTypeCommand.DeleteFacilityIdentificationType deleteCmd = new DeleteFacilityIdentificationTypeDto();

            deleteCmd.setCommandId(commandId);
            deleteCmd.setRequesterId(requesterId);
            deleteCmd.setVersion(version);
            FacilityIdentificationTypeResourceUtils.setNullIdOrThrowOnInconsistentIds(facilityIdentificationTypeId, deleteCmd);
            deleteCmd.setRequesterId(SecurityContextUtil.getRequesterId());
            facilityIdentificationTypeApplicationService.when(deleteCmd);

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }

    @GetMapping("_metadata/filteringFields")
    public List<PropertyMetadataDto> getMetadataFilteringFields() {
        try {

            List<PropertyMetadataDto> filtering = new ArrayList<>();
            FacilityIdentificationTypeMetadata.propertyTypeMap.forEach((key, value) -> {
                filtering.add(new PropertyMetadataDto(key, value, true));
            });
            return filtering;

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }

    @GetMapping("{facilityIdentificationTypeId}/_events/{version}")
    @Transactional(readOnly = true)
    public FacilityIdentificationTypeEvent getEvent(@PathVariable("facilityIdentificationTypeId") String facilityIdentificationTypeId, @PathVariable("version") long version) {
        try {

            String idObj = facilityIdentificationTypeId;
            //FacilityIdentificationTypeStateEventDtoConverter dtoConverter = getFacilityIdentificationTypeStateEventDtoConverter();
            return facilityIdentificationTypeApplicationService.getEvent(idObj, version);

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }

    @GetMapping("{facilityIdentificationTypeId}/_historyStates/{version}")
    @Transactional(readOnly = true)
    public FacilityIdentificationTypeStateDto getHistoryState(@PathVariable("facilityIdentificationTypeId") String facilityIdentificationTypeId, @PathVariable("version") long version, @RequestParam(value = "fields", required = false) String fields) {
        try {

            String idObj = facilityIdentificationTypeId;
            FacilityIdentificationTypeStateDto.DtoConverter dtoConverter = new FacilityIdentificationTypeStateDto.DtoConverter();
            if (StringHelper.isNullOrEmpty(fields)) {
                dtoConverter.setAllFieldsReturned(true);
            } else {
                dtoConverter.setReturnedFieldsString(fields);
            }
            return dtoConverter.toFacilityIdentificationTypeStateDto(facilityIdentificationTypeApplicationService.getHistoryState(idObj, version));

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }



    //protected  FacilityIdentificationTypeStateEventDtoConverter getFacilityIdentificationTypeStateEventDtoConverter() {
    //    return new FacilityIdentificationTypeStateEventDtoConverter();
    //}

    protected TypeConverter getCriterionTypeConverter() {
        return new DefaultTypeConverter();
    }

    protected PropertyTypeResolver getPropertyTypeResolver() {
        return new PropertyTypeResolver() {
            @Override
            public Class resolveTypeByPropertyName(String propertyName) {
                return FacilityIdentificationTypeResourceUtils.getFilterPropertyType(propertyName);
            }
        };
    }

 
    public static class FacilityIdentificationTypeResourceUtils {

        public static void setNullIdOrThrowOnInconsistentIds(String facilityIdentificationTypeId, org.dddml.ffvtraceability.domain.facilityidentificationtype.FacilityIdentificationTypeCommand value) {
            String idObj = facilityIdentificationTypeId;
            if (value.getFacilityIdentificationTypeId() == null) {
                value.setFacilityIdentificationTypeId(idObj);
            } else if (!value.getFacilityIdentificationTypeId().equals(idObj)) {
                throw DomainError.named("inconsistentId", "Argument Id %1$s NOT equals body Id %2$s", facilityIdentificationTypeId, value.getFacilityIdentificationTypeId());
            }
        }
    
        public static List<String> getQueryOrders(String str, String separator) {
            return QueryParamUtils.getQueryOrders(str, separator, FacilityIdentificationTypeMetadata.aliasMap);
        }

        public static List<String> getQuerySorts(Map<String, String[]> queryNameValuePairs) {
            String[] values = queryNameValuePairs.get("sort");
            return QueryParamUtils.getQuerySorts(values, FacilityIdentificationTypeMetadata.aliasMap);
        }

        public static String getFilterPropertyName(String fieldName) {
            if ("sort".equalsIgnoreCase(fieldName)
                    || "firstResult".equalsIgnoreCase(fieldName)
                    || "maxResults".equalsIgnoreCase(fieldName)
                    || "fields".equalsIgnoreCase(fieldName)) {
                return null;
            }
            if (FacilityIdentificationTypeMetadata.aliasMap.containsKey(fieldName)) {
                return FacilityIdentificationTypeMetadata.aliasMap.get(fieldName);
            }
            return null;
        }

        public static Class getFilterPropertyType(String propertyName) {
            if (FacilityIdentificationTypeMetadata.propertyTypeMap.containsKey(propertyName)) {
                String propertyType = FacilityIdentificationTypeMetadata.propertyTypeMap.get(propertyName);
                if (!StringHelper.isNullOrEmpty(propertyType)) {
                    if (BoundedContextMetadata.CLASS_MAP.containsKey(propertyType)) {
                        return BoundedContextMetadata.CLASS_MAP.get(propertyType);
                    }
                }
            }
            return String.class;
        }

        public static Iterable<Map.Entry<String, Object>> getQueryFilterMap(Map<String, String[]> queryNameValuePairs) {
            Map<String, Object> filter = new HashMap<>();
            queryNameValuePairs.forEach((key, values) -> {
                if (values.length > 0) {
                    String pName = getFilterPropertyName(key);
                    if (!StringHelper.isNullOrEmpty(pName)) {
                        Class pClass = getFilterPropertyType(pName);
                        filter.put(pName, ApplicationContext.current.getTypeConverter().convertFromString(pClass, values[0]));
                    }
                }
            });
            return filter.entrySet();
        }

        public static FacilityIdentificationTypeStateDto[] toFacilityIdentificationTypeStateDtoArray(Iterable<String> ids) {
            List<FacilityIdentificationTypeStateDto> states = new ArrayList<>();
            ids.forEach(i -> {
                FacilityIdentificationTypeStateDto dto = new FacilityIdentificationTypeStateDto();
                dto.setFacilityIdentificationTypeId(i);
                states.add(dto);
            });
            return states.toArray(new FacilityIdentificationTypeStateDto[0]);
        }

    }

}

