// <autogenerated>
//   This file was generated by dddappp code generator.
//   Any changes made to this file manually will be lost next time the file is regenerated.
// </autogenerated>

package org.dddml.ffvtraceability.restful.resource;

import java.util.*;
import java.util.stream.*;
import jakarta.servlet.http.*;
import jakarta.validation.constraints.*;
import org.springframework.http.MediaType;
import org.springframework.http.HttpStatus;
import org.springframework.web.bind.annotation.*;
import org.springframework.transaction.annotation.Transactional;
import org.dddml.support.criterion.*;
import java.time.OffsetDateTime;
import org.dddml.ffvtraceability.domain.*;
import org.dddml.ffvtraceability.specialization.*;
import org.dddml.ffvtraceability.domain.statustype.*;
import static org.dddml.ffvtraceability.domain.meta.M.*;

import com.fasterxml.jackson.databind.ObjectMapper;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.security.access.prepost.*;
import org.dddml.support.criterion.TypeConverter;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

@RequestMapping(path = "StatusTypes", produces = MediaType.APPLICATION_JSON_VALUE)
@RestController
public class StatusTypeResource {
    private Logger logger = LoggerFactory.getLogger(this.getClass());


    @Autowired
    private StatusTypeApplicationService statusTypeApplicationService;


    /**
     * Retrieve.
     * Retrieve StatusTypes
     */
    @GetMapping
    @Transactional(readOnly = true)
    public StatusTypeStateDto[] getAll( HttpServletRequest request,
                    @RequestParam(value = "sort", required = false) String sort,
                    @RequestParam(value = "fields", required = false) String fields,
                    @RequestParam(value = "firstResult", defaultValue = "0") Integer firstResult,
                    @RequestParam(value = "maxResults", defaultValue = "2147483647") Integer maxResults,
                    @RequestParam(value = "filter", required = false) String filter) {
        try {
        if (firstResult < 0) { firstResult = 0; }
        if (maxResults == null || maxResults < 1) { maxResults = Integer.MAX_VALUE; }

            Iterable<StatusTypeState> states = null; 
            CriterionDto criterion = null;
            if (!StringHelper.isNullOrEmpty(filter)) {
                criterion = new ObjectMapper().readValue(filter, CriterionDto.class);
            } else {
                criterion = QueryParamUtils.getQueryCriterionDto(request.getParameterMap().entrySet().stream()
                    .filter(kv -> StatusTypeResourceUtils.getFilterPropertyName(kv.getKey()) != null)
                    .collect(Collectors.toMap(kv -> kv.getKey(), kv -> kv.getValue())));
            }
            Criterion c = CriterionDto.toSubclass(criterion, getCriterionTypeConverter(), getPropertyTypeResolver(), 
                n -> (StatusTypeMetadata.aliasMap.containsKey(n) ? StatusTypeMetadata.aliasMap.get(n) : n));
            states = statusTypeApplicationService.get(
                c,
                StatusTypeResourceUtils.getQuerySorts(request.getParameterMap()),
                firstResult, maxResults);

            StatusTypeStateDto.DtoConverter dtoConverter = new StatusTypeStateDto.DtoConverter();
            if (StringHelper.isNullOrEmpty(fields)) {
                dtoConverter.setAllFieldsReturned(true);
            } else {
                dtoConverter.setReturnedFieldsString(fields);
            }
            return dtoConverter.toStatusTypeStateDtoArray(states);

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }

    /**
     * Retrieve in pages.
     * Retrieve StatusTypes in pages.
     */
    @GetMapping("_page")
    @Transactional(readOnly = true)
    public Page<StatusTypeStateDto> getPage( HttpServletRequest request,
                    @RequestParam(value = "fields", required = false) String fields,
                    @RequestParam(value = "page", defaultValue = "0") Integer page,
                    @RequestParam(value = "size", defaultValue = "20") Integer size,
                    @RequestParam(value = "filter", required = false) String filter) {
        try {
            Integer firstResult = (page == null ? 0 : page) * (size == null ? 20 : size);
            Integer maxResults = (size == null ? 20 : size);
            Iterable<StatusTypeState> states = null; 
            CriterionDto criterion = null;
            if (!StringHelper.isNullOrEmpty(filter)) {
                criterion = new ObjectMapper().readValue(filter, CriterionDto.class);
            } else {
                criterion = QueryParamUtils.getQueryCriterionDto(request.getParameterMap().entrySet().stream()
                    .filter(kv -> StatusTypeResourceUtils.getFilterPropertyName(kv.getKey()) != null)
                    .collect(Collectors.toMap(kv -> kv.getKey(), kv -> kv.getValue())));
            }
            Criterion c = CriterionDto.toSubclass(criterion, getCriterionTypeConverter(), getPropertyTypeResolver(), 
                n -> (StatusTypeMetadata.aliasMap.containsKey(n) ? StatusTypeMetadata.aliasMap.get(n) : n));
            states = statusTypeApplicationService.get(
                c,
                StatusTypeResourceUtils.getQuerySorts(request.getParameterMap()),
                firstResult, maxResults);
            long count = statusTypeApplicationService.getCount(c);

            StatusTypeStateDto.DtoConverter dtoConverter = new StatusTypeStateDto.DtoConverter();
            if (StringHelper.isNullOrEmpty(fields)) {
                dtoConverter.setAllFieldsReturned(true);
            } else {
                dtoConverter.setReturnedFieldsString(fields);
            }
            Page.PageImpl<StatusTypeStateDto> statePage =  new Page.PageImpl<>(dtoConverter.toStatusTypeStateDtoList(states), count);
            statePage.setSize(size);
            statePage.setNumber(page);
            return statePage;

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }

    /**
     * Retrieve.
     * Retrieves StatusType with the specified ID.
     */
    @GetMapping("{statusTypeId}")
    @Transactional(readOnly = true)
    public StatusTypeStateDto get(@PathVariable("statusTypeId") String statusTypeId, @RequestParam(value = "fields", required = false) String fields) {
        try {
            String idObj = statusTypeId;
            StatusTypeState state = statusTypeApplicationService.get(idObj);
            if (state == null) { return null; }

            StatusTypeStateDto.DtoConverter dtoConverter = new StatusTypeStateDto.DtoConverter();
            if (StringHelper.isNullOrEmpty(fields)) {
                dtoConverter.setAllFieldsReturned(true);
            } else {
                dtoConverter.setReturnedFieldsString(fields);
            }
            return dtoConverter.toStatusTypeStateDto(state);

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }

    @GetMapping("_count")
    @Transactional(readOnly = true)
    public long getCount( HttpServletRequest request,
                         @RequestParam(value = "filter", required = false) String filter) {
        try {
            long count = 0;
            CriterionDto criterion = null;
            if (!StringHelper.isNullOrEmpty(filter)) {
                criterion = new ObjectMapper().readValue(filter, CriterionDto.class);
            } else {
                criterion = QueryParamUtils.getQueryCriterionDto(request.getParameterMap());
            }
            Criterion c = CriterionDto.toSubclass(criterion,
                getCriterionTypeConverter(), 
                getPropertyTypeResolver(), 
                n -> (StatusTypeMetadata.aliasMap.containsKey(n) ? StatusTypeMetadata.aliasMap.get(n) : n));
            count = statusTypeApplicationService.getCount(c);
            return count;

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }


    /**
     * Create.
     * Create StatusType
     */
    @PostMapping @ResponseBody @ResponseStatus(HttpStatus.CREATED)
    public String post(@RequestBody CreateOrMergePatchStatusTypeDto.CreateStatusTypeDto value,  HttpServletResponse response) {
        try {
            StatusTypeCommand.CreateStatusType cmd = value;//.toCreateStatusType();
            if (cmd.getStatusTypeId() == null) {
                throw DomainError.named("nullId", "Aggregate Id in cmd is null, aggregate name: %1$s.", "StatusType");
            }
            String idObj = cmd.getStatusTypeId();
            cmd.setRequesterId(SecurityContextUtil.getRequesterId());
            statusTypeApplicationService.when(cmd);

            return idObj;
        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }


    /**
     * Create or update.
     * Create or update StatusType
     */
    @PutMapping("{statusTypeId}")
    public void put(@PathVariable("statusTypeId") String statusTypeId, @RequestBody CreateOrMergePatchStatusTypeDto value) {
        try {
            if (value.getVersion() != null) {
                value.setCommandType(Command.COMMAND_TYPE_MERGE_PATCH);
                StatusTypeCommand.MergePatchStatusType cmd = (StatusTypeCommand.MergePatchStatusType) value.toSubclass();
                StatusTypeResourceUtils.setNullIdOrThrowOnInconsistentIds(statusTypeId, cmd);
                cmd.setRequesterId(SecurityContextUtil.getRequesterId());
                statusTypeApplicationService.when(cmd);
                return;
            }

            value.setCommandType(Command.COMMAND_TYPE_CREATE);
            StatusTypeCommand.CreateStatusType cmd = (StatusTypeCommand.CreateStatusType) value.toSubclass();
            StatusTypeResourceUtils.setNullIdOrThrowOnInconsistentIds(statusTypeId, cmd);
            cmd.setRequesterId(SecurityContextUtil.getRequesterId());
            statusTypeApplicationService.when(cmd);

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }


    /**
     * Patch.
     * Patch StatusType
     */
    @PatchMapping("{statusTypeId}")
    public void patch(@PathVariable("statusTypeId") String statusTypeId, @RequestBody CreateOrMergePatchStatusTypeDto.MergePatchStatusTypeDto value) {
        try {

            StatusTypeCommand.MergePatchStatusType cmd = value;//.toMergePatchStatusType();
            StatusTypeResourceUtils.setNullIdOrThrowOnInconsistentIds(statusTypeId, cmd);
            cmd.setRequesterId(SecurityContextUtil.getRequesterId());
            statusTypeApplicationService.when(cmd);

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }

    @GetMapping("_metadata/filteringFields")
    public List<PropertyMetadataDto> getMetadataFilteringFields() {
        try {

            List<PropertyMetadataDto> filtering = new ArrayList<>();
            StatusTypeMetadata.propertyTypeMap.forEach((key, value) -> {
                filtering.add(new PropertyMetadataDto(key, value, true));
            });
            return filtering;

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }


    protected TypeConverter getCriterionTypeConverter() {
        return new DefaultTypeConverter();
    }

    protected PropertyTypeResolver getPropertyTypeResolver() {
        return new PropertyTypeResolver() {
            @Override
            public Class resolveTypeByPropertyName(String propertyName) {
                return StatusTypeResourceUtils.getFilterPropertyType(propertyName);
            }
        };
    }

 
    public static class StatusTypeResourceUtils {

        public static void setNullIdOrThrowOnInconsistentIds(String statusTypeId, org.dddml.ffvtraceability.domain.statustype.StatusTypeCommand value) {
            String idObj = statusTypeId;
            if (value.getStatusTypeId() == null) {
                value.setStatusTypeId(idObj);
            } else if (!value.getStatusTypeId().equals(idObj)) {
                throw DomainError.named("inconsistentId", "Argument Id %1$s NOT equals body Id %2$s", statusTypeId, value.getStatusTypeId());
            }
        }
    
        public static List<String> getQueryOrders(String str, String separator) {
            return QueryParamUtils.getQueryOrders(str, separator, StatusTypeMetadata.aliasMap);
        }

        public static List<String> getQuerySorts(Map<String, String[]> queryNameValuePairs) {
            String[] values = queryNameValuePairs.get("sort");
            return QueryParamUtils.getQuerySorts(values, StatusTypeMetadata.aliasMap);
        }

        public static String getFilterPropertyName(String fieldName) {
            if ("sort".equalsIgnoreCase(fieldName)
                    || "firstResult".equalsIgnoreCase(fieldName)
                    || "maxResults".equalsIgnoreCase(fieldName)
                    || "fields".equalsIgnoreCase(fieldName)) {
                return null;
            }
            if (StatusTypeMetadata.aliasMap.containsKey(fieldName)) {
                return StatusTypeMetadata.aliasMap.get(fieldName);
            }
            return null;
        }

        public static Class getFilterPropertyType(String propertyName) {
            if (StatusTypeMetadata.propertyTypeMap.containsKey(propertyName)) {
                String propertyType = StatusTypeMetadata.propertyTypeMap.get(propertyName);
                if (!StringHelper.isNullOrEmpty(propertyType)) {
                    if (BoundedContextMetadata.CLASS_MAP.containsKey(propertyType)) {
                        return BoundedContextMetadata.CLASS_MAP.get(propertyType);
                    }
                }
            }
            return String.class;
        }

        public static Iterable<Map.Entry<String, Object>> getQueryFilterMap(Map<String, String[]> queryNameValuePairs) {
            Map<String, Object> filter = new HashMap<>();
            queryNameValuePairs.forEach((key, values) -> {
                if (values.length > 0) {
                    String pName = getFilterPropertyName(key);
                    if (!StringHelper.isNullOrEmpty(pName)) {
                        Class pClass = getFilterPropertyType(pName);
                        filter.put(pName, ApplicationContext.current.getTypeConverter().convertFromString(pClass, values[0]));
                    }
                }
            });
            return filter.entrySet();
        }

        public static StatusTypeStateDto[] toStatusTypeStateDtoArray(Iterable<String> ids) {
            List<StatusTypeStateDto> states = new ArrayList<>();
            ids.forEach(i -> {
                StatusTypeStateDto dto = new StatusTypeStateDto();
                dto.setStatusTypeId(i);
                states.add(dto);
            });
            return states.toArray(new StatusTypeStateDto[0]);
        }

    }

}

