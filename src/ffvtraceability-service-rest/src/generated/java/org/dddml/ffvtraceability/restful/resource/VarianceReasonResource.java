// <autogenerated>
//   This file was generated by dddappp code generator.
//   Any changes made to this file manually will be lost next time the file is regenerated.
// </autogenerated>

package org.dddml.ffvtraceability.restful.resource;

import java.util.*;
import java.util.stream.*;
import jakarta.servlet.http.*;
import jakarta.validation.constraints.*;
import org.springframework.http.MediaType;
import org.springframework.http.HttpStatus;
import org.springframework.web.bind.annotation.*;
import org.springframework.transaction.annotation.Transactional;
import org.dddml.support.criterion.*;
import java.time.OffsetDateTime;
import org.dddml.ffvtraceability.domain.*;
import org.dddml.ffvtraceability.specialization.*;
import org.dddml.ffvtraceability.domain.variancereason.*;
import static org.dddml.ffvtraceability.domain.meta.M.*;

import com.fasterxml.jackson.databind.ObjectMapper;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.security.access.prepost.*;
import org.dddml.support.criterion.TypeConverter;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

@RequestMapping(path = "VarianceReasons", produces = MediaType.APPLICATION_JSON_VALUE)
@RestController
public class VarianceReasonResource {
    private Logger logger = LoggerFactory.getLogger(this.getClass());

    private static CriterionDto deserializeCriterionDto(String filter) {
        return deserializeJsonArgument(filter, CriterionDto.class);
    }

    private static <T> T deserializeJsonArgument(String s, Class<T> aClass) {
        try {
            return new ObjectMapper().readValue(s, aClass);
        } catch (com.fasterxml.jackson.core.JsonProcessingException e) {
            throw new IllegalArgumentException(e);
        }
    }


    @Autowired
    private VarianceReasonApplicationService varianceReasonApplicationService;


    /**
     * Retrieve.
     * Retrieve VarianceReasons
     */
    @GetMapping
    @Transactional(readOnly = true)
    public VarianceReasonStateDto[] getAll( HttpServletRequest request,
                    @RequestParam(value = "sort", required = false) String sort,
                    @RequestParam(value = "fields", required = false) String fields,
                    @RequestParam(value = "firstResult", defaultValue = "0") Integer firstResult,
                    @RequestParam(value = "maxResults", defaultValue = "2147483647") Integer maxResults,
                    @RequestParam(value = "filter", required = false) String filter) {
        
        if (firstResult < 0) { firstResult = 0; }
        if (maxResults == null || maxResults < 1) { maxResults = Integer.MAX_VALUE; }

            Iterable<VarianceReasonState> states = null; 
            CriterionDto criterion = null;
            if (!StringHelper.isNullOrEmpty(filter)) {
                criterion = deserializeCriterionDto(filter);
            } else {
                criterion = QueryParamUtils.getQueryCriterionDto(request.getParameterMap().entrySet().stream()
                    .filter(kv -> VarianceReasonResourceUtils.getFilterPropertyName(kv.getKey()) != null)
                    .collect(Collectors.toMap(kv -> kv.getKey(), kv -> kv.getValue())));
            }
            Criterion c = CriterionDto.toSubclass(criterion, getCriterionTypeConverter(), getPropertyTypeResolver(), 
                n -> (VarianceReasonMetadata.aliasMap.containsKey(n) ? VarianceReasonMetadata.aliasMap.get(n) : n));
            states = varianceReasonApplicationService.get(
                c,
                VarianceReasonResourceUtils.getQuerySorts(request.getParameterMap()),
                firstResult, maxResults);

            VarianceReasonStateDto.DtoConverter dtoConverter = new VarianceReasonStateDto.DtoConverter();
            if (StringHelper.isNullOrEmpty(fields)) {
                dtoConverter.setAllFieldsReturned(true);
            } else {
                dtoConverter.setReturnedFieldsString(fields);
            }
            return dtoConverter.toVarianceReasonStateDtoArray(states);

        
    }

    /**
     * Retrieve in pages.
     * Retrieve VarianceReasons in pages.
     */
    @GetMapping("_page")
    @Transactional(readOnly = true)
    public Page<VarianceReasonStateDto> getPage( HttpServletRequest request,
                    @RequestParam(value = "fields", required = false) String fields,
                    @RequestParam(value = "page", defaultValue = "0") Integer page,
                    @RequestParam(value = "size", defaultValue = "20") Integer size,
                    @RequestParam(value = "filter", required = false) String filter) {
        
            Integer firstResult = (page == null ? 0 : page) * (size == null ? 20 : size);
            Integer maxResults = (size == null ? 20 : size);
            Iterable<VarianceReasonState> states = null; 
            CriterionDto criterion = null;
            if (!StringHelper.isNullOrEmpty(filter)) {
                criterion = deserializeCriterionDto(filter);
            } else {
                criterion = QueryParamUtils.getQueryCriterionDto(request.getParameterMap().entrySet().stream()
                    .filter(kv -> VarianceReasonResourceUtils.getFilterPropertyName(kv.getKey()) != null)
                    .collect(Collectors.toMap(kv -> kv.getKey(), kv -> kv.getValue())));
            }
            Criterion c = CriterionDto.toSubclass(criterion, getCriterionTypeConverter(), getPropertyTypeResolver(), 
                n -> (VarianceReasonMetadata.aliasMap.containsKey(n) ? VarianceReasonMetadata.aliasMap.get(n) : n));
            states = varianceReasonApplicationService.get(
                c,
                VarianceReasonResourceUtils.getQuerySorts(request.getParameterMap()),
                firstResult, maxResults);
            long count = varianceReasonApplicationService.getCount(c);

            VarianceReasonStateDto.DtoConverter dtoConverter = new VarianceReasonStateDto.DtoConverter();
            if (StringHelper.isNullOrEmpty(fields)) {
                dtoConverter.setAllFieldsReturned(true);
            } else {
                dtoConverter.setReturnedFieldsString(fields);
            }
            Page.PageImpl<VarianceReasonStateDto> statePage =  new Page.PageImpl<>(dtoConverter.toVarianceReasonStateDtoList(states), count);
            statePage.setSize(size);
            statePage.setNumber(page);
            return statePage;

        
    }

    /**
     * Retrieve.
     * Retrieves VarianceReason with the specified ID.
     */
    @GetMapping("{varianceReasonId}")
    @Transactional(readOnly = true)
    public VarianceReasonStateDto get(@PathVariable("varianceReasonId") String varianceReasonId, @RequestParam(value = "fields", required = false) String fields) {
        
            String idObj = varianceReasonId;
            VarianceReasonState state = varianceReasonApplicationService.get(idObj);
            if (state == null) { return null; }

            VarianceReasonStateDto.DtoConverter dtoConverter = new VarianceReasonStateDto.DtoConverter();
            if (StringHelper.isNullOrEmpty(fields)) {
                dtoConverter.setAllFieldsReturned(true);
            } else {
                dtoConverter.setReturnedFieldsString(fields);
            }
            return dtoConverter.toVarianceReasonStateDto(state);

        
    }

    @GetMapping("_count")
    @Transactional(readOnly = true)
    public long getCount( HttpServletRequest request,
                         @RequestParam(value = "filter", required = false) String filter) {
        
            long count = 0;
            CriterionDto criterion = null;
            if (!StringHelper.isNullOrEmpty(filter)) {
                criterion = deserializeCriterionDto(filter);
            } else {
                criterion = QueryParamUtils.getQueryCriterionDto(request.getParameterMap());
            }
            Criterion c = CriterionDto.toSubclass(criterion,
                getCriterionTypeConverter(), 
                getPropertyTypeResolver(), 
                n -> (VarianceReasonMetadata.aliasMap.containsKey(n) ? VarianceReasonMetadata.aliasMap.get(n) : n));
            count = varianceReasonApplicationService.getCount(c);
            return count;

        
    }


    /**
     * Create.
     * Create VarianceReason
     */
    @PostMapping @ResponseBody @ResponseStatus(HttpStatus.CREATED)
    public String post(@RequestBody CreateOrMergePatchVarianceReasonDto.CreateVarianceReasonDto value,  HttpServletResponse response) {
        
            VarianceReasonCommand.CreateVarianceReason cmd = value;//.toCreateVarianceReason();
            if (cmd.getVarianceReasonId() == null) {
                throw DomainError.named("nullId", "Aggregate Id in cmd is null, aggregate name: %1$s.", "VarianceReason");
            }
            String idObj = cmd.getVarianceReasonId();
            cmd.setRequesterId(SecurityContextUtil.getRequesterId());
            varianceReasonApplicationService.when(cmd);

            return idObj;
        
    }


    /**
     * Create or update.
     * Create or update VarianceReason
     */
    @PutMapping("{varianceReasonId}")
    public void put(@PathVariable("varianceReasonId") String varianceReasonId, @RequestBody CreateOrMergePatchVarianceReasonDto value) {
        
            if (value.getVersion() != null) {
                value.setCommandType(Command.COMMAND_TYPE_MERGE_PATCH);
                VarianceReasonCommand.MergePatchVarianceReason cmd = (VarianceReasonCommand.MergePatchVarianceReason) value.toSubclass();
                VarianceReasonResourceUtils.setNullIdOrThrowOnInconsistentIds(varianceReasonId, cmd);
                cmd.setRequesterId(SecurityContextUtil.getRequesterId());
                varianceReasonApplicationService.when(cmd);
                return;
            }

            value.setCommandType(Command.COMMAND_TYPE_CREATE);
            VarianceReasonCommand.CreateVarianceReason cmd = (VarianceReasonCommand.CreateVarianceReason) value.toSubclass();
            VarianceReasonResourceUtils.setNullIdOrThrowOnInconsistentIds(varianceReasonId, cmd);
            cmd.setRequesterId(SecurityContextUtil.getRequesterId());
            varianceReasonApplicationService.when(cmd);

        
    }


    /**
     * Patch.
     * Patch VarianceReason
     */
    @PatchMapping("{varianceReasonId}")
    public void patch(@PathVariable("varianceReasonId") String varianceReasonId, @RequestBody CreateOrMergePatchVarianceReasonDto.MergePatchVarianceReasonDto value) {
        

            VarianceReasonCommand.MergePatchVarianceReason cmd = value;//.toMergePatchVarianceReason();
            VarianceReasonResourceUtils.setNullIdOrThrowOnInconsistentIds(varianceReasonId, cmd);
            cmd.setRequesterId(SecurityContextUtil.getRequesterId());
            varianceReasonApplicationService.when(cmd);

        
    }

    @GetMapping("_metadata/filteringFields")
    public List<PropertyMetadataDto> getMetadataFilteringFields() {
        

            List<PropertyMetadataDto> filtering = new ArrayList<>();
            VarianceReasonMetadata.propertyTypeMap.forEach((key, value) -> {
                filtering.add(new PropertyMetadataDto(key, value, true));
            });
            return filtering;

        
    }


    protected TypeConverter getCriterionTypeConverter() {
        return new DefaultTypeConverter();
    }

    protected PropertyTypeResolver getPropertyTypeResolver() {
        return new PropertyTypeResolver() {
            @Override
            public Class resolveTypeByPropertyName(String propertyName) {
                return VarianceReasonResourceUtils.getFilterPropertyType(propertyName);
            }
        };
    }

 
    public static class VarianceReasonResourceUtils {

        public static void setNullIdOrThrowOnInconsistentIds(String varianceReasonId, org.dddml.ffvtraceability.domain.variancereason.VarianceReasonCommand value) {
            String idObj = varianceReasonId;
            if (value.getVarianceReasonId() == null) {
                value.setVarianceReasonId(idObj);
            } else if (!value.getVarianceReasonId().equals(idObj)) {
                throw DomainError.named("inconsistentId", "Argument Id %1$s NOT equals body Id %2$s", varianceReasonId, value.getVarianceReasonId());
            }
        }
    
        public static List<String> getQueryOrders(String str, String separator) {
            return QueryParamUtils.getQueryOrders(str, separator, VarianceReasonMetadata.aliasMap);
        }

        public static List<String> getQuerySorts(Map<String, String[]> queryNameValuePairs) {
            String[] values = queryNameValuePairs.get("sort");
            return QueryParamUtils.getQuerySorts(values, VarianceReasonMetadata.aliasMap);
        }

        public static String getFilterPropertyName(String fieldName) {
            if ("sort".equalsIgnoreCase(fieldName)
                    || "firstResult".equalsIgnoreCase(fieldName)
                    || "maxResults".equalsIgnoreCase(fieldName)
                    || "fields".equalsIgnoreCase(fieldName)) {
                return null;
            }
            if (VarianceReasonMetadata.aliasMap.containsKey(fieldName)) {
                return VarianceReasonMetadata.aliasMap.get(fieldName);
            }
            return null;
        }

        public static Class getFilterPropertyType(String propertyName) {
            if (VarianceReasonMetadata.propertyTypeMap.containsKey(propertyName)) {
                String propertyType = VarianceReasonMetadata.propertyTypeMap.get(propertyName);
                if (!StringHelper.isNullOrEmpty(propertyType)) {
                    if (BoundedContextMetadata.CLASS_MAP.containsKey(propertyType)) {
                        return BoundedContextMetadata.CLASS_MAP.get(propertyType);
                    }
                }
            }
            return String.class;
        }

        public static Iterable<Map.Entry<String, Object>> getQueryFilterMap(Map<String, String[]> queryNameValuePairs) {
            Map<String, Object> filter = new HashMap<>();
            queryNameValuePairs.forEach((key, values) -> {
                if (values.length > 0) {
                    String pName = getFilterPropertyName(key);
                    if (!StringHelper.isNullOrEmpty(pName)) {
                        Class pClass = getFilterPropertyType(pName);
                        filter.put(pName, ApplicationContext.current.getTypeConverter().convertFromString(pClass, values[0]));
                    }
                }
            });
            return filter.entrySet();
        }

        public static VarianceReasonStateDto[] toVarianceReasonStateDtoArray(Iterable<String> ids) {
            List<VarianceReasonStateDto> states = new ArrayList<>();
            ids.forEach(i -> {
                VarianceReasonStateDto dto = new VarianceReasonStateDto();
                dto.setVarianceReasonId(i);
                states.add(dto);
            });
            return states.toArray(new VarianceReasonStateDto[0]);
        }

    }

}

