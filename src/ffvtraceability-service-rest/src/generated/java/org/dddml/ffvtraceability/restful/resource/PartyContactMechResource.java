// <autogenerated>
//   This file was generated by dddappp code generator.
//   Any changes made to this file manually will be lost next time the file is regenerated.
// </autogenerated>

package org.dddml.ffvtraceability.restful.resource;

import java.util.*;
import java.util.stream.*;
import jakarta.servlet.http.*;
import jakarta.validation.constraints.*;
import org.springframework.http.MediaType;
import org.springframework.http.HttpStatus;
import org.springframework.web.bind.annotation.*;
import org.springframework.transaction.annotation.Transactional;
import org.dddml.support.criterion.*;
import java.time.OffsetDateTime;
import org.dddml.ffvtraceability.domain.*;
import org.dddml.ffvtraceability.specialization.*;
import org.dddml.ffvtraceability.domain.partycontactmech.*;
import static org.dddml.ffvtraceability.domain.meta.M.*;

import com.fasterxml.jackson.databind.ObjectMapper;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.security.access.prepost.*;
import org.dddml.support.criterion.TypeConverter;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

@RequestMapping(path = "PartyContactMeches", produces = MediaType.APPLICATION_JSON_VALUE)
@RestController
public class PartyContactMechResource {
    private Logger logger = LoggerFactory.getLogger(this.getClass());


    @Autowired
    private PartyContactMechApplicationService partyContactMechApplicationService;


    /**
     * Retrieve.
     * Retrieve PartyContactMeches
     */
    @GetMapping
    @Transactional(readOnly = true)
    public PartyContactMechStateDto[] getAll( HttpServletRequest request,
                    @RequestParam(value = "sort", required = false) String sort,
                    @RequestParam(value = "fields", required = false) String fields,
                    @RequestParam(value = "firstResult", defaultValue = "0") Integer firstResult,
                    @RequestParam(value = "maxResults", defaultValue = "2147483647") Integer maxResults,
                    @RequestParam(value = "filter", required = false) String filter) {
        try {
        if (firstResult < 0) { firstResult = 0; }
        if (maxResults == null || maxResults < 1) { maxResults = Integer.MAX_VALUE; }

            Iterable<PartyContactMechState> states = null; 
            CriterionDto criterion = null;
            if (!StringHelper.isNullOrEmpty(filter)) {
                criterion = new ObjectMapper().readValue(filter, CriterionDto.class);
            } else {
                criterion = QueryParamUtils.getQueryCriterionDto(request.getParameterMap().entrySet().stream()
                    .filter(kv -> PartyContactMechResourceUtils.getFilterPropertyName(kv.getKey()) != null)
                    .collect(Collectors.toMap(kv -> kv.getKey(), kv -> kv.getValue())));
            }
            Criterion c = CriterionDto.toSubclass(criterion, getCriterionTypeConverter(), getPropertyTypeResolver(), 
                n -> (PartyContactMechMetadata.aliasMap.containsKey(n) ? PartyContactMechMetadata.aliasMap.get(n) : n));
            states = partyContactMechApplicationService.get(
                c,
                PartyContactMechResourceUtils.getQuerySorts(request.getParameterMap()),
                firstResult, maxResults);

            PartyContactMechStateDto.DtoConverter dtoConverter = new PartyContactMechStateDto.DtoConverter();
            if (StringHelper.isNullOrEmpty(fields)) {
                dtoConverter.setAllFieldsReturned(true);
            } else {
                dtoConverter.setReturnedFieldsString(fields);
            }
            return dtoConverter.toPartyContactMechStateDtoArray(states);

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }

    /**
     * Retrieve in pages.
     * Retrieve PartyContactMeches in pages.
     */
    @GetMapping("_page")
    @Transactional(readOnly = true)
    public Page<PartyContactMechStateDto> getPage( HttpServletRequest request,
                    @RequestParam(value = "fields", required = false) String fields,
                    @RequestParam(value = "page", defaultValue = "0") Integer page,
                    @RequestParam(value = "size", defaultValue = "20") Integer size,
                    @RequestParam(value = "filter", required = false) String filter) {
        try {
            Integer firstResult = (page == null ? 0 : page) * (size == null ? 20 : size);
            Integer maxResults = (size == null ? 20 : size);
            Iterable<PartyContactMechState> states = null; 
            CriterionDto criterion = null;
            if (!StringHelper.isNullOrEmpty(filter)) {
                criterion = new ObjectMapper().readValue(filter, CriterionDto.class);
            } else {
                criterion = QueryParamUtils.getQueryCriterionDto(request.getParameterMap().entrySet().stream()
                    .filter(kv -> PartyContactMechResourceUtils.getFilterPropertyName(kv.getKey()) != null)
                    .collect(Collectors.toMap(kv -> kv.getKey(), kv -> kv.getValue())));
            }
            Criterion c = CriterionDto.toSubclass(criterion, getCriterionTypeConverter(), getPropertyTypeResolver(), 
                n -> (PartyContactMechMetadata.aliasMap.containsKey(n) ? PartyContactMechMetadata.aliasMap.get(n) : n));
            states = partyContactMechApplicationService.get(
                c,
                PartyContactMechResourceUtils.getQuerySorts(request.getParameterMap()),
                firstResult, maxResults);
            long count = partyContactMechApplicationService.getCount(c);

            PartyContactMechStateDto.DtoConverter dtoConverter = new PartyContactMechStateDto.DtoConverter();
            if (StringHelper.isNullOrEmpty(fields)) {
                dtoConverter.setAllFieldsReturned(true);
            } else {
                dtoConverter.setReturnedFieldsString(fields);
            }
            Page.PageImpl<PartyContactMechStateDto> statePage =  new Page.PageImpl<>(dtoConverter.toPartyContactMechStateDtoList(states), count);
            statePage.setSize(size);
            statePage.setNumber(page);
            return statePage;

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }

    /**
     * Retrieve.
     * Retrieves PartyContactMech with the specified ID.
     */
    @GetMapping("{partyContactMechId}")
    @Transactional(readOnly = true)
    public PartyContactMechStateDto get(@PathVariable("partyContactMechId") String partyContactMechId, @RequestParam(value = "fields", required = false) String fields) {
        try {
            PartyContactMechId idObj = PartyContactMechResourceUtils.parseIdString(partyContactMechId);
            PartyContactMechState state = partyContactMechApplicationService.get(idObj);
            if (state == null) { return null; }

            PartyContactMechStateDto.DtoConverter dtoConverter = new PartyContactMechStateDto.DtoConverter();
            if (StringHelper.isNullOrEmpty(fields)) {
                dtoConverter.setAllFieldsReturned(true);
            } else {
                dtoConverter.setReturnedFieldsString(fields);
            }
            return dtoConverter.toPartyContactMechStateDto(state);

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }

    @GetMapping("_count")
    @Transactional(readOnly = true)
    public long getCount( HttpServletRequest request,
                         @RequestParam(value = "filter", required = false) String filter) {
        try {
            long count = 0;
            CriterionDto criterion = null;
            if (!StringHelper.isNullOrEmpty(filter)) {
                criterion = new ObjectMapper().readValue(filter, CriterionDto.class);
            } else {
                criterion = QueryParamUtils.getQueryCriterionDto(request.getParameterMap());
            }
            Criterion c = CriterionDto.toSubclass(criterion,
                getCriterionTypeConverter(), 
                getPropertyTypeResolver(), 
                n -> (PartyContactMechMetadata.aliasMap.containsKey(n) ? PartyContactMechMetadata.aliasMap.get(n) : n));
            count = partyContactMechApplicationService.getCount(c);
            return count;

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }


    /**
     * Create.
     * Create PartyContactMech
     */
    @PostMapping @ResponseStatus(HttpStatus.CREATED)
    public PartyContactMechId post(@RequestBody CreateOrMergePatchPartyContactMechDto.CreatePartyContactMechDto value,  HttpServletResponse response) {
        try {
            PartyContactMechCommand.CreatePartyContactMech cmd = value;//.toCreatePartyContactMech();
            if (cmd.getPartyContactMechId() == null) {
                throw DomainError.named("nullId", "Aggregate Id in cmd is null, aggregate name: %1$s.", "PartyContactMech");
            }
            PartyContactMechId idObj = cmd.getPartyContactMechId();
            cmd.setRequesterId(SecurityContextUtil.getRequesterId());
            partyContactMechApplicationService.when(cmd);

            return idObj;
        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }


    /**
     * Create or update.
     * Create or update PartyContactMech
     */
    @PutMapping("{partyContactMechId}")
    public void put(@PathVariable("partyContactMechId") String partyContactMechId, @RequestBody CreateOrMergePatchPartyContactMechDto value) {
        try {
            if (value.getVersion() != null) {
                value.setCommandType(Command.COMMAND_TYPE_MERGE_PATCH);
                PartyContactMechCommand.MergePatchPartyContactMech cmd = (PartyContactMechCommand.MergePatchPartyContactMech) value.toSubclass();
                PartyContactMechResourceUtils.setNullIdOrThrowOnInconsistentIds(partyContactMechId, cmd);
                cmd.setRequesterId(SecurityContextUtil.getRequesterId());
                partyContactMechApplicationService.when(cmd);
                return;
            }

            value.setCommandType(Command.COMMAND_TYPE_CREATE);
            PartyContactMechCommand.CreatePartyContactMech cmd = (PartyContactMechCommand.CreatePartyContactMech) value.toSubclass();
            PartyContactMechResourceUtils.setNullIdOrThrowOnInconsistentIds(partyContactMechId, cmd);
            cmd.setRequesterId(SecurityContextUtil.getRequesterId());
            partyContactMechApplicationService.when(cmd);

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }


    /**
     * Patch.
     * Patch PartyContactMech
     */
    @PatchMapping("{partyContactMechId}")
    public void patch(@PathVariable("partyContactMechId") String partyContactMechId, @RequestBody CreateOrMergePatchPartyContactMechDto.MergePatchPartyContactMechDto value) {
        try {

            PartyContactMechCommand.MergePatchPartyContactMech cmd = value;//.toMergePatchPartyContactMech();
            PartyContactMechResourceUtils.setNullIdOrThrowOnInconsistentIds(partyContactMechId, cmd);
            cmd.setRequesterId(SecurityContextUtil.getRequesterId());
            partyContactMechApplicationService.when(cmd);

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }

    @GetMapping("_metadata/filteringFields")
    public List<PropertyMetadataDto> getMetadataFilteringFields() {
        try {

            List<PropertyMetadataDto> filtering = new ArrayList<>();
            PartyContactMechMetadata.propertyTypeMap.forEach((key, value) -> {
                filtering.add(new PropertyMetadataDto(key, value, true));
            });
            return filtering;

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }

    @GetMapping("{partyContactMechId}/_events/{version}")
    @Transactional(readOnly = true)
    public PartyContactMechEvent getEvent(@PathVariable("partyContactMechId") String partyContactMechId, @PathVariable("version") long version) {
        try {

            PartyContactMechId idObj = PartyContactMechResourceUtils.parseIdString(partyContactMechId);
            //PartyContactMechStateEventDtoConverter dtoConverter = getPartyContactMechStateEventDtoConverter();
            return partyContactMechApplicationService.getEvent(idObj, version);

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }

    @GetMapping("{partyContactMechId}/_historyStates/{version}")
    @Transactional(readOnly = true)
    public PartyContactMechStateDto getHistoryState(@PathVariable("partyContactMechId") String partyContactMechId, @PathVariable("version") long version, @RequestParam(value = "fields", required = false) String fields) {
        try {

            PartyContactMechId idObj = PartyContactMechResourceUtils.parseIdString(partyContactMechId);
            PartyContactMechStateDto.DtoConverter dtoConverter = new PartyContactMechStateDto.DtoConverter();
            if (StringHelper.isNullOrEmpty(fields)) {
                dtoConverter.setAllFieldsReturned(true);
            } else {
                dtoConverter.setReturnedFieldsString(fields);
            }
            return dtoConverter.toPartyContactMechStateDto(partyContactMechApplicationService.getHistoryState(idObj, version));

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }

    /**
     * Retrieve.
     * Retrieves PartyContactMechPurpose with the specified ContactMechPurposeTypeId.
     */
    @GetMapping("{partyContactMechId}/PartyContactMechPurposes/{contactMechPurposeTypeId}")
    @Transactional(readOnly = true)
    public PartyContactMechPurposeStateDto getPartyContactMechPurpose(@PathVariable("partyContactMechId") String partyContactMechId, @PathVariable("contactMechPurposeTypeId") String contactMechPurposeTypeId) {
        try {

            PartyContactMechPurposeState state = partyContactMechApplicationService.getPartyContactMechPurpose(new com.fasterxml.jackson.databind.ObjectMapper().readValue(partyContactMechId, PartyContactMechId.class), contactMechPurposeTypeId);
            if (state == null) { return null; }
            PartyContactMechPurposeStateDto.DtoConverter dtoConverter = new PartyContactMechPurposeStateDto.DtoConverter();
            PartyContactMechPurposeStateDto stateDto = dtoConverter.toPartyContactMechPurposeStateDto(state);
            dtoConverter.setAllFieldsReturned(true);
            return stateDto;

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }

    /**
     * Create or update.
     * Create or update PartyContactMechPurpose
     */
    @PutMapping(path = "{partyContactMechId}/PartyContactMechPurposes/{contactMechPurposeTypeId}", consumes = MediaType.APPLICATION_JSON_VALUE)
    public void putPartyContactMechPurpose(@PathVariable("partyContactMechId") String partyContactMechId, @PathVariable("contactMechPurposeTypeId") String contactMechPurposeTypeId,
                       @RequestParam(value = "commandId", required = false) String commandId,
                       @RequestParam(value = "version", required = false) Long version,
                       @RequestParam(value = "requesterId", required = false) String requesterId,
                       @RequestBody CreateOrMergePatchPartyContactMechPurposeDto.MergePatchPartyContactMechPurposeDto body) {
        try {
            PartyContactMechCommand.MergePatchPartyContactMech mergePatchPartyContactMech = new CreateOrMergePatchPartyContactMechDto.MergePatchPartyContactMechDto();
            mergePatchPartyContactMech.setPartyContactMechId(new com.fasterxml.jackson.databind.ObjectMapper().readValue(partyContactMechId, PartyContactMechId.class));
            mergePatchPartyContactMech.setCommandId(commandId != null && !commandId.isEmpty() ? commandId : body.getCommandId());
            if (version != null) { mergePatchPartyContactMech.setVersion(version); }
            mergePatchPartyContactMech.setRequesterId(requesterId != null && !requesterId.isEmpty() ? requesterId : body.getRequesterId());
            PartyContactMechPurposeCommand.MergePatchPartyContactMechPurpose mergePatchPartyContactMechPurpose = body;//.toMergePatchPartyContactMechPurpose();
            mergePatchPartyContactMechPurpose.setContactMechPurposeTypeId(contactMechPurposeTypeId);
            mergePatchPartyContactMech.getPartyContactMechPurposeCommands().add(mergePatchPartyContactMechPurpose);
            mergePatchPartyContactMech.setRequesterId(SecurityContextUtil.getRequesterId());
            partyContactMechApplicationService.when(mergePatchPartyContactMech);
        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }

    /**
     * Delete.
     * Delete PartyContactMechPurpose
     */
    @DeleteMapping("{partyContactMechId}/PartyContactMechPurposes/{contactMechPurposeTypeId}")
    public void deletePartyContactMechPurpose(@PathVariable("partyContactMechId") String partyContactMechId, @PathVariable("contactMechPurposeTypeId") String contactMechPurposeTypeId,
                       @RequestParam(value = "commandId", required = false) String commandId,
                       @RequestParam(value = "version", required = false) Long version,
                       @RequestParam(value = "requesterId", required = false) String requesterId) {
        try {
            PartyContactMechCommand.MergePatchPartyContactMech mergePatchPartyContactMech = new CreateOrMergePatchPartyContactMechDto.MergePatchPartyContactMechDto();
            mergePatchPartyContactMech.setPartyContactMechId(new com.fasterxml.jackson.databind.ObjectMapper().readValue(partyContactMechId, PartyContactMechId.class));
            mergePatchPartyContactMech.setCommandId(commandId);// != null && !commandId.isEmpty() ? commandId : body.getCommandId());
            if (version != null) { 
                mergePatchPartyContactMech.setVersion(version); 
            } else {
                mergePatchPartyContactMech.setVersion(partyContactMechApplicationService.get(new com.fasterxml.jackson.databind.ObjectMapper().readValue(partyContactMechId, PartyContactMechId.class)).getVersion());
            }
            mergePatchPartyContactMech.setRequesterId(requesterId);// != null && !requesterId.isEmpty() ? requesterId : body.getRequesterId());
            PartyContactMechPurposeCommand.RemovePartyContactMechPurpose removePartyContactMechPurpose = new RemovePartyContactMechPurposeDto();
            removePartyContactMechPurpose.setContactMechPurposeTypeId(contactMechPurposeTypeId);
            mergePatchPartyContactMech.getPartyContactMechPurposeCommands().add(removePartyContactMechPurpose);
            mergePatchPartyContactMech.setRequesterId(SecurityContextUtil.getRequesterId());
            partyContactMechApplicationService.when(mergePatchPartyContactMech);
        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }

    /**
     * PartyContactMechPurpose List
     */
    @GetMapping("{partyContactMechId}/PartyContactMechPurposes")
    @Transactional(readOnly = true)
    public PartyContactMechPurposeStateDto[] getPartyContactMechPurposes(@PathVariable("partyContactMechId") String partyContactMechId,
                    @RequestParam(value = "sort", required = false) String sort,
                    @RequestParam(value = "fields", required = false) String fields,
                    @RequestParam(value = "filter", required = false) String filter,
                     HttpServletRequest request) {
        try {
            CriterionDto criterion = null;
            if (!StringHelper.isNullOrEmpty(filter)) {
                criterion = new ObjectMapper().readValue(filter, CriterionDto.class);
            } else {
                criterion = QueryParamUtils.getQueryCriterionDto(request.getParameterMap().entrySet().stream()
                    .filter(kv -> PartyContactMechResourceUtils.getPartyContactMechPurposeFilterPropertyName(kv.getKey()) != null)
                    .collect(Collectors.toMap(kv -> kv.getKey(), kv -> kv.getValue())));
            }
            Criterion c = CriterionDto.toSubclass(criterion, getCriterionTypeConverter(), getPropertyTypeResolver(), 
                n -> (PartyContactMechPurposeMetadata.aliasMap.containsKey(n) ? PartyContactMechPurposeMetadata.aliasMap.get(n) : n));
            Iterable<PartyContactMechPurposeState> states = partyContactMechApplicationService.getPartyContactMechPurposes(new com.fasterxml.jackson.databind.ObjectMapper().readValue(partyContactMechId, PartyContactMechId.class), c,
                    PartyContactMechResourceUtils.getPartyContactMechPurposeQuerySorts(request.getParameterMap()));
            if (states == null) { return null; }
            PartyContactMechPurposeStateDto.DtoConverter dtoConverter = new PartyContactMechPurposeStateDto.DtoConverter();
            if (StringHelper.isNullOrEmpty(fields)) {
                dtoConverter.setAllFieldsReturned(true);
            } else {
                dtoConverter.setReturnedFieldsString(fields);
            }
            return dtoConverter.toPartyContactMechPurposeStateDtoArray(states);
        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }

    /**
     * Create.
     * Create PartyContactMechPurpose
     */
    @PostMapping(path = "{partyContactMechId}/PartyContactMechPurposes", consumes = MediaType.APPLICATION_JSON_VALUE)
    public void postPartyContactMechPurpose(@PathVariable("partyContactMechId") String partyContactMechId,
                       @RequestParam(value = "commandId", required = false) String commandId,
                       @RequestParam(value = "version", required = false) Long version,
                       @RequestParam(value = "requesterId", required = false) String requesterId,
                       @RequestBody CreateOrMergePatchPartyContactMechPurposeDto.CreatePartyContactMechPurposeDto body) {
        try {
            PartyContactMechCommand.MergePatchPartyContactMech mergePatchPartyContactMech = new AbstractPartyContactMechCommand.SimpleMergePatchPartyContactMech();
            mergePatchPartyContactMech.setPartyContactMechId(new com.fasterxml.jackson.databind.ObjectMapper().readValue(partyContactMechId, PartyContactMechId.class));
            mergePatchPartyContactMech.setCommandId(commandId != null && !commandId.isEmpty() ? commandId : body.getCommandId());
            if (version != null) { mergePatchPartyContactMech.setVersion(version); }
            mergePatchPartyContactMech.setRequesterId(requesterId != null && !requesterId.isEmpty() ? requesterId : body.getRequesterId());
            PartyContactMechPurposeCommand.CreatePartyContactMechPurpose createPartyContactMechPurpose = body.toCreatePartyContactMechPurpose();
            mergePatchPartyContactMech.getPartyContactMechPurposeCommands().add(createPartyContactMechPurpose);
            mergePatchPartyContactMech.setRequesterId(SecurityContextUtil.getRequesterId());
            partyContactMechApplicationService.when(mergePatchPartyContactMech);
        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }



    //protected  PartyContactMechStateEventDtoConverter getPartyContactMechStateEventDtoConverter() {
    //    return new PartyContactMechStateEventDtoConverter();
    //}

    protected TypeConverter getCriterionTypeConverter() {
        return new DefaultTypeConverter();
    }

    protected PropertyTypeResolver getPropertyTypeResolver() {
        return new PropertyTypeResolver() {
            @Override
            public Class resolveTypeByPropertyName(String propertyName) {
                return PartyContactMechResourceUtils.getFilterPropertyType(propertyName);
            }
        };
    }

    protected PropertyTypeResolver getPartyContactMechPurposePropertyTypeResolver() {
        return new PropertyTypeResolver() {
            @Override
            public Class resolveTypeByPropertyName(String propertyName) {
                return PartyContactMechResourceUtils.getPartyContactMechPurposeFilterPropertyType(propertyName);
            }
        };
    }

 
    public static class PartyContactMechResourceUtils {

        private static final com.fasterxml.jackson.databind.ObjectMapper objectMapper;

        static {
            objectMapper = new com.fasterxml.jackson.databind.ObjectMapper();
            objectMapper.setSerializationInclusion(com.fasterxml.jackson.annotation.JsonInclude.Include.NON_NULL);
            objectMapper.configure(com.fasterxml.jackson.databind.DeserializationFeature.FAIL_ON_UNKNOWN_PROPERTIES, false);
            objectMapper.registerModule(new com.fasterxml.jackson.datatype.jsr310.JavaTimeModule())
                    .setDateFormat(new com.fasterxml.jackson.databind.util.StdDateFormat().withColonInTimeZone(true))
                    .configure(com.fasterxml.jackson.databind.SerializationFeature.WRITE_DATES_AS_TIMESTAMPS, false)
                    .configure(com.fasterxml.jackson.databind.SerializationFeature.WRITE_DATE_TIMESTAMPS_AS_NANOSECONDS, false)
                    .configure(com.fasterxml.jackson.databind.DeserializationFeature.ADJUST_DATES_TO_CONTEXT_TIME_ZONE, false);
        }

        public static PartyContactMechId parseIdString(String idString) {
            try {
                return objectMapper.readValue(idString, PartyContactMechId.class);
            } catch (com.fasterxml.jackson.core.JsonProcessingException e) {
                throw new RuntimeException(e);
            }
        }

        public static void setNullIdOrThrowOnInconsistentIds(String partyContactMechId, org.dddml.ffvtraceability.domain.partycontactmech.PartyContactMechCommand value) {
            PartyContactMechId idObj = parseIdString(partyContactMechId);
            if (value.getPartyContactMechId() == null) {
                value.setPartyContactMechId(idObj);
            } else if (!value.getPartyContactMechId().equals(idObj)) {
                throw DomainError.named("inconsistentId", "Argument Id %1$s NOT equals body Id %2$s", partyContactMechId, value.getPartyContactMechId());
            }
        }
    
        public static List<String> getQueryOrders(String str, String separator) {
            return QueryParamUtils.getQueryOrders(str, separator, PartyContactMechMetadata.aliasMap);
        }

        public static List<String> getQuerySorts(Map<String, String[]> queryNameValuePairs) {
            String[] values = queryNameValuePairs.get("sort");
            return QueryParamUtils.getQuerySorts(values, PartyContactMechMetadata.aliasMap);
        }

        public static String getFilterPropertyName(String fieldName) {
            if ("sort".equalsIgnoreCase(fieldName)
                    || "firstResult".equalsIgnoreCase(fieldName)
                    || "maxResults".equalsIgnoreCase(fieldName)
                    || "fields".equalsIgnoreCase(fieldName)) {
                return null;
            }
            if (PartyContactMechMetadata.aliasMap.containsKey(fieldName)) {
                return PartyContactMechMetadata.aliasMap.get(fieldName);
            }
            return null;
        }

        public static Class getFilterPropertyType(String propertyName) {
            if (PartyContactMechMetadata.propertyTypeMap.containsKey(propertyName)) {
                String propertyType = PartyContactMechMetadata.propertyTypeMap.get(propertyName);
                if (!StringHelper.isNullOrEmpty(propertyType)) {
                    if (BoundedContextMetadata.CLASS_MAP.containsKey(propertyType)) {
                        return BoundedContextMetadata.CLASS_MAP.get(propertyType);
                    }
                }
            }
            return String.class;
        }

        public static Iterable<Map.Entry<String, Object>> getQueryFilterMap(Map<String, String[]> queryNameValuePairs) {
            Map<String, Object> filter = new HashMap<>();
            queryNameValuePairs.forEach((key, values) -> {
                if (values.length > 0) {
                    String pName = getFilterPropertyName(key);
                    if (!StringHelper.isNullOrEmpty(pName)) {
                        Class pClass = getFilterPropertyType(pName);
                        filter.put(pName, ApplicationContext.current.getTypeConverter().convertFromString(pClass, values[0]));
                    }
                }
            });
            return filter.entrySet();
        }

        public static List<String> getPartyContactMechPurposeQueryOrders(String str, String separator) {
            return QueryParamUtils.getQueryOrders(str, separator, PartyContactMechPurposeMetadata.aliasMap);
        }

        public static List<String> getPartyContactMechPurposeQuerySorts(Map<String, String[]> queryNameValuePairs) {
            String[] values = queryNameValuePairs.get("sort");
            return QueryParamUtils.getQuerySorts(values, PartyContactMechPurposeMetadata.aliasMap);
        }

        public static String getPartyContactMechPurposeFilterPropertyName(String fieldName) {
            if ("sort".equalsIgnoreCase(fieldName)
                    || "firstResult".equalsIgnoreCase(fieldName)
                    || "maxResults".equalsIgnoreCase(fieldName)
                    || "fields".equalsIgnoreCase(fieldName)) {
                return null;
            }
            if (PartyContactMechPurposeMetadata.aliasMap.containsKey(fieldName)) {
                return PartyContactMechPurposeMetadata.aliasMap.get(fieldName);
            }
            return null;
        }

        public static Class getPartyContactMechPurposeFilterPropertyType(String propertyName) {
            if (PartyContactMechPurposeMetadata.propertyTypeMap.containsKey(propertyName)) {
                String propertyType = PartyContactMechPurposeMetadata.propertyTypeMap.get(propertyName);
                if (!StringHelper.isNullOrEmpty(propertyType)) {
                    if (BoundedContextMetadata.CLASS_MAP.containsKey(propertyType)) {
                        return BoundedContextMetadata.CLASS_MAP.get(propertyType);
                    }
                }
            }
            return String.class;
        }

        public static Iterable<Map.Entry<String, Object>> getPartyContactMechPurposeQueryFilterMap(Map<String, String[]> queryNameValuePairs) {
            Map<String, Object> filter = new HashMap<>();
            queryNameValuePairs.forEach((key, values) -> {
                if (values.length > 0) {
                    String pName = getPartyContactMechPurposeFilterPropertyName(key);
                    if (!StringHelper.isNullOrEmpty(pName)) {
                        Class pClass = getPartyContactMechPurposeFilterPropertyType(pName);
                        filter.put(pName, ApplicationContext.current.getTypeConverter().convertFromString(pClass, values[0]));
                    }
                }
            });
            return filter.entrySet();
        }

        public static PartyContactMechStateDto[] toPartyContactMechStateDtoArray(Iterable<PartyContactMechId> ids) {
            List<PartyContactMechStateDto> states = new ArrayList<>();
            ids.forEach(i -> {
                PartyContactMechStateDto dto = new PartyContactMechStateDto();
                dto.setPartyContactMechId(i);
                states.add(dto);
            });
            return states.toArray(new PartyContactMechStateDto[0]);
        }

    }

}

