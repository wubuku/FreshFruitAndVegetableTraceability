// <autogenerated>
//   This file was generated by dddappp code generator.
//   Any changes made to this file manually will be lost next time the file is regenerated.
// </autogenerated>

package org.dddml.ffvtraceability.restful.resource;

import java.util.*;
import java.util.stream.*;
import jakarta.servlet.http.*;
import jakarta.validation.constraints.*;
import org.springframework.http.MediaType;
import org.springframework.http.HttpStatus;
import org.springframework.web.bind.annotation.*;
import org.springframework.transaction.annotation.Transactional;
import org.dddml.support.criterion.*;
import java.time.OffsetDateTime;
import org.dddml.ffvtraceability.domain.*;
import org.dddml.ffvtraceability.specialization.*;
import org.dddml.ffvtraceability.domain.workeffortinventoryassign.*;
import static org.dddml.ffvtraceability.domain.meta.M.*;

import com.fasterxml.jackson.databind.ObjectMapper;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.security.access.prepost.*;
import org.dddml.support.criterion.TypeConverter;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

@RequestMapping(path = "WorkEffortInventoryAssigns", produces = MediaType.APPLICATION_JSON_VALUE)
@RestController
public class WorkEffortInventoryAssignResource {
    private Logger logger = LoggerFactory.getLogger(this.getClass());

    private static CriterionDto deserializeCriterionDto(String filter) {
        return deserializeJsonArgument(filter, CriterionDto.class);
    }

    private static <T> T deserializeJsonArgument(String s, Class<T> aClass) {
        try {
            return new ObjectMapper().readValue(s, aClass);
        } catch (com.fasterxml.jackson.core.JsonProcessingException e) {
            throw new IllegalArgumentException(e);
        }
    }


    @Autowired
    private WorkEffortInventoryAssignApplicationService workEffortInventoryAssignApplicationService;


    /**
     * Retrieve.
     * Retrieve WorkEffortInventoryAssigns
     */
    @GetMapping
    @Transactional(readOnly = true)
    public WorkEffortInventoryAssignStateDto[] getAll( HttpServletRequest request,
                    @RequestParam(value = "sort", required = false) String sort,
                    @RequestParam(value = "fields", required = false) String fields,
                    @RequestParam(value = "firstResult", defaultValue = "0") Integer firstResult,
                    @RequestParam(value = "maxResults", defaultValue = "2147483647") Integer maxResults,
                    @RequestParam(value = "filter", required = false) String filter) {
        
        if (firstResult < 0) { firstResult = 0; }
        if (maxResults == null || maxResults < 1) { maxResults = Integer.MAX_VALUE; }

            Iterable<WorkEffortInventoryAssignState> states = null; 
            CriterionDto criterion = null;
            if (!StringHelper.isNullOrEmpty(filter)) {
                criterion = deserializeCriterionDto(filter);
            } else {
                criterion = QueryParamUtils.getQueryCriterionDto(request.getParameterMap().entrySet().stream()
                    .filter(kv -> WorkEffortInventoryAssignResourceUtils.getFilterPropertyName(kv.getKey()) != null)
                    .collect(Collectors.toMap(kv -> kv.getKey(), kv -> kv.getValue())));
            }
            Criterion c = CriterionDto.toSubclass(criterion, getCriterionTypeConverter(), getPropertyTypeResolver(), 
                n -> (WorkEffortInventoryAssignMetadata.aliasMap.containsKey(n) ? WorkEffortInventoryAssignMetadata.aliasMap.get(n) : n));
            states = workEffortInventoryAssignApplicationService.get(
                c,
                WorkEffortInventoryAssignResourceUtils.getQuerySorts(request.getParameterMap()),
                firstResult, maxResults);

            WorkEffortInventoryAssignStateDto.DtoConverter dtoConverter = new WorkEffortInventoryAssignStateDto.DtoConverter();
            if (StringHelper.isNullOrEmpty(fields)) {
                dtoConverter.setAllFieldsReturned(true);
            } else {
                dtoConverter.setReturnedFieldsString(fields);
            }
            return dtoConverter.toWorkEffortInventoryAssignStateDtoArray(states);

        
    }

    /**
     * Retrieve in pages.
     * Retrieve WorkEffortInventoryAssigns in pages.
     */
    @GetMapping("_page")
    @Transactional(readOnly = true)
    public Page<WorkEffortInventoryAssignStateDto> getPage( HttpServletRequest request,
                    @RequestParam(value = "fields", required = false) String fields,
                    @RequestParam(value = "page", defaultValue = "0") Integer page,
                    @RequestParam(value = "size", defaultValue = "20") Integer size,
                    @RequestParam(value = "filter", required = false) String filter) {
        
            Integer firstResult = (page == null ? 0 : page) * (size == null ? 20 : size);
            Integer maxResults = (size == null ? 20 : size);
            Iterable<WorkEffortInventoryAssignState> states = null; 
            CriterionDto criterion = null;
            if (!StringHelper.isNullOrEmpty(filter)) {
                criterion = deserializeCriterionDto(filter);
            } else {
                criterion = QueryParamUtils.getQueryCriterionDto(request.getParameterMap().entrySet().stream()
                    .filter(kv -> WorkEffortInventoryAssignResourceUtils.getFilterPropertyName(kv.getKey()) != null)
                    .collect(Collectors.toMap(kv -> kv.getKey(), kv -> kv.getValue())));
            }
            Criterion c = CriterionDto.toSubclass(criterion, getCriterionTypeConverter(), getPropertyTypeResolver(), 
                n -> (WorkEffortInventoryAssignMetadata.aliasMap.containsKey(n) ? WorkEffortInventoryAssignMetadata.aliasMap.get(n) : n));
            states = workEffortInventoryAssignApplicationService.get(
                c,
                WorkEffortInventoryAssignResourceUtils.getQuerySorts(request.getParameterMap()),
                firstResult, maxResults);
            long count = workEffortInventoryAssignApplicationService.getCount(c);

            WorkEffortInventoryAssignStateDto.DtoConverter dtoConverter = new WorkEffortInventoryAssignStateDto.DtoConverter();
            if (StringHelper.isNullOrEmpty(fields)) {
                dtoConverter.setAllFieldsReturned(true);
            } else {
                dtoConverter.setReturnedFieldsString(fields);
            }
            Page.PageImpl<WorkEffortInventoryAssignStateDto> statePage =  new Page.PageImpl<>(dtoConverter.toWorkEffortInventoryAssignStateDtoList(states), count);
            statePage.setSize(size);
            statePage.setNumber(page);
            return statePage;

        
    }

    /**
     * Retrieve.
     * Retrieves WorkEffortInventoryAssign with the specified ID.
     */
    @GetMapping("{workEffortInventoryAssignId}")
    @Transactional(readOnly = true)
    public WorkEffortInventoryAssignStateDto get(@PathVariable("workEffortInventoryAssignId") String workEffortInventoryAssignId, @RequestParam(value = "fields", required = false) String fields) {
        
            WorkEffortInventoryAssignId idObj = WorkEffortInventoryAssignResourceUtils.parseIdString(workEffortInventoryAssignId);
            WorkEffortInventoryAssignState state = workEffortInventoryAssignApplicationService.get(idObj);
            if (state == null) { return null; }

            WorkEffortInventoryAssignStateDto.DtoConverter dtoConverter = new WorkEffortInventoryAssignStateDto.DtoConverter();
            if (StringHelper.isNullOrEmpty(fields)) {
                dtoConverter.setAllFieldsReturned(true);
            } else {
                dtoConverter.setReturnedFieldsString(fields);
            }
            return dtoConverter.toWorkEffortInventoryAssignStateDto(state);

        
    }

    @GetMapping("_count")
    @Transactional(readOnly = true)
    public long getCount( HttpServletRequest request,
                         @RequestParam(value = "filter", required = false) String filter) {
        
            long count = 0;
            CriterionDto criterion = null;
            if (!StringHelper.isNullOrEmpty(filter)) {
                criterion = deserializeCriterionDto(filter);
            } else {
                criterion = QueryParamUtils.getQueryCriterionDto(request.getParameterMap());
            }
            Criterion c = CriterionDto.toSubclass(criterion,
                getCriterionTypeConverter(), 
                getPropertyTypeResolver(), 
                n -> (WorkEffortInventoryAssignMetadata.aliasMap.containsKey(n) ? WorkEffortInventoryAssignMetadata.aliasMap.get(n) : n));
            count = workEffortInventoryAssignApplicationService.getCount(c);
            return count;

        
    }


    /**
     * Create.
     * Create WorkEffortInventoryAssign
     */
    @PostMapping @ResponseStatus(HttpStatus.CREATED)
    public WorkEffortInventoryAssignId post(@RequestBody CreateOrMergePatchWorkEffortInventoryAssignDto.CreateWorkEffortInventoryAssignDto value,  HttpServletResponse response) {
        
            WorkEffortInventoryAssignCommand.CreateWorkEffortInventoryAssign cmd = value;//.toCreateWorkEffortInventoryAssign();
            if (cmd.getWorkEffortInventoryAssignId() == null) {
                throw DomainError.named("nullId", "Aggregate Id in cmd is null, aggregate name: %1$s.", "WorkEffortInventoryAssign");
            }
            WorkEffortInventoryAssignId idObj = cmd.getWorkEffortInventoryAssignId();
            cmd.setRequesterId(SecurityContextUtil.getRequesterId());
            workEffortInventoryAssignApplicationService.when(cmd);

            return idObj;
        
    }


    /**
     * Create or update.
     * Create or update WorkEffortInventoryAssign
     */
    @PutMapping("{workEffortInventoryAssignId}")
    public void put(@PathVariable("workEffortInventoryAssignId") String workEffortInventoryAssignId, @RequestBody CreateOrMergePatchWorkEffortInventoryAssignDto value) {
        
            if (value.getVersion() != null) {
                value.setCommandType(Command.COMMAND_TYPE_MERGE_PATCH);
                WorkEffortInventoryAssignCommand.MergePatchWorkEffortInventoryAssign cmd = (WorkEffortInventoryAssignCommand.MergePatchWorkEffortInventoryAssign) value.toSubclass();
                WorkEffortInventoryAssignResourceUtils.setNullIdOrThrowOnInconsistentIds(workEffortInventoryAssignId, cmd);
                cmd.setRequesterId(SecurityContextUtil.getRequesterId());
                workEffortInventoryAssignApplicationService.when(cmd);
                return;
            }

            value.setCommandType(Command.COMMAND_TYPE_CREATE);
            WorkEffortInventoryAssignCommand.CreateWorkEffortInventoryAssign cmd = (WorkEffortInventoryAssignCommand.CreateWorkEffortInventoryAssign) value.toSubclass();
            WorkEffortInventoryAssignResourceUtils.setNullIdOrThrowOnInconsistentIds(workEffortInventoryAssignId, cmd);
            cmd.setRequesterId(SecurityContextUtil.getRequesterId());
            workEffortInventoryAssignApplicationService.when(cmd);

        
    }


    /**
     * Patch.
     * Patch WorkEffortInventoryAssign
     */
    @PatchMapping("{workEffortInventoryAssignId}")
    public void patch(@PathVariable("workEffortInventoryAssignId") String workEffortInventoryAssignId, @RequestBody CreateOrMergePatchWorkEffortInventoryAssignDto.MergePatchWorkEffortInventoryAssignDto value) {
        

            WorkEffortInventoryAssignCommand.MergePatchWorkEffortInventoryAssign cmd = value;//.toMergePatchWorkEffortInventoryAssign();
            WorkEffortInventoryAssignResourceUtils.setNullIdOrThrowOnInconsistentIds(workEffortInventoryAssignId, cmd);
            cmd.setRequesterId(SecurityContextUtil.getRequesterId());
            workEffortInventoryAssignApplicationService.when(cmd);

        
    }

    @GetMapping("_metadata/filteringFields")
    public List<PropertyMetadataDto> getMetadataFilteringFields() {
        

            List<PropertyMetadataDto> filtering = new ArrayList<>();
            WorkEffortInventoryAssignMetadata.propertyTypeMap.forEach((key, value) -> {
                filtering.add(new PropertyMetadataDto(key, value, true));
            });
            return filtering;

        
    }

    @GetMapping("{workEffortInventoryAssignId}/_events/{version}")
    @Transactional(readOnly = true)
    public WorkEffortInventoryAssignEvent getEvent(@PathVariable("workEffortInventoryAssignId") String workEffortInventoryAssignId, @PathVariable("version") long version) {
        

            WorkEffortInventoryAssignId idObj = WorkEffortInventoryAssignResourceUtils.parseIdString(workEffortInventoryAssignId);
            //WorkEffortInventoryAssignStateEventDtoConverter dtoConverter = getWorkEffortInventoryAssignStateEventDtoConverter();
            return workEffortInventoryAssignApplicationService.getEvent(idObj, version);

        
    }

    @GetMapping("{workEffortInventoryAssignId}/_historyStates/{version}")
    @Transactional(readOnly = true)
    public WorkEffortInventoryAssignStateDto getHistoryState(@PathVariable("workEffortInventoryAssignId") String workEffortInventoryAssignId, @PathVariable("version") long version, @RequestParam(value = "fields", required = false) String fields) {
        

            WorkEffortInventoryAssignId idObj = WorkEffortInventoryAssignResourceUtils.parseIdString(workEffortInventoryAssignId);
            WorkEffortInventoryAssignStateDto.DtoConverter dtoConverter = new WorkEffortInventoryAssignStateDto.DtoConverter();
            if (StringHelper.isNullOrEmpty(fields)) {
                dtoConverter.setAllFieldsReturned(true);
            } else {
                dtoConverter.setReturnedFieldsString(fields);
            }
            return dtoConverter.toWorkEffortInventoryAssignStateDto(workEffortInventoryAssignApplicationService.getHistoryState(idObj, version));

        
    }



    //protected  WorkEffortInventoryAssignStateEventDtoConverter getWorkEffortInventoryAssignStateEventDtoConverter() {
    //    return new WorkEffortInventoryAssignStateEventDtoConverter();
    //}

    protected TypeConverter getCriterionTypeConverter() {
        return new DefaultTypeConverter();
    }

    protected PropertyTypeResolver getPropertyTypeResolver() {
        return new PropertyTypeResolver() {
            @Override
            public Class resolveTypeByPropertyName(String propertyName) {
                return WorkEffortInventoryAssignResourceUtils.getFilterPropertyType(propertyName);
            }
        };
    }

 
    public static class WorkEffortInventoryAssignResourceUtils {

        private static final com.fasterxml.jackson.databind.ObjectMapper objectMapper;

        static {
            objectMapper = new com.fasterxml.jackson.databind.ObjectMapper();
            objectMapper.setSerializationInclusion(com.fasterxml.jackson.annotation.JsonInclude.Include.NON_NULL);
            objectMapper.configure(com.fasterxml.jackson.databind.DeserializationFeature.FAIL_ON_UNKNOWN_PROPERTIES, false);
            objectMapper.registerModule(new com.fasterxml.jackson.datatype.jsr310.JavaTimeModule())
                    .setDateFormat(new com.fasterxml.jackson.databind.util.StdDateFormat().withColonInTimeZone(true))
                    .configure(com.fasterxml.jackson.databind.SerializationFeature.WRITE_DATES_AS_TIMESTAMPS, false)
                    .configure(com.fasterxml.jackson.databind.SerializationFeature.WRITE_DATE_TIMESTAMPS_AS_NANOSECONDS, false)
                    .configure(com.fasterxml.jackson.databind.DeserializationFeature.ADJUST_DATES_TO_CONTEXT_TIME_ZONE, false);
        }

        public static WorkEffortInventoryAssignId parseIdString(String idString) {
            try {
                return objectMapper.readValue(idString, WorkEffortInventoryAssignId.class);
            } catch (com.fasterxml.jackson.core.JsonProcessingException e) {
                throw new RuntimeException(e);
            }
        }

        public static void setNullIdOrThrowOnInconsistentIds(String workEffortInventoryAssignId, org.dddml.ffvtraceability.domain.workeffortinventoryassign.WorkEffortInventoryAssignCommand value) {
            WorkEffortInventoryAssignId idObj = parseIdString(workEffortInventoryAssignId);
            if (value.getWorkEffortInventoryAssignId() == null) {
                value.setWorkEffortInventoryAssignId(idObj);
            } else if (!value.getWorkEffortInventoryAssignId().equals(idObj)) {
                throw DomainError.named("inconsistentId", "Argument Id %1$s NOT equals body Id %2$s", workEffortInventoryAssignId, value.getWorkEffortInventoryAssignId());
            }
        }
    
        public static List<String> getQueryOrders(String str, String separator) {
            return QueryParamUtils.getQueryOrders(str, separator, WorkEffortInventoryAssignMetadata.aliasMap);
        }

        public static List<String> getQuerySorts(Map<String, String[]> queryNameValuePairs) {
            String[] values = queryNameValuePairs.get("sort");
            return QueryParamUtils.getQuerySorts(values, WorkEffortInventoryAssignMetadata.aliasMap);
        }

        public static String getFilterPropertyName(String fieldName) {
            if ("sort".equalsIgnoreCase(fieldName)
                    || "firstResult".equalsIgnoreCase(fieldName)
                    || "maxResults".equalsIgnoreCase(fieldName)
                    || "fields".equalsIgnoreCase(fieldName)) {
                return null;
            }
            if (WorkEffortInventoryAssignMetadata.aliasMap.containsKey(fieldName)) {
                return WorkEffortInventoryAssignMetadata.aliasMap.get(fieldName);
            }
            return null;
        }

        public static Class getFilterPropertyType(String propertyName) {
            if (WorkEffortInventoryAssignMetadata.propertyTypeMap.containsKey(propertyName)) {
                String propertyType = WorkEffortInventoryAssignMetadata.propertyTypeMap.get(propertyName);
                if (!StringHelper.isNullOrEmpty(propertyType)) {
                    if (BoundedContextMetadata.CLASS_MAP.containsKey(propertyType)) {
                        return BoundedContextMetadata.CLASS_MAP.get(propertyType);
                    }
                }
            }
            return String.class;
        }

        public static Iterable<Map.Entry<String, Object>> getQueryFilterMap(Map<String, String[]> queryNameValuePairs) {
            Map<String, Object> filter = new HashMap<>();
            queryNameValuePairs.forEach((key, values) -> {
                if (values.length > 0) {
                    String pName = getFilterPropertyName(key);
                    if (!StringHelper.isNullOrEmpty(pName)) {
                        Class pClass = getFilterPropertyType(pName);
                        filter.put(pName, ApplicationContext.current.getTypeConverter().convertFromString(pClass, values[0]));
                    }
                }
            });
            return filter.entrySet();
        }

        public static WorkEffortInventoryAssignStateDto[] toWorkEffortInventoryAssignStateDtoArray(Iterable<WorkEffortInventoryAssignId> ids) {
            List<WorkEffortInventoryAssignStateDto> states = new ArrayList<>();
            ids.forEach(i -> {
                WorkEffortInventoryAssignStateDto dto = new WorkEffortInventoryAssignStateDto();
                dto.setWorkEffortInventoryAssignId(i);
                states.add(dto);
            });
            return states.toArray(new WorkEffortInventoryAssignStateDto[0]);
        }

    }

}

