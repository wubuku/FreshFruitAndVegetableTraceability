// <autogenerated>
//   This file was generated by dddappp code generator.
//   Any changes made to this file manually will be lost next time the file is regenerated.
// </autogenerated>

package org.dddml.ffvtraceability.restful.resource;

import java.util.*;
import java.util.stream.*;
import jakarta.servlet.http.*;
import jakarta.validation.constraints.*;
import org.springframework.http.MediaType;
import org.springframework.http.HttpStatus;
import org.springframework.web.bind.annotation.*;
import org.springframework.transaction.annotation.Transactional;
import org.dddml.support.criterion.*;
import java.time.OffsetDateTime;
import org.dddml.ffvtraceability.domain.*;
import org.dddml.ffvtraceability.specialization.*;
import org.dddml.ffvtraceability.domain.contactmechpurposetype.*;
import static org.dddml.ffvtraceability.domain.meta.M.*;

import com.fasterxml.jackson.databind.ObjectMapper;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.security.access.prepost.*;
import org.dddml.support.criterion.TypeConverter;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

@RequestMapping(path = "ContactMechPurposeTypes", produces = MediaType.APPLICATION_JSON_VALUE)
@RestController
public class ContactMechPurposeTypeResource {
    private Logger logger = LoggerFactory.getLogger(this.getClass());


    @Autowired
    private ContactMechPurposeTypeApplicationService contactMechPurposeTypeApplicationService;


    /**
     * Retrieve.
     * Retrieve ContactMechPurposeTypes
     */
    @GetMapping
    @Transactional(readOnly = true)
    public ContactMechPurposeTypeStateDto[] getAll( HttpServletRequest request,
                    @RequestParam(value = "sort", required = false) String sort,
                    @RequestParam(value = "fields", required = false) String fields,
                    @RequestParam(value = "firstResult", defaultValue = "0") Integer firstResult,
                    @RequestParam(value = "maxResults", defaultValue = "2147483647") Integer maxResults,
                    @RequestParam(value = "filter", required = false) String filter) {
        try {
        if (firstResult < 0) { firstResult = 0; }
        if (maxResults == null || maxResults < 1) { maxResults = Integer.MAX_VALUE; }

            Iterable<ContactMechPurposeTypeState> states = null; 
            CriterionDto criterion = null;
            if (!StringHelper.isNullOrEmpty(filter)) {
                criterion = new ObjectMapper().readValue(filter, CriterionDto.class);
            } else {
                criterion = QueryParamUtils.getQueryCriterionDto(request.getParameterMap().entrySet().stream()
                    .filter(kv -> ContactMechPurposeTypeResourceUtils.getFilterPropertyName(kv.getKey()) != null)
                    .collect(Collectors.toMap(kv -> kv.getKey(), kv -> kv.getValue())));
            }
            Criterion c = CriterionDto.toSubclass(criterion, getCriterionTypeConverter(), getPropertyTypeResolver(), 
                n -> (ContactMechPurposeTypeMetadata.aliasMap.containsKey(n) ? ContactMechPurposeTypeMetadata.aliasMap.get(n) : n));
            states = contactMechPurposeTypeApplicationService.get(
                c,
                ContactMechPurposeTypeResourceUtils.getQuerySorts(request.getParameterMap()),
                firstResult, maxResults);

            ContactMechPurposeTypeStateDto.DtoConverter dtoConverter = new ContactMechPurposeTypeStateDto.DtoConverter();
            if (StringHelper.isNullOrEmpty(fields)) {
                dtoConverter.setAllFieldsReturned(true);
            } else {
                dtoConverter.setReturnedFieldsString(fields);
            }
            return dtoConverter.toContactMechPurposeTypeStateDtoArray(states);

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }

    /**
     * Retrieve in pages.
     * Retrieve ContactMechPurposeTypes in pages.
     */
    @GetMapping("_page")
    @Transactional(readOnly = true)
    public Page<ContactMechPurposeTypeStateDto> getPage( HttpServletRequest request,
                    @RequestParam(value = "fields", required = false) String fields,
                    @RequestParam(value = "page", defaultValue = "0") Integer page,
                    @RequestParam(value = "size", defaultValue = "20") Integer size,
                    @RequestParam(value = "filter", required = false) String filter) {
        try {
            Integer firstResult = (page == null ? 0 : page) * (size == null ? 20 : size);
            Integer maxResults = (size == null ? 20 : size);
            Iterable<ContactMechPurposeTypeState> states = null; 
            CriterionDto criterion = null;
            if (!StringHelper.isNullOrEmpty(filter)) {
                criterion = new ObjectMapper().readValue(filter, CriterionDto.class);
            } else {
                criterion = QueryParamUtils.getQueryCriterionDto(request.getParameterMap().entrySet().stream()
                    .filter(kv -> ContactMechPurposeTypeResourceUtils.getFilterPropertyName(kv.getKey()) != null)
                    .collect(Collectors.toMap(kv -> kv.getKey(), kv -> kv.getValue())));
            }
            Criterion c = CriterionDto.toSubclass(criterion, getCriterionTypeConverter(), getPropertyTypeResolver(), 
                n -> (ContactMechPurposeTypeMetadata.aliasMap.containsKey(n) ? ContactMechPurposeTypeMetadata.aliasMap.get(n) : n));
            states = contactMechPurposeTypeApplicationService.get(
                c,
                ContactMechPurposeTypeResourceUtils.getQuerySorts(request.getParameterMap()),
                firstResult, maxResults);
            long count = contactMechPurposeTypeApplicationService.getCount(c);

            ContactMechPurposeTypeStateDto.DtoConverter dtoConverter = new ContactMechPurposeTypeStateDto.DtoConverter();
            if (StringHelper.isNullOrEmpty(fields)) {
                dtoConverter.setAllFieldsReturned(true);
            } else {
                dtoConverter.setReturnedFieldsString(fields);
            }
            Page.PageImpl<ContactMechPurposeTypeStateDto> statePage =  new Page.PageImpl<>(dtoConverter.toContactMechPurposeTypeStateDtoList(states), count);
            statePage.setSize(size);
            statePage.setNumber(page);
            return statePage;

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }

    /**
     * Retrieve.
     * Retrieves ContactMechPurposeType with the specified ID.
     */
    @GetMapping("{contactMechPurposeTypeId}")
    @Transactional(readOnly = true)
    public ContactMechPurposeTypeStateDto get(@PathVariable("contactMechPurposeTypeId") String contactMechPurposeTypeId, @RequestParam(value = "fields", required = false) String fields) {
        try {
            String idObj = contactMechPurposeTypeId;
            ContactMechPurposeTypeState state = contactMechPurposeTypeApplicationService.get(idObj);
            if (state == null) { return null; }

            ContactMechPurposeTypeStateDto.DtoConverter dtoConverter = new ContactMechPurposeTypeStateDto.DtoConverter();
            if (StringHelper.isNullOrEmpty(fields)) {
                dtoConverter.setAllFieldsReturned(true);
            } else {
                dtoConverter.setReturnedFieldsString(fields);
            }
            return dtoConverter.toContactMechPurposeTypeStateDto(state);

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }

    @GetMapping("_count")
    @Transactional(readOnly = true)
    public long getCount( HttpServletRequest request,
                         @RequestParam(value = "filter", required = false) String filter) {
        try {
            long count = 0;
            CriterionDto criterion = null;
            if (!StringHelper.isNullOrEmpty(filter)) {
                criterion = new ObjectMapper().readValue(filter, CriterionDto.class);
            } else {
                criterion = QueryParamUtils.getQueryCriterionDto(request.getParameterMap());
            }
            Criterion c = CriterionDto.toSubclass(criterion,
                getCriterionTypeConverter(), 
                getPropertyTypeResolver(), 
                n -> (ContactMechPurposeTypeMetadata.aliasMap.containsKey(n) ? ContactMechPurposeTypeMetadata.aliasMap.get(n) : n));
            count = contactMechPurposeTypeApplicationService.getCount(c);
            return count;

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }


    /**
     * Create.
     * Create ContactMechPurposeType
     */
    @PostMapping @ResponseBody @ResponseStatus(HttpStatus.CREATED)
    public String post(@RequestBody CreateOrMergePatchContactMechPurposeTypeDto.CreateContactMechPurposeTypeDto value,  HttpServletResponse response) {
        try {
            ContactMechPurposeTypeCommand.CreateContactMechPurposeType cmd = value;//.toCreateContactMechPurposeType();
            if (cmd.getContactMechPurposeTypeId() == null) {
                throw DomainError.named("nullId", "Aggregate Id in cmd is null, aggregate name: %1$s.", "ContactMechPurposeType");
            }
            String idObj = cmd.getContactMechPurposeTypeId();
            cmd.setRequesterId(SecurityContextUtil.getRequesterId());
            contactMechPurposeTypeApplicationService.when(cmd);

            return idObj;
        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }


    /**
     * Create or update.
     * Create or update ContactMechPurposeType
     */
    @PutMapping("{contactMechPurposeTypeId}")
    public void put(@PathVariable("contactMechPurposeTypeId") String contactMechPurposeTypeId, @RequestBody CreateOrMergePatchContactMechPurposeTypeDto value) {
        try {
            if (value.getVersion() != null) {
                value.setCommandType(Command.COMMAND_TYPE_MERGE_PATCH);
                ContactMechPurposeTypeCommand.MergePatchContactMechPurposeType cmd = (ContactMechPurposeTypeCommand.MergePatchContactMechPurposeType) value.toSubclass();
                ContactMechPurposeTypeResourceUtils.setNullIdOrThrowOnInconsistentIds(contactMechPurposeTypeId, cmd);
                cmd.setRequesterId(SecurityContextUtil.getRequesterId());
                contactMechPurposeTypeApplicationService.when(cmd);
                return;
            }

            value.setCommandType(Command.COMMAND_TYPE_CREATE);
            ContactMechPurposeTypeCommand.CreateContactMechPurposeType cmd = (ContactMechPurposeTypeCommand.CreateContactMechPurposeType) value.toSubclass();
            ContactMechPurposeTypeResourceUtils.setNullIdOrThrowOnInconsistentIds(contactMechPurposeTypeId, cmd);
            cmd.setRequesterId(SecurityContextUtil.getRequesterId());
            contactMechPurposeTypeApplicationService.when(cmd);

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }


    /**
     * Patch.
     * Patch ContactMechPurposeType
     */
    @PatchMapping("{contactMechPurposeTypeId}")
    public void patch(@PathVariable("contactMechPurposeTypeId") String contactMechPurposeTypeId, @RequestBody CreateOrMergePatchContactMechPurposeTypeDto.MergePatchContactMechPurposeTypeDto value) {
        try {

            ContactMechPurposeTypeCommand.MergePatchContactMechPurposeType cmd = value;//.toMergePatchContactMechPurposeType();
            ContactMechPurposeTypeResourceUtils.setNullIdOrThrowOnInconsistentIds(contactMechPurposeTypeId, cmd);
            cmd.setRequesterId(SecurityContextUtil.getRequesterId());
            contactMechPurposeTypeApplicationService.when(cmd);

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }

    @GetMapping("_metadata/filteringFields")
    public List<PropertyMetadataDto> getMetadataFilteringFields() {
        try {

            List<PropertyMetadataDto> filtering = new ArrayList<>();
            ContactMechPurposeTypeMetadata.propertyTypeMap.forEach((key, value) -> {
                filtering.add(new PropertyMetadataDto(key, value, true));
            });
            return filtering;

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }


    protected TypeConverter getCriterionTypeConverter() {
        return new DefaultTypeConverter();
    }

    protected PropertyTypeResolver getPropertyTypeResolver() {
        return new PropertyTypeResolver() {
            @Override
            public Class resolveTypeByPropertyName(String propertyName) {
                return ContactMechPurposeTypeResourceUtils.getFilterPropertyType(propertyName);
            }
        };
    }

 
    public static class ContactMechPurposeTypeResourceUtils {

        public static void setNullIdOrThrowOnInconsistentIds(String contactMechPurposeTypeId, org.dddml.ffvtraceability.domain.contactmechpurposetype.ContactMechPurposeTypeCommand value) {
            String idObj = contactMechPurposeTypeId;
            if (value.getContactMechPurposeTypeId() == null) {
                value.setContactMechPurposeTypeId(idObj);
            } else if (!value.getContactMechPurposeTypeId().equals(idObj)) {
                throw DomainError.named("inconsistentId", "Argument Id %1$s NOT equals body Id %2$s", contactMechPurposeTypeId, value.getContactMechPurposeTypeId());
            }
        }
    
        public static List<String> getQueryOrders(String str, String separator) {
            return QueryParamUtils.getQueryOrders(str, separator, ContactMechPurposeTypeMetadata.aliasMap);
        }

        public static List<String> getQuerySorts(Map<String, String[]> queryNameValuePairs) {
            String[] values = queryNameValuePairs.get("sort");
            return QueryParamUtils.getQuerySorts(values, ContactMechPurposeTypeMetadata.aliasMap);
        }

        public static String getFilterPropertyName(String fieldName) {
            if ("sort".equalsIgnoreCase(fieldName)
                    || "firstResult".equalsIgnoreCase(fieldName)
                    || "maxResults".equalsIgnoreCase(fieldName)
                    || "fields".equalsIgnoreCase(fieldName)) {
                return null;
            }
            if (ContactMechPurposeTypeMetadata.aliasMap.containsKey(fieldName)) {
                return ContactMechPurposeTypeMetadata.aliasMap.get(fieldName);
            }
            return null;
        }

        public static Class getFilterPropertyType(String propertyName) {
            if (ContactMechPurposeTypeMetadata.propertyTypeMap.containsKey(propertyName)) {
                String propertyType = ContactMechPurposeTypeMetadata.propertyTypeMap.get(propertyName);
                if (!StringHelper.isNullOrEmpty(propertyType)) {
                    if (BoundedContextMetadata.CLASS_MAP.containsKey(propertyType)) {
                        return BoundedContextMetadata.CLASS_MAP.get(propertyType);
                    }
                }
            }
            return String.class;
        }

        public static Iterable<Map.Entry<String, Object>> getQueryFilterMap(Map<String, String[]> queryNameValuePairs) {
            Map<String, Object> filter = new HashMap<>();
            queryNameValuePairs.forEach((key, values) -> {
                if (values.length > 0) {
                    String pName = getFilterPropertyName(key);
                    if (!StringHelper.isNullOrEmpty(pName)) {
                        Class pClass = getFilterPropertyType(pName);
                        filter.put(pName, ApplicationContext.current.getTypeConverter().convertFromString(pClass, values[0]));
                    }
                }
            });
            return filter.entrySet();
        }

        public static ContactMechPurposeTypeStateDto[] toContactMechPurposeTypeStateDtoArray(Iterable<String> ids) {
            List<ContactMechPurposeTypeStateDto> states = new ArrayList<>();
            ids.forEach(i -> {
                ContactMechPurposeTypeStateDto dto = new ContactMechPurposeTypeStateDto();
                dto.setContactMechPurposeTypeId(i);
                states.add(dto);
            });
            return states.toArray(new ContactMechPurposeTypeStateDto[0]);
        }

    }

}

