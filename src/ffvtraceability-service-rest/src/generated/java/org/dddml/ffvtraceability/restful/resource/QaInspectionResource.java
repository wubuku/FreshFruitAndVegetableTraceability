// <autogenerated>
//   This file was generated by dddappp code generator.
//   Any changes made to this file manually will be lost next time the file is regenerated.
// </autogenerated>

package org.dddml.ffvtraceability.restful.resource;

import java.util.*;
import java.util.stream.*;
import jakarta.servlet.http.*;
import jakarta.validation.constraints.*;
import org.springframework.http.MediaType;
import org.springframework.http.HttpStatus;
import org.springframework.web.bind.annotation.*;
import org.springframework.transaction.annotation.Transactional;
import org.dddml.support.criterion.*;
import java.time.OffsetDateTime;
import org.dddml.ffvtraceability.domain.*;
import org.dddml.ffvtraceability.specialization.*;
import org.dddml.ffvtraceability.domain.qainspection.*;
import static org.dddml.ffvtraceability.domain.meta.M.*;

import com.fasterxml.jackson.databind.ObjectMapper;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.security.access.prepost.*;
import org.dddml.support.criterion.TypeConverter;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

@RequestMapping(path = "QaInspections", produces = MediaType.APPLICATION_JSON_VALUE)
@RestController
public class QaInspectionResource {
    private Logger logger = LoggerFactory.getLogger(this.getClass());


    @Autowired
    private QaInspectionApplicationService qaInspectionApplicationService;


    /**
     * Retrieve.
     * Retrieve QaInspections
     */
    @GetMapping
    @Transactional(readOnly = true)
    public QaInspectionStateDto[] getAll( HttpServletRequest request,
                    @RequestParam(value = "sort", required = false) String sort,
                    @RequestParam(value = "fields", required = false) String fields,
                    @RequestParam(value = "firstResult", defaultValue = "0") Integer firstResult,
                    @RequestParam(value = "maxResults", defaultValue = "2147483647") Integer maxResults,
                    @RequestParam(value = "filter", required = false) String filter) {
        try {
        if (firstResult < 0) { firstResult = 0; }
        if (maxResults == null || maxResults < 1) { maxResults = Integer.MAX_VALUE; }

            Iterable<QaInspectionState> states = null; 
            CriterionDto criterion = null;
            if (!StringHelper.isNullOrEmpty(filter)) {
                criterion = new ObjectMapper().readValue(filter, CriterionDto.class);
            } else {
                criterion = QueryParamUtils.getQueryCriterionDto(request.getParameterMap().entrySet().stream()
                    .filter(kv -> QaInspectionResourceUtils.getFilterPropertyName(kv.getKey()) != null)
                    .collect(Collectors.toMap(kv -> kv.getKey(), kv -> kv.getValue())));
            }
            Criterion c = CriterionDto.toSubclass(criterion, getCriterionTypeConverter(), getPropertyTypeResolver(), 
                n -> (QaInspectionMetadata.aliasMap.containsKey(n) ? QaInspectionMetadata.aliasMap.get(n) : n));
            states = qaInspectionApplicationService.get(
                c,
                QaInspectionResourceUtils.getQuerySorts(request.getParameterMap()),
                firstResult, maxResults);

            QaInspectionStateDto.DtoConverter dtoConverter = new QaInspectionStateDto.DtoConverter();
            if (StringHelper.isNullOrEmpty(fields)) {
                dtoConverter.setAllFieldsReturned(true);
            } else {
                dtoConverter.setReturnedFieldsString(fields);
            }
            return dtoConverter.toQaInspectionStateDtoArray(states);

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }

    /**
     * Retrieve in pages.
     * Retrieve QaInspections in pages.
     */
    @GetMapping("_page")
    @Transactional(readOnly = true)
    public Page<QaInspectionStateDto> getPage( HttpServletRequest request,
                    @RequestParam(value = "fields", required = false) String fields,
                    @RequestParam(value = "page", defaultValue = "0") Integer page,
                    @RequestParam(value = "size", defaultValue = "20") Integer size,
                    @RequestParam(value = "filter", required = false) String filter) {
        try {
            Integer firstResult = (page == null ? 0 : page) * (size == null ? 20 : size);
            Integer maxResults = (size == null ? 20 : size);
            Iterable<QaInspectionState> states = null; 
            CriterionDto criterion = null;
            if (!StringHelper.isNullOrEmpty(filter)) {
                criterion = new ObjectMapper().readValue(filter, CriterionDto.class);
            } else {
                criterion = QueryParamUtils.getQueryCriterionDto(request.getParameterMap().entrySet().stream()
                    .filter(kv -> QaInspectionResourceUtils.getFilterPropertyName(kv.getKey()) != null)
                    .collect(Collectors.toMap(kv -> kv.getKey(), kv -> kv.getValue())));
            }
            Criterion c = CriterionDto.toSubclass(criterion, getCriterionTypeConverter(), getPropertyTypeResolver(), 
                n -> (QaInspectionMetadata.aliasMap.containsKey(n) ? QaInspectionMetadata.aliasMap.get(n) : n));
            states = qaInspectionApplicationService.get(
                c,
                QaInspectionResourceUtils.getQuerySorts(request.getParameterMap()),
                firstResult, maxResults);
            long count = qaInspectionApplicationService.getCount(c);

            QaInspectionStateDto.DtoConverter dtoConverter = new QaInspectionStateDto.DtoConverter();
            if (StringHelper.isNullOrEmpty(fields)) {
                dtoConverter.setAllFieldsReturned(true);
            } else {
                dtoConverter.setReturnedFieldsString(fields);
            }
            Page.PageImpl<QaInspectionStateDto> statePage =  new Page.PageImpl<>(dtoConverter.toQaInspectionStateDtoList(states), count);
            statePage.setSize(size);
            statePage.setNumber(page);
            return statePage;

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }

    /**
     * Retrieve.
     * Retrieves QaInspection with the specified ID.
     */
    @GetMapping("{qaInspectionId}")
    @Transactional(readOnly = true)
    public QaInspectionStateDto get(@PathVariable("qaInspectionId") String qaInspectionId, @RequestParam(value = "fields", required = false) String fields) {
        try {
            String idObj = qaInspectionId;
            QaInspectionState state = qaInspectionApplicationService.get(idObj);
            if (state == null) { return null; }

            QaInspectionStateDto.DtoConverter dtoConverter = new QaInspectionStateDto.DtoConverter();
            if (StringHelper.isNullOrEmpty(fields)) {
                dtoConverter.setAllFieldsReturned(true);
            } else {
                dtoConverter.setReturnedFieldsString(fields);
            }
            return dtoConverter.toQaInspectionStateDto(state);

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }

    @GetMapping("_count")
    @Transactional(readOnly = true)
    public long getCount( HttpServletRequest request,
                         @RequestParam(value = "filter", required = false) String filter) {
        try {
            long count = 0;
            CriterionDto criterion = null;
            if (!StringHelper.isNullOrEmpty(filter)) {
                criterion = new ObjectMapper().readValue(filter, CriterionDto.class);
            } else {
                criterion = QueryParamUtils.getQueryCriterionDto(request.getParameterMap());
            }
            Criterion c = CriterionDto.toSubclass(criterion,
                getCriterionTypeConverter(), 
                getPropertyTypeResolver(), 
                n -> (QaInspectionMetadata.aliasMap.containsKey(n) ? QaInspectionMetadata.aliasMap.get(n) : n));
            count = qaInspectionApplicationService.getCount(c);
            return count;

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }


    /**
     * Create.
     * Create QaInspection
     */
    @PostMapping @ResponseBody @ResponseStatus(HttpStatus.CREATED)
    public String post(@RequestBody CreateOrMergePatchQaInspectionDto.CreateQaInspectionDto value,  HttpServletResponse response) {
        try {
            QaInspectionCommand.CreateQaInspection cmd = value;//.toCreateQaInspection();
            if (cmd.getQaInspectionId() == null) {
                throw DomainError.named("nullId", "Aggregate Id in cmd is null, aggregate name: %1$s.", "QaInspection");
            }
            String idObj = cmd.getQaInspectionId();
            cmd.setRequesterId(SecurityContextUtil.getRequesterId());
            qaInspectionApplicationService.when(cmd);

            return idObj;
        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }


    /**
     * Create or update.
     * Create or update QaInspection
     */
    @PutMapping("{qaInspectionId}")
    public void put(@PathVariable("qaInspectionId") String qaInspectionId, @RequestBody CreateOrMergePatchQaInspectionDto value) {
        try {
            if (value.getVersion() != null) {
                value.setCommandType(Command.COMMAND_TYPE_MERGE_PATCH);
                QaInspectionCommand.MergePatchQaInspection cmd = (QaInspectionCommand.MergePatchQaInspection) value.toSubclass();
                QaInspectionResourceUtils.setNullIdOrThrowOnInconsistentIds(qaInspectionId, cmd);
                cmd.setRequesterId(SecurityContextUtil.getRequesterId());
                qaInspectionApplicationService.when(cmd);
                return;
            }

            value.setCommandType(Command.COMMAND_TYPE_CREATE);
            QaInspectionCommand.CreateQaInspection cmd = (QaInspectionCommand.CreateQaInspection) value.toSubclass();
            QaInspectionResourceUtils.setNullIdOrThrowOnInconsistentIds(qaInspectionId, cmd);
            cmd.setRequesterId(SecurityContextUtil.getRequesterId());
            qaInspectionApplicationService.when(cmd);

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }


    /**
     * Patch.
     * Patch QaInspection
     */
    @PatchMapping("{qaInspectionId}")
    public void patch(@PathVariable("qaInspectionId") String qaInspectionId, @RequestBody CreateOrMergePatchQaInspectionDto.MergePatchQaInspectionDto value) {
        try {

            QaInspectionCommand.MergePatchQaInspection cmd = value;//.toMergePatchQaInspection();
            QaInspectionResourceUtils.setNullIdOrThrowOnInconsistentIds(qaInspectionId, cmd);
            cmd.setRequesterId(SecurityContextUtil.getRequesterId());
            qaInspectionApplicationService.when(cmd);

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }

    @GetMapping("_metadata/filteringFields")
    public List<PropertyMetadataDto> getMetadataFilteringFields() {
        try {

            List<PropertyMetadataDto> filtering = new ArrayList<>();
            QaInspectionMetadata.propertyTypeMap.forEach((key, value) -> {
                filtering.add(new PropertyMetadataDto(key, value, true));
            });
            return filtering;

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }

    @GetMapping("{qaInspectionId}/_events/{version}")
    @Transactional(readOnly = true)
    public QaInspectionEvent getEvent(@PathVariable("qaInspectionId") String qaInspectionId, @PathVariable("version") long version) {
        try {

            String idObj = qaInspectionId;
            //QaInspectionStateEventDtoConverter dtoConverter = getQaInspectionStateEventDtoConverter();
            return qaInspectionApplicationService.getEvent(idObj, version);

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }

    @GetMapping("{qaInspectionId}/_historyStates/{version}")
    @Transactional(readOnly = true)
    public QaInspectionStateDto getHistoryState(@PathVariable("qaInspectionId") String qaInspectionId, @PathVariable("version") long version, @RequestParam(value = "fields", required = false) String fields) {
        try {

            String idObj = qaInspectionId;
            QaInspectionStateDto.DtoConverter dtoConverter = new QaInspectionStateDto.DtoConverter();
            if (StringHelper.isNullOrEmpty(fields)) {
                dtoConverter.setAllFieldsReturned(true);
            } else {
                dtoConverter.setReturnedFieldsString(fields);
            }
            return dtoConverter.toQaInspectionStateDto(qaInspectionApplicationService.getHistoryState(idObj, version));

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }



    //protected  QaInspectionStateEventDtoConverter getQaInspectionStateEventDtoConverter() {
    //    return new QaInspectionStateEventDtoConverter();
    //}

    protected TypeConverter getCriterionTypeConverter() {
        return new DefaultTypeConverter();
    }

    protected PropertyTypeResolver getPropertyTypeResolver() {
        return new PropertyTypeResolver() {
            @Override
            public Class resolveTypeByPropertyName(String propertyName) {
                return QaInspectionResourceUtils.getFilterPropertyType(propertyName);
            }
        };
    }

 
    public static class QaInspectionResourceUtils {

        public static void setNullIdOrThrowOnInconsistentIds(String qaInspectionId, org.dddml.ffvtraceability.domain.qainspection.QaInspectionCommand value) {
            String idObj = qaInspectionId;
            if (value.getQaInspectionId() == null) {
                value.setQaInspectionId(idObj);
            } else if (!value.getQaInspectionId().equals(idObj)) {
                throw DomainError.named("inconsistentId", "Argument Id %1$s NOT equals body Id %2$s", qaInspectionId, value.getQaInspectionId());
            }
        }
    
        public static List<String> getQueryOrders(String str, String separator) {
            return QueryParamUtils.getQueryOrders(str, separator, QaInspectionMetadata.aliasMap);
        }

        public static List<String> getQuerySorts(Map<String, String[]> queryNameValuePairs) {
            String[] values = queryNameValuePairs.get("sort");
            return QueryParamUtils.getQuerySorts(values, QaInspectionMetadata.aliasMap);
        }

        public static String getFilterPropertyName(String fieldName) {
            if ("sort".equalsIgnoreCase(fieldName)
                    || "firstResult".equalsIgnoreCase(fieldName)
                    || "maxResults".equalsIgnoreCase(fieldName)
                    || "fields".equalsIgnoreCase(fieldName)) {
                return null;
            }
            if (QaInspectionMetadata.aliasMap.containsKey(fieldName)) {
                return QaInspectionMetadata.aliasMap.get(fieldName);
            }
            return null;
        }

        public static Class getFilterPropertyType(String propertyName) {
            if (QaInspectionMetadata.propertyTypeMap.containsKey(propertyName)) {
                String propertyType = QaInspectionMetadata.propertyTypeMap.get(propertyName);
                if (!StringHelper.isNullOrEmpty(propertyType)) {
                    if (BoundedContextMetadata.CLASS_MAP.containsKey(propertyType)) {
                        return BoundedContextMetadata.CLASS_MAP.get(propertyType);
                    }
                }
            }
            return String.class;
        }

        public static Iterable<Map.Entry<String, Object>> getQueryFilterMap(Map<String, String[]> queryNameValuePairs) {
            Map<String, Object> filter = new HashMap<>();
            queryNameValuePairs.forEach((key, values) -> {
                if (values.length > 0) {
                    String pName = getFilterPropertyName(key);
                    if (!StringHelper.isNullOrEmpty(pName)) {
                        Class pClass = getFilterPropertyType(pName);
                        filter.put(pName, ApplicationContext.current.getTypeConverter().convertFromString(pClass, values[0]));
                    }
                }
            });
            return filter.entrySet();
        }

        public static QaInspectionStateDto[] toQaInspectionStateDtoArray(Iterable<String> ids) {
            List<QaInspectionStateDto> states = new ArrayList<>();
            ids.forEach(i -> {
                QaInspectionStateDto dto = new QaInspectionStateDto();
                dto.setQaInspectionId(i);
                states.add(dto);
            });
            return states.toArray(new QaInspectionStateDto[0]);
        }

    }

}

