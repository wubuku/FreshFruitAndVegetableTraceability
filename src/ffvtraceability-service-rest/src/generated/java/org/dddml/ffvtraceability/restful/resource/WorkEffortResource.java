// <autogenerated>
//   This file was generated by dddappp code generator.
//   Any changes made to this file manually will be lost next time the file is regenerated.
// </autogenerated>

package org.dddml.ffvtraceability.restful.resource;

import java.util.*;
import java.util.stream.*;
import jakarta.servlet.http.*;
import jakarta.validation.constraints.*;
import org.springframework.http.MediaType;
import org.springframework.http.HttpStatus;
import org.springframework.web.bind.annotation.*;
import org.springframework.transaction.annotation.Transactional;
import org.dddml.support.criterion.*;
import java.time.OffsetDateTime;
import org.dddml.ffvtraceability.domain.*;
import org.dddml.ffvtraceability.specialization.*;
import org.dddml.ffvtraceability.domain.workeffort.*;
import static org.dddml.ffvtraceability.domain.meta.M.*;

import com.fasterxml.jackson.databind.ObjectMapper;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.security.access.prepost.*;
import org.dddml.support.criterion.TypeConverter;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

@RequestMapping(path = "WorkEfforts", produces = MediaType.APPLICATION_JSON_VALUE)
@RestController
public class WorkEffortResource {
    private Logger logger = LoggerFactory.getLogger(this.getClass());

    private static CriterionDto deserializeCriterionDto(String filter) {
        return deserializeJsonArgument(filter, CriterionDto.class);
    }

    private static <T> T deserializeJsonArgument(String s, Class<T> aClass) {
        try {
            return new ObjectMapper().readValue(s, aClass);
        } catch (com.fasterxml.jackson.core.JsonProcessingException e) {
            throw new IllegalArgumentException(e);
        }
    }


    @Autowired
    private WorkEffortApplicationService workEffortApplicationService;


    /**
     * Retrieve.
     * Retrieve WorkEfforts
     */
    @GetMapping
    @Transactional(readOnly = true)
    public WorkEffortStateDto[] getAll( HttpServletRequest request,
                    @RequestParam(value = "sort", required = false) String sort,
                    @RequestParam(value = "fields", required = false) String fields,
                    @RequestParam(value = "firstResult", defaultValue = "0") Integer firstResult,
                    @RequestParam(value = "maxResults", defaultValue = "2147483647") Integer maxResults,
                    @RequestParam(value = "filter", required = false) String filter) {
        
        if (firstResult < 0) { firstResult = 0; }
        if (maxResults == null || maxResults < 1) { maxResults = Integer.MAX_VALUE; }

            Iterable<WorkEffortState> states = null; 
            CriterionDto criterion = null;
            if (!StringHelper.isNullOrEmpty(filter)) {
                criterion = deserializeCriterionDto(filter);
            } else {
                criterion = QueryParamUtils.getQueryCriterionDto(request.getParameterMap().entrySet().stream()
                    .filter(kv -> WorkEffortResourceUtils.getFilterPropertyName(kv.getKey()) != null)
                    .collect(Collectors.toMap(kv -> kv.getKey(), kv -> kv.getValue())));
            }
            Criterion c = CriterionDto.toSubclass(criterion, getCriterionTypeConverter(), getPropertyTypeResolver(), 
                n -> (WorkEffortMetadata.aliasMap.containsKey(n) ? WorkEffortMetadata.aliasMap.get(n) : n));
            states = workEffortApplicationService.get(
                c,
                WorkEffortResourceUtils.getQuerySorts(request.getParameterMap()),
                firstResult, maxResults);

            WorkEffortStateDto.DtoConverter dtoConverter = new WorkEffortStateDto.DtoConverter();
            if (StringHelper.isNullOrEmpty(fields)) {
                dtoConverter.setAllFieldsReturned(true);
            } else {
                dtoConverter.setReturnedFieldsString(fields);
            }
            return dtoConverter.toWorkEffortStateDtoArray(states);

        
    }

    /**
     * Retrieve in pages.
     * Retrieve WorkEfforts in pages.
     */
    @GetMapping("_page")
    @Transactional(readOnly = true)
    public Page<WorkEffortStateDto> getPage( HttpServletRequest request,
                    @RequestParam(value = "fields", required = false) String fields,
                    @RequestParam(value = "page", defaultValue = "0") Integer page,
                    @RequestParam(value = "size", defaultValue = "20") Integer size,
                    @RequestParam(value = "filter", required = false) String filter) {
        
            Integer firstResult = (page == null ? 0 : page) * (size == null ? 20 : size);
            Integer maxResults = (size == null ? 20 : size);
            Iterable<WorkEffortState> states = null; 
            CriterionDto criterion = null;
            if (!StringHelper.isNullOrEmpty(filter)) {
                criterion = deserializeCriterionDto(filter);
            } else {
                criterion = QueryParamUtils.getQueryCriterionDto(request.getParameterMap().entrySet().stream()
                    .filter(kv -> WorkEffortResourceUtils.getFilterPropertyName(kv.getKey()) != null)
                    .collect(Collectors.toMap(kv -> kv.getKey(), kv -> kv.getValue())));
            }
            Criterion c = CriterionDto.toSubclass(criterion, getCriterionTypeConverter(), getPropertyTypeResolver(), 
                n -> (WorkEffortMetadata.aliasMap.containsKey(n) ? WorkEffortMetadata.aliasMap.get(n) : n));
            states = workEffortApplicationService.get(
                c,
                WorkEffortResourceUtils.getQuerySorts(request.getParameterMap()),
                firstResult, maxResults);
            long count = workEffortApplicationService.getCount(c);

            WorkEffortStateDto.DtoConverter dtoConverter = new WorkEffortStateDto.DtoConverter();
            if (StringHelper.isNullOrEmpty(fields)) {
                dtoConverter.setAllFieldsReturned(true);
            } else {
                dtoConverter.setReturnedFieldsString(fields);
            }
            Page.PageImpl<WorkEffortStateDto> statePage =  new Page.PageImpl<>(dtoConverter.toWorkEffortStateDtoList(states), count);
            statePage.setSize(size);
            statePage.setNumber(page);
            return statePage;

        
    }

    /**
     * Retrieve.
     * Retrieves WorkEffort with the specified ID.
     */
    @GetMapping("{workEffortId}")
    @Transactional(readOnly = true)
    public WorkEffortStateDto get(@PathVariable("workEffortId") String workEffortId, @RequestParam(value = "fields", required = false) String fields) {
        
            String idObj = workEffortId;
            WorkEffortState state = workEffortApplicationService.get(idObj);
            if (state == null) { return null; }

            WorkEffortStateDto.DtoConverter dtoConverter = new WorkEffortStateDto.DtoConverter();
            if (StringHelper.isNullOrEmpty(fields)) {
                dtoConverter.setAllFieldsReturned(true);
            } else {
                dtoConverter.setReturnedFieldsString(fields);
            }
            return dtoConverter.toWorkEffortStateDto(state);

        
    }

    @GetMapping("_count")
    @Transactional(readOnly = true)
    public long getCount( HttpServletRequest request,
                         @RequestParam(value = "filter", required = false) String filter) {
        
            long count = 0;
            CriterionDto criterion = null;
            if (!StringHelper.isNullOrEmpty(filter)) {
                criterion = deserializeCriterionDto(filter);
            } else {
                criterion = QueryParamUtils.getQueryCriterionDto(request.getParameterMap());
            }
            Criterion c = CriterionDto.toSubclass(criterion,
                getCriterionTypeConverter(), 
                getPropertyTypeResolver(), 
                n -> (WorkEffortMetadata.aliasMap.containsKey(n) ? WorkEffortMetadata.aliasMap.get(n) : n));
            count = workEffortApplicationService.getCount(c);
            return count;

        
    }


    /**
     * Create.
     * Create WorkEffort
     */
    @PostMapping @ResponseBody @ResponseStatus(HttpStatus.CREATED)
    public String post(@RequestBody CreateOrMergePatchWorkEffortDto.CreateWorkEffortDto value,  HttpServletResponse response) {
        
            WorkEffortCommand.CreateWorkEffort cmd = value;//.toCreateWorkEffort();
            if (cmd.getWorkEffortId() == null) {
                throw DomainError.named("nullId", "Aggregate Id in cmd is null, aggregate name: %1$s.", "WorkEffort");
            }
            String idObj = cmd.getWorkEffortId();
            cmd.setRequesterId(SecurityContextUtil.getRequesterId());
            workEffortApplicationService.when(cmd);

            return idObj;
        
    }


    /**
     * Create or update.
     * Create or update WorkEffort
     */
    @PutMapping("{workEffortId}")
    public void put(@PathVariable("workEffortId") String workEffortId, @RequestBody CreateOrMergePatchWorkEffortDto value) {
        
            if (value.getVersion() != null) {
                value.setCommandType(Command.COMMAND_TYPE_MERGE_PATCH);
                WorkEffortCommand.MergePatchWorkEffort cmd = (WorkEffortCommand.MergePatchWorkEffort) value.toSubclass();
                WorkEffortResourceUtils.setNullIdOrThrowOnInconsistentIds(workEffortId, cmd);
                cmd.setRequesterId(SecurityContextUtil.getRequesterId());
                workEffortApplicationService.when(cmd);
                return;
            }

            value.setCommandType(Command.COMMAND_TYPE_CREATE);
            WorkEffortCommand.CreateWorkEffort cmd = (WorkEffortCommand.CreateWorkEffort) value.toSubclass();
            WorkEffortResourceUtils.setNullIdOrThrowOnInconsistentIds(workEffortId, cmd);
            cmd.setRequesterId(SecurityContextUtil.getRequesterId());
            workEffortApplicationService.when(cmd);

        
    }


    /**
     * Patch.
     * Patch WorkEffort
     */
    @PatchMapping("{workEffortId}")
    public void patch(@PathVariable("workEffortId") String workEffortId, @RequestBody CreateOrMergePatchWorkEffortDto.MergePatchWorkEffortDto value) {
        

            WorkEffortCommand.MergePatchWorkEffort cmd = value;//.toMergePatchWorkEffort();
            WorkEffortResourceUtils.setNullIdOrThrowOnInconsistentIds(workEffortId, cmd);
            cmd.setRequesterId(SecurityContextUtil.getRequesterId());
            workEffortApplicationService.when(cmd);

        
    }

    @GetMapping("_metadata/filteringFields")
    public List<PropertyMetadataDto> getMetadataFilteringFields() {
        

            List<PropertyMetadataDto> filtering = new ArrayList<>();
            WorkEffortMetadata.propertyTypeMap.forEach((key, value) -> {
                filtering.add(new PropertyMetadataDto(key, value, true));
            });
            return filtering;

        
    }

    @GetMapping("{workEffortId}/_events/{version}")
    @Transactional(readOnly = true)
    public WorkEffortEvent getEvent(@PathVariable("workEffortId") String workEffortId, @PathVariable("version") long version) {
        

            String idObj = workEffortId;
            //WorkEffortStateEventDtoConverter dtoConverter = getWorkEffortStateEventDtoConverter();
            return workEffortApplicationService.getEvent(idObj, version);

        
    }

    @GetMapping("{workEffortId}/_historyStates/{version}")
    @Transactional(readOnly = true)
    public WorkEffortStateDto getHistoryState(@PathVariable("workEffortId") String workEffortId, @PathVariable("version") long version, @RequestParam(value = "fields", required = false) String fields) {
        

            String idObj = workEffortId;
            WorkEffortStateDto.DtoConverter dtoConverter = new WorkEffortStateDto.DtoConverter();
            if (StringHelper.isNullOrEmpty(fields)) {
                dtoConverter.setAllFieldsReturned(true);
            } else {
                dtoConverter.setReturnedFieldsString(fields);
            }
            return dtoConverter.toWorkEffortStateDto(workEffortApplicationService.getHistoryState(idObj, version));

        
    }



    //protected  WorkEffortStateEventDtoConverter getWorkEffortStateEventDtoConverter() {
    //    return new WorkEffortStateEventDtoConverter();
    //}

    protected TypeConverter getCriterionTypeConverter() {
        return new DefaultTypeConverter();
    }

    protected PropertyTypeResolver getPropertyTypeResolver() {
        return new PropertyTypeResolver() {
            @Override
            public Class resolveTypeByPropertyName(String propertyName) {
                return WorkEffortResourceUtils.getFilterPropertyType(propertyName);
            }
        };
    }

 
    public static class WorkEffortResourceUtils {

        public static void setNullIdOrThrowOnInconsistentIds(String workEffortId, org.dddml.ffvtraceability.domain.workeffort.WorkEffortCommand value) {
            String idObj = workEffortId;
            if (value.getWorkEffortId() == null) {
                value.setWorkEffortId(idObj);
            } else if (!value.getWorkEffortId().equals(idObj)) {
                throw DomainError.named("inconsistentId", "Argument Id %1$s NOT equals body Id %2$s", workEffortId, value.getWorkEffortId());
            }
        }
    
        public static List<String> getQueryOrders(String str, String separator) {
            return QueryParamUtils.getQueryOrders(str, separator, WorkEffortMetadata.aliasMap);
        }

        public static List<String> getQuerySorts(Map<String, String[]> queryNameValuePairs) {
            String[] values = queryNameValuePairs.get("sort");
            return QueryParamUtils.getQuerySorts(values, WorkEffortMetadata.aliasMap);
        }

        public static String getFilterPropertyName(String fieldName) {
            if ("sort".equalsIgnoreCase(fieldName)
                    || "firstResult".equalsIgnoreCase(fieldName)
                    || "maxResults".equalsIgnoreCase(fieldName)
                    || "fields".equalsIgnoreCase(fieldName)) {
                return null;
            }
            if (WorkEffortMetadata.aliasMap.containsKey(fieldName)) {
                return WorkEffortMetadata.aliasMap.get(fieldName);
            }
            return null;
        }

        public static Class getFilterPropertyType(String propertyName) {
            if (WorkEffortMetadata.propertyTypeMap.containsKey(propertyName)) {
                String propertyType = WorkEffortMetadata.propertyTypeMap.get(propertyName);
                if (!StringHelper.isNullOrEmpty(propertyType)) {
                    if (BoundedContextMetadata.CLASS_MAP.containsKey(propertyType)) {
                        return BoundedContextMetadata.CLASS_MAP.get(propertyType);
                    }
                }
            }
            return String.class;
        }

        public static Iterable<Map.Entry<String, Object>> getQueryFilterMap(Map<String, String[]> queryNameValuePairs) {
            Map<String, Object> filter = new HashMap<>();
            queryNameValuePairs.forEach((key, values) -> {
                if (values.length > 0) {
                    String pName = getFilterPropertyName(key);
                    if (!StringHelper.isNullOrEmpty(pName)) {
                        Class pClass = getFilterPropertyType(pName);
                        filter.put(pName, ApplicationContext.current.getTypeConverter().convertFromString(pClass, values[0]));
                    }
                }
            });
            return filter.entrySet();
        }

        public static WorkEffortStateDto[] toWorkEffortStateDtoArray(Iterable<String> ids) {
            List<WorkEffortStateDto> states = new ArrayList<>();
            ids.forEach(i -> {
                WorkEffortStateDto dto = new WorkEffortStateDto();
                dto.setWorkEffortId(i);
                states.add(dto);
            });
            return states.toArray(new WorkEffortStateDto[0]);
        }

    }

}

