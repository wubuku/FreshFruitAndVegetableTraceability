// <autogenerated>
//   This file was generated by dddappp code generator.
//   Any changes made to this file manually will be lost next time the file is regenerated.
// </autogenerated>

package org.dddml.ffvtraceability.restful.resource;

import java.util.*;
import java.util.stream.*;
import jakarta.servlet.http.*;
import jakarta.validation.constraints.*;
import org.springframework.http.MediaType;
import org.springframework.http.HttpStatus;
import org.springframework.web.bind.annotation.*;
import org.springframework.transaction.annotation.Transactional;
import org.dddml.support.criterion.*;
import java.time.OffsetDateTime;
import org.dddml.ffvtraceability.domain.*;
import org.dddml.ffvtraceability.specialization.*;
import org.dddml.ffvtraceability.domain.documentnumbergenerator.*;
import static org.dddml.ffvtraceability.domain.meta.M.*;

import com.fasterxml.jackson.databind.ObjectMapper;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.security.access.prepost.*;
import org.dddml.support.criterion.TypeConverter;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

@RequestMapping(path = "DocumentNumberGenerators", produces = MediaType.APPLICATION_JSON_VALUE)
@RestController
public class DocumentNumberGeneratorResource {
    private Logger logger = LoggerFactory.getLogger(this.getClass());

    private static CriterionDto deserializeCriterionDto(String filter) {
        return deserializeJsonArgument(filter, CriterionDto.class);
    }

    private static <T> T deserializeJsonArgument(String s, Class<T> aClass) {
        try {
            return new ObjectMapper().readValue(s, aClass);
        } catch (com.fasterxml.jackson.core.JsonProcessingException e) {
            throw new IllegalArgumentException(e);
        }
    }


    @Autowired
    private DocumentNumberGeneratorApplicationService documentNumberGeneratorApplicationService;


    /**
     * Retrieve.
     * Retrieve DocumentNumberGenerators
     */
    @GetMapping
    @Transactional(readOnly = true)
    public DocumentNumberGeneratorStateDto[] getAll( HttpServletRequest request,
                    @RequestParam(value = "sort", required = false) String sort,
                    @RequestParam(value = "fields", required = false) String fields,
                    @RequestParam(value = "firstResult", defaultValue = "0") Integer firstResult,
                    @RequestParam(value = "maxResults", defaultValue = "2147483647") Integer maxResults,
                    @RequestParam(value = "filter", required = false) String filter) {
        
        if (firstResult < 0) { firstResult = 0; }
        if (maxResults == null || maxResults < 1) { maxResults = Integer.MAX_VALUE; }

            Iterable<DocumentNumberGeneratorState> states = null; 
            CriterionDto criterion = null;
            if (!StringHelper.isNullOrEmpty(filter)) {
                criterion = deserializeCriterionDto(filter);
            } else {
                criterion = QueryParamUtils.getQueryCriterionDto(request.getParameterMap().entrySet().stream()
                    .filter(kv -> DocumentNumberGeneratorResourceUtils.getFilterPropertyName(kv.getKey()) != null)
                    .collect(Collectors.toMap(kv -> kv.getKey(), kv -> kv.getValue())));
            }
            Criterion c = CriterionDto.toSubclass(criterion, getCriterionTypeConverter(), getPropertyTypeResolver(), 
                n -> (DocumentNumberGeneratorMetadata.aliasMap.containsKey(n) ? DocumentNumberGeneratorMetadata.aliasMap.get(n) : n));
            states = documentNumberGeneratorApplicationService.get(
                c,
                DocumentNumberGeneratorResourceUtils.getQuerySorts(request.getParameterMap()),
                firstResult, maxResults);

            DocumentNumberGeneratorStateDto.DtoConverter dtoConverter = new DocumentNumberGeneratorStateDto.DtoConverter();
            if (StringHelper.isNullOrEmpty(fields)) {
                dtoConverter.setAllFieldsReturned(true);
            } else {
                dtoConverter.setReturnedFieldsString(fields);
            }
            return dtoConverter.toDocumentNumberGeneratorStateDtoArray(states);

        
    }

    /**
     * Retrieve in pages.
     * Retrieve DocumentNumberGenerators in pages.
     */
    @GetMapping("_page")
    @Transactional(readOnly = true)
    public Page<DocumentNumberGeneratorStateDto> getPage( HttpServletRequest request,
                    @RequestParam(value = "fields", required = false) String fields,
                    @RequestParam(value = "page", defaultValue = "0") Integer page,
                    @RequestParam(value = "size", defaultValue = "20") Integer size,
                    @RequestParam(value = "filter", required = false) String filter) {
        
            Integer firstResult = (page == null ? 0 : page) * (size == null ? 20 : size);
            Integer maxResults = (size == null ? 20 : size);
            Iterable<DocumentNumberGeneratorState> states = null; 
            CriterionDto criterion = null;
            if (!StringHelper.isNullOrEmpty(filter)) {
                criterion = deserializeCriterionDto(filter);
            } else {
                criterion = QueryParamUtils.getQueryCriterionDto(request.getParameterMap().entrySet().stream()
                    .filter(kv -> DocumentNumberGeneratorResourceUtils.getFilterPropertyName(kv.getKey()) != null)
                    .collect(Collectors.toMap(kv -> kv.getKey(), kv -> kv.getValue())));
            }
            Criterion c = CriterionDto.toSubclass(criterion, getCriterionTypeConverter(), getPropertyTypeResolver(), 
                n -> (DocumentNumberGeneratorMetadata.aliasMap.containsKey(n) ? DocumentNumberGeneratorMetadata.aliasMap.get(n) : n));
            states = documentNumberGeneratorApplicationService.get(
                c,
                DocumentNumberGeneratorResourceUtils.getQuerySorts(request.getParameterMap()),
                firstResult, maxResults);
            long count = documentNumberGeneratorApplicationService.getCount(c);

            DocumentNumberGeneratorStateDto.DtoConverter dtoConverter = new DocumentNumberGeneratorStateDto.DtoConverter();
            if (StringHelper.isNullOrEmpty(fields)) {
                dtoConverter.setAllFieldsReturned(true);
            } else {
                dtoConverter.setReturnedFieldsString(fields);
            }
            Page.PageImpl<DocumentNumberGeneratorStateDto> statePage =  new Page.PageImpl<>(dtoConverter.toDocumentNumberGeneratorStateDtoList(states), count);
            statePage.setSize(size);
            statePage.setNumber(page);
            return statePage;

        
    }

    /**
     * Retrieve.
     * Retrieves DocumentNumberGenerator with the specified ID.
     */
    @GetMapping("{generatorId}")
    @Transactional(readOnly = true)
    public DocumentNumberGeneratorStateDto get(@PathVariable("generatorId") String generatorId, @RequestParam(value = "fields", required = false) String fields) {
        
            String idObj = generatorId;
            DocumentNumberGeneratorState state = documentNumberGeneratorApplicationService.get(idObj);
            if (state == null) { return null; }

            DocumentNumberGeneratorStateDto.DtoConverter dtoConverter = new DocumentNumberGeneratorStateDto.DtoConverter();
            if (StringHelper.isNullOrEmpty(fields)) {
                dtoConverter.setAllFieldsReturned(true);
            } else {
                dtoConverter.setReturnedFieldsString(fields);
            }
            return dtoConverter.toDocumentNumberGeneratorStateDto(state);

        
    }

    @GetMapping("_count")
    @Transactional(readOnly = true)
    public long getCount( HttpServletRequest request,
                         @RequestParam(value = "filter", required = false) String filter) {
        
            long count = 0;
            CriterionDto criterion = null;
            if (!StringHelper.isNullOrEmpty(filter)) {
                criterion = deserializeCriterionDto(filter);
            } else {
                criterion = QueryParamUtils.getQueryCriterionDto(request.getParameterMap());
            }
            Criterion c = CriterionDto.toSubclass(criterion,
                getCriterionTypeConverter(), 
                getPropertyTypeResolver(), 
                n -> (DocumentNumberGeneratorMetadata.aliasMap.containsKey(n) ? DocumentNumberGeneratorMetadata.aliasMap.get(n) : n));
            count = documentNumberGeneratorApplicationService.getCount(c);
            return count;

        
    }


    @PostMapping("{generatorId}/_commands/GenerateNextNumber")
    public String generateNextNumber(@PathVariable("generatorId") String generatorId, @RequestBody DocumentNumberGeneratorCommands.GenerateNextNumber content) {
        

            DocumentNumberGeneratorCommands.GenerateNextNumber cmd = content;//.toGenerateNextNumber();
            String idObj = generatorId;
            if (cmd.getGeneratorId() == null) {
                cmd.setGeneratorId(idObj);
            } else if (!cmd.getGeneratorId().equals(idObj)) {
                throw DomainError.named("inconsistentId", "Argument Id %1$s NOT equals body Id %2$s", generatorId, cmd.getGeneratorId());
            }
            cmd.setRequesterId(SecurityContextUtil.getRequesterId());
            return documentNumberGeneratorApplicationService.when(cmd);

        
    }

    @GetMapping("_metadata/filteringFields")
    public List<PropertyMetadataDto> getMetadataFilteringFields() {
        

            List<PropertyMetadataDto> filtering = new ArrayList<>();
            DocumentNumberGeneratorMetadata.propertyTypeMap.forEach((key, value) -> {
                filtering.add(new PropertyMetadataDto(key, value, true));
            });
            return filtering;

        
    }


    protected TypeConverter getCriterionTypeConverter() {
        return new DefaultTypeConverter();
    }

    protected PropertyTypeResolver getPropertyTypeResolver() {
        return new PropertyTypeResolver() {
            @Override
            public Class resolveTypeByPropertyName(String propertyName) {
                return DocumentNumberGeneratorResourceUtils.getFilterPropertyType(propertyName);
            }
        };
    }

 
    public static class DocumentNumberGeneratorResourceUtils {

        public static void setNullIdOrThrowOnInconsistentIds(String generatorId, org.dddml.ffvtraceability.domain.documentnumbergenerator.DocumentNumberGeneratorCommand value) {
            String idObj = generatorId;
            if (value.getGeneratorId() == null) {
                value.setGeneratorId(idObj);
            } else if (!value.getGeneratorId().equals(idObj)) {
                throw DomainError.named("inconsistentId", "Argument Id %1$s NOT equals body Id %2$s", generatorId, value.getGeneratorId());
            }
        }
    
        public static List<String> getQueryOrders(String str, String separator) {
            return QueryParamUtils.getQueryOrders(str, separator, DocumentNumberGeneratorMetadata.aliasMap);
        }

        public static List<String> getQuerySorts(Map<String, String[]> queryNameValuePairs) {
            String[] values = queryNameValuePairs.get("sort");
            return QueryParamUtils.getQuerySorts(values, DocumentNumberGeneratorMetadata.aliasMap);
        }

        public static String getFilterPropertyName(String fieldName) {
            if ("sort".equalsIgnoreCase(fieldName)
                    || "firstResult".equalsIgnoreCase(fieldName)
                    || "maxResults".equalsIgnoreCase(fieldName)
                    || "fields".equalsIgnoreCase(fieldName)) {
                return null;
            }
            if (DocumentNumberGeneratorMetadata.aliasMap.containsKey(fieldName)) {
                return DocumentNumberGeneratorMetadata.aliasMap.get(fieldName);
            }
            return null;
        }

        public static Class getFilterPropertyType(String propertyName) {
            if (DocumentNumberGeneratorMetadata.propertyTypeMap.containsKey(propertyName)) {
                String propertyType = DocumentNumberGeneratorMetadata.propertyTypeMap.get(propertyName);
                if (!StringHelper.isNullOrEmpty(propertyType)) {
                    if (BoundedContextMetadata.CLASS_MAP.containsKey(propertyType)) {
                        return BoundedContextMetadata.CLASS_MAP.get(propertyType);
                    }
                }
            }
            return String.class;
        }

        public static Iterable<Map.Entry<String, Object>> getQueryFilterMap(Map<String, String[]> queryNameValuePairs) {
            Map<String, Object> filter = new HashMap<>();
            queryNameValuePairs.forEach((key, values) -> {
                if (values.length > 0) {
                    String pName = getFilterPropertyName(key);
                    if (!StringHelper.isNullOrEmpty(pName)) {
                        Class pClass = getFilterPropertyType(pName);
                        filter.put(pName, ApplicationContext.current.getTypeConverter().convertFromString(pClass, values[0]));
                    }
                }
            });
            return filter.entrySet();
        }

        public static DocumentNumberGeneratorStateDto[] toDocumentNumberGeneratorStateDtoArray(Iterable<String> ids) {
            List<DocumentNumberGeneratorStateDto> states = new ArrayList<>();
            ids.forEach(i -> {
                DocumentNumberGeneratorStateDto dto = new DocumentNumberGeneratorStateDto();
                dto.setGeneratorId(i);
                states.add(dto);
            });
            return states.toArray(new DocumentNumberGeneratorStateDto[0]);
        }

    }

}

