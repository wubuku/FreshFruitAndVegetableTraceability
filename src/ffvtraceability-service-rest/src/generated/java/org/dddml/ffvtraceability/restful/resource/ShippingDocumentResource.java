// <autogenerated>
//   This file was generated by dddappp code generator.
//   Any changes made to this file manually will be lost next time the file is regenerated.
// </autogenerated>

package org.dddml.ffvtraceability.restful.resource;

import java.util.*;
import java.util.stream.*;
import jakarta.servlet.http.*;
import jakarta.validation.constraints.*;
import org.springframework.http.MediaType;
import org.springframework.http.HttpStatus;
import org.springframework.web.bind.annotation.*;
import org.springframework.transaction.annotation.Transactional;
import org.dddml.support.criterion.*;
import java.time.OffsetDateTime;
import org.dddml.ffvtraceability.domain.*;
import org.dddml.ffvtraceability.specialization.*;
import org.dddml.ffvtraceability.domain.shippingdocument.*;
import static org.dddml.ffvtraceability.domain.meta.M.*;

import com.fasterxml.jackson.databind.ObjectMapper;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.security.access.prepost.*;
import org.dddml.support.criterion.TypeConverter;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

@RequestMapping(path = "ShippingDocuments", produces = MediaType.APPLICATION_JSON_VALUE)
@RestController
public class ShippingDocumentResource {
    private Logger logger = LoggerFactory.getLogger(this.getClass());

    private static CriterionDto deserializeCriterionDto(String filter) {
        return deserializeJsonArgument(filter, CriterionDto.class);
    }

    private static <T> T deserializeJsonArgument(String s, Class<T> aClass) {
        try {
            return new ObjectMapper().readValue(s, aClass);
        } catch (com.fasterxml.jackson.core.JsonProcessingException e) {
            throw new IllegalArgumentException(e);
        }
    }


    @Autowired
    private ShippingDocumentApplicationService shippingDocumentApplicationService;


    /**
     * Retrieve.
     * Retrieve ShippingDocuments
     */
    @GetMapping
    @Transactional(readOnly = true)
    public ShippingDocumentStateDto[] getAll( HttpServletRequest request,
                    @RequestParam(value = "sort", required = false) String sort,
                    @RequestParam(value = "fields", required = false) String fields,
                    @RequestParam(value = "firstResult", defaultValue = "0") Integer firstResult,
                    @RequestParam(value = "maxResults", defaultValue = "2147483647") Integer maxResults,
                    @RequestParam(value = "filter", required = false) String filter) {
        
        if (firstResult < 0) { firstResult = 0; }
        if (maxResults == null || maxResults < 1) { maxResults = Integer.MAX_VALUE; }

            Iterable<ShippingDocumentState> states = null; 
            CriterionDto criterion = null;
            if (!StringHelper.isNullOrEmpty(filter)) {
                criterion = deserializeCriterionDto(filter);
            } else {
                criterion = QueryParamUtils.getQueryCriterionDto(request.getParameterMap().entrySet().stream()
                    .filter(kv -> ShippingDocumentResourceUtils.getFilterPropertyName(kv.getKey()) != null)
                    .collect(Collectors.toMap(kv -> kv.getKey(), kv -> kv.getValue())));
            }
            Criterion c = CriterionDto.toSubclass(criterion, getCriterionTypeConverter(), getPropertyTypeResolver(), 
                n -> (ShippingDocumentMetadata.aliasMap.containsKey(n) ? ShippingDocumentMetadata.aliasMap.get(n) : n));
            states = shippingDocumentApplicationService.get(
                c,
                ShippingDocumentResourceUtils.getQuerySorts(request.getParameterMap()),
                firstResult, maxResults);

            ShippingDocumentStateDto.DtoConverter dtoConverter = new ShippingDocumentStateDto.DtoConverter();
            if (StringHelper.isNullOrEmpty(fields)) {
                dtoConverter.setAllFieldsReturned(true);
            } else {
                dtoConverter.setReturnedFieldsString(fields);
            }
            return dtoConverter.toShippingDocumentStateDtoArray(states);

        
    }

    /**
     * Retrieve in pages.
     * Retrieve ShippingDocuments in pages.
     */
    @GetMapping("_page")
    @Transactional(readOnly = true)
    public Page<ShippingDocumentStateDto> getPage( HttpServletRequest request,
                    @RequestParam(value = "fields", required = false) String fields,
                    @RequestParam(value = "page", defaultValue = "0") Integer page,
                    @RequestParam(value = "size", defaultValue = "20") Integer size,
                    @RequestParam(value = "filter", required = false) String filter) {
        
            Integer firstResult = (page == null ? 0 : page) * (size == null ? 20 : size);
            Integer maxResults = (size == null ? 20 : size);
            Iterable<ShippingDocumentState> states = null; 
            CriterionDto criterion = null;
            if (!StringHelper.isNullOrEmpty(filter)) {
                criterion = deserializeCriterionDto(filter);
            } else {
                criterion = QueryParamUtils.getQueryCriterionDto(request.getParameterMap().entrySet().stream()
                    .filter(kv -> ShippingDocumentResourceUtils.getFilterPropertyName(kv.getKey()) != null)
                    .collect(Collectors.toMap(kv -> kv.getKey(), kv -> kv.getValue())));
            }
            Criterion c = CriterionDto.toSubclass(criterion, getCriterionTypeConverter(), getPropertyTypeResolver(), 
                n -> (ShippingDocumentMetadata.aliasMap.containsKey(n) ? ShippingDocumentMetadata.aliasMap.get(n) : n));
            states = shippingDocumentApplicationService.get(
                c,
                ShippingDocumentResourceUtils.getQuerySorts(request.getParameterMap()),
                firstResult, maxResults);
            long count = shippingDocumentApplicationService.getCount(c);

            ShippingDocumentStateDto.DtoConverter dtoConverter = new ShippingDocumentStateDto.DtoConverter();
            if (StringHelper.isNullOrEmpty(fields)) {
                dtoConverter.setAllFieldsReturned(true);
            } else {
                dtoConverter.setReturnedFieldsString(fields);
            }
            Page.PageImpl<ShippingDocumentStateDto> statePage =  new Page.PageImpl<>(dtoConverter.toShippingDocumentStateDtoList(states), count);
            statePage.setSize(size);
            statePage.setNumber(page);
            return statePage;

        
    }

    /**
     * Retrieve.
     * Retrieves ShippingDocument with the specified ID.
     */
    @GetMapping("{documentId}")
    @Transactional(readOnly = true)
    public ShippingDocumentStateDto get(@PathVariable("documentId") String documentId, @RequestParam(value = "fields", required = false) String fields) {
        
            String idObj = documentId;
            ShippingDocumentState state = shippingDocumentApplicationService.get(idObj);
            if (state == null) { return null; }

            ShippingDocumentStateDto.DtoConverter dtoConverter = new ShippingDocumentStateDto.DtoConverter();
            if (StringHelper.isNullOrEmpty(fields)) {
                dtoConverter.setAllFieldsReturned(true);
            } else {
                dtoConverter.setReturnedFieldsString(fields);
            }
            return dtoConverter.toShippingDocumentStateDto(state);

        
    }

    @GetMapping("_count")
    @Transactional(readOnly = true)
    public long getCount( HttpServletRequest request,
                         @RequestParam(value = "filter", required = false) String filter) {
        
            long count = 0;
            CriterionDto criterion = null;
            if (!StringHelper.isNullOrEmpty(filter)) {
                criterion = deserializeCriterionDto(filter);
            } else {
                criterion = QueryParamUtils.getQueryCriterionDto(request.getParameterMap());
            }
            Criterion c = CriterionDto.toSubclass(criterion,
                getCriterionTypeConverter(), 
                getPropertyTypeResolver(), 
                n -> (ShippingDocumentMetadata.aliasMap.containsKey(n) ? ShippingDocumentMetadata.aliasMap.get(n) : n));
            count = shippingDocumentApplicationService.getCount(c);
            return count;

        
    }


    /**
     * Create.
     * Create ShippingDocument
     */
    @PostMapping @ResponseBody @ResponseStatus(HttpStatus.CREATED)
    public String post(@RequestBody CreateOrMergePatchShippingDocumentDto.CreateShippingDocumentDto value,  HttpServletResponse response) {
        
            ShippingDocumentCommand.CreateShippingDocument cmd = value;//.toCreateShippingDocument();
            if (cmd.getDocumentId() == null) {
                throw DomainError.named("nullId", "Aggregate Id in cmd is null, aggregate name: %1$s.", "ShippingDocument");
            }
            String idObj = cmd.getDocumentId();
            cmd.setRequesterId(SecurityContextUtil.getRequesterId());
            shippingDocumentApplicationService.when(cmd);

            return idObj;
        
    }


    /**
     * Create or update.
     * Create or update ShippingDocument
     */
    @PutMapping("{documentId}")
    public void put(@PathVariable("documentId") String documentId, @RequestBody CreateOrMergePatchShippingDocumentDto value) {
        
            if (value.getVersion() != null) {
                value.setCommandType(Command.COMMAND_TYPE_MERGE_PATCH);
                ShippingDocumentCommand.MergePatchShippingDocument cmd = (ShippingDocumentCommand.MergePatchShippingDocument) value.toSubclass();
                ShippingDocumentResourceUtils.setNullIdOrThrowOnInconsistentIds(documentId, cmd);
                cmd.setRequesterId(SecurityContextUtil.getRequesterId());
                shippingDocumentApplicationService.when(cmd);
                return;
            }

            value.setCommandType(Command.COMMAND_TYPE_CREATE);
            ShippingDocumentCommand.CreateShippingDocument cmd = (ShippingDocumentCommand.CreateShippingDocument) value.toSubclass();
            ShippingDocumentResourceUtils.setNullIdOrThrowOnInconsistentIds(documentId, cmd);
            cmd.setRequesterId(SecurityContextUtil.getRequesterId());
            shippingDocumentApplicationService.when(cmd);

        
    }


    /**
     * Patch.
     * Patch ShippingDocument
     */
    @PatchMapping("{documentId}")
    public void patch(@PathVariable("documentId") String documentId, @RequestBody CreateOrMergePatchShippingDocumentDto.MergePatchShippingDocumentDto value) {
        

            ShippingDocumentCommand.MergePatchShippingDocument cmd = value;//.toMergePatchShippingDocument();
            ShippingDocumentResourceUtils.setNullIdOrThrowOnInconsistentIds(documentId, cmd);
            cmd.setRequesterId(SecurityContextUtil.getRequesterId());
            shippingDocumentApplicationService.when(cmd);

        
    }

    /**
     * Delete.
     * Delete ShippingDocument
     */
    @DeleteMapping("{documentId}")
    public void delete(@PathVariable("documentId") String documentId,
                       @NotNull @RequestParam(value = "commandId", required = false) String commandId,
                       @NotNull @RequestParam(value = "version", required = false) @Min(value = -1) Long version,
                       @RequestParam(value = "requesterId", required = false) String requesterId) {
        

            ShippingDocumentCommand.DeleteShippingDocument deleteCmd = new DeleteShippingDocumentDto();

            deleteCmd.setCommandId(commandId);
            deleteCmd.setRequesterId(requesterId);
            deleteCmd.setVersion(version);
            ShippingDocumentResourceUtils.setNullIdOrThrowOnInconsistentIds(documentId, deleteCmd);
            deleteCmd.setRequesterId(SecurityContextUtil.getRequesterId());
            shippingDocumentApplicationService.when(deleteCmd);

        
    }

    @GetMapping("_metadata/filteringFields")
    public List<PropertyMetadataDto> getMetadataFilteringFields() {
        

            List<PropertyMetadataDto> filtering = new ArrayList<>();
            ShippingDocumentMetadata.propertyTypeMap.forEach((key, value) -> {
                filtering.add(new PropertyMetadataDto(key, value, true));
            });
            return filtering;

        
    }

    @GetMapping("{documentId}/_events/{version}")
    @Transactional(readOnly = true)
    public ShippingDocumentEvent getEvent(@PathVariable("documentId") String documentId, @PathVariable("version") long version) {
        

            String idObj = documentId;
            //ShippingDocumentStateEventDtoConverter dtoConverter = getShippingDocumentStateEventDtoConverter();
            return shippingDocumentApplicationService.getEvent(idObj, version);

        
    }

    @GetMapping("{documentId}/_historyStates/{version}")
    @Transactional(readOnly = true)
    public ShippingDocumentStateDto getHistoryState(@PathVariable("documentId") String documentId, @PathVariable("version") long version, @RequestParam(value = "fields", required = false) String fields) {
        

            String idObj = documentId;
            ShippingDocumentStateDto.DtoConverter dtoConverter = new ShippingDocumentStateDto.DtoConverter();
            if (StringHelper.isNullOrEmpty(fields)) {
                dtoConverter.setAllFieldsReturned(true);
            } else {
                dtoConverter.setReturnedFieldsString(fields);
            }
            return dtoConverter.toShippingDocumentStateDto(shippingDocumentApplicationService.getHistoryState(idObj, version));

        
    }



    //protected  ShippingDocumentStateEventDtoConverter getShippingDocumentStateEventDtoConverter() {
    //    return new ShippingDocumentStateEventDtoConverter();
    //}

    protected TypeConverter getCriterionTypeConverter() {
        return new DefaultTypeConverter();
    }

    protected PropertyTypeResolver getPropertyTypeResolver() {
        return new PropertyTypeResolver() {
            @Override
            public Class resolveTypeByPropertyName(String propertyName) {
                return ShippingDocumentResourceUtils.getFilterPropertyType(propertyName);
            }
        };
    }

 
    public static class ShippingDocumentResourceUtils {

        public static void setNullIdOrThrowOnInconsistentIds(String documentId, org.dddml.ffvtraceability.domain.shippingdocument.ShippingDocumentCommand value) {
            String idObj = documentId;
            if (value.getDocumentId() == null) {
                value.setDocumentId(idObj);
            } else if (!value.getDocumentId().equals(idObj)) {
                throw DomainError.named("inconsistentId", "Argument Id %1$s NOT equals body Id %2$s", documentId, value.getDocumentId());
            }
        }
    
        public static List<String> getQueryOrders(String str, String separator) {
            return QueryParamUtils.getQueryOrders(str, separator, ShippingDocumentMetadata.aliasMap);
        }

        public static List<String> getQuerySorts(Map<String, String[]> queryNameValuePairs) {
            String[] values = queryNameValuePairs.get("sort");
            return QueryParamUtils.getQuerySorts(values, ShippingDocumentMetadata.aliasMap);
        }

        public static String getFilterPropertyName(String fieldName) {
            if ("sort".equalsIgnoreCase(fieldName)
                    || "firstResult".equalsIgnoreCase(fieldName)
                    || "maxResults".equalsIgnoreCase(fieldName)
                    || "fields".equalsIgnoreCase(fieldName)) {
                return null;
            }
            if (ShippingDocumentMetadata.aliasMap.containsKey(fieldName)) {
                return ShippingDocumentMetadata.aliasMap.get(fieldName);
            }
            return null;
        }

        public static Class getFilterPropertyType(String propertyName) {
            if (ShippingDocumentMetadata.propertyTypeMap.containsKey(propertyName)) {
                String propertyType = ShippingDocumentMetadata.propertyTypeMap.get(propertyName);
                if (!StringHelper.isNullOrEmpty(propertyType)) {
                    if (BoundedContextMetadata.CLASS_MAP.containsKey(propertyType)) {
                        return BoundedContextMetadata.CLASS_MAP.get(propertyType);
                    }
                }
            }
            return String.class;
        }

        public static Iterable<Map.Entry<String, Object>> getQueryFilterMap(Map<String, String[]> queryNameValuePairs) {
            Map<String, Object> filter = new HashMap<>();
            queryNameValuePairs.forEach((key, values) -> {
                if (values.length > 0) {
                    String pName = getFilterPropertyName(key);
                    if (!StringHelper.isNullOrEmpty(pName)) {
                        Class pClass = getFilterPropertyType(pName);
                        filter.put(pName, ApplicationContext.current.getTypeConverter().convertFromString(pClass, values[0]));
                    }
                }
            });
            return filter.entrySet();
        }

        public static ShippingDocumentStateDto[] toShippingDocumentStateDtoArray(Iterable<String> ids) {
            List<ShippingDocumentStateDto> states = new ArrayList<>();
            ids.forEach(i -> {
                ShippingDocumentStateDto dto = new ShippingDocumentStateDto();
                dto.setDocumentId(i);
                states.add(dto);
            });
            return states.toArray(new ShippingDocumentStateDto[0]);
        }

    }

}

