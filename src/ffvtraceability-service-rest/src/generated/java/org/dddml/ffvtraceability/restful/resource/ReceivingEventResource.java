// <autogenerated>
//   This file was generated by dddappp code generator.
//   Any changes made to this file manually will be lost next time the file is regenerated.
// </autogenerated>

package org.dddml.ffvtraceability.restful.resource;

import java.util.*;
import java.util.stream.*;
import jakarta.servlet.http.*;
import javax.validation.constraints.*;
import org.springframework.http.MediaType;
import org.springframework.http.HttpStatus;
import org.springframework.web.bind.annotation.*;
import org.springframework.transaction.annotation.Transactional;
import org.dddml.support.criterion.*;
import org.dddml.ffvtraceability.domain.*;
import java.time.OffsetDateTime;
import org.dddml.ffvtraceability.specialization.*;
import org.dddml.ffvtraceability.domain.receivingevent.*;
import static org.dddml.ffvtraceability.domain.meta.M.*;

import com.fasterxml.jackson.databind.ObjectMapper;
import org.springframework.beans.factory.annotation.Autowired;
import org.dddml.support.criterion.TypeConverter;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

@RequestMapping(path = "ReceivingEvents", produces = MediaType.APPLICATION_JSON_VALUE)
@RestController
public class ReceivingEventResource {
    private Logger logger = LoggerFactory.getLogger(this.getClass());


    @Autowired
    private ReceivingEventApplicationService receivingEventApplicationService;


    /**
     * Retrieve.
     * Retrieve ReceivingEvents
     */
    @GetMapping
    @Transactional(readOnly = true)
    public ReceivingEventStateDto[] getAll( HttpServletRequest request,
                    @RequestParam(value = "sort", required = false) String sort,
                    @RequestParam(value = "fields", required = false) String fields,
                    @RequestParam(value = "firstResult", defaultValue = "0") Integer firstResult,
                    @RequestParam(value = "maxResults", defaultValue = "2147483647") Integer maxResults,
                    @RequestParam(value = "filter", required = false) String filter) {
        try {
        if (firstResult < 0) { firstResult = 0; }
        if (maxResults == null || maxResults < 1) { maxResults = Integer.MAX_VALUE; }

            Iterable<ReceivingEventState> states = null; 
            CriterionDto criterion = null;
            if (!StringHelper.isNullOrEmpty(filter)) {
                criterion = new ObjectMapper().readValue(filter, CriterionDto.class);
            } else {
                criterion = QueryParamUtils.getQueryCriterionDto(request.getParameterMap().entrySet().stream()
                    .filter(kv -> ReceivingEventResourceUtils.getFilterPropertyName(kv.getKey()) != null)
                    .collect(Collectors.toMap(kv -> kv.getKey(), kv -> kv.getValue())));
            }
            Criterion c = CriterionDto.toSubclass(criterion, getCriterionTypeConverter(), getPropertyTypeResolver(), 
                n -> (ReceivingEventMetadata.aliasMap.containsKey(n) ? ReceivingEventMetadata.aliasMap.get(n) : n));
            states = receivingEventApplicationService.get(
                c,
                ReceivingEventResourceUtils.getQuerySorts(request.getParameterMap()),
                firstResult, maxResults);

            ReceivingEventStateDto.DtoConverter dtoConverter = new ReceivingEventStateDto.DtoConverter();
            if (StringHelper.isNullOrEmpty(fields)) {
                dtoConverter.setAllFieldsReturned(true);
            } else {
                dtoConverter.setReturnedFieldsString(fields);
            }
            return dtoConverter.toReceivingEventStateDtoArray(states);

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }

    /**
     * Retrieve in pages.
     * Retrieve ReceivingEvents in pages.
     */
    @GetMapping("_page")
    @Transactional(readOnly = true)
    public Page<ReceivingEventStateDto> getPage( HttpServletRequest request,
                    @RequestParam(value = "fields", required = false) String fields,
                    @RequestParam(value = "page", defaultValue = "0") Integer page,
                    @RequestParam(value = "size", defaultValue = "20") Integer size,
                    @RequestParam(value = "filter", required = false) String filter) {
        try {
            Integer firstResult = (page == null ? 0 : page) * (size == null ? 20 : size);
            Integer maxResults = (size == null ? 20 : size);
            Iterable<ReceivingEventState> states = null; 
            CriterionDto criterion = null;
            if (!StringHelper.isNullOrEmpty(filter)) {
                criterion = new ObjectMapper().readValue(filter, CriterionDto.class);
            } else {
                criterion = QueryParamUtils.getQueryCriterionDto(request.getParameterMap().entrySet().stream()
                    .filter(kv -> ReceivingEventResourceUtils.getFilterPropertyName(kv.getKey()) != null)
                    .collect(Collectors.toMap(kv -> kv.getKey(), kv -> kv.getValue())));
            }
            Criterion c = CriterionDto.toSubclass(criterion, getCriterionTypeConverter(), getPropertyTypeResolver(), 
                n -> (ReceivingEventMetadata.aliasMap.containsKey(n) ? ReceivingEventMetadata.aliasMap.get(n) : n));
            states = receivingEventApplicationService.get(
                c,
                ReceivingEventResourceUtils.getQuerySorts(request.getParameterMap()),
                firstResult, maxResults);
            long count = receivingEventApplicationService.getCount(c);

            ReceivingEventStateDto.DtoConverter dtoConverter = new ReceivingEventStateDto.DtoConverter();
            if (StringHelper.isNullOrEmpty(fields)) {
                dtoConverter.setAllFieldsReturned(true);
            } else {
                dtoConverter.setReturnedFieldsString(fields);
            }
            Page.PageImpl<ReceivingEventStateDto> statePage =  new Page.PageImpl<>(dtoConverter.toReceivingEventStateDtoList(states), count);
            statePage.setSize(size);
            statePage.setNumber(page);
            return statePage;

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }

    /**
     * Retrieve.
     * Retrieves ReceivingEvent with the specified ID.
     */
    @GetMapping("{eventId}")
    @Transactional(readOnly = true)
    public ReceivingEventStateDto get(@PathVariable("eventId") Long eventId, @RequestParam(value = "fields", required = false) String fields) {
        try {
            Long idObj = eventId;
            ReceivingEventState state = receivingEventApplicationService.get(idObj);
            if (state == null) { return null; }

            ReceivingEventStateDto.DtoConverter dtoConverter = new ReceivingEventStateDto.DtoConverter();
            if (StringHelper.isNullOrEmpty(fields)) {
                dtoConverter.setAllFieldsReturned(true);
            } else {
                dtoConverter.setReturnedFieldsString(fields);
            }
            return dtoConverter.toReceivingEventStateDto(state);

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }

    @GetMapping("_count")
    @Transactional(readOnly = true)
    public long getCount( HttpServletRequest request,
                         @RequestParam(value = "filter", required = false) String filter) {
        try {
            long count = 0;
            CriterionDto criterion = null;
            if (!StringHelper.isNullOrEmpty(filter)) {
                criterion = new ObjectMapper().readValue(filter, CriterionDto.class);
            } else {
                criterion = QueryParamUtils.getQueryCriterionDto(request.getParameterMap());
            }
            Criterion c = CriterionDto.toSubclass(criterion,
                getCriterionTypeConverter(), 
                getPropertyTypeResolver(), 
                n -> (ReceivingEventMetadata.aliasMap.containsKey(n) ? ReceivingEventMetadata.aliasMap.get(n) : n));
            count = receivingEventApplicationService.getCount(c);
            return count;

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }


    /**
     * Create.
     * Create ReceivingEvent
     */
    @PostMapping @ResponseStatus(HttpStatus.CREATED)
    public Long post(@RequestBody CreateOrMergePatchReceivingEventDto.CreateReceivingEventDto value,  HttpServletResponse response) {
        try {
            ReceivingEventCommand.CreateReceivingEvent cmd = value;//.toCreateReceivingEvent();
            cmd.setRequesterId(SecurityContextUtil.getRequesterId());
            Long idObj = receivingEventApplicationService.createWithoutId(cmd);

            return idObj;
        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }


    /**
     * Create or update.
     * Create or update ReceivingEvent
     */
    @PutMapping("{eventId}")
    public void put(@PathVariable("eventId") Long eventId, @RequestBody CreateOrMergePatchReceivingEventDto value) {
        try {
            if (value.getVersion() != null) {
                value.setCommandType(Command.COMMAND_TYPE_MERGE_PATCH);
                ReceivingEventCommand.MergePatchReceivingEvent cmd = (ReceivingEventCommand.MergePatchReceivingEvent) value.toSubclass();
                ReceivingEventResourceUtils.setNullIdOrThrowOnInconsistentIds(eventId, cmd);
                cmd.setRequesterId(SecurityContextUtil.getRequesterId());
                receivingEventApplicationService.when(cmd);
                return;
            }
            throw DomainError.named("unsupportedOperation", "Unsupported HTTP PUT to create, aggregate Id %1$s", eventId);
        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }


    /**
     * Patch.
     * Patch ReceivingEvent
     */
    @PatchMapping("{eventId}")
    public void patch(@PathVariable("eventId") Long eventId, @RequestBody CreateOrMergePatchReceivingEventDto.MergePatchReceivingEventDto value) {
        try {

            ReceivingEventCommand.MergePatchReceivingEvent cmd = value;//.toMergePatchReceivingEvent();
            ReceivingEventResourceUtils.setNullIdOrThrowOnInconsistentIds(eventId, cmd);
            cmd.setRequesterId(SecurityContextUtil.getRequesterId());
            receivingEventApplicationService.when(cmd);

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }

    /**
     * Delete.
     * Delete ReceivingEvent
     */
    @DeleteMapping("{eventId}")
    public void delete(@PathVariable("eventId") Long eventId,
                       @NotNull @RequestParam(value = "commandId", required = false) String commandId,
                       @NotNull @RequestParam(value = "version", required = false) @Min(value = -1) Long version,
                       @RequestParam(value = "requesterId", required = false) String requesterId) {
        try {

            ReceivingEventCommand.DeleteReceivingEvent deleteCmd = new DeleteReceivingEventDto();//.toDeleteReceivingEvent();;

            deleteCmd.setCommandId(commandId);
            deleteCmd.setRequesterId(requesterId);
            deleteCmd.setVersion(version);
            ReceivingEventResourceUtils.setNullIdOrThrowOnInconsistentIds(eventId, deleteCmd);
            deleteCmd.setRequesterId(SecurityContextUtil.getRequesterId());
            receivingEventApplicationService.when(deleteCmd);

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }

    @GetMapping("_metadata/filteringFields")
    public List<PropertyMetadataDto> getMetadataFilteringFields() {
        try {

            List<PropertyMetadataDto> filtering = new ArrayList<>();
            ReceivingEventMetadata.propertyTypeMap.forEach((key, value) -> {
                filtering.add(new PropertyMetadataDto(key, value, true));
            });
            return filtering;

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }


    protected TypeConverter getCriterionTypeConverter() {
        return new DefaultTypeConverter();
    }

    protected PropertyTypeResolver getPropertyTypeResolver() {
        return new PropertyTypeResolver() {
            @Override
            public Class resolveTypeByPropertyName(String propertyName) {
                return ReceivingEventResourceUtils.getFilterPropertyType(propertyName);
            }
        };
    }

    // ////////////////////////////////
 
    public static class ReceivingEventResourceUtils {

        public static void setNullIdOrThrowOnInconsistentIds(Long eventId, org.dddml.ffvtraceability.domain.receivingevent.ReceivingEventCommand value) {
            Long idObj = eventId;
            if (value.getEventId() == null) {
                value.setEventId(idObj);
            } else if (!value.getEventId().equals(idObj)) {
                throw DomainError.named("inconsistentId", "Argument Id %1$s NOT equals body Id %2$s", eventId, value.getEventId());
            }
        }
    
        public static List<String> getQueryOrders(String str, String separator) {
            return QueryParamUtils.getQueryOrders(str, separator, ReceivingEventMetadata.aliasMap);
        }

        public static List<String> getQuerySorts(Map<String, String[]> queryNameValuePairs) {
            String[] values = queryNameValuePairs.get("sort");
            return QueryParamUtils.getQuerySorts(values, ReceivingEventMetadata.aliasMap);
        }

        public static String getFilterPropertyName(String fieldName) {
            if ("sort".equalsIgnoreCase(fieldName)
                    || "firstResult".equalsIgnoreCase(fieldName)
                    || "maxResults".equalsIgnoreCase(fieldName)
                    || "fields".equalsIgnoreCase(fieldName)) {
                return null;
            }
            if (ReceivingEventMetadata.aliasMap.containsKey(fieldName)) {
                return ReceivingEventMetadata.aliasMap.get(fieldName);
            }
            return null;
        }

        public static Class getFilterPropertyType(String propertyName) {
            if (ReceivingEventMetadata.propertyTypeMap.containsKey(propertyName)) {
                String propertyType = ReceivingEventMetadata.propertyTypeMap.get(propertyName);
                if (!StringHelper.isNullOrEmpty(propertyType)) {
                    if (BoundedContextMetadata.CLASS_MAP.containsKey(propertyType)) {
                        return BoundedContextMetadata.CLASS_MAP.get(propertyType);
                    }
                }
            }
            return String.class;
        }

        public static Iterable<Map.Entry<String, Object>> getQueryFilterMap(Map<String, String[]> queryNameValuePairs) {
            Map<String, Object> filter = new HashMap<>();
            queryNameValuePairs.forEach((key, values) -> {
                if (values.length > 0) {
                    String pName = getFilterPropertyName(key);
                    if (!StringHelper.isNullOrEmpty(pName)) {
                        Class pClass = getFilterPropertyType(pName);
                        filter.put(pName, ApplicationContext.current.getTypeConverter().convertFromString(pClass, values[0]));
                    }
                }
            });
            return filter.entrySet();
        }

        public static ReceivingEventStateDto[] toReceivingEventStateDtoArray(Iterable<Long> ids) {
            List<ReceivingEventStateDto> states = new ArrayList<>();
            ids.forEach(i -> {
                ReceivingEventStateDto dto = new ReceivingEventStateDto();
                dto.setEventId(i);
                states.add(dto);
            });
            return states.toArray(new ReceivingEventStateDto[0]);
        }

    }

}

