// <autogenerated>
//   This file was generated by dddappp code generator.
//   Any changes made to this file manually will be lost next time the file is regenerated.
// </autogenerated>

package org.dddml.ffvtraceability.restful.resource;

import java.util.*;
import java.util.stream.*;
import jakarta.servlet.http.*;
import jakarta.validation.constraints.*;
import org.springframework.http.MediaType;
import org.springframework.http.HttpStatus;
import org.springframework.web.bind.annotation.*;
import org.springframework.transaction.annotation.Transactional;
import org.dddml.support.criterion.*;
import java.time.OffsetDateTime;
import org.dddml.ffvtraceability.domain.*;
import org.dddml.ffvtraceability.specialization.*;
import org.dddml.ffvtraceability.domain.party.*;
import static org.dddml.ffvtraceability.domain.meta.M.*;

import com.fasterxml.jackson.databind.ObjectMapper;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.security.access.prepost.*;
import org.dddml.support.criterion.TypeConverter;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

@RequestMapping(path = "Families", produces = MediaType.APPLICATION_JSON_VALUE)
@RestController
public class FamilyResource {
    private Logger logger = LoggerFactory.getLogger(this.getClass());


    @Autowired
    private PartyApplicationService partyApplicationService;


    /**
     * Retrieve.
     * Retrieve Families
     */
    @GetMapping
    @Transactional(readOnly = true)
    public FamilyStateDto[] getAll( HttpServletRequest request,
                    @RequestParam(value = "sort", required = false) String sort,
                    @RequestParam(value = "fields", required = false) String fields,
                    @RequestParam(value = "firstResult", defaultValue = "0") Integer firstResult,
                    @RequestParam(value = "maxResults", defaultValue = "2147483647") Integer maxResults,
                    @RequestParam(value = "filter", required = false) String filter) {
        try {
        if (firstResult < 0) { firstResult = 0; }
        if (maxResults == null || maxResults < 1) { maxResults = Integer.MAX_VALUE; }

            Iterable<PartyState> states = null; 
            CriterionDto criterion = null;
            if (!StringHelper.isNullOrEmpty(filter)) {
                criterion = new ObjectMapper().readValue(filter, CriterionDto.class);
            } else {
                criterion = QueryParamUtils.getQueryCriterionDto(request.getParameterMap().entrySet().stream()
                    .filter(kv -> FamilyResourceUtils.getFilterPropertyName(kv.getKey()) != null)
                    .collect(Collectors.toMap(kv -> kv.getKey(), kv -> kv.getValue())));
            }
            Criterion c = CriterionDto.toSubclass(criterion, getCriterionTypeConverter(), getPropertyTypeResolver(), 
                n -> (PartyMetadata.aliasMap.containsKey(n) ? PartyMetadata.aliasMap.get(n) : n));
            states = partyApplicationService.get(FamilyState.class, 
                c,
                FamilyResourceUtils.getQuerySorts(request.getParameterMap()),
                firstResult, maxResults);

            FamilyStateDto.DtoConverter dtoConverter = new FamilyStateDto.DtoConverter();
            if (StringHelper.isNullOrEmpty(fields)) {
                dtoConverter.setAllFieldsReturned(true);
            } else {
                dtoConverter.setReturnedFieldsString(fields);
            }
            return dtoConverter.toFamilyStateDtoArray(states);

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }

    /**
     * Retrieve in pages.
     * Retrieve Families in pages.
     */
    @GetMapping("_page")
    @Transactional(readOnly = true)
    public Page<FamilyStateDto> getPage( HttpServletRequest request,
                    @RequestParam(value = "fields", required = false) String fields,
                    @RequestParam(value = "page", defaultValue = "0") Integer page,
                    @RequestParam(value = "size", defaultValue = "20") Integer size,
                    @RequestParam(value = "filter", required = false) String filter) {
        try {
            Integer firstResult = (page == null ? 0 : page) * (size == null ? 20 : size);
            Integer maxResults = (size == null ? 20 : size);
            Iterable<PartyState> states = null; 
            CriterionDto criterion = null;
            if (!StringHelper.isNullOrEmpty(filter)) {
                criterion = new ObjectMapper().readValue(filter, CriterionDto.class);
            } else {
                criterion = QueryParamUtils.getQueryCriterionDto(request.getParameterMap().entrySet().stream()
                    .filter(kv -> FamilyResourceUtils.getFilterPropertyName(kv.getKey()) != null)
                    .collect(Collectors.toMap(kv -> kv.getKey(), kv -> kv.getValue())));
            }
            Criterion c = CriterionDto.toSubclass(criterion, getCriterionTypeConverter(), getPropertyTypeResolver(), 
                n -> (PartyMetadata.aliasMap.containsKey(n) ? PartyMetadata.aliasMap.get(n) : n));
            states = partyApplicationService.get(FamilyState.class, 
                c,
                FamilyResourceUtils.getQuerySorts(request.getParameterMap()),
                firstResult, maxResults);
            long count = partyApplicationService.getCount(FamilyState.class, c);

            FamilyStateDto.DtoConverter dtoConverter = new FamilyStateDto.DtoConverter();
            if (StringHelper.isNullOrEmpty(fields)) {
                dtoConverter.setAllFieldsReturned(true);
            } else {
                dtoConverter.setReturnedFieldsString(fields);
            }
            Page.PageImpl<FamilyStateDto> statePage =  new Page.PageImpl<>(dtoConverter.toFamilyStateDtoList(states), count);
            statePage.setSize(size);
            statePage.setNumber(page);
            return statePage;

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }

    /**
     * Retrieve.
     * Retrieves Party with the specified ID.
     */
    @GetMapping("{partyId}")
    @Transactional(readOnly = true)
    public FamilyStateDto get(@PathVariable("partyId") String partyId, @RequestParam(value = "fields", required = false) String fields) {
        try {
            String idObj = partyId;
            PartyState state = partyApplicationService.get(idObj);
            if (state == null) { return null; }

            if (state != null && !FamilyState.class.isAssignableFrom(state.getClass())) {
                return null;
            }
            FamilyStateDto.DtoConverter dtoConverter = new FamilyStateDto.DtoConverter();
            if (StringHelper.isNullOrEmpty(fields)) {
                dtoConverter.setAllFieldsReturned(true);
            } else {
                dtoConverter.setReturnedFieldsString(fields);
            }
            return dtoConverter.toFamilyStateDto(state);

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }

    @GetMapping("_count")
    @Transactional(readOnly = true)
    public long getCount( HttpServletRequest request,
                         @RequestParam(value = "filter", required = false) String filter) {
        try {
            long count = 0;
            CriterionDto criterion = null;
            if (!StringHelper.isNullOrEmpty(filter)) {
                criterion = new ObjectMapper().readValue(filter, CriterionDto.class);
            } else {
                criterion = QueryParamUtils.getQueryCriterionDto(request.getParameterMap());
            }
            Criterion c = CriterionDto.toSubclass(criterion,
                getCriterionTypeConverter(), 
                getPropertyTypeResolver(), 
                n -> (PartyMetadata.aliasMap.containsKey(n) ? PartyMetadata.aliasMap.get(n) : n));
            count = partyApplicationService.getCount(FamilyState.class, c);
            return count;

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }


    /**
     * Create.
     * Create Party
     */
    @PostMapping @ResponseBody @ResponseStatus(HttpStatus.CREATED)
    public String post(@RequestBody CreateOrMergePatchPartyDto.CreatePartyDto value,  HttpServletResponse response) {
        try {
            value.setPartyTypeId(PartyTypeId.FAMILY);
            PartyCommand.CreateParty cmd = value;//.toCreateParty();
            if (cmd.getPartyId() == null) {
                throw DomainError.named("nullId", "Aggregate Id in cmd is null, aggregate name: %1$s.", "Party");
            }
            String idObj = cmd.getPartyId();
            cmd.setRequesterId(SecurityContextUtil.getRequesterId());
            partyApplicationService.when(cmd);

            return idObj;
        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }


    /**
     * Create or update.
     * Create or update Party
     */
    @PutMapping("{partyId}")
    public void put(@PathVariable("partyId") String partyId, @RequestBody CreateOrMergePatchPartyDto value) {
        try {
            value.setPartyTypeId(PartyTypeId.FAMILY);
            if (value.getVersion() != null) {
                value.setCommandType(Command.COMMAND_TYPE_MERGE_PATCH);
                PartyCommand.MergePatchParty cmd = (PartyCommand.MergePatchParty) value.toSubclass();
                FamilyResourceUtils.setNullIdOrThrowOnInconsistentIds(partyId, cmd);
                cmd.setRequesterId(SecurityContextUtil.getRequesterId());
                partyApplicationService.when(cmd);
                return;
            }

            value.setCommandType(Command.COMMAND_TYPE_CREATE);
            PartyCommand.CreateParty cmd = (PartyCommand.CreateParty) value.toSubclass();
            FamilyResourceUtils.setNullIdOrThrowOnInconsistentIds(partyId, cmd);
            cmd.setRequesterId(SecurityContextUtil.getRequesterId());
            partyApplicationService.when(cmd);

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }


    /**
     * Patch.
     * Patch Party
     */
    @PatchMapping("{partyId}")
    public void patch(@PathVariable("partyId") String partyId, @RequestBody CreateOrMergePatchPartyDto.MergePatchPartyDto value) {
        try {

            value.setPartyTypeId(PartyTypeId.FAMILY);
            PartyCommand.MergePatchParty cmd = value;//.toMergePatchParty();
            FamilyResourceUtils.setNullIdOrThrowOnInconsistentIds(partyId, cmd);
            cmd.setRequesterId(SecurityContextUtil.getRequesterId());
            partyApplicationService.when(cmd);

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }

    @GetMapping("_metadata/filteringFields")
    public List<PropertyMetadataDto> getMetadataFilteringFields() {
        try {

            List<PropertyMetadataDto> filtering = new ArrayList<>();
            PartyMetadata.propertyTypeMap.forEach((key, value) -> {
                filtering.add(new PropertyMetadataDto(key, value, true));
            });
            return filtering;

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }

    @GetMapping("{partyId}/_events/{version}")
    @Transactional(readOnly = true)
    public PartyEvent getEvent(@PathVariable("partyId") String partyId, @PathVariable("version") long version) {
        try {

            String idObj = partyId;
            //PartyStateEventDtoConverter dtoConverter = getPartyStateEventDtoConverter();
            return partyApplicationService.getEvent(idObj, version);

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }

    @GetMapping("{partyId}/_historyStates/{version}")
    @Transactional(readOnly = true)
    public FamilyStateDto getHistoryState(@PathVariable("partyId") String partyId, @PathVariable("version") long version, @RequestParam(value = "fields", required = false) String fields) {
        try {

            String idObj = partyId;
            FamilyStateDto.DtoConverter dtoConverter = new FamilyStateDto.DtoConverter();
            if (StringHelper.isNullOrEmpty(fields)) {
                dtoConverter.setAllFieldsReturned(true);
            } else {
                dtoConverter.setReturnedFieldsString(fields);
            }
            return dtoConverter.toFamilyStateDto(partyApplicationService.getHistoryState(idObj, version));

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }

    /**
     * Retrieve.
     * Retrieves PartyIdentification with the specified PartyIdentificationTypeId.
     */
    @GetMapping("{partyId}/PartyIdentifications/{partyIdentificationTypeId}")
    @Transactional(readOnly = true)
    public PartyIdentificationStateDto getPartyIdentification(@PathVariable("partyId") String partyId, @PathVariable("partyIdentificationTypeId") String partyIdentificationTypeId) {
        try {

            PartyIdentificationState state = partyApplicationService.getPartyIdentification(partyId, partyIdentificationTypeId);
            if (state == null) { return null; }
            PartyIdentificationStateDto.DtoConverter dtoConverter = new PartyIdentificationStateDto.DtoConverter();
            PartyIdentificationStateDto stateDto = dtoConverter.toPartyIdentificationStateDto(state);
            dtoConverter.setAllFieldsReturned(true);
            return stateDto;

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }

    /**
     * Create or update.
     * Create or update PartyIdentification
     */
    @PutMapping(path = "{partyId}/PartyIdentifications/{partyIdentificationTypeId}", consumes = MediaType.APPLICATION_JSON_VALUE)
    public void putPartyIdentification(@PathVariable("partyId") String partyId, @PathVariable("partyIdentificationTypeId") String partyIdentificationTypeId,
                       @RequestParam(value = "commandId", required = false) String commandId,
                       @RequestParam(value = "version", required = false) Long version,
                       @RequestParam(value = "requesterId", required = false) String requesterId,
                       @RequestBody CreateOrMergePatchPartyIdentificationDto.MergePatchPartyIdentificationDto body) {
        try {
            PartyCommand.MergePatchParty mergePatchParty = new CreateOrMergePatchPartyDto.MergePatchPartyDto();
            mergePatchParty.setPartyId(partyId);
            mergePatchParty.setCommandId(commandId != null && !commandId.isEmpty() ? commandId : body.getCommandId());
            if (version != null) { mergePatchParty.setVersion(version); }
            mergePatchParty.setRequesterId(requesterId != null && !requesterId.isEmpty() ? requesterId : body.getRequesterId());
            PartyIdentificationCommand.MergePatchPartyIdentification mergePatchPartyIdentification = body;//.toMergePatchPartyIdentification();
            mergePatchPartyIdentification.setPartyIdentificationTypeId(partyIdentificationTypeId);
            mergePatchParty.getPartyIdentificationCommands().add(mergePatchPartyIdentification);
            mergePatchParty.setRequesterId(SecurityContextUtil.getRequesterId());
            partyApplicationService.when(mergePatchParty);
        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }

    /**
     * Delete.
     * Delete PartyIdentification
     */
    @DeleteMapping("{partyId}/PartyIdentifications/{partyIdentificationTypeId}")
    public void deletePartyIdentification(@PathVariable("partyId") String partyId, @PathVariable("partyIdentificationTypeId") String partyIdentificationTypeId,
                       @RequestParam(value = "commandId", required = false) String commandId,
                       @RequestParam(value = "version", required = false) Long version,
                       @RequestParam(value = "requesterId", required = false) String requesterId) {
        try {
            PartyCommand.MergePatchParty mergePatchParty = new CreateOrMergePatchPartyDto.MergePatchPartyDto();
            mergePatchParty.setPartyId(partyId);
            mergePatchParty.setCommandId(commandId);// != null && !commandId.isEmpty() ? commandId : body.getCommandId());
            if (version != null) { 
                mergePatchParty.setVersion(version); 
            } else {
                mergePatchParty.setVersion(partyApplicationService.get(partyId).getVersion());
            }
            mergePatchParty.setRequesterId(requesterId);// != null && !requesterId.isEmpty() ? requesterId : body.getRequesterId());
            PartyIdentificationCommand.RemovePartyIdentification removePartyIdentification = new RemovePartyIdentificationDto();
            removePartyIdentification.setPartyIdentificationTypeId(partyIdentificationTypeId);
            mergePatchParty.getPartyIdentificationCommands().add(removePartyIdentification);
            mergePatchParty.setRequesterId(SecurityContextUtil.getRequesterId());
            partyApplicationService.when(mergePatchParty);
        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }

    /**
     * PartyIdentification List
     */
    @GetMapping("{partyId}/PartyIdentifications")
    @Transactional(readOnly = true)
    public PartyIdentificationStateDto[] getPartyIdentifications(@PathVariable("partyId") String partyId,
                    @RequestParam(value = "sort", required = false) String sort,
                    @RequestParam(value = "fields", required = false) String fields,
                    @RequestParam(value = "filter", required = false) String filter,
                     HttpServletRequest request) {
        try {
            CriterionDto criterion = null;
            if (!StringHelper.isNullOrEmpty(filter)) {
                criterion = new ObjectMapper().readValue(filter, CriterionDto.class);
            } else {
                criterion = QueryParamUtils.getQueryCriterionDto(request.getParameterMap().entrySet().stream()
                    .filter(kv -> FamilyResourceUtils.getPartyIdentificationFilterPropertyName(kv.getKey()) != null)
                    .collect(Collectors.toMap(kv -> kv.getKey(), kv -> kv.getValue())));
            }
            Criterion c = CriterionDto.toSubclass(criterion, getCriterionTypeConverter(), getPropertyTypeResolver(), 
                n -> (PartyIdentificationMetadata.aliasMap.containsKey(n) ? PartyIdentificationMetadata.aliasMap.get(n) : n));
            Iterable<PartyIdentificationState> states = partyApplicationService.getPartyIdentifications(partyId, c,
                    FamilyResourceUtils.getPartyIdentificationQuerySorts(request.getParameterMap()));
            if (states == null) { return null; }
            PartyIdentificationStateDto.DtoConverter dtoConverter = new PartyIdentificationStateDto.DtoConverter();
            if (StringHelper.isNullOrEmpty(fields)) {
                dtoConverter.setAllFieldsReturned(true);
            } else {
                dtoConverter.setReturnedFieldsString(fields);
            }
            return dtoConverter.toPartyIdentificationStateDtoArray(states);
        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }

    /**
     * Create.
     * Create PartyIdentification
     */
    @PostMapping(path = "{partyId}/PartyIdentifications", consumes = MediaType.APPLICATION_JSON_VALUE)
    public void postPartyIdentifications(@PathVariable("partyId") String partyId,
                       @RequestParam(value = "commandId", required = false) String commandId,
                       @RequestParam(value = "version", required = false) Long version,
                       @RequestParam(value = "requesterId", required = false) String requesterId,
                       @RequestBody CreateOrMergePatchPartyIdentificationDto.CreatePartyIdentificationDto body) {
        try {
            PartyCommand.MergePatchParty mergePatchParty = new AbstractPartyCommand.SimpleMergePatchParty();
            mergePatchParty.setPartyId(partyId);
            mergePatchParty.setCommandId(commandId != null && !commandId.isEmpty() ? commandId : body.getCommandId());
            if (version != null) { mergePatchParty.setVersion(version); }
            mergePatchParty.setRequesterId(requesterId != null && !requesterId.isEmpty() ? requesterId : body.getRequesterId());
            PartyIdentificationCommand.CreatePartyIdentification createPartyIdentification = body.toCreatePartyIdentification();
            mergePatchParty.getPartyIdentificationCommands().add(createPartyIdentification);
            mergePatchParty.setRequesterId(SecurityContextUtil.getRequesterId());
            partyApplicationService.when(mergePatchParty);
        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }



    //protected  PartyStateEventDtoConverter getPartyStateEventDtoConverter() {
    //    return new PartyStateEventDtoConverter();
    //}

    protected TypeConverter getCriterionTypeConverter() {
        return new DefaultTypeConverter();
    }

    protected PropertyTypeResolver getPropertyTypeResolver() {
        return new PropertyTypeResolver() {
            @Override
            public Class resolveTypeByPropertyName(String propertyName) {
                return FamilyResourceUtils.getFilterPropertyType(propertyName);
            }
        };
    }

    protected PropertyTypeResolver getPartyIdentificationPropertyTypeResolver() {
        return new PropertyTypeResolver() {
            @Override
            public Class resolveTypeByPropertyName(String propertyName) {
                return FamilyResourceUtils.getPartyIdentificationFilterPropertyType(propertyName);
            }
        };
    }

 
    public static class FamilyResourceUtils {

        public static void setNullIdOrThrowOnInconsistentIds(String partyId, org.dddml.ffvtraceability.domain.party.PartyCommand value) {
            String idObj = partyId;
            if (value.getPartyId() == null) {
                value.setPartyId(idObj);
            } else if (!value.getPartyId().equals(idObj)) {
                throw DomainError.named("inconsistentId", "Argument Id %1$s NOT equals body Id %2$s", partyId, value.getPartyId());
            }
        }
    
        public static List<String> getQueryOrders(String str, String separator) {
            return QueryParamUtils.getQueryOrders(str, separator, PartyMetadata.aliasMap);
        }

        public static List<String> getQuerySorts(Map<String, String[]> queryNameValuePairs) {
            String[] values = queryNameValuePairs.get("sort");
            return QueryParamUtils.getQuerySorts(values, PartyMetadata.aliasMap);
        }

        public static String getFilterPropertyName(String fieldName) {
            if ("sort".equalsIgnoreCase(fieldName)
                    || "firstResult".equalsIgnoreCase(fieldName)
                    || "maxResults".equalsIgnoreCase(fieldName)
                    || "fields".equalsIgnoreCase(fieldName)) {
                return null;
            }
            if (PartyMetadata.aliasMap.containsKey(fieldName)) {
                return PartyMetadata.aliasMap.get(fieldName);
            }
            return null;
        }

        public static Class getFilterPropertyType(String propertyName) {
            if (PartyMetadata.propertyTypeMap.containsKey(propertyName)) {
                String propertyType = PartyMetadata.propertyTypeMap.get(propertyName);
                if (!StringHelper.isNullOrEmpty(propertyType)) {
                    if (BoundedContextMetadata.CLASS_MAP.containsKey(propertyType)) {
                        return BoundedContextMetadata.CLASS_MAP.get(propertyType);
                    }
                }
            }
            return String.class;
        }

        public static Iterable<Map.Entry<String, Object>> getQueryFilterMap(Map<String, String[]> queryNameValuePairs) {
            Map<String, Object> filter = new HashMap<>();
            queryNameValuePairs.forEach((key, values) -> {
                if (values.length > 0) {
                    String pName = getFilterPropertyName(key);
                    if (!StringHelper.isNullOrEmpty(pName)) {
                        Class pClass = getFilterPropertyType(pName);
                        filter.put(pName, ApplicationContext.current.getTypeConverter().convertFromString(pClass, values[0]));
                    }
                }
            });
            return filter.entrySet();
        }

        public static List<String> getPartyIdentificationQueryOrders(String str, String separator) {
            return QueryParamUtils.getQueryOrders(str, separator, PartyIdentificationMetadata.aliasMap);
        }

        public static List<String> getPartyIdentificationQuerySorts(Map<String, String[]> queryNameValuePairs) {
            String[] values = queryNameValuePairs.get("sort");
            return QueryParamUtils.getQuerySorts(values, PartyIdentificationMetadata.aliasMap);
        }

        public static String getPartyIdentificationFilterPropertyName(String fieldName) {
            if ("sort".equalsIgnoreCase(fieldName)
                    || "firstResult".equalsIgnoreCase(fieldName)
                    || "maxResults".equalsIgnoreCase(fieldName)
                    || "fields".equalsIgnoreCase(fieldName)) {
                return null;
            }
            if (PartyIdentificationMetadata.aliasMap.containsKey(fieldName)) {
                return PartyIdentificationMetadata.aliasMap.get(fieldName);
            }
            return null;
        }

        public static Class getPartyIdentificationFilterPropertyType(String propertyName) {
            if (PartyIdentificationMetadata.propertyTypeMap.containsKey(propertyName)) {
                String propertyType = PartyIdentificationMetadata.propertyTypeMap.get(propertyName);
                if (!StringHelper.isNullOrEmpty(propertyType)) {
                    if (BoundedContextMetadata.CLASS_MAP.containsKey(propertyType)) {
                        return BoundedContextMetadata.CLASS_MAP.get(propertyType);
                    }
                }
            }
            return String.class;
        }

        public static Iterable<Map.Entry<String, Object>> getPartyIdentificationQueryFilterMap(Map<String, String[]> queryNameValuePairs) {
            Map<String, Object> filter = new HashMap<>();
            queryNameValuePairs.forEach((key, values) -> {
                if (values.length > 0) {
                    String pName = getPartyIdentificationFilterPropertyName(key);
                    if (!StringHelper.isNullOrEmpty(pName)) {
                        Class pClass = getPartyIdentificationFilterPropertyType(pName);
                        filter.put(pName, ApplicationContext.current.getTypeConverter().convertFromString(pClass, values[0]));
                    }
                }
            });
            return filter.entrySet();
        }

        public static FamilyStateDto[] toFamilyStateDtoArray(Iterable<String> ids) {
            List<FamilyStateDto> states = new ArrayList<>();
            ids.forEach(i -> {
                FamilyStateDto dto = new FamilyStateDto();
                dto.setPartyId(i);
                states.add(dto);
            });
            return states.toArray(new FamilyStateDto[0]);
        }

    }

}

