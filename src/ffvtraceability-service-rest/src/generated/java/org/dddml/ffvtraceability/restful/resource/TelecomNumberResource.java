// <autogenerated>
//   This file was generated by dddappp code generator.
//   Any changes made to this file manually will be lost next time the file is regenerated.
// </autogenerated>

package org.dddml.ffvtraceability.restful.resource;

import java.util.*;
import java.util.stream.*;
import jakarta.servlet.http.*;
import jakarta.validation.constraints.*;
import org.springframework.http.MediaType;
import org.springframework.http.HttpStatus;
import org.springframework.web.bind.annotation.*;
import org.springframework.transaction.annotation.Transactional;
import org.dddml.support.criterion.*;
import java.time.OffsetDateTime;
import org.dddml.ffvtraceability.domain.*;
import org.dddml.ffvtraceability.specialization.*;
import org.dddml.ffvtraceability.domain.contactmech.*;
import static org.dddml.ffvtraceability.domain.meta.M.*;

import com.fasterxml.jackson.databind.ObjectMapper;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.security.access.prepost.*;
import org.dddml.support.criterion.TypeConverter;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

@RequestMapping(path = "TelecomNumbers", produces = MediaType.APPLICATION_JSON_VALUE)
@RestController
public class TelecomNumberResource {
    private Logger logger = LoggerFactory.getLogger(this.getClass());

    private static CriterionDto deserializeCriterionDto(String filter) {
        return deserializeJsonArgument(filter, CriterionDto.class);
    }

    private static <T> T deserializeJsonArgument(String s, Class<T> aClass) {
        try {
            return new ObjectMapper().readValue(s, aClass);
        } catch (com.fasterxml.jackson.core.JsonProcessingException e) {
            throw new IllegalArgumentException(e);
        }
    }


    @Autowired
    private ContactMechApplicationService contactMechApplicationService;


    /**
     * Retrieve.
     * Retrieve TelecomNumbers
     */
    @GetMapping
    @Transactional(readOnly = true)
    public TelecomNumberStateDto[] getAll( HttpServletRequest request,
                    @RequestParam(value = "sort", required = false) String sort,
                    @RequestParam(value = "fields", required = false) String fields,
                    @RequestParam(value = "firstResult", defaultValue = "0") Integer firstResult,
                    @RequestParam(value = "maxResults", defaultValue = "2147483647") Integer maxResults,
                    @RequestParam(value = "filter", required = false) String filter) {
        
        if (firstResult < 0) { firstResult = 0; }
        if (maxResults == null || maxResults < 1) { maxResults = Integer.MAX_VALUE; }

            Iterable<ContactMechState> states = null; 
            CriterionDto criterion = null;
            if (!StringHelper.isNullOrEmpty(filter)) {
                criterion = deserializeCriterionDto(filter);
            } else {
                criterion = QueryParamUtils.getQueryCriterionDto(request.getParameterMap().entrySet().stream()
                    .filter(kv -> TelecomNumberResourceUtils.getFilterPropertyName(kv.getKey()) != null)
                    .collect(Collectors.toMap(kv -> kv.getKey(), kv -> kv.getValue())));
            }
            Criterion c = CriterionDto.toSubclass(criterion, getCriterionTypeConverter(), getPropertyTypeResolver(), 
                n -> (ContactMechMetadata.aliasMap.containsKey(n) ? ContactMechMetadata.aliasMap.get(n) : n));
            states = contactMechApplicationService.get(TelecomNumberState.class, 
                c,
                TelecomNumberResourceUtils.getQuerySorts(request.getParameterMap()),
                firstResult, maxResults);

            TelecomNumberStateDto.DtoConverter dtoConverter = new TelecomNumberStateDto.DtoConverter();
            if (StringHelper.isNullOrEmpty(fields)) {
                dtoConverter.setAllFieldsReturned(true);
            } else {
                dtoConverter.setReturnedFieldsString(fields);
            }
            return dtoConverter.toTelecomNumberStateDtoArray(states);

        
    }

    /**
     * Retrieve in pages.
     * Retrieve TelecomNumbers in pages.
     */
    @GetMapping("_page")
    @Transactional(readOnly = true)
    public Page<TelecomNumberStateDto> getPage( HttpServletRequest request,
                    @RequestParam(value = "fields", required = false) String fields,
                    @RequestParam(value = "page", defaultValue = "0") Integer page,
                    @RequestParam(value = "size", defaultValue = "20") Integer size,
                    @RequestParam(value = "filter", required = false) String filter) {
        
            Integer firstResult = (page == null ? 0 : page) * (size == null ? 20 : size);
            Integer maxResults = (size == null ? 20 : size);
            Iterable<ContactMechState> states = null; 
            CriterionDto criterion = null;
            if (!StringHelper.isNullOrEmpty(filter)) {
                criterion = deserializeCriterionDto(filter);
            } else {
                criterion = QueryParamUtils.getQueryCriterionDto(request.getParameterMap().entrySet().stream()
                    .filter(kv -> TelecomNumberResourceUtils.getFilterPropertyName(kv.getKey()) != null)
                    .collect(Collectors.toMap(kv -> kv.getKey(), kv -> kv.getValue())));
            }
            Criterion c = CriterionDto.toSubclass(criterion, getCriterionTypeConverter(), getPropertyTypeResolver(), 
                n -> (ContactMechMetadata.aliasMap.containsKey(n) ? ContactMechMetadata.aliasMap.get(n) : n));
            states = contactMechApplicationService.get(TelecomNumberState.class, 
                c,
                TelecomNumberResourceUtils.getQuerySorts(request.getParameterMap()),
                firstResult, maxResults);
            long count = contactMechApplicationService.getCount(TelecomNumberState.class, c);

            TelecomNumberStateDto.DtoConverter dtoConverter = new TelecomNumberStateDto.DtoConverter();
            if (StringHelper.isNullOrEmpty(fields)) {
                dtoConverter.setAllFieldsReturned(true);
            } else {
                dtoConverter.setReturnedFieldsString(fields);
            }
            Page.PageImpl<TelecomNumberStateDto> statePage =  new Page.PageImpl<>(dtoConverter.toTelecomNumberStateDtoList(states), count);
            statePage.setSize(size);
            statePage.setNumber(page);
            return statePage;

        
    }

    /**
     * Retrieve.
     * Retrieves ContactMech with the specified ID.
     */
    @GetMapping("{contactMechId}")
    @Transactional(readOnly = true)
    public TelecomNumberStateDto get(@PathVariable("contactMechId") String contactMechId, @RequestParam(value = "fields", required = false) String fields) {
        
            String idObj = contactMechId;
            ContactMechState state = contactMechApplicationService.get(idObj);
            if (state == null) { return null; }

            if (state != null && !TelecomNumberState.class.isAssignableFrom(state.getClass())) {
                return null;
            }
            TelecomNumberStateDto.DtoConverter dtoConverter = new TelecomNumberStateDto.DtoConverter();
            if (StringHelper.isNullOrEmpty(fields)) {
                dtoConverter.setAllFieldsReturned(true);
            } else {
                dtoConverter.setReturnedFieldsString(fields);
            }
            return dtoConverter.toTelecomNumberStateDto(state);

        
    }

    @GetMapping("_count")
    @Transactional(readOnly = true)
    public long getCount( HttpServletRequest request,
                         @RequestParam(value = "filter", required = false) String filter) {
        
            long count = 0;
            CriterionDto criterion = null;
            if (!StringHelper.isNullOrEmpty(filter)) {
                criterion = deserializeCriterionDto(filter);
            } else {
                criterion = QueryParamUtils.getQueryCriterionDto(request.getParameterMap());
            }
            Criterion c = CriterionDto.toSubclass(criterion,
                getCriterionTypeConverter(), 
                getPropertyTypeResolver(), 
                n -> (ContactMechMetadata.aliasMap.containsKey(n) ? ContactMechMetadata.aliasMap.get(n) : n));
            count = contactMechApplicationService.getCount(TelecomNumberState.class, c);
            return count;

        
    }


    /**
     * Create.
     * Create ContactMech
     */
    @PostMapping @ResponseBody @ResponseStatus(HttpStatus.CREATED)
    public String post(@RequestBody CreateOrMergePatchContactMechDto.CreateContactMechDto value,  HttpServletResponse response) {
        
            value.setContactMechTypeId(ContactMechTypeId.TELECOM_NUMBER);
            ContactMechCommand.CreateContactMech cmd = value;//.toCreateContactMech();
            if (cmd.getContactMechId() == null) {
                throw DomainError.named("nullId", "Aggregate Id in cmd is null, aggregate name: %1$s.", "ContactMech");
            }
            String idObj = cmd.getContactMechId();
            cmd.setRequesterId(SecurityContextUtil.getRequesterId());
            contactMechApplicationService.when(cmd);

            return idObj;
        
    }


    /**
     * Create or update.
     * Create or update ContactMech
     */
    @PutMapping("{contactMechId}")
    public void put(@PathVariable("contactMechId") String contactMechId, @RequestBody CreateOrMergePatchContactMechDto value) {
        
            value.setContactMechTypeId(ContactMechTypeId.TELECOM_NUMBER);
            if (value.getVersion() != null) {
                value.setCommandType(Command.COMMAND_TYPE_MERGE_PATCH);
                ContactMechCommand.MergePatchContactMech cmd = (ContactMechCommand.MergePatchContactMech) value.toSubclass();
                TelecomNumberResourceUtils.setNullIdOrThrowOnInconsistentIds(contactMechId, cmd);
                cmd.setRequesterId(SecurityContextUtil.getRequesterId());
                contactMechApplicationService.when(cmd);
                return;
            }

            value.setCommandType(Command.COMMAND_TYPE_CREATE);
            ContactMechCommand.CreateContactMech cmd = (ContactMechCommand.CreateContactMech) value.toSubclass();
            TelecomNumberResourceUtils.setNullIdOrThrowOnInconsistentIds(contactMechId, cmd);
            cmd.setRequesterId(SecurityContextUtil.getRequesterId());
            contactMechApplicationService.when(cmd);

        
    }


    /**
     * Patch.
     * Patch ContactMech
     */
    @PatchMapping("{contactMechId}")
    public void patch(@PathVariable("contactMechId") String contactMechId, @RequestBody CreateOrMergePatchContactMechDto.MergePatchContactMechDto value) {
        

            value.setContactMechTypeId(ContactMechTypeId.TELECOM_NUMBER);
            ContactMechCommand.MergePatchContactMech cmd = value;//.toMergePatchContactMech();
            TelecomNumberResourceUtils.setNullIdOrThrowOnInconsistentIds(contactMechId, cmd);
            cmd.setRequesterId(SecurityContextUtil.getRequesterId());
            contactMechApplicationService.when(cmd);

        
    }

    @GetMapping("_metadata/filteringFields")
    public List<PropertyMetadataDto> getMetadataFilteringFields() {
        

            List<PropertyMetadataDto> filtering = new ArrayList<>();
            ContactMechMetadata.propertyTypeMap.forEach((key, value) -> {
                filtering.add(new PropertyMetadataDto(key, value, true));
            });
            return filtering;

        
    }

    @GetMapping("{contactMechId}/_events/{version}")
    @Transactional(readOnly = true)
    public ContactMechEvent getEvent(@PathVariable("contactMechId") String contactMechId, @PathVariable("version") long version) {
        

            String idObj = contactMechId;
            //ContactMechStateEventDtoConverter dtoConverter = getContactMechStateEventDtoConverter();
            return contactMechApplicationService.getEvent(idObj, version);

        
    }

    @GetMapping("{contactMechId}/_historyStates/{version}")
    @Transactional(readOnly = true)
    public TelecomNumberStateDto getHistoryState(@PathVariable("contactMechId") String contactMechId, @PathVariable("version") long version, @RequestParam(value = "fields", required = false) String fields) {
        

            String idObj = contactMechId;
            TelecomNumberStateDto.DtoConverter dtoConverter = new TelecomNumberStateDto.DtoConverter();
            if (StringHelper.isNullOrEmpty(fields)) {
                dtoConverter.setAllFieldsReturned(true);
            } else {
                dtoConverter.setReturnedFieldsString(fields);
            }
            return dtoConverter.toTelecomNumberStateDto(contactMechApplicationService.getHistoryState(idObj, version));

        
    }



    //protected  ContactMechStateEventDtoConverter getContactMechStateEventDtoConverter() {
    //    return new ContactMechStateEventDtoConverter();
    //}

    protected TypeConverter getCriterionTypeConverter() {
        return new DefaultTypeConverter();
    }

    protected PropertyTypeResolver getPropertyTypeResolver() {
        return new PropertyTypeResolver() {
            @Override
            public Class resolveTypeByPropertyName(String propertyName) {
                return TelecomNumberResourceUtils.getFilterPropertyType(propertyName);
            }
        };
    }

 
    public static class TelecomNumberResourceUtils {

        public static void setNullIdOrThrowOnInconsistentIds(String contactMechId, org.dddml.ffvtraceability.domain.contactmech.ContactMechCommand value) {
            String idObj = contactMechId;
            if (value.getContactMechId() == null) {
                value.setContactMechId(idObj);
            } else if (!value.getContactMechId().equals(idObj)) {
                throw DomainError.named("inconsistentId", "Argument Id %1$s NOT equals body Id %2$s", contactMechId, value.getContactMechId());
            }
        }
    
        public static List<String> getQueryOrders(String str, String separator) {
            return QueryParamUtils.getQueryOrders(str, separator, ContactMechMetadata.aliasMap);
        }

        public static List<String> getQuerySorts(Map<String, String[]> queryNameValuePairs) {
            String[] values = queryNameValuePairs.get("sort");
            return QueryParamUtils.getQuerySorts(values, ContactMechMetadata.aliasMap);
        }

        public static String getFilterPropertyName(String fieldName) {
            if ("sort".equalsIgnoreCase(fieldName)
                    || "firstResult".equalsIgnoreCase(fieldName)
                    || "maxResults".equalsIgnoreCase(fieldName)
                    || "fields".equalsIgnoreCase(fieldName)) {
                return null;
            }
            if (ContactMechMetadata.aliasMap.containsKey(fieldName)) {
                return ContactMechMetadata.aliasMap.get(fieldName);
            }
            return null;
        }

        public static Class getFilterPropertyType(String propertyName) {
            if (ContactMechMetadata.propertyTypeMap.containsKey(propertyName)) {
                String propertyType = ContactMechMetadata.propertyTypeMap.get(propertyName);
                if (!StringHelper.isNullOrEmpty(propertyType)) {
                    if (BoundedContextMetadata.CLASS_MAP.containsKey(propertyType)) {
                        return BoundedContextMetadata.CLASS_MAP.get(propertyType);
                    }
                }
            }
            return String.class;
        }

        public static Iterable<Map.Entry<String, Object>> getQueryFilterMap(Map<String, String[]> queryNameValuePairs) {
            Map<String, Object> filter = new HashMap<>();
            queryNameValuePairs.forEach((key, values) -> {
                if (values.length > 0) {
                    String pName = getFilterPropertyName(key);
                    if (!StringHelper.isNullOrEmpty(pName)) {
                        Class pClass = getFilterPropertyType(pName);
                        filter.put(pName, ApplicationContext.current.getTypeConverter().convertFromString(pClass, values[0]));
                    }
                }
            });
            return filter.entrySet();
        }

        public static TelecomNumberStateDto[] toTelecomNumberStateDtoArray(Iterable<String> ids) {
            List<TelecomNumberStateDto> states = new ArrayList<>();
            ids.forEach(i -> {
                TelecomNumberStateDto dto = new TelecomNumberStateDto();
                dto.setContactMechId(i);
                states.add(dto);
            });
            return states.toArray(new TelecomNumberStateDto[0]);
        }

    }

}

