// <autogenerated>
//   This file was generated by dddappp code generator.
//   Any changes made to this file manually will be lost next time the file is regenerated.
// </autogenerated>

package org.dddml.ffvtraceability.restful.resource;

import java.util.*;
import java.util.stream.*;
import jakarta.servlet.http.*;
import jakarta.validation.constraints.*;
import org.springframework.http.MediaType;
import org.springframework.http.HttpStatus;
import org.springframework.web.bind.annotation.*;
import org.springframework.transaction.annotation.Transactional;
import org.dddml.support.criterion.*;
import org.dddml.ffvtraceability.domain.*;
import java.time.OffsetDateTime;
import org.dddml.ffvtraceability.specialization.*;
import org.dddml.ffvtraceability.domain.transformationevent.*;
import static org.dddml.ffvtraceability.domain.meta.M.*;

import com.fasterxml.jackson.databind.ObjectMapper;
import org.springframework.beans.factory.annotation.Autowired;
import org.dddml.support.criterion.TypeConverter;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

@RequestMapping(path = "TransformationEvents", produces = MediaType.APPLICATION_JSON_VALUE)
@RestController
public class TransformationEventResource {
    private Logger logger = LoggerFactory.getLogger(this.getClass());


    @Autowired
    private TransformationEventApplicationService transformationEventApplicationService;


    /**
     * Retrieve.
     * Retrieve TransformationEvents
     */
    @GetMapping
    @Transactional(readOnly = true)
    public TransformationEventStateDto[] getAll( HttpServletRequest request,
                    @RequestParam(value = "sort", required = false) String sort,
                    @RequestParam(value = "fields", required = false) String fields,
                    @RequestParam(value = "firstResult", defaultValue = "0") Integer firstResult,
                    @RequestParam(value = "maxResults", defaultValue = "2147483647") Integer maxResults,
                    @RequestParam(value = "filter", required = false) String filter) {
        try {
        if (firstResult < 0) { firstResult = 0; }
        if (maxResults == null || maxResults < 1) { maxResults = Integer.MAX_VALUE; }

            Iterable<TransformationEventState> states = null; 
            CriterionDto criterion = null;
            if (!StringHelper.isNullOrEmpty(filter)) {
                criterion = new ObjectMapper().readValue(filter, CriterionDto.class);
            } else {
                criterion = QueryParamUtils.getQueryCriterionDto(request.getParameterMap().entrySet().stream()
                    .filter(kv -> TransformationEventResourceUtils.getFilterPropertyName(kv.getKey()) != null)
                    .collect(Collectors.toMap(kv -> kv.getKey(), kv -> kv.getValue())));
            }
            Criterion c = CriterionDto.toSubclass(criterion, getCriterionTypeConverter(), getPropertyTypeResolver(), 
                n -> (TransformationEventMetadata.aliasMap.containsKey(n) ? TransformationEventMetadata.aliasMap.get(n) : n));
            states = transformationEventApplicationService.get(
                c,
                TransformationEventResourceUtils.getQuerySorts(request.getParameterMap()),
                firstResult, maxResults);

            TransformationEventStateDto.DtoConverter dtoConverter = new TransformationEventStateDto.DtoConverter();
            if (StringHelper.isNullOrEmpty(fields)) {
                dtoConverter.setAllFieldsReturned(true);
            } else {
                dtoConverter.setReturnedFieldsString(fields);
            }
            return dtoConverter.toTransformationEventStateDtoArray(states);

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }

    /**
     * Retrieve in pages.
     * Retrieve TransformationEvents in pages.
     */
    @GetMapping("_page")
    @Transactional(readOnly = true)
    public Page<TransformationEventStateDto> getPage( HttpServletRequest request,
                    @RequestParam(value = "fields", required = false) String fields,
                    @RequestParam(value = "page", defaultValue = "0") Integer page,
                    @RequestParam(value = "size", defaultValue = "20") Integer size,
                    @RequestParam(value = "filter", required = false) String filter) {
        try {
            Integer firstResult = (page == null ? 0 : page) * (size == null ? 20 : size);
            Integer maxResults = (size == null ? 20 : size);
            Iterable<TransformationEventState> states = null; 
            CriterionDto criterion = null;
            if (!StringHelper.isNullOrEmpty(filter)) {
                criterion = new ObjectMapper().readValue(filter, CriterionDto.class);
            } else {
                criterion = QueryParamUtils.getQueryCriterionDto(request.getParameterMap().entrySet().stream()
                    .filter(kv -> TransformationEventResourceUtils.getFilterPropertyName(kv.getKey()) != null)
                    .collect(Collectors.toMap(kv -> kv.getKey(), kv -> kv.getValue())));
            }
            Criterion c = CriterionDto.toSubclass(criterion, getCriterionTypeConverter(), getPropertyTypeResolver(), 
                n -> (TransformationEventMetadata.aliasMap.containsKey(n) ? TransformationEventMetadata.aliasMap.get(n) : n));
            states = transformationEventApplicationService.get(
                c,
                TransformationEventResourceUtils.getQuerySorts(request.getParameterMap()),
                firstResult, maxResults);
            long count = transformationEventApplicationService.getCount(c);

            TransformationEventStateDto.DtoConverter dtoConverter = new TransformationEventStateDto.DtoConverter();
            if (StringHelper.isNullOrEmpty(fields)) {
                dtoConverter.setAllFieldsReturned(true);
            } else {
                dtoConverter.setReturnedFieldsString(fields);
            }
            Page.PageImpl<TransformationEventStateDto> statePage =  new Page.PageImpl<>(dtoConverter.toTransformationEventStateDtoList(states), count);
            statePage.setSize(size);
            statePage.setNumber(page);
            return statePage;

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }

    /**
     * Retrieve.
     * Retrieves TransformationEvent with the specified ID.
     */
    @GetMapping("{eventId}")
    @Transactional(readOnly = true)
    public TransformationEventStateDto get(@PathVariable("eventId") Long eventId, @RequestParam(value = "fields", required = false) String fields) {
        try {
            Long idObj = eventId;
            TransformationEventState state = transformationEventApplicationService.get(idObj);
            if (state == null) { return null; }

            TransformationEventStateDto.DtoConverter dtoConverter = new TransformationEventStateDto.DtoConverter();
            if (StringHelper.isNullOrEmpty(fields)) {
                dtoConverter.setAllFieldsReturned(true);
            } else {
                dtoConverter.setReturnedFieldsString(fields);
            }
            return dtoConverter.toTransformationEventStateDto(state);

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }

    @GetMapping("_count")
    @Transactional(readOnly = true)
    public long getCount( HttpServletRequest request,
                         @RequestParam(value = "filter", required = false) String filter) {
        try {
            long count = 0;
            CriterionDto criterion = null;
            if (!StringHelper.isNullOrEmpty(filter)) {
                criterion = new ObjectMapper().readValue(filter, CriterionDto.class);
            } else {
                criterion = QueryParamUtils.getQueryCriterionDto(request.getParameterMap());
            }
            Criterion c = CriterionDto.toSubclass(criterion,
                getCriterionTypeConverter(), 
                getPropertyTypeResolver(), 
                n -> (TransformationEventMetadata.aliasMap.containsKey(n) ? TransformationEventMetadata.aliasMap.get(n) : n));
            count = transformationEventApplicationService.getCount(c);
            return count;

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }


    /**
     * Create.
     * Create TransformationEvent
     */
    @PostMapping @ResponseStatus(HttpStatus.CREATED)
    public Long post(@RequestBody CreateOrMergePatchTransformationEventDto.CreateTransformationEventDto value,  HttpServletResponse response) {
        try {
            TransformationEventCommand.CreateTransformationEvent cmd = value;//.toCreateTransformationEvent();
            if (cmd.getEventId() == null) {
                throw DomainError.named("nullId", "Aggregate Id in cmd is null, aggregate name: %1$s.", "TransformationEvent");
            }
            Long idObj = cmd.getEventId();
            cmd.setRequesterId(SecurityContextUtil.getRequesterId());
            transformationEventApplicationService.when(cmd);

            return idObj;
        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }


    /**
     * Create or update.
     * Create or update TransformationEvent
     */
    @PutMapping("{eventId}")
    public void put(@PathVariable("eventId") Long eventId, @RequestBody CreateOrMergePatchTransformationEventDto value) {
        try {
            if (value.getVersion() != null) {
                value.setCommandType(Command.COMMAND_TYPE_MERGE_PATCH);
                TransformationEventCommand.MergePatchTransformationEvent cmd = (TransformationEventCommand.MergePatchTransformationEvent) value.toSubclass();
                TransformationEventResourceUtils.setNullIdOrThrowOnInconsistentIds(eventId, cmd);
                cmd.setRequesterId(SecurityContextUtil.getRequesterId());
                transformationEventApplicationService.when(cmd);
                return;
            }

            value.setCommandType(Command.COMMAND_TYPE_CREATE);
            TransformationEventCommand.CreateTransformationEvent cmd = (TransformationEventCommand.CreateTransformationEvent) value.toSubclass();
            TransformationEventResourceUtils.setNullIdOrThrowOnInconsistentIds(eventId, cmd);
            cmd.setRequesterId(SecurityContextUtil.getRequesterId());
            transformationEventApplicationService.when(cmd);

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }


    /**
     * Patch.
     * Patch TransformationEvent
     */
    @PatchMapping("{eventId}")
    public void patch(@PathVariable("eventId") Long eventId, @RequestBody CreateOrMergePatchTransformationEventDto.MergePatchTransformationEventDto value) {
        try {

            TransformationEventCommand.MergePatchTransformationEvent cmd = value;//.toMergePatchTransformationEvent();
            TransformationEventResourceUtils.setNullIdOrThrowOnInconsistentIds(eventId, cmd);
            cmd.setRequesterId(SecurityContextUtil.getRequesterId());
            transformationEventApplicationService.when(cmd);

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }

    /**
     * Delete.
     * Delete TransformationEvent
     */
    @DeleteMapping("{eventId}")
    public void delete(@PathVariable("eventId") Long eventId,
                       @NotNull @RequestParam(value = "commandId", required = false) String commandId,
                       @NotNull @RequestParam(value = "version", required = false) @Min(value = -1) Long version,
                       @RequestParam(value = "requesterId", required = false) String requesterId) {
        try {

            TransformationEventCommand.DeleteTransformationEvent deleteCmd = new DeleteTransformationEventDto();//.toDeleteTransformationEvent();;

            deleteCmd.setCommandId(commandId);
            deleteCmd.setRequesterId(requesterId);
            deleteCmd.setVersion(version);
            TransformationEventResourceUtils.setNullIdOrThrowOnInconsistentIds(eventId, deleteCmd);
            deleteCmd.setRequesterId(SecurityContextUtil.getRequesterId());
            transformationEventApplicationService.when(deleteCmd);

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }

    @GetMapping("_metadata/filteringFields")
    public List<PropertyMetadataDto> getMetadataFilteringFields() {
        try {

            List<PropertyMetadataDto> filtering = new ArrayList<>();
            TransformationEventMetadata.propertyTypeMap.forEach((key, value) -> {
                filtering.add(new PropertyMetadataDto(key, value, true));
            });
            return filtering;

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }


    protected TypeConverter getCriterionTypeConverter() {
        return new DefaultTypeConverter();
    }

    protected PropertyTypeResolver getPropertyTypeResolver() {
        return new PropertyTypeResolver() {
            @Override
            public Class resolveTypeByPropertyName(String propertyName) {
                return TransformationEventResourceUtils.getFilterPropertyType(propertyName);
            }
        };
    }

    // ////////////////////////////////
 
    public static class TransformationEventResourceUtils {

        public static void setNullIdOrThrowOnInconsistentIds(Long eventId, org.dddml.ffvtraceability.domain.transformationevent.TransformationEventCommand value) {
            Long idObj = eventId;
            if (value.getEventId() == null) {
                value.setEventId(idObj);
            } else if (!value.getEventId().equals(idObj)) {
                throw DomainError.named("inconsistentId", "Argument Id %1$s NOT equals body Id %2$s", eventId, value.getEventId());
            }
        }
    
        public static List<String> getQueryOrders(String str, String separator) {
            return QueryParamUtils.getQueryOrders(str, separator, TransformationEventMetadata.aliasMap);
        }

        public static List<String> getQuerySorts(Map<String, String[]> queryNameValuePairs) {
            String[] values = queryNameValuePairs.get("sort");
            return QueryParamUtils.getQuerySorts(values, TransformationEventMetadata.aliasMap);
        }

        public static String getFilterPropertyName(String fieldName) {
            if ("sort".equalsIgnoreCase(fieldName)
                    || "firstResult".equalsIgnoreCase(fieldName)
                    || "maxResults".equalsIgnoreCase(fieldName)
                    || "fields".equalsIgnoreCase(fieldName)) {
                return null;
            }
            if (TransformationEventMetadata.aliasMap.containsKey(fieldName)) {
                return TransformationEventMetadata.aliasMap.get(fieldName);
            }
            return null;
        }

        public static Class getFilterPropertyType(String propertyName) {
            if (TransformationEventMetadata.propertyTypeMap.containsKey(propertyName)) {
                String propertyType = TransformationEventMetadata.propertyTypeMap.get(propertyName);
                if (!StringHelper.isNullOrEmpty(propertyType)) {
                    if (BoundedContextMetadata.CLASS_MAP.containsKey(propertyType)) {
                        return BoundedContextMetadata.CLASS_MAP.get(propertyType);
                    }
                }
            }
            return String.class;
        }

        public static Iterable<Map.Entry<String, Object>> getQueryFilterMap(Map<String, String[]> queryNameValuePairs) {
            Map<String, Object> filter = new HashMap<>();
            queryNameValuePairs.forEach((key, values) -> {
                if (values.length > 0) {
                    String pName = getFilterPropertyName(key);
                    if (!StringHelper.isNullOrEmpty(pName)) {
                        Class pClass = getFilterPropertyType(pName);
                        filter.put(pName, ApplicationContext.current.getTypeConverter().convertFromString(pClass, values[0]));
                    }
                }
            });
            return filter.entrySet();
        }

        public static TransformationEventStateDto[] toTransformationEventStateDtoArray(Iterable<Long> ids) {
            List<TransformationEventStateDto> states = new ArrayList<>();
            ids.forEach(i -> {
                TransformationEventStateDto dto = new TransformationEventStateDto();
                dto.setEventId(i);
                states.add(dto);
            });
            return states.toArray(new TransformationEventStateDto[0]);
        }

    }

}

