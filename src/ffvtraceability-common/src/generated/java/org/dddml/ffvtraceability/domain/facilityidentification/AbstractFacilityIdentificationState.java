// <autogenerated>
//   This file was generated by dddappp code generator.
//   Any changes made to this file manually will be lost next time the file is regenerated.
// </autogenerated>

package org.dddml.ffvtraceability.domain.facilityidentification;

import java.util.*;
import java.math.*;
import java.time.OffsetDateTime;
import org.dddml.ffvtraceability.domain.*;
import org.dddml.ffvtraceability.specialization.*;
import org.dddml.ffvtraceability.domain.facilityidentification.FacilityIdentificationEvent.*;

public abstract class AbstractFacilityIdentificationState implements FacilityIdentificationState.SqlFacilityIdentificationState {

    private String facilityIdentificationTypeId;

    public String getFacilityIdentificationTypeId() {
        return this.facilityIdentificationTypeId;
    }

    public void setFacilityIdentificationTypeId(String facilityIdentificationTypeId) {
        this.facilityIdentificationTypeId = facilityIdentificationTypeId;
    }

    private String idValue;

    public String getIdValue() {
        return this.idValue;
    }

    public void setIdValue(String idValue) {
        this.idValue = idValue;
    }

    private Long version;

    public Long getVersion() {
        return this.version;
    }

    public void setVersion(Long version) {
        this.version = version;
    }

    private String createdBy;

    public String getCreatedBy() {
        return this.createdBy;
    }

    public void setCreatedBy(String createdBy) {
        this.createdBy = createdBy;
    }

    private OffsetDateTime createdAt;

    public OffsetDateTime getCreatedAt() {
        return this.createdAt;
    }

    public void setCreatedAt(OffsetDateTime createdAt) {
        this.createdAt = createdAt;
    }

    private String updatedBy;

    public String getUpdatedBy() {
        return this.updatedBy;
    }

    public void setUpdatedBy(String updatedBy) {
        this.updatedBy = updatedBy;
    }

    private OffsetDateTime updatedAt;

    public OffsetDateTime getUpdatedAt() {
        return this.updatedAt;
    }

    public void setUpdatedAt(OffsetDateTime updatedAt) {
        this.updatedAt = updatedAt;
    }

    private Boolean deleted;

    public Boolean getDeleted() {
        return this.deleted;
    }

    public void setDeleted(Boolean deleted) {
        this.deleted = deleted;
    }

    public boolean isStateUnsaved() {
        return this.getVersion() == null;
    }

    private Boolean stateReadOnly;

    public Boolean getStateReadOnly() { return this.stateReadOnly; }

    public void setStateReadOnly(Boolean readOnly) { this.stateReadOnly = readOnly; }

    private boolean forReapplying;

    public boolean getForReapplying() {
        return forReapplying;
    }

    public void setForReapplying(boolean forReapplying) {
        this.forReapplying = forReapplying;
    }

    public AbstractFacilityIdentificationState(List<Event> events) {
        initializeForReapplying();
        if (events != null && events.size() > 0) {
            this.setFacilityIdentificationTypeId(((FacilityIdentificationEvent.SqlFacilityIdentificationEvent) events.get(0)).getFacilityIdentificationEventId().getFacilityIdentificationTypeId());
            for (Event e : events) {
                mutate(e);
                this.setVersion((this.getVersion() == null ? FacilityIdentificationState.VERSION_NULL : this.getVersion()) + 1);
            }
        }
    }


    public AbstractFacilityIdentificationState() {
        initializeProperties();
    }

    protected void initializeForReapplying() {
        this.forReapplying = true;

        initializeProperties();
    }
    
    protected void initializeProperties() {
    }

    @Override
    public int hashCode() {
        return getFacilityIdentificationTypeId().hashCode();
    }

    @Override
    public boolean equals(Object obj) {
        if (this == obj) { return true; }
        if (obj instanceof FacilityIdentificationState) {
            return Objects.equals(this.getFacilityIdentificationTypeId(), ((FacilityIdentificationState)obj).getFacilityIdentificationTypeId());
        }
        return false;
    }


    public void mutate(Event e) {
        setStateReadOnly(false);
        if (e instanceof FacilityIdentificationStateCreated) {
            when((FacilityIdentificationStateCreated) e);
        } else if (e instanceof FacilityIdentificationStateMergePatched) {
            when((FacilityIdentificationStateMergePatched) e);
        } else if (e instanceof FacilityIdentificationStateDeleted) {
            when((FacilityIdentificationStateDeleted) e);
        } else {
            throw new UnsupportedOperationException(String.format("Unsupported event type: %1$s", e.getClass().getName()));
        }
    }

    public void when(FacilityIdentificationStateCreated e) {
        throwOnWrongEvent(e);

        this.setIdValue(e.getIdValue());

        this.setDeleted(false);

        this.setCreatedBy(e.getCreatedBy());
        this.setCreatedAt(e.getCreatedAt());

    }

    public void merge(FacilityIdentificationState s) {
        if (s == this) {
            return;
        }
        this.setIdValue(s.getIdValue());
    }

    public void when(FacilityIdentificationStateMergePatched e) {
        throwOnWrongEvent(e);

        if (e.getIdValue() == null) {
            if (e.getIsPropertyIdValueRemoved() != null && e.getIsPropertyIdValueRemoved()) {
                this.setIdValue(null);
            }
        } else {
            this.setIdValue(e.getIdValue());
        }

        this.setUpdatedBy(e.getCreatedBy());
        this.setUpdatedAt(e.getCreatedAt());

    }

    public void when(FacilityIdentificationStateDeleted e) {
        throwOnWrongEvent(e);

        this.setDeleted(true);
        this.setUpdatedBy(e.getCreatedBy());
        this.setUpdatedAt(e.getCreatedAt());

    }

    public void save() {
    }

    protected void throwOnWrongEvent(FacilityIdentificationEvent event) {
        String stateEntityId = this.getFacilityIdentificationTypeId(); // Aggregate Id
        String eventEntityId = ((FacilityIdentificationEvent.SqlFacilityIdentificationEvent)event).getFacilityIdentificationEventId().getFacilityIdentificationTypeId(); // EntityBase.Aggregate.GetEventIdPropertyIdName();
        if (!stateEntityId.equals(eventEntityId)) {
            throw DomainError.named("mutateWrongEntity", "Entity Id %1$s in state but entity id %2$s in event", stateEntityId, eventEntityId);
        }


        Long stateVersion = this.getVersion();
        Long eventVersion = ((FacilityIdentificationEvent.SqlFacilityIdentificationEvent)event).getFacilityIdentificationEventId().getVersion();// Event Version
        if (eventVersion == null) {
            throw new NullPointerException("event.getFacilityIdentificationEventId().getVersion() == null");
        }
        if (!(stateVersion == null && eventVersion.equals(VERSION_NULL)) && !eventVersion.equals(stateVersion)) {
            throw DomainError.named("concurrencyConflict", "Conflict between state version (%1$s) and event version (%2$s)", stateVersion, eventVersion == VERSION_NULL ? "NULL" : eventVersion + "");
        }

    }


    public static class SimpleFacilityIdentificationState extends AbstractFacilityIdentificationState {

        public SimpleFacilityIdentificationState() {
        }

        public SimpleFacilityIdentificationState(List<Event> events) {
            super(events);
        }

        public static SimpleFacilityIdentificationState newForReapplying() {
            SimpleFacilityIdentificationState s = new SimpleFacilityIdentificationState();
            s.initializeForReapplying();
            return s;
        }

    }



}

