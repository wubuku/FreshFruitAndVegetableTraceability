// <autogenerated>
//   This file was generated by dddappp code generator.
//   Any changes made to this file manually will be lost next time the file is regenerated.
// </autogenerated>

package org.dddml.ffvtraceability.domain.geoassoc;

import java.util.*;
import java.util.function.Consumer;
import org.dddml.support.criterion.Criterion;
import java.time.OffsetDateTime;
import org.dddml.ffvtraceability.domain.*;
import org.dddml.ffvtraceability.specialization.*;

public abstract class AbstractGeoAssocApplicationService implements GeoAssocApplicationService {
    private GeoAssocStateRepository stateRepository;

    protected GeoAssocStateRepository getStateRepository() {
        return stateRepository;
    }

    private GeoAssocStateQueryRepository stateQueryRepository;

    protected GeoAssocStateQueryRepository getStateQueryRepository() {
        return stateQueryRepository;
    }

    public AbstractGeoAssocApplicationService(GeoAssocStateRepository stateRepository, GeoAssocStateQueryRepository stateQueryRepository) {
        this.stateRepository = stateRepository;
        this.stateQueryRepository = stateQueryRepository;
    }

    public void when(GeoAssocCommand.CreateGeoAssoc c) {
        update(c, s -> {
        // //////////////////////////
        throwOnConcurrencyConflict(s, c);
        GeoAssocState.SqlGeoAssocState ss = ((GeoAssocState.SqlGeoAssocState)s);
        ss.setGeoAssocTypeId(c.getGeoAssocTypeId());
        ss.setCreatedBy(c.getRequesterId());
        ss.setCreatedAt((OffsetDateTime)ApplicationContext.current.getTimestampService().now(OffsetDateTime.class));
        ss.setCommandId(c.getCommandId());
        // //////////////////////////
        });
    }

    public void when(GeoAssocCommand.MergePatchGeoAssoc c) {
        update(c, s -> {
        // //////////////////////////////////
        throwOnConcurrencyConflict(s, c);
        GeoAssocState.SqlGeoAssocState ss = ((GeoAssocState.SqlGeoAssocState)s);
        if (c.getGeoAssocTypeId() == null) {
            if (c.getIsPropertyGeoAssocTypeIdRemoved() != null && c.getIsPropertyGeoAssocTypeIdRemoved()) {
                ss.setGeoAssocTypeId(null);
            }
        } else {
            ss.setGeoAssocTypeId(c.getGeoAssocTypeId());
        }
        ss.setUpdatedBy(c.getRequesterId());
        ss.setUpdatedAt((OffsetDateTime)ApplicationContext.current.getTimestampService().now(OffsetDateTime.class));
        ss.setCommandId(c.getCommandId());
        // //////////////////////////////////
        });
    }

    public GeoAssocState get(GeoAssocId id) {
        GeoAssocState state = getStateRepository().get(id, true);
        return state;
    }

    public Iterable<GeoAssocState> getAll(Integer firstResult, Integer maxResults) {
        return getStateQueryRepository().getAll(firstResult, maxResults);
    }

    public Iterable<GeoAssocState> get(Iterable<Map.Entry<String, Object>> filter, List<String> orders, Integer firstResult, Integer maxResults) {
        return getStateQueryRepository().get(filter, orders, firstResult, maxResults);
    }

    public Iterable<GeoAssocState> get(Criterion filter, List<String> orders, Integer firstResult, Integer maxResults) {
        return getStateQueryRepository().get(filter, orders, firstResult, maxResults);
    }

    public Iterable<GeoAssocState> getByProperty(String propertyName, Object propertyValue, List<String> orders, Integer firstResult, Integer maxResults) {
        return getStateQueryRepository().getByProperty(propertyName, propertyValue, orders, firstResult, maxResults);
    }

    public long getCount(Iterable<Map.Entry<String, Object>> filter) {
        return getStateQueryRepository().getCount(filter);
    }

    public long getCount(Criterion filter) {
        return getStateQueryRepository().getCount(filter);
    }

    public EventStoreAggregateId toEventStoreAggregateId(GeoAssocId aggregateId) {
        return new EventStoreAggregateId.SimpleEventStoreAggregateId(aggregateId);
    }

    protected void update(GeoAssocCommand c, Consumer<GeoAssocState> action) {
        GeoAssocId aggregateId = c.getGeoAssocId();
        EventStoreAggregateId eventStoreAggregateId = toEventStoreAggregateId(aggregateId);
        GeoAssocState state = getStateRepository().get(aggregateId, false);
        boolean duplicate = isDuplicateCommand(c, eventStoreAggregateId, state);
        if (duplicate) { return; }

        GeoAssocCommand.throwOnInvalidStateTransition(state, c);
        action.accept(state);
        persist(eventStoreAggregateId, c.getVersion() == null ? GeoAssocState.VERSION_NULL : c.getVersion(), state); // State version may be null!

    }

    private DomainEventPublisher domainEventPublisher;

    public void setDomainEventPublisher(DomainEventPublisher domainEventPublisher) {
        this.domainEventPublisher = domainEventPublisher;
    }

    public DomainEventPublisher getDomainEventPublisher() {
        if (domainEventPublisher != null) { return domainEventPublisher; }
        return ApplicationContext.current.get(DomainEventPublisher.class);
    }

    private void persist(EventStoreAggregateId eventStoreAggregateId, long version, GeoAssocState state) {
        getStateRepository().save(state);
    }

    protected boolean isDuplicateCommand(GeoAssocCommand command, EventStoreAggregateId eventStoreAggregateId, GeoAssocState state) {
        boolean duplicate = false;
        if (command.getVersion() == null) { command.setVersion(GeoAssocState.VERSION_NULL); }
        if (state.getVersion() != null && state.getVersion() == command.getVersion() + 1) {
            if (command.getCommandId() != null && command.getCommandId().equals(state.getCommandId())) {
                duplicate = true;
            }
        }
        return duplicate;
    }

    protected static void throwOnConcurrencyConflict(GeoAssocState s, GeoAssocCommand c) {
        Long stateVersion = s.getVersion();
        Long commandVersion = c.getVersion();
        if (commandVersion == null) { commandVersion = GeoAssocState.VERSION_NULL; }
        if (!(stateVersion == null && commandVersion.equals(GeoAssocState.VERSION_NULL)) && !commandVersion.equals(stateVersion)) {
            throw DomainError.named("concurrencyConflict", "Conflict between state version (%1$s) and command version (%2$s)", stateVersion, commandVersion);
        }
    }

    public static class SimpleGeoAssocApplicationService extends AbstractGeoAssocApplicationService {
        public SimpleGeoAssocApplicationService(GeoAssocStateRepository stateRepository, GeoAssocStateQueryRepository stateQueryRepository)
        {
            super(stateRepository, stateQueryRepository);
        }
    }

}

