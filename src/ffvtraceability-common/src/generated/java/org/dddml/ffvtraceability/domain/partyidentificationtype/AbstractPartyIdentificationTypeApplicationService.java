// <autogenerated>
//   This file was generated by dddappp code generator.
//   Any changes made to this file manually will be lost next time the file is regenerated.
// </autogenerated>

package org.dddml.ffvtraceability.domain.partyidentificationtype;

import java.util.*;
import java.util.function.Consumer;
import org.dddml.support.criterion.Criterion;
import java.time.OffsetDateTime;
import org.dddml.ffvtraceability.domain.*;
import org.dddml.ffvtraceability.specialization.*;

public abstract class AbstractPartyIdentificationTypeApplicationService implements PartyIdentificationTypeApplicationService {
    private PartyIdentificationTypeStateRepository stateRepository;

    protected PartyIdentificationTypeStateRepository getStateRepository() {
        return stateRepository;
    }

    private PartyIdentificationTypeStateQueryRepository stateQueryRepository;

    protected PartyIdentificationTypeStateQueryRepository getStateQueryRepository() {
        return stateQueryRepository;
    }

    public AbstractPartyIdentificationTypeApplicationService(PartyIdentificationTypeStateRepository stateRepository, PartyIdentificationTypeStateQueryRepository stateQueryRepository) {
        this.stateRepository = stateRepository;
        this.stateQueryRepository = stateQueryRepository;
    }

    public void when(PartyIdentificationTypeCommand.CreatePartyIdentificationType c) {
        update(c, s -> {
        // //////////////////////////
        throwOnConcurrencyConflict(s, c);
        PartyIdentificationTypeState.SqlPartyIdentificationTypeState ss = ((PartyIdentificationTypeState.SqlPartyIdentificationTypeState)s);
        ss.setParentTypeId(c.getParentTypeId());
        ss.setHasTable(c.getHasTable());
        ss.setDescription(c.getDescription());
        ss.setCreatedBy(c.getRequesterId());
        ss.setCreatedAt((OffsetDateTime)ApplicationContext.current.getTimestampService().now(OffsetDateTime.class));
        ss.setCommandId(c.getCommandId());
        // //////////////////////////
        });
    }

    public void when(PartyIdentificationTypeCommand.MergePatchPartyIdentificationType c) {
        update(c, s -> {
        // //////////////////////////////////
        throwOnConcurrencyConflict(s, c);
        PartyIdentificationTypeState.SqlPartyIdentificationTypeState ss = ((PartyIdentificationTypeState.SqlPartyIdentificationTypeState)s);
        if (c.getParentTypeId() == null) {
            if (c.getIsPropertyParentTypeIdRemoved() != null && c.getIsPropertyParentTypeIdRemoved()) {
                ss.setParentTypeId(null);
            }
        } else {
            ss.setParentTypeId(c.getParentTypeId());
        }
        if (c.getHasTable() == null) {
            if (c.getIsPropertyHasTableRemoved() != null && c.getIsPropertyHasTableRemoved()) {
                ss.setHasTable(null);
            }
        } else {
            ss.setHasTable(c.getHasTable());
        }
        if (c.getDescription() == null) {
            if (c.getIsPropertyDescriptionRemoved() != null && c.getIsPropertyDescriptionRemoved()) {
                ss.setDescription(null);
            }
        } else {
            ss.setDescription(c.getDescription());
        }
        ss.setUpdatedBy(c.getRequesterId());
        ss.setUpdatedAt((OffsetDateTime)ApplicationContext.current.getTimestampService().now(OffsetDateTime.class));
        ss.setCommandId(c.getCommandId());
        // //////////////////////////////////
        });
    }

    public PartyIdentificationTypeState get(String id) {
        PartyIdentificationTypeState state = getStateRepository().get(id, true);
        return state;
    }

    public Iterable<PartyIdentificationTypeState> getAll(Integer firstResult, Integer maxResults) {
        return getStateQueryRepository().getAll(firstResult, maxResults);
    }

    public Iterable<PartyIdentificationTypeState> get(Iterable<Map.Entry<String, Object>> filter, List<String> orders, Integer firstResult, Integer maxResults) {
        return getStateQueryRepository().get(filter, orders, firstResult, maxResults);
    }

    public Iterable<PartyIdentificationTypeState> get(Criterion filter, List<String> orders, Integer firstResult, Integer maxResults) {
        return getStateQueryRepository().get(filter, orders, firstResult, maxResults);
    }

    public Iterable<PartyIdentificationTypeState> getByProperty(String propertyName, Object propertyValue, List<String> orders, Integer firstResult, Integer maxResults) {
        return getStateQueryRepository().getByProperty(propertyName, propertyValue, orders, firstResult, maxResults);
    }

    public long getCount(Iterable<Map.Entry<String, Object>> filter) {
        return getStateQueryRepository().getCount(filter);
    }

    public long getCount(Criterion filter) {
        return getStateQueryRepository().getCount(filter);
    }

    public EventStoreAggregateId toEventStoreAggregateId(String aggregateId) {
        return new EventStoreAggregateId.SimpleEventStoreAggregateId(aggregateId);
    }

    protected void update(PartyIdentificationTypeCommand c, Consumer<PartyIdentificationTypeState> action) {
        String aggregateId = c.getPartyIdentificationTypeId();
        EventStoreAggregateId eventStoreAggregateId = toEventStoreAggregateId(aggregateId);
        PartyIdentificationTypeState state = getStateRepository().get(aggregateId, false);
        boolean duplicate = isDuplicateCommand(c, eventStoreAggregateId, state);
        if (duplicate) { return; }

        PartyIdentificationTypeCommand.throwOnInvalidStateTransition(state, c);
        action.accept(state);
        persist(eventStoreAggregateId, c.getVersion() == null ? PartyIdentificationTypeState.VERSION_NULL : c.getVersion(), state); // State version may be null!

    }

    private void persist(EventStoreAggregateId eventStoreAggregateId, long version, PartyIdentificationTypeState state) {
        getStateRepository().save(state);
    }

    protected boolean isDuplicateCommand(PartyIdentificationTypeCommand command, EventStoreAggregateId eventStoreAggregateId, PartyIdentificationTypeState state) {
        boolean duplicate = false;
        if (command.getVersion() == null) { command.setVersion(PartyIdentificationTypeState.VERSION_NULL); }
        if (state.getVersion() != null && state.getVersion() == command.getVersion() + 1) {
            if (command.getCommandId() != null && command.getCommandId().equals(state.getCommandId())) {
                duplicate = true;
            }
        }
        return duplicate;
    }

    protected static void throwOnConcurrencyConflict(PartyIdentificationTypeState s, PartyIdentificationTypeCommand c) {
        Long stateVersion = s.getVersion();
        Long commandVersion = c.getVersion();
        if (commandVersion == null) { commandVersion = PartyIdentificationTypeState.VERSION_NULL; }
        if (!(stateVersion == null && commandVersion.equals(PartyIdentificationTypeState.VERSION_NULL)) && !commandVersion.equals(stateVersion)) {
            throw DomainError.named("concurrencyConflict", "Conflict between state version (%1$s) and command version (%2$s)", stateVersion, commandVersion);
        }
    }

    public static class SimplePartyIdentificationTypeApplicationService extends AbstractPartyIdentificationTypeApplicationService {
        public SimplePartyIdentificationTypeApplicationService(PartyIdentificationTypeStateRepository stateRepository, PartyIdentificationTypeStateQueryRepository stateQueryRepository)
        {
            super(stateRepository, stateQueryRepository);
        }
    }

}

