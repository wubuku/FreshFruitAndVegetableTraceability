// <autogenerated>
//   This file was generated by dddappp code generator.
//   Any changes made to this file manually will be lost next time the file is regenerated.
// </autogenerated>

package org.dddml.ffvtraceability.domain.lotidentificationtype;

import java.util.*;
import java.util.function.Consumer;
import org.dddml.support.criterion.Criterion;
import java.time.OffsetDateTime;
import org.dddml.ffvtraceability.domain.*;
import org.dddml.ffvtraceability.specialization.*;

public abstract class AbstractLotIdentificationTypeApplicationService implements LotIdentificationTypeApplicationService {

    private EventStore eventStore;

    protected EventStore getEventStore()
    {
        return eventStore;
    }

    private LotIdentificationTypeStateRepository stateRepository;

    protected LotIdentificationTypeStateRepository getStateRepository() {
        return stateRepository;
    }

    private LotIdentificationTypeStateQueryRepository stateQueryRepository;

    protected LotIdentificationTypeStateQueryRepository getStateQueryRepository() {
        return stateQueryRepository;
    }

    private AggregateEventListener<LotIdentificationTypeAggregate, LotIdentificationTypeState> aggregateEventListener;

    public AggregateEventListener<LotIdentificationTypeAggregate, LotIdentificationTypeState> getAggregateEventListener() {
        return aggregateEventListener;
    }

    public void setAggregateEventListener(AggregateEventListener<LotIdentificationTypeAggregate, LotIdentificationTypeState> eventListener) {
        this.aggregateEventListener = eventListener;
    }

    public AbstractLotIdentificationTypeApplicationService(EventStore eventStore, LotIdentificationTypeStateRepository stateRepository, LotIdentificationTypeStateQueryRepository stateQueryRepository) {
        this.eventStore = eventStore;
        this.stateRepository = stateRepository;
        this.stateQueryRepository = stateQueryRepository;
    }

    public void when(LotIdentificationTypeCommand.CreateLotIdentificationType c) {
        update(c, ar -> ar.create(c));
    }

    public void when(LotIdentificationTypeCommand.MergePatchLotIdentificationType c) {
        update(c, ar -> ar.mergePatch(c));
    }

    public LotIdentificationTypeState get(String id) {
        LotIdentificationTypeState state = getStateRepository().get(id, true);
        return state;
    }

    public Iterable<LotIdentificationTypeState> getAll(Integer firstResult, Integer maxResults) {
        return getStateQueryRepository().getAll(firstResult, maxResults);
    }

    public Iterable<LotIdentificationTypeState> get(Iterable<Map.Entry<String, Object>> filter, List<String> orders, Integer firstResult, Integer maxResults) {
        return getStateQueryRepository().get(filter, orders, firstResult, maxResults);
    }

    public Iterable<LotIdentificationTypeState> get(Criterion filter, List<String> orders, Integer firstResult, Integer maxResults) {
        return getStateQueryRepository().get(filter, orders, firstResult, maxResults);
    }

    public Iterable<LotIdentificationTypeState> getByProperty(String propertyName, Object propertyValue, List<String> orders, Integer firstResult, Integer maxResults) {
        return getStateQueryRepository().getByProperty(propertyName, propertyValue, orders, firstResult, maxResults);
    }

    public long getCount(Iterable<Map.Entry<String, Object>> filter) {
        return getStateQueryRepository().getCount(filter);
    }

    public long getCount(Criterion filter) {
        return getStateQueryRepository().getCount(filter);
    }

    public LotIdentificationTypeEvent getEvent(String lotIdentificationTypeId, long version) {
        LotIdentificationTypeEvent e = (LotIdentificationTypeEvent)getEventStore().getEvent(toEventStoreAggregateId(lotIdentificationTypeId), version);
        if (e != null) {
            ((LotIdentificationTypeEvent.SqlLotIdentificationTypeEvent)e).setEventReadOnly(true); 
        } else if (version == -1) {
            return getEvent(lotIdentificationTypeId, 0);
        }
        return e;
    }

    public LotIdentificationTypeState getHistoryState(String lotIdentificationTypeId, long version) {
        EventStream eventStream = getEventStore().loadEventStream(AbstractLotIdentificationTypeEvent.class, toEventStoreAggregateId(lotIdentificationTypeId), version - 1);
        return new AbstractLotIdentificationTypeState.SimpleLotIdentificationTypeState(eventStream.getEvents());
    }


    public LotIdentificationTypeAggregate getLotIdentificationTypeAggregate(LotIdentificationTypeState state) {
        return new AbstractLotIdentificationTypeAggregate.SimpleLotIdentificationTypeAggregate(state);
    }

    public EventStoreAggregateId toEventStoreAggregateId(String aggregateId) {
        return new EventStoreAggregateId.SimpleEventStoreAggregateId(aggregateId);
    }

    protected void update(LotIdentificationTypeCommand c, Consumer<LotIdentificationTypeAggregate> action) {
        String aggregateId = c.getLotIdentificationTypeId();
        EventStoreAggregateId eventStoreAggregateId = toEventStoreAggregateId(aggregateId);
        LotIdentificationTypeState state = getStateRepository().get(aggregateId, false);
        boolean duplicate = isDuplicateCommand(c, eventStoreAggregateId, state);
        if (duplicate) { return; }

        LotIdentificationTypeAggregate aggregate = getLotIdentificationTypeAggregate(state);
        aggregate.throwOnInvalidStateTransition(c);
        action.accept(aggregate);
        persist(eventStoreAggregateId, c.getVersion() == null ? LotIdentificationTypeState.VERSION_NULL : c.getVersion(), aggregate, state); // State version may be null!

    }

    private void persist(EventStoreAggregateId eventStoreAggregateId, long version, LotIdentificationTypeAggregate aggregate, LotIdentificationTypeState state) {
        getEventStore().appendEvents(eventStoreAggregateId, version, 
            aggregate.getChanges(), (events) -> { 
                getStateRepository().save(state); 
            });
        if (aggregateEventListener != null) {
            aggregateEventListener.eventAppended(new AggregateEvent<>(aggregate, state, aggregate.getChanges()));
        }
    }

    public void initialize(LotIdentificationTypeEvent.LotIdentificationTypeStateCreated stateCreated) {
        String aggregateId = ((LotIdentificationTypeEvent.SqlLotIdentificationTypeEvent)stateCreated).getLotIdentificationTypeEventId().getLotIdentificationTypeId();
        LotIdentificationTypeState.SqlLotIdentificationTypeState state = new AbstractLotIdentificationTypeState.SimpleLotIdentificationTypeState();
        state.setLotIdentificationTypeId(aggregateId);

        LotIdentificationTypeAggregate aggregate = getLotIdentificationTypeAggregate(state);
        ((AbstractLotIdentificationTypeAggregate) aggregate).apply(stateCreated);

        EventStoreAggregateId eventStoreAggregateId = toEventStoreAggregateId(aggregateId);
        persist(eventStoreAggregateId, ((LotIdentificationTypeEvent.SqlLotIdentificationTypeEvent)stateCreated).getLotIdentificationTypeEventId().getVersion(), aggregate, state);
    }

    protected boolean isDuplicateCommand(LotIdentificationTypeCommand command, EventStoreAggregateId eventStoreAggregateId, LotIdentificationTypeState state) {
        boolean duplicate = false;
        if (command.getVersion() == null) { command.setVersion(LotIdentificationTypeState.VERSION_NULL); }
        if (state.getVersion() != null && state.getVersion() > command.getVersion()) {
            Event lastEvent = getEventStore().getEvent(AbstractLotIdentificationTypeEvent.class, eventStoreAggregateId, command.getVersion());
            if (lastEvent != null && lastEvent instanceof AbstractEvent
               && command.getCommandId() != null && command.getCommandId().equals(((AbstractEvent) lastEvent).getCommandId())) {
                duplicate = true;
            }
        }
        return duplicate;
    }

    public static class SimpleLotIdentificationTypeApplicationService extends AbstractLotIdentificationTypeApplicationService {
        public SimpleLotIdentificationTypeApplicationService(EventStore eventStore, LotIdentificationTypeStateRepository stateRepository, LotIdentificationTypeStateQueryRepository stateQueryRepository)
        {
            super(eventStore, stateRepository, stateQueryRepository);
        }
    }

}

