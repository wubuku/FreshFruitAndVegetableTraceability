// <autogenerated>
//   This file was generated by dddappp code generator.
//   Any changes made to this file manually will be lost next time the file is regenerated.
// </autogenerated>

package org.dddml.ffvtraceability.domain.enumeration;

import java.util.*;
import java.util.function.Consumer;
import org.dddml.support.criterion.Criterion;
import java.time.OffsetDateTime;
import org.dddml.ffvtraceability.domain.*;
import org.dddml.ffvtraceability.specialization.*;

public abstract class AbstractEnumerationApplicationService implements EnumerationApplicationService {
    private EnumerationStateRepository stateRepository;

    protected EnumerationStateRepository getStateRepository() {
        return stateRepository;
    }

    private EnumerationStateQueryRepository stateQueryRepository;

    protected EnumerationStateQueryRepository getStateQueryRepository() {
        return stateQueryRepository;
    }

    public AbstractEnumerationApplicationService(EnumerationStateRepository stateRepository, EnumerationStateQueryRepository stateQueryRepository) {
        this.stateRepository = stateRepository;
        this.stateQueryRepository = stateQueryRepository;
    }

    public void when(EnumerationCommand.CreateEnumeration c) {
        update(c, s -> {
        // //////////////////////////
        throwOnConcurrencyConflict(s, c);
        EnumerationState.SqlEnumerationState ss = ((EnumerationState.SqlEnumerationState)s);
        ss.setEnumTypeId(c.getEnumTypeId());
        ss.setEnumCode(c.getEnumCode());
        ss.setSequenceId(c.getSequenceId());
        ss.setDescription(c.getDescription());
        ss.setCreatedBy(c.getRequesterId());
        ss.setCreatedAt((OffsetDateTime)ApplicationContext.current.getTimestampService().now(OffsetDateTime.class));
        ss.setCommandId(c.getCommandId());
        // //////////////////////////
        });
    }

    public void when(EnumerationCommand.MergePatchEnumeration c) {
        update(c, s -> {
        // //////////////////////////////////
        throwOnConcurrencyConflict(s, c);
        EnumerationState.SqlEnumerationState ss = ((EnumerationState.SqlEnumerationState)s);
        if (c.getEnumTypeId() == null) {
            if (c.getIsPropertyEnumTypeIdRemoved() != null && c.getIsPropertyEnumTypeIdRemoved()) {
                ss.setEnumTypeId(null);
            }
        } else {
            ss.setEnumTypeId(c.getEnumTypeId());
        }
        if (c.getEnumCode() == null) {
            if (c.getIsPropertyEnumCodeRemoved() != null && c.getIsPropertyEnumCodeRemoved()) {
                ss.setEnumCode(null);
            }
        } else {
            ss.setEnumCode(c.getEnumCode());
        }
        if (c.getSequenceId() == null) {
            if (c.getIsPropertySequenceIdRemoved() != null && c.getIsPropertySequenceIdRemoved()) {
                ss.setSequenceId(null);
            }
        } else {
            ss.setSequenceId(c.getSequenceId());
        }
        if (c.getDescription() == null) {
            if (c.getIsPropertyDescriptionRemoved() != null && c.getIsPropertyDescriptionRemoved()) {
                ss.setDescription(null);
            }
        } else {
            ss.setDescription(c.getDescription());
        }
        ss.setUpdatedBy(c.getRequesterId());
        ss.setUpdatedAt((OffsetDateTime)ApplicationContext.current.getTimestampService().now(OffsetDateTime.class));
        ss.setCommandId(c.getCommandId());
        // //////////////////////////////////
        });
    }

    public EnumerationState get(String id) {
        EnumerationState state = getStateRepository().get(id, true);
        return state;
    }

    public Iterable<EnumerationState> getAll(Integer firstResult, Integer maxResults) {
        return getStateQueryRepository().getAll(firstResult, maxResults);
    }

    public Iterable<EnumerationState> get(Iterable<Map.Entry<String, Object>> filter, List<String> orders, Integer firstResult, Integer maxResults) {
        return getStateQueryRepository().get(filter, orders, firstResult, maxResults);
    }

    public Iterable<EnumerationState> get(Criterion filter, List<String> orders, Integer firstResult, Integer maxResults) {
        return getStateQueryRepository().get(filter, orders, firstResult, maxResults);
    }

    public Iterable<EnumerationState> getByProperty(String propertyName, Object propertyValue, List<String> orders, Integer firstResult, Integer maxResults) {
        return getStateQueryRepository().getByProperty(propertyName, propertyValue, orders, firstResult, maxResults);
    }

    public long getCount(Iterable<Map.Entry<String, Object>> filter) {
        return getStateQueryRepository().getCount(filter);
    }

    public long getCount(Criterion filter) {
        return getStateQueryRepository().getCount(filter);
    }

    public EventStoreAggregateId toEventStoreAggregateId(String aggregateId) {
        return new EventStoreAggregateId.SimpleEventStoreAggregateId(aggregateId);
    }

    protected void update(EnumerationCommand c, Consumer<EnumerationState> action) {
        String aggregateId = c.getEnumId();
        EventStoreAggregateId eventStoreAggregateId = toEventStoreAggregateId(aggregateId);
        EnumerationState state = getStateRepository().get(aggregateId, false);
        boolean duplicate = isDuplicateCommand(c, eventStoreAggregateId, state);
        if (duplicate) { return; }

        EnumerationCommand.throwOnInvalidStateTransition(state, c);
        action.accept(state);
        persist(eventStoreAggregateId, c.getVersion() == null ? EnumerationState.VERSION_NULL : c.getVersion(), state); // State version may be null!

    }

    private void persist(EventStoreAggregateId eventStoreAggregateId, long version, EnumerationState state) {
        getStateRepository().save(state);
    }

    protected boolean isDuplicateCommand(EnumerationCommand command, EventStoreAggregateId eventStoreAggregateId, EnumerationState state) {
        boolean duplicate = false;
        if (command.getVersion() == null) { command.setVersion(EnumerationState.VERSION_NULL); }
        if (state.getVersion() != null && state.getVersion() == command.getVersion() + 1) {
            if (command.getCommandId() != null && command.getCommandId().equals(state.getCommandId())) {
                duplicate = true;
            }
        }
        return duplicate;
    }

    protected static void throwOnConcurrencyConflict(EnumerationState s, EnumerationCommand c) {
        Long stateVersion = s.getVersion();
        Long commandVersion = c.getVersion();
        if (commandVersion == null) { commandVersion = EnumerationState.VERSION_NULL; }
        if (!(stateVersion == null && commandVersion.equals(EnumerationState.VERSION_NULL)) && !commandVersion.equals(stateVersion)) {
            throw DomainError.named("concurrencyConflict", "Conflict between state version (%1$s) and command version (%2$s)", stateVersion, commandVersion);
        }
    }

    public static class SimpleEnumerationApplicationService extends AbstractEnumerationApplicationService {
        public SimpleEnumerationApplicationService(EnumerationStateRepository stateRepository, EnumerationStateQueryRepository stateQueryRepository)
        {
            super(stateRepository, stateQueryRepository);
        }
    }

}

