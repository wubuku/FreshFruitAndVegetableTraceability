// <autogenerated>
//   This file was generated by dddappp code generator.
//   Any changes made to this file manually will be lost next time the file is regenerated.
// </autogenerated>

package org.dddml.ffvtraceability.domain.tenant;

import java.util.*;
import java.math.*;
import java.time.OffsetDateTime;
import org.dddml.ffvtraceability.domain.*;
import org.dddml.ffvtraceability.specialization.*;
import org.dddml.ffvtraceability.domain.tenant.TenantEvent.*;

public abstract class AbstractTenantState implements TenantState.SqlTenantState {

    private String tenantId;

    public String getTenantId() {
        return this.tenantId;
    }

    public void setTenantId(String tenantId) {
        this.tenantId = tenantId;
    }

    private String partyId;

    public String getPartyId() {
        return this.partyId;
    }

    public void setPartyId(String partyId) {
        this.partyId = partyId;
    }

    private String description;

    public String getDescription() {
        return this.description;
    }

    public void setDescription(String description) {
        this.description = description;
    }

    private String longDescription;

    public String getLongDescription() {
        return this.longDescription;
    }

    public void setLongDescription(String longDescription) {
        this.longDescription = longDescription;
    }

    private Long version;

    public Long getVersion() {
        return this.version;
    }

    public void setVersion(Long version) {
        this.version = version;
    }

    private String createdBy;

    public String getCreatedBy() {
        return this.createdBy;
    }

    public void setCreatedBy(String createdBy) {
        this.createdBy = createdBy;
    }

    private OffsetDateTime createdAt;

    public OffsetDateTime getCreatedAt() {
        return this.createdAt;
    }

    public void setCreatedAt(OffsetDateTime createdAt) {
        this.createdAt = createdAt;
    }

    private String updatedBy;

    public String getUpdatedBy() {
        return this.updatedBy;
    }

    public void setUpdatedBy(String updatedBy) {
        this.updatedBy = updatedBy;
    }

    private OffsetDateTime updatedAt;

    public OffsetDateTime getUpdatedAt() {
        return this.updatedAt;
    }

    public void setUpdatedAt(OffsetDateTime updatedAt) {
        this.updatedAt = updatedAt;
    }

    public boolean isStateUnsaved() {
        return this.getVersion() == null;
    }

    private Boolean stateReadOnly;

    public Boolean getStateReadOnly() { return this.stateReadOnly; }

    public void setStateReadOnly(Boolean readOnly) { this.stateReadOnly = readOnly; }

    private boolean forReapplying;

    public boolean getForReapplying() {
        return forReapplying;
    }

    public void setForReapplying(boolean forReapplying) {
        this.forReapplying = forReapplying;
    }

    public AbstractTenantState(List<Event> events) {
        initializeForReapplying();
        if (events != null && events.size() > 0) {
            this.setTenantId(((TenantEvent.SqlTenantEvent) events.get(0)).getTenantEventId().getTenantId());
            for (Event e : events) {
                mutate(e);
                this.setVersion((this.getVersion() == null ? TenantState.VERSION_NULL : this.getVersion()) + 1);
            }
        }
    }


    public AbstractTenantState() {
        initializeProperties();
    }

    protected void initializeForReapplying() {
        this.forReapplying = true;

        initializeProperties();
    }
    
    protected void initializeProperties() {
    }

    @Override
    public int hashCode() {
        return getTenantId().hashCode();
    }

    @Override
    public boolean equals(Object obj) {
        if (this == obj) { return true; }
        if (obj instanceof TenantState) {
            return Objects.equals(this.getTenantId(), ((TenantState)obj).getTenantId());
        }
        return false;
    }


    public void mutate(Event e) {
        setStateReadOnly(false);
        if (e instanceof TenantStateCreated) {
            when((TenantStateCreated) e);
        } else if (e instanceof TenantStateMergePatched) {
            when((TenantStateMergePatched) e);
        } else {
            throw new UnsupportedOperationException(String.format("Unsupported event type: %1$s", e.getClass().getName()));
        }
    }

    public void when(TenantStateCreated e) {
        throwOnWrongEvent(e);

        this.setPartyId(e.getPartyId());
        this.setDescription(e.getDescription());
        this.setLongDescription(e.getLongDescription());

        this.setCreatedBy(e.getCreatedBy());
        this.setCreatedAt(e.getCreatedAt());

    }

    public void merge(TenantState s) {
        if (s == this) {
            return;
        }
        this.setPartyId(s.getPartyId());
        this.setDescription(s.getDescription());
        this.setLongDescription(s.getLongDescription());
    }

    public void when(TenantStateMergePatched e) {
        throwOnWrongEvent(e);

        if (e.getPartyId() == null) {
            if (e.getIsPropertyPartyIdRemoved() != null && e.getIsPropertyPartyIdRemoved()) {
                this.setPartyId(null);
            }
        } else {
            this.setPartyId(e.getPartyId());
        }
        if (e.getDescription() == null) {
            if (e.getIsPropertyDescriptionRemoved() != null && e.getIsPropertyDescriptionRemoved()) {
                this.setDescription(null);
            }
        } else {
            this.setDescription(e.getDescription());
        }
        if (e.getLongDescription() == null) {
            if (e.getIsPropertyLongDescriptionRemoved() != null && e.getIsPropertyLongDescriptionRemoved()) {
                this.setLongDescription(null);
            }
        } else {
            this.setLongDescription(e.getLongDescription());
        }

        this.setUpdatedBy(e.getCreatedBy());
        this.setUpdatedAt(e.getCreatedAt());

    }

    public void save() {
    }

    protected void throwOnWrongEvent(TenantEvent event) {
        String stateEntityId = this.getTenantId(); // Aggregate Id
        String eventEntityId = ((TenantEvent.SqlTenantEvent)event).getTenantEventId().getTenantId(); // EntityBase.Aggregate.GetEventIdPropertyIdName();
        if (!stateEntityId.equals(eventEntityId)) {
            throw DomainError.named("mutateWrongEntity", "Entity Id %1$s in state but entity id %2$s in event", stateEntityId, eventEntityId);
        }


        Long stateVersion = this.getVersion();
        Long eventVersion = ((TenantEvent.SqlTenantEvent)event).getTenantEventId().getVersion();// Event Version
        if (eventVersion == null) {
            throw new NullPointerException("event.getTenantEventId().getVersion() == null");
        }
        if (!(stateVersion == null && eventVersion.equals(VERSION_NULL)) && !eventVersion.equals(stateVersion)) {
            throw DomainError.named("concurrencyConflict", "Conflict between state version (%1$s) and event version (%2$s)", stateVersion, eventVersion == VERSION_NULL ? "NULL" : eventVersion + "");
        }

    }


    public static class SimpleTenantState extends AbstractTenantState {

        public SimpleTenantState() {
        }

        public SimpleTenantState(List<Event> events) {
            super(events);
        }

        public static SimpleTenantState newForReapplying() {
            SimpleTenantState s = new SimpleTenantState();
            s.initializeForReapplying();
            return s;
        }

    }



}

