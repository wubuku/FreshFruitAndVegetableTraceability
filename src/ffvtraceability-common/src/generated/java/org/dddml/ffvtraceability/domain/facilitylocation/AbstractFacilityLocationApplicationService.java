// <autogenerated>
//   This file was generated by dddappp code generator.
//   Any changes made to this file manually will be lost next time the file is regenerated.
// </autogenerated>

package org.dddml.ffvtraceability.domain.facilitylocation;

import java.util.*;
import java.util.function.Consumer;
import org.dddml.support.criterion.Criterion;
import java.time.OffsetDateTime;
import org.dddml.ffvtraceability.domain.*;
import org.dddml.ffvtraceability.specialization.*;

public abstract class AbstractFacilityLocationApplicationService implements FacilityLocationApplicationService {
    private FacilityLocationStateRepository stateRepository;

    protected FacilityLocationStateRepository getStateRepository() {
        return stateRepository;
    }

    private FacilityLocationStateQueryRepository stateQueryRepository;

    protected FacilityLocationStateQueryRepository getStateQueryRepository() {
        return stateQueryRepository;
    }

    public AbstractFacilityLocationApplicationService(FacilityLocationStateRepository stateRepository, FacilityLocationStateQueryRepository stateQueryRepository) {
        this.stateRepository = stateRepository;
        this.stateQueryRepository = stateQueryRepository;
    }

    public void when(FacilityLocationCommand.CreateFacilityLocation c) {
        update(c, s -> {
        // //////////////////////////
        throwOnConcurrencyConflict(s, c);
        FacilityLocationState.SqlFacilityLocationState ss = ((FacilityLocationState.SqlFacilityLocationState)s);
        ss.setLocationTypeEnumId(c.getLocationTypeEnumId());
        ss.setAreaId(c.getAreaId());
        ss.setAisleId(c.getAisleId());
        ss.setSectionId(c.getSectionId());
        ss.setLevelId(c.getLevelId());
        ss.setPositionId(c.getPositionId());
        ss.setGeoPointId(c.getGeoPointId());
        ss.setActive(c.getActive());
        ss.setLocationCode(c.getLocationCode());
        ss.setGln(c.getGln());
        ss.setDescription(c.getDescription());
        ss.setLocationName(c.getLocationName());
        ss.setCreatedBy(c.getRequesterId());
        ss.setCreatedAt((OffsetDateTime)ApplicationContext.current.getTimestampService().now(OffsetDateTime.class));
        ss.setCommandId(c.getCommandId());
        // //////////////////////////
        });
    }

    public void when(FacilityLocationCommand.MergePatchFacilityLocation c) {
        update(c, s -> {
        // //////////////////////////////////
        throwOnConcurrencyConflict(s, c);
        FacilityLocationState.SqlFacilityLocationState ss = ((FacilityLocationState.SqlFacilityLocationState)s);
        if (c.getLocationTypeEnumId() == null) {
            if (c.getIsPropertyLocationTypeEnumIdRemoved() != null && c.getIsPropertyLocationTypeEnumIdRemoved()) {
                ss.setLocationTypeEnumId(null);
            }
        } else {
            ss.setLocationTypeEnumId(c.getLocationTypeEnumId());
        }
        if (c.getAreaId() == null) {
            if (c.getIsPropertyAreaIdRemoved() != null && c.getIsPropertyAreaIdRemoved()) {
                ss.setAreaId(null);
            }
        } else {
            ss.setAreaId(c.getAreaId());
        }
        if (c.getAisleId() == null) {
            if (c.getIsPropertyAisleIdRemoved() != null && c.getIsPropertyAisleIdRemoved()) {
                ss.setAisleId(null);
            }
        } else {
            ss.setAisleId(c.getAisleId());
        }
        if (c.getSectionId() == null) {
            if (c.getIsPropertySectionIdRemoved() != null && c.getIsPropertySectionIdRemoved()) {
                ss.setSectionId(null);
            }
        } else {
            ss.setSectionId(c.getSectionId());
        }
        if (c.getLevelId() == null) {
            if (c.getIsPropertyLevelIdRemoved() != null && c.getIsPropertyLevelIdRemoved()) {
                ss.setLevelId(null);
            }
        } else {
            ss.setLevelId(c.getLevelId());
        }
        if (c.getPositionId() == null) {
            if (c.getIsPropertyPositionIdRemoved() != null && c.getIsPropertyPositionIdRemoved()) {
                ss.setPositionId(null);
            }
        } else {
            ss.setPositionId(c.getPositionId());
        }
        if (c.getGeoPointId() == null) {
            if (c.getIsPropertyGeoPointIdRemoved() != null && c.getIsPropertyGeoPointIdRemoved()) {
                ss.setGeoPointId(null);
            }
        } else {
            ss.setGeoPointId(c.getGeoPointId());
        }
        if (c.getActive() == null) {
            if (c.getIsPropertyActiveRemoved() != null && c.getIsPropertyActiveRemoved()) {
                ss.setActive(null);
            }
        } else {
            ss.setActive(c.getActive());
        }
        if (c.getLocationCode() == null) {
            if (c.getIsPropertyLocationCodeRemoved() != null && c.getIsPropertyLocationCodeRemoved()) {
                ss.setLocationCode(null);
            }
        } else {
            ss.setLocationCode(c.getLocationCode());
        }
        if (c.getGln() == null) {
            if (c.getIsPropertyGlnRemoved() != null && c.getIsPropertyGlnRemoved()) {
                ss.setGln(null);
            }
        } else {
            ss.setGln(c.getGln());
        }
        if (c.getDescription() == null) {
            if (c.getIsPropertyDescriptionRemoved() != null && c.getIsPropertyDescriptionRemoved()) {
                ss.setDescription(null);
            }
        } else {
            ss.setDescription(c.getDescription());
        }
        if (c.getLocationName() == null) {
            if (c.getIsPropertyLocationNameRemoved() != null && c.getIsPropertyLocationNameRemoved()) {
                ss.setLocationName(null);
            }
        } else {
            ss.setLocationName(c.getLocationName());
        }
        ss.setUpdatedBy(c.getRequesterId());
        ss.setUpdatedAt((OffsetDateTime)ApplicationContext.current.getTimestampService().now(OffsetDateTime.class));
        ss.setCommandId(c.getCommandId());
        // //////////////////////////////////
        });
    }

    public FacilityLocationState get(FacilityLocationId id) {
        FacilityLocationState state = getStateRepository().get(id, true);
        return state;
    }

    public Iterable<FacilityLocationState> getAll(Integer firstResult, Integer maxResults) {
        return getStateQueryRepository().getAll(firstResult, maxResults);
    }

    public Iterable<FacilityLocationState> get(Iterable<Map.Entry<String, Object>> filter, List<String> orders, Integer firstResult, Integer maxResults) {
        return getStateQueryRepository().get(filter, orders, firstResult, maxResults);
    }

    public Iterable<FacilityLocationState> get(Criterion filter, List<String> orders, Integer firstResult, Integer maxResults) {
        return getStateQueryRepository().get(filter, orders, firstResult, maxResults);
    }

    public Iterable<FacilityLocationState> getByProperty(String propertyName, Object propertyValue, List<String> orders, Integer firstResult, Integer maxResults) {
        return getStateQueryRepository().getByProperty(propertyName, propertyValue, orders, firstResult, maxResults);
    }

    public long getCount(Iterable<Map.Entry<String, Object>> filter) {
        return getStateQueryRepository().getCount(filter);
    }

    public long getCount(Criterion filter) {
        return getStateQueryRepository().getCount(filter);
    }

    public EventStoreAggregateId toEventStoreAggregateId(FacilityLocationId aggregateId) {
        return new EventStoreAggregateId.SimpleEventStoreAggregateId(aggregateId);
    }

    protected void update(FacilityLocationCommand c, Consumer<FacilityLocationState> action) {
        FacilityLocationId aggregateId = c.getFacilityLocationId();
        EventStoreAggregateId eventStoreAggregateId = toEventStoreAggregateId(aggregateId);
        FacilityLocationState state = getStateRepository().get(aggregateId, false);
        boolean duplicate = isDuplicateCommand(c, eventStoreAggregateId, state);
        if (duplicate) { return; }

        FacilityLocationCommand.throwOnInvalidStateTransition(state, c);
        action.accept(state);
        persist(eventStoreAggregateId, c.getVersion() == null ? FacilityLocationState.VERSION_NULL : c.getVersion(), state); // State version may be null!

    }

    private void persist(EventStoreAggregateId eventStoreAggregateId, long version, FacilityLocationState state) {
        getStateRepository().save(state);
    }

    protected boolean isDuplicateCommand(FacilityLocationCommand command, EventStoreAggregateId eventStoreAggregateId, FacilityLocationState state) {
        boolean duplicate = false;
        if (command.getVersion() == null) { command.setVersion(FacilityLocationState.VERSION_NULL); }
        if (state.getVersion() != null && state.getVersion() == command.getVersion() + 1) {
            if (command.getCommandId() != null && command.getCommandId().equals(state.getCommandId())) {
                duplicate = true;
            }
        }
        return duplicate;
    }

    protected static void throwOnConcurrencyConflict(FacilityLocationState s, FacilityLocationCommand c) {
        Long stateVersion = s.getVersion();
        Long commandVersion = c.getVersion();
        if (commandVersion == null) { commandVersion = FacilityLocationState.VERSION_NULL; }
        if (!(stateVersion == null && commandVersion.equals(FacilityLocationState.VERSION_NULL)) && !commandVersion.equals(stateVersion)) {
            throw DomainError.named("concurrencyConflict", "Conflict between state version (%1$s) and command version (%2$s)", stateVersion, commandVersion);
        }
    }

    public static class SimpleFacilityLocationApplicationService extends AbstractFacilityLocationApplicationService {
        public SimpleFacilityLocationApplicationService(FacilityLocationStateRepository stateRepository, FacilityLocationStateQueryRepository stateQueryRepository)
        {
            super(stateRepository, stateQueryRepository);
        }
    }

}

