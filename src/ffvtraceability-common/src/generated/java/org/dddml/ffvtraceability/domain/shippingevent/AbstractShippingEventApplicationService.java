// <autogenerated>
//   This file was generated by dddappp code generator.
//   Any changes made to this file manually will be lost next time the file is regenerated.
// </autogenerated>

package org.dddml.ffvtraceability.domain.shippingevent;

import java.util.*;
import java.util.function.Consumer;
import org.dddml.support.criterion.Criterion;
import org.dddml.ffvtraceability.domain.*;
import java.time.OffsetDateTime;
import org.dddml.ffvtraceability.specialization.*;

public abstract class AbstractShippingEventApplicationService implements ShippingEventApplicationService {
    private ShippingEventStateRepository stateRepository;

    protected ShippingEventStateRepository getStateRepository() {
        return stateRepository;
    }

    private ShippingEventStateQueryRepository stateQueryRepository;

    protected ShippingEventStateQueryRepository getStateQueryRepository() {
        return stateQueryRepository;
    }

    public AbstractShippingEventApplicationService(ShippingEventStateRepository stateRepository, ShippingEventStateQueryRepository stateQueryRepository) {
        this.stateRepository = stateRepository;
        this.stateQueryRepository = stateQueryRepository;
    }

    public void when(ShippingEventCommand.CreateShippingEvent c) {
        update(c, s -> {
        // //////////////////////////
        throwOnConcurrencyConflict(s, c);
        ShippingEventState.SqlShippingEventState ss = ((ShippingEventState.SqlShippingEventState)s);
        ss.setTraceabilityLotCode(c.getTraceabilityLotCode());
        ss.setQuantityAndUom(c.getQuantityAndUom());
        ss.setProductDescription(c.getProductDescription());
        ss.setShipToLocation(c.getShipToLocation());
        ss.setShipFromLocation(c.getShipFromLocation());
        ss.setShipDate(c.getShipDate());
        ss.setTlcSourceOrTlcSourceReference(c.getTlcSourceOrTlcSourceReference());
        ss.setReferenceDocuments(new HashSet<>(Arrays.asList(c.getReferenceDocuments())));
        ss.setCreatedBy(c.getRequesterId());
        ss.setCreatedAt((OffsetDateTime)ApplicationContext.current.getTimestampService().now(OffsetDateTime.class));
        ss.setCommandId(c.getCommandId());
        // //////////////////////////
        });
    }

    public void when(ShippingEventCommand.MergePatchShippingEvent c) {
        update(c, s -> {
        // //////////////////////////////////
        throwOnConcurrencyConflict(s, c);
        ShippingEventState.SqlShippingEventState ss = ((ShippingEventState.SqlShippingEventState)s);
        if (c.getTraceabilityLotCode() == null) {
            if (c.getIsPropertyTraceabilityLotCodeRemoved() != null && c.getIsPropertyTraceabilityLotCodeRemoved()) {
                ss.setTraceabilityLotCode(null);
            }
        } else {
            ss.setTraceabilityLotCode(c.getTraceabilityLotCode());
        }
        if (c.getQuantityAndUom() == null) {
            if (c.getIsPropertyQuantityAndUomRemoved() != null && c.getIsPropertyQuantityAndUomRemoved()) {
                ss.setQuantityAndUom(null);
            }
        } else {
            ss.setQuantityAndUom(c.getQuantityAndUom());
        }
        if (c.getProductDescription() == null) {
            if (c.getIsPropertyProductDescriptionRemoved() != null && c.getIsPropertyProductDescriptionRemoved()) {
                ss.setProductDescription(null);
            }
        } else {
            ss.setProductDescription(c.getProductDescription());
        }
        if (c.getShipToLocation() == null) {
            if (c.getIsPropertyShipToLocationRemoved() != null && c.getIsPropertyShipToLocationRemoved()) {
                ss.setShipToLocation(null);
            }
        } else {
            ss.setShipToLocation(c.getShipToLocation());
        }
        if (c.getShipFromLocation() == null) {
            if (c.getIsPropertyShipFromLocationRemoved() != null && c.getIsPropertyShipFromLocationRemoved()) {
                ss.setShipFromLocation(null);
            }
        } else {
            ss.setShipFromLocation(c.getShipFromLocation());
        }
        if (c.getShipDate() == null) {
            if (c.getIsPropertyShipDateRemoved() != null && c.getIsPropertyShipDateRemoved()) {
                ss.setShipDate(null);
            }
        } else {
            ss.setShipDate(c.getShipDate());
        }
        if (c.getTlcSourceOrTlcSourceReference() == null) {
            if (c.getIsPropertyTlcSourceOrTlcSourceReferenceRemoved() != null && c.getIsPropertyTlcSourceOrTlcSourceReferenceRemoved()) {
                ss.setTlcSourceOrTlcSourceReference(null);
            }
        } else {
            ss.setTlcSourceOrTlcSourceReference(c.getTlcSourceOrTlcSourceReference());
        }
        if (c.getReferenceDocuments() == null) {
            if (c.getIsPropertyReferenceDocumentsRemoved() != null && c.getIsPropertyReferenceDocumentsRemoved()) {
                ss.setReferenceDocuments(null);
            }
        } else {
            ss.setReferenceDocuments(new HashSet<>(Arrays.asList(c.getReferenceDocuments())));
        }
        ss.setUpdatedBy(c.getRequesterId());
        ss.setUpdatedAt((OffsetDateTime)ApplicationContext.current.getTimestampService().now(OffsetDateTime.class));
        ss.setCommandId(c.getCommandId());
        // //////////////////////////////////
        });
    }

    public ShippingEventState get(String id) {
        ShippingEventState state = getStateRepository().get(id, true);
        return state;
    }

    public Iterable<ShippingEventState> getAll(Integer firstResult, Integer maxResults) {
        return getStateQueryRepository().getAll(firstResult, maxResults);
    }

    public Iterable<ShippingEventState> get(Iterable<Map.Entry<String, Object>> filter, List<String> orders, Integer firstResult, Integer maxResults) {
        return getStateQueryRepository().get(filter, orders, firstResult, maxResults);
    }

    public Iterable<ShippingEventState> get(Criterion filter, List<String> orders, Integer firstResult, Integer maxResults) {
        return getStateQueryRepository().get(filter, orders, firstResult, maxResults);
    }

    public Iterable<ShippingEventState> getByProperty(String propertyName, Object propertyValue, List<String> orders, Integer firstResult, Integer maxResults) {
        return getStateQueryRepository().getByProperty(propertyName, propertyValue, orders, firstResult, maxResults);
    }

    public long getCount(Iterable<Map.Entry<String, Object>> filter) {
        return getStateQueryRepository().getCount(filter);
    }

    public long getCount(Criterion filter) {
        return getStateQueryRepository().getCount(filter);
    }

    public EventStoreAggregateId toEventStoreAggregateId(String aggregateId) {
        return new EventStoreAggregateId.SimpleEventStoreAggregateId(aggregateId);
    }

    protected void update(ShippingEventCommand c, Consumer<ShippingEventState> action) {
        String aggregateId = c.getEventId();
        EventStoreAggregateId eventStoreAggregateId = toEventStoreAggregateId(aggregateId);
        ShippingEventState state = getStateRepository().get(aggregateId, false);
        boolean duplicate = isDuplicateCommand(c, eventStoreAggregateId, state);
        if (duplicate) { return; }

        ShippingEventCommand.throwOnInvalidStateTransition(state, c);
        action.accept(state);
        persist(eventStoreAggregateId, c.getVersion() == null ? ShippingEventState.VERSION_NULL : c.getVersion(), state); // State version may be null!

    }

    private DomainEventPublisher domainEventPublisher;

    public void setDomainEventPublisher(DomainEventPublisher domainEventPublisher) {
        this.domainEventPublisher = domainEventPublisher;
    }

    public DomainEventPublisher getDomainEventPublisher() {
        if (domainEventPublisher != null) { return domainEventPublisher; }
        return ApplicationContext.current.get(DomainEventPublisher.class);
    }

    private void persist(EventStoreAggregateId eventStoreAggregateId, long version, ShippingEventState state) {
        getStateRepository().save(state);
    }

    protected boolean isDuplicateCommand(ShippingEventCommand command, EventStoreAggregateId eventStoreAggregateId, ShippingEventState state) {
        boolean duplicate = false;
        if (command.getVersion() == null) { command.setVersion(ShippingEventState.VERSION_NULL); }
        if (state.getVersion() != null && state.getVersion() == command.getVersion() + 1) {
            if (command.getCommandId() != null && command.getCommandId().equals(state.getCommandId())) {
                duplicate = true;
            }
        }
        return duplicate;
    }

    protected static void throwOnConcurrencyConflict(ShippingEventState s, ShippingEventCommand c) {
        Long stateVersion = s.getVersion();
        Long commandVersion = c.getVersion();
        if (commandVersion == null) { commandVersion = ShippingEventState.VERSION_NULL; }
        if (!(stateVersion == null && commandVersion.equals(ShippingEventState.VERSION_NULL)) && !commandVersion.equals(stateVersion)) {
            throw DomainError.named("concurrencyConflict", "Conflict between state version (%1$s) and command version (%2$s)", stateVersion, commandVersion);
        }
    }

    public static class SimpleShippingEventApplicationService extends AbstractShippingEventApplicationService {
        public SimpleShippingEventApplicationService(ShippingEventStateRepository stateRepository, ShippingEventStateQueryRepository stateQueryRepository)
        {
            super(stateRepository, stateQueryRepository);
        }
    }

}

