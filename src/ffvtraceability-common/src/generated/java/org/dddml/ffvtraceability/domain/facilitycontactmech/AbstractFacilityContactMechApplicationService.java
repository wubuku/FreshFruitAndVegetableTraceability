// <autogenerated>
//   This file was generated by dddappp code generator.
//   Any changes made to this file manually will be lost next time the file is regenerated.
// </autogenerated>

package org.dddml.ffvtraceability.domain.facilitycontactmech;

import java.util.*;
import java.util.function.Consumer;
import org.dddml.support.criterion.Criterion;
import java.time.OffsetDateTime;
import org.dddml.ffvtraceability.domain.*;
import org.dddml.ffvtraceability.specialization.*;

public abstract class AbstractFacilityContactMechApplicationService implements FacilityContactMechApplicationService {

    private EventStore eventStore;

    protected EventStore getEventStore()
    {
        return eventStore;
    }

    private FacilityContactMechStateRepository stateRepository;

    protected FacilityContactMechStateRepository getStateRepository() {
        return stateRepository;
    }

    private FacilityContactMechStateQueryRepository stateQueryRepository;

    protected FacilityContactMechStateQueryRepository getStateQueryRepository() {
        return stateQueryRepository;
    }

    private AggregateEventListener<FacilityContactMechAggregate, FacilityContactMechState> aggregateEventListener;

    public AggregateEventListener<FacilityContactMechAggregate, FacilityContactMechState> getAggregateEventListener() {
        return aggregateEventListener;
    }

    public void setAggregateEventListener(AggregateEventListener<FacilityContactMechAggregate, FacilityContactMechState> eventListener) {
        this.aggregateEventListener = eventListener;
    }

    public AbstractFacilityContactMechApplicationService(EventStore eventStore, FacilityContactMechStateRepository stateRepository, FacilityContactMechStateQueryRepository stateQueryRepository) {
        this.eventStore = eventStore;
        this.stateRepository = stateRepository;
        this.stateQueryRepository = stateQueryRepository;
    }

    public void when(FacilityContactMechCommand.CreateFacilityContactMech c) {
        update(c, ar -> ar.create(c));
    }

    public void when(FacilityContactMechCommand.MergePatchFacilityContactMech c) {
        update(c, ar -> ar.mergePatch(c));
    }

    public FacilityContactMechState get(FacilityContactMechId id) {
        FacilityContactMechState state = getStateRepository().get(id, true);
        return state;
    }

    public Iterable<FacilityContactMechState> getAll(Integer firstResult, Integer maxResults) {
        return getStateQueryRepository().getAll(firstResult, maxResults);
    }

    public Iterable<FacilityContactMechState> get(Iterable<Map.Entry<String, Object>> filter, List<String> orders, Integer firstResult, Integer maxResults) {
        return getStateQueryRepository().get(filter, orders, firstResult, maxResults);
    }

    public Iterable<FacilityContactMechState> get(Criterion filter, List<String> orders, Integer firstResult, Integer maxResults) {
        return getStateQueryRepository().get(filter, orders, firstResult, maxResults);
    }

    public Iterable<FacilityContactMechState> getByProperty(String propertyName, Object propertyValue, List<String> orders, Integer firstResult, Integer maxResults) {
        return getStateQueryRepository().getByProperty(propertyName, propertyValue, orders, firstResult, maxResults);
    }

    public long getCount(Iterable<Map.Entry<String, Object>> filter) {
        return getStateQueryRepository().getCount(filter);
    }

    public long getCount(Criterion filter) {
        return getStateQueryRepository().getCount(filter);
    }

    public FacilityContactMechEvent getEvent(FacilityContactMechId facilityContactMechId, long version) {
        FacilityContactMechEvent e = (FacilityContactMechEvent)getEventStore().getEvent(toEventStoreAggregateId(facilityContactMechId), version);
        if (e != null) {
            ((FacilityContactMechEvent.SqlFacilityContactMechEvent)e).setEventReadOnly(true); 
        } else if (version == -1) {
            return getEvent(facilityContactMechId, 0);
        }
        return e;
    }

    public FacilityContactMechState getHistoryState(FacilityContactMechId facilityContactMechId, long version) {
        EventStream eventStream = getEventStore().loadEventStream(AbstractFacilityContactMechEvent.class, toEventStoreAggregateId(facilityContactMechId), version - 1);
        return new AbstractFacilityContactMechState.SimpleFacilityContactMechState(eventStream.getEvents());
    }

    public FacilityContactMechPurposeState getFacilityContactMechPurpose(FacilityContactMechId facilityContactMechId, String contactMechPurposeTypeId) {
        return getStateQueryRepository().getFacilityContactMechPurpose(facilityContactMechId, contactMechPurposeTypeId);
    }

    public Iterable<FacilityContactMechPurposeState> getFacilityContactMechPurposes(FacilityContactMechId facilityContactMechId, Criterion filter, List<String> orders) {
        return getStateQueryRepository().getFacilityContactMechPurposes(facilityContactMechId, filter, orders);
    }


    public FacilityContactMechAggregate getFacilityContactMechAggregate(FacilityContactMechState state) {
        return new AbstractFacilityContactMechAggregate.SimpleFacilityContactMechAggregate(state);
    }

    public EventStoreAggregateId toEventStoreAggregateId(FacilityContactMechId aggregateId) {
        return new EventStoreAggregateId.SimpleEventStoreAggregateId(aggregateId);
    }

    protected void update(FacilityContactMechCommand c, Consumer<FacilityContactMechAggregate> action) {
        FacilityContactMechId aggregateId = c.getFacilityContactMechId();
        EventStoreAggregateId eventStoreAggregateId = toEventStoreAggregateId(aggregateId);
        FacilityContactMechState state = getStateRepository().get(aggregateId, false);
        boolean duplicate = isDuplicateCommand(c, eventStoreAggregateId, state);
        if (duplicate) { return; }

        FacilityContactMechAggregate aggregate = getFacilityContactMechAggregate(state);
        aggregate.throwOnInvalidStateTransition(c);
        action.accept(aggregate);
        persist(eventStoreAggregateId, c.getVersion() == null ? FacilityContactMechState.VERSION_NULL : c.getVersion(), aggregate, state); // State version may be null!

    }

    private void persist(EventStoreAggregateId eventStoreAggregateId, long version, FacilityContactMechAggregate aggregate, FacilityContactMechState state) {
        getEventStore().appendEvents(eventStoreAggregateId, version, 
            aggregate.getChanges(), (events) -> { 
                getStateRepository().save(state); 
            });
        if (aggregateEventListener != null) {
            aggregateEventListener.eventAppended(new AggregateEvent<>(aggregate, state, aggregate.getChanges()));
        }
    }

    public void initialize(FacilityContactMechEvent.FacilityContactMechStateCreated stateCreated) {
        FacilityContactMechId aggregateId = ((FacilityContactMechEvent.SqlFacilityContactMechEvent)stateCreated).getFacilityContactMechEventId().getFacilityContactMechId();
        FacilityContactMechState.SqlFacilityContactMechState state = new AbstractFacilityContactMechState.SimpleFacilityContactMechState();
        state.setFacilityContactMechId(aggregateId);

        FacilityContactMechAggregate aggregate = getFacilityContactMechAggregate(state);
        ((AbstractFacilityContactMechAggregate) aggregate).apply(stateCreated);

        EventStoreAggregateId eventStoreAggregateId = toEventStoreAggregateId(aggregateId);
        persist(eventStoreAggregateId, ((FacilityContactMechEvent.SqlFacilityContactMechEvent)stateCreated).getFacilityContactMechEventId().getVersion(), aggregate, state);
    }

    protected boolean isDuplicateCommand(FacilityContactMechCommand command, EventStoreAggregateId eventStoreAggregateId, FacilityContactMechState state) {
        boolean duplicate = false;
        if (command.getVersion() == null) { command.setVersion(FacilityContactMechState.VERSION_NULL); }
        if (state.getVersion() != null && state.getVersion() > command.getVersion()) {
            Event lastEvent = getEventStore().getEvent(AbstractFacilityContactMechEvent.class, eventStoreAggregateId, command.getVersion());
            if (lastEvent != null && lastEvent instanceof AbstractEvent
               && command.getCommandId() != null && command.getCommandId().equals(((AbstractEvent) lastEvent).getCommandId())) {
                duplicate = true;
            }
        }
        return duplicate;
    }

    public static class SimpleFacilityContactMechApplicationService extends AbstractFacilityContactMechApplicationService {
        public SimpleFacilityContactMechApplicationService(EventStore eventStore, FacilityContactMechStateRepository stateRepository, FacilityContactMechStateQueryRepository stateQueryRepository)
        {
            super(eventStore, stateRepository, stateQueryRepository);
        }
    }

}

