// <autogenerated>
//   This file was generated by dddappp code generator.
//   Any changes made to this file manually will be lost next time the file is regenerated.
// </autogenerated>

package org.dddml.ffvtraceability.domain.workeffortinventoryassign;

import java.util.*;
import java.util.function.Consumer;
import org.dddml.support.criterion.Criterion;
import java.time.OffsetDateTime;
import org.dddml.ffvtraceability.domain.*;
import org.dddml.ffvtraceability.specialization.*;

public abstract class AbstractWorkEffortInventoryAssignApplicationService implements WorkEffortInventoryAssignApplicationService {

    private EventStore eventStore;

    protected EventStore getEventStore()
    {
        return eventStore;
    }

    private WorkEffortInventoryAssignStateRepository stateRepository;

    protected WorkEffortInventoryAssignStateRepository getStateRepository() {
        return stateRepository;
    }

    private WorkEffortInventoryAssignStateQueryRepository stateQueryRepository;

    protected WorkEffortInventoryAssignStateQueryRepository getStateQueryRepository() {
        return stateQueryRepository;
    }

    private AggregateEventListener<WorkEffortInventoryAssignAggregate, WorkEffortInventoryAssignState> aggregateEventListener;

    public AggregateEventListener<WorkEffortInventoryAssignAggregate, WorkEffortInventoryAssignState> getAggregateEventListener() {
        return aggregateEventListener;
    }

    public void setAggregateEventListener(AggregateEventListener<WorkEffortInventoryAssignAggregate, WorkEffortInventoryAssignState> eventListener) {
        this.aggregateEventListener = eventListener;
    }

    public AbstractWorkEffortInventoryAssignApplicationService(EventStore eventStore, WorkEffortInventoryAssignStateRepository stateRepository, WorkEffortInventoryAssignStateQueryRepository stateQueryRepository) {
        this.eventStore = eventStore;
        this.stateRepository = stateRepository;
        this.stateQueryRepository = stateQueryRepository;
    }

    public void when(WorkEffortInventoryAssignCommand.CreateWorkEffortInventoryAssign c) {
        update(c, ar -> ar.create(c));
    }

    public void when(WorkEffortInventoryAssignCommand.MergePatchWorkEffortInventoryAssign c) {
        update(c, ar -> ar.mergePatch(c));
    }

    public WorkEffortInventoryAssignState get(WorkEffortInventoryAssignId id) {
        WorkEffortInventoryAssignState state = getStateRepository().get(id, true);
        return state;
    }

    public Iterable<WorkEffortInventoryAssignState> getAll(Integer firstResult, Integer maxResults) {
        return getStateQueryRepository().getAll(firstResult, maxResults);
    }

    public Iterable<WorkEffortInventoryAssignState> get(Iterable<Map.Entry<String, Object>> filter, List<String> orders, Integer firstResult, Integer maxResults) {
        return getStateQueryRepository().get(filter, orders, firstResult, maxResults);
    }

    public Iterable<WorkEffortInventoryAssignState> get(Criterion filter, List<String> orders, Integer firstResult, Integer maxResults) {
        return getStateQueryRepository().get(filter, orders, firstResult, maxResults);
    }

    public Iterable<WorkEffortInventoryAssignState> getByProperty(String propertyName, Object propertyValue, List<String> orders, Integer firstResult, Integer maxResults) {
        return getStateQueryRepository().getByProperty(propertyName, propertyValue, orders, firstResult, maxResults);
    }

    public long getCount(Iterable<Map.Entry<String, Object>> filter) {
        return getStateQueryRepository().getCount(filter);
    }

    public long getCount(Criterion filter) {
        return getStateQueryRepository().getCount(filter);
    }

    public WorkEffortInventoryAssignEvent getEvent(WorkEffortInventoryAssignId workEffortInventoryAssignId, long version) {
        WorkEffortInventoryAssignEvent e = (WorkEffortInventoryAssignEvent)getEventStore().getEvent(toEventStoreAggregateId(workEffortInventoryAssignId), version);
        if (e != null) {
            ((WorkEffortInventoryAssignEvent.SqlWorkEffortInventoryAssignEvent)e).setEventReadOnly(true); 
        } else if (version == -1) {
            return getEvent(workEffortInventoryAssignId, 0);
        }
        return e;
    }

    public WorkEffortInventoryAssignState getHistoryState(WorkEffortInventoryAssignId workEffortInventoryAssignId, long version) {
        EventStream eventStream = getEventStore().loadEventStream(AbstractWorkEffortInventoryAssignEvent.class, toEventStoreAggregateId(workEffortInventoryAssignId), version - 1);
        return new AbstractWorkEffortInventoryAssignState.SimpleWorkEffortInventoryAssignState(eventStream.getEvents());
    }


    public WorkEffortInventoryAssignAggregate getWorkEffortInventoryAssignAggregate(WorkEffortInventoryAssignState state) {
        return new AbstractWorkEffortInventoryAssignAggregate.SimpleWorkEffortInventoryAssignAggregate(state);
    }

    public EventStoreAggregateId toEventStoreAggregateId(WorkEffortInventoryAssignId aggregateId) {
        return new EventStoreAggregateId.SimpleEventStoreAggregateId(aggregateId);
    }

    protected void update(WorkEffortInventoryAssignCommand c, Consumer<WorkEffortInventoryAssignAggregate> action) {
        WorkEffortInventoryAssignId aggregateId = c.getWorkEffortInventoryAssignId();
        EventStoreAggregateId eventStoreAggregateId = toEventStoreAggregateId(aggregateId);
        WorkEffortInventoryAssignState state = getStateRepository().get(aggregateId, false);
        boolean duplicate = isDuplicateCommand(c, eventStoreAggregateId, state);
        if (duplicate) { return; }

        WorkEffortInventoryAssignAggregate aggregate = getWorkEffortInventoryAssignAggregate(state);
        aggregate.throwOnInvalidStateTransition(c);
        action.accept(aggregate);
        persist(eventStoreAggregateId, c.getVersion() == null ? WorkEffortInventoryAssignState.VERSION_NULL : c.getVersion(), aggregate, state); // State version may be null!

    }

    private DomainEventPublisher domainEventPublisher;

    public void setDomainEventPublisher(DomainEventPublisher domainEventPublisher) {
        this.domainEventPublisher = domainEventPublisher;
    }

    public DomainEventPublisher getDomainEventPublisher() {
        if (domainEventPublisher != null) { return domainEventPublisher; }
        return ApplicationContext.current.get(DomainEventPublisher.class);
    }

    private void persist(EventStoreAggregateId eventStoreAggregateId, long version, WorkEffortInventoryAssignAggregate aggregate, WorkEffortInventoryAssignState state) {
        final DomainEventPublisher ep = getDomainEventPublisher();
        getEventStore().appendEvents(eventStoreAggregateId, version, 
            aggregate.getChanges(), (events) -> { 
                getStateRepository().save(state); 
                if (ep != null) {
                    ep.publish(org.dddml.ffvtraceability.domain.workeffortinventoryassign.WorkEffortInventoryAssignAggregate.class,
                        eventStoreAggregateId.getId(),
                        (List<Event>)events);
                }
            });
        if (aggregateEventListener != null) {
            aggregateEventListener.eventAppended(new AggregateEvent<>(aggregate, state, aggregate.getChanges()));
        }
    }

    void initialize(WorkEffortInventoryAssignEvent.WorkEffortInventoryAssignStateCreated stateCreated) {
        WorkEffortInventoryAssignId aggregateId = ((WorkEffortInventoryAssignEvent.SqlWorkEffortInventoryAssignEvent)stateCreated).getWorkEffortInventoryAssignEventId().getWorkEffortInventoryAssignId();
        WorkEffortInventoryAssignState.SqlWorkEffortInventoryAssignState state = new AbstractWorkEffortInventoryAssignState.SimpleWorkEffortInventoryAssignState();
        state.setWorkEffortInventoryAssignId(aggregateId);

        WorkEffortInventoryAssignAggregate aggregate = getWorkEffortInventoryAssignAggregate(state);
        ((AbstractWorkEffortInventoryAssignAggregate) aggregate).apply(stateCreated);

        EventStoreAggregateId eventStoreAggregateId = toEventStoreAggregateId(aggregateId);
        persist(eventStoreAggregateId, ((WorkEffortInventoryAssignEvent.SqlWorkEffortInventoryAssignEvent)stateCreated).getWorkEffortInventoryAssignEventId().getVersion(), aggregate, state);
    }

    protected boolean isDuplicateCommand(WorkEffortInventoryAssignCommand command, EventStoreAggregateId eventStoreAggregateId, WorkEffortInventoryAssignState state) {
        boolean duplicate = false;
        if (command.getVersion() == null) { command.setVersion(WorkEffortInventoryAssignState.VERSION_NULL); }
        if (state.getVersion() != null && state.getVersion() > command.getVersion()) {
            Event lastEvent = getEventStore().getEvent(AbstractWorkEffortInventoryAssignEvent.class, eventStoreAggregateId, command.getVersion());
            if (lastEvent != null && lastEvent instanceof AbstractEvent
               && command.getCommandId() != null && command.getCommandId().equals(((AbstractEvent) lastEvent).getCommandId())) {
                duplicate = true;
            }
        }
        return duplicate;
    }

    public static class SimpleWorkEffortInventoryAssignApplicationService extends AbstractWorkEffortInventoryAssignApplicationService {
        public SimpleWorkEffortInventoryAssignApplicationService(EventStore eventStore, WorkEffortInventoryAssignStateRepository stateRepository, WorkEffortInventoryAssignStateQueryRepository stateQueryRepository)
        {
            super(eventStore, stateRepository, stateQueryRepository);
        }
    }

}

