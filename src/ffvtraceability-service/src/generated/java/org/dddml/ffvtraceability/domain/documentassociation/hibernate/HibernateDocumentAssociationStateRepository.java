// <autogenerated>
//   This file was generated by dddappp code generator.
//   Any changes made to this file manually will be lost next time the file is regenerated.
// </autogenerated>

package org.dddml.ffvtraceability.domain.documentassociation.hibernate;

import java.util.*;
import org.dddml.ffvtraceability.domain.*;
import java.time.OffsetDateTime;
import jakarta.persistence.EntityManager;
import jakarta.persistence.PersistenceContext;
import org.springframework.stereotype.Repository;
import org.dddml.ffvtraceability.domain.documentassociation.*;
import org.dddml.ffvtraceability.specialization.*;
import org.dddml.ffvtraceability.specialization.hibernate.*;
import org.springframework.transaction.annotation.Transactional;

@Repository("documentAssociationStateRepository")
public class HibernateDocumentAssociationStateRepository implements DocumentAssociationStateRepository {
    @PersistenceContext
    private EntityManager entityManager;

    protected EntityManager getEntityManager() {
        return this.entityManager;
    }

    private static final Set<String> readOnlyPropertyPascalCaseNames = new HashSet<String>(Arrays.asList("DocumentAssociationId", "ThruDate", "Version", "CreatedBy", "CreatedAt", "UpdatedBy", "UpdatedAt", "Active", "Deleted"));
    
    private ReadOnlyProxyGenerator readOnlyProxyGenerator;
    
    public ReadOnlyProxyGenerator getReadOnlyProxyGenerator() {
        return readOnlyProxyGenerator;
    }

    public void setReadOnlyProxyGenerator(ReadOnlyProxyGenerator readOnlyProxyGenerator) {
        this.readOnlyProxyGenerator = readOnlyProxyGenerator;
    }

    @Transactional(readOnly = true)
    public DocumentAssociationState get(DocumentAssociationId id, boolean nullAllowed) {
        DocumentAssociationState.SqlDocumentAssociationState state = (DocumentAssociationState.SqlDocumentAssociationState)getEntityManager().find(AbstractDocumentAssociationState.SimpleDocumentAssociationState.class, id);
        if (!nullAllowed && state == null) {
            state = new AbstractDocumentAssociationState.SimpleDocumentAssociationState();
            state.setDocumentAssociationId(id);
        }
        if (getReadOnlyProxyGenerator() != null && state != null) {
            return (DocumentAssociationState) getReadOnlyProxyGenerator().createProxy(state, new Class[]{DocumentAssociationState.SqlDocumentAssociationState.class}, "getStateReadOnly", readOnlyPropertyPascalCaseNames);
        }
        return state;
    }

    public void save(DocumentAssociationState state) {
        DocumentAssociationState s = state;
        if (getReadOnlyProxyGenerator() != null) {
            s = (DocumentAssociationState) getReadOnlyProxyGenerator().getTarget(state);
        }
        if (s.getVersion() == null) {
            entityManager.persist(s);
        } else {
            entityManager.merge(s);
        }

        if (s instanceof Saveable) {
            Saveable saveable = (Saveable) s;
            saveable.save();
        }
        entityManager.flush();
    }

    public void merge(DocumentAssociationState detached) {
        DocumentAssociationState persistent = getEntityManager().find(AbstractDocumentAssociationState.SimpleDocumentAssociationState.class, detached.getDocumentAssociationId());
        if (persistent != null) {
            merge(persistent, detached);
            entityManager.merge(persistent);
        } else {
            entityManager.persist(detached);
        }
        entityManager.flush();
    }

    private void merge(DocumentAssociationState persistent, DocumentAssociationState detached) {
        ((AbstractDocumentAssociationState) persistent).merge(detached);
    }

}

