// <autogenerated>
//   This file was generated by dddappp code generator.
//   Any changes made to this file manually will be lost next time the file is regenerated.
// </autogenerated>

package org.dddml.ffvtraceability.domain.facilitylocation.hibernate;

import java.util.*;
import java.time.OffsetDateTime;
import org.dddml.ffvtraceability.domain.*;
import jakarta.persistence.EntityManager;
import jakarta.persistence.PersistenceContext;
import org.springframework.stereotype.Repository;
import org.dddml.ffvtraceability.domain.facilitylocation.*;
import org.dddml.ffvtraceability.specialization.*;
import org.dddml.ffvtraceability.specialization.hibernate.*;
import org.springframework.transaction.annotation.Transactional;

@Repository("facilityLocationStateRepository")
public class HibernateFacilityLocationStateRepository implements FacilityLocationStateRepository {
    @PersistenceContext
    private EntityManager entityManager;

    protected EntityManager getEntityManager() {
        EntityManager em = this.entityManager;
        String currentTenantId = TenantContext.getTenantId();
        if (currentTenantId == null || currentTenantId.isEmpty()) {
            throw new IllegalStateException("Tenant context not set");
        }
        if (TenantSupport.SUPER_TENANT_ID != null && !TenantSupport.SUPER_TENANT_ID.isEmpty()
            && TenantSupport.SUPER_TENANT_ID.equals(currentTenantId)) {
            return em;
        }
        org.hibernate.Session session = em.unwrap(org.hibernate.Session.class);
        org.hibernate.Filter filter = session.enableFilter("tenantFilter");
        filter.setParameter("tenantId", currentTenantId);
        filter.validate();
        return em;
    }

    private static final Set<String> readOnlyPropertyPascalCaseNames = new HashSet<String>(Arrays.asList("FacilityLocationId", "LocationTypeEnumId", "AreaId", "AisleId", "SectionId", "LevelId", "PositionId", "GeoPointId", "Active", "LocationCode", "Gln", "Description", "LocationName", "Version", "CreatedBy", "CreatedAt", "UpdatedBy", "UpdatedAt"));
    
    private ReadOnlyProxyGenerator readOnlyProxyGenerator;
    
    public ReadOnlyProxyGenerator getReadOnlyProxyGenerator() {
        return readOnlyProxyGenerator;
    }

    public void setReadOnlyProxyGenerator(ReadOnlyProxyGenerator readOnlyProxyGenerator) {
        this.readOnlyProxyGenerator = readOnlyProxyGenerator;
    }

    @Transactional(readOnly = true)
    public FacilityLocationState get(FacilityLocationId id, boolean nullAllowed) {
        FacilityLocationState.SqlFacilityLocationState state = (FacilityLocationState.SqlFacilityLocationState)getEntityManager().find(AbstractFacilityLocationState.SimpleFacilityLocationState.class, id);
        if (!nullAllowed && state == null) {
            state = new AbstractFacilityLocationState.SimpleFacilityLocationState();
            state.setFacilityLocationId(id);
        }
        return state;
    }

    @Transactional
    public void save(FacilityLocationState state) {
        FacilityLocationState s = state;
        if (s.getVersion() == null) {
            entityManager.persist(s);
        } else {
            entityManager.merge(s);
        }

        if (s instanceof Saveable) {
            Saveable saveable = (Saveable) s;
            saveable.save();
        }
        entityManager.flush();
    }

    public void merge(FacilityLocationState detached) {
        FacilityLocationState persistent = getEntityManager().find(AbstractFacilityLocationState.SimpleFacilityLocationState.class, detached.getFacilityLocationId());
        if (persistent != null) {
            merge(persistent, detached);
            entityManager.merge(persistent);
        } else {
            entityManager.persist(detached);
        }
        entityManager.flush();
    }

    private void merge(FacilityLocationState persistent, FacilityLocationState detached) {
        ((AbstractFacilityLocationState) persistent).merge(detached);
    }

}

