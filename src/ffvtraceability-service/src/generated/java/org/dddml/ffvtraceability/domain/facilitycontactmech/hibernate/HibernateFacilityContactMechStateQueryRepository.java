// <autogenerated>
//   This file was generated by dddappp code generator.
//   Any changes made to this file manually will be lost next time the file is regenerated.
// </autogenerated>

package org.dddml.ffvtraceability.domain.facilitycontactmech.hibernate;

import java.util.*;
import java.time.OffsetDateTime;
import org.dddml.ffvtraceability.domain.*;
import org.hibernate.Session;
import jakarta.persistence.EntityManager;
import jakarta.persistence.criteria.*;
import jakarta.persistence.TypedQuery;
import jakarta.persistence.PersistenceContext;
import org.springframework.stereotype.Repository;
import java.util.stream.Collectors;
import org.dddml.ffvtraceability.domain.facilitycontactmech.*;
import org.dddml.ffvtraceability.specialization.*;
import org.dddml.ffvtraceability.specialization.jpa.*;
import org.springframework.transaction.annotation.Transactional;

@Repository
public class HibernateFacilityContactMechStateQueryRepository implements FacilityContactMechStateQueryRepository {
    @PersistenceContext
    private EntityManager entityManager;

    protected EntityManager getEntityManager() {
        return this.entityManager;
    }

    private static final Set<String> readOnlyPropertyPascalCaseNames = new HashSet<String>(Arrays.asList("FacilityContactMechId", "ThruDate", "Extension", "Comments", "FacilityContactMechPurposes", "Version", "CreatedBy", "CreatedAt", "UpdatedBy", "UpdatedAt"));

    private ReadOnlyProxyGenerator readOnlyProxyGenerator;

    public ReadOnlyProxyGenerator getReadOnlyProxyGenerator() {
        return readOnlyProxyGenerator;
    }

    public void setReadOnlyProxyGenerator(ReadOnlyProxyGenerator readOnlyProxyGenerator) {
        this.readOnlyProxyGenerator = readOnlyProxyGenerator;
    }

    @Transactional(readOnly = true)
    public FacilityContactMechState get(FacilityContactMechId id) {
        FacilityContactMechState state = (FacilityContactMechState)getEntityManager().find(AbstractFacilityContactMechState.SimpleFacilityContactMechState.class, id);
        return state;
    }

    @Transactional(readOnly = true)
    public Iterable<FacilityContactMechState> getAll(Integer firstResult, Integer maxResults) {
        EntityManager em = getEntityManager();
        CriteriaBuilder cb = em.getCriteriaBuilder();
        CriteriaQuery<AbstractFacilityContactMechState.SimpleFacilityContactMechState> cq = cb.createQuery(AbstractFacilityContactMechState.SimpleFacilityContactMechState.class);
        Root<AbstractFacilityContactMechState.SimpleFacilityContactMechState> root = cq.from(AbstractFacilityContactMechState.SimpleFacilityContactMechState.class);
        cq.select(root);
        addNotDeletedRestriction(cb, cq, root);
        TypedQuery<AbstractFacilityContactMechState.SimpleFacilityContactMechState> query = em.createQuery(cq);
        JpaUtils.applyPagination(query, firstResult, maxResults);
        return query.getResultList().stream().map(FacilityContactMechState.class::cast).collect(Collectors.toList());
    }

    @Transactional(readOnly = true)
    public Iterable<FacilityContactMechState> get(Iterable<Map.Entry<String, Object>> filter, List<String> orders, Integer firstResult, Integer maxResults) {
        EntityManager em = getEntityManager();
        CriteriaBuilder cb = em.getCriteriaBuilder();
        CriteriaQuery<AbstractFacilityContactMechState.SimpleFacilityContactMechState> cq = cb.createQuery(AbstractFacilityContactMechState.SimpleFacilityContactMechState.class);
        Root<AbstractFacilityContactMechState.SimpleFacilityContactMechState> root = cq.from(AbstractFacilityContactMechState.SimpleFacilityContactMechState.class);
        cq.select(root);
        JpaUtils.criteriaAddFilterAndOrders(cb, cq, root, filter, orders);
        addNotDeletedRestriction(cb, cq, root);
        TypedQuery<AbstractFacilityContactMechState.SimpleFacilityContactMechState> query = em.createQuery(cq);
        JpaUtils.applyPagination(query, firstResult, maxResults);
        return query.getResultList().stream().map(FacilityContactMechState.class::cast).collect(Collectors.toList());
    }

    @Transactional(readOnly = true)
    public Iterable<FacilityContactMechState> get(org.dddml.support.criterion.Criterion filter, List<String> orders, Integer firstResult, Integer maxResults) {
        EntityManager em = getEntityManager();
        CriteriaBuilder cb = em.getCriteriaBuilder();
        CriteriaQuery<AbstractFacilityContactMechState.SimpleFacilityContactMechState> cq = cb.createQuery(AbstractFacilityContactMechState.SimpleFacilityContactMechState.class);
        Root<AbstractFacilityContactMechState.SimpleFacilityContactMechState> root = cq.from(AbstractFacilityContactMechState.SimpleFacilityContactMechState.class);
        cq.select(root);
        JpaUtils.criteriaAddFilterAndOrders(cb, cq, root, filter, orders);
        addNotDeletedRestriction(cb, cq, root);
        TypedQuery<AbstractFacilityContactMechState.SimpleFacilityContactMechState> query = em.createQuery(cq);
        JpaUtils.applyPagination(query, firstResult, maxResults);
        return query.getResultList().stream().map(FacilityContactMechState.class::cast).collect(Collectors.toList());
    }

    @Transactional(readOnly = true)
    public FacilityContactMechState getFirst(Iterable<Map.Entry<String, Object>> filter, List<String> orders) {
        List<FacilityContactMechState> list = (List<FacilityContactMechState>)get(filter, orders, 0, 1);
        if (list == null || list.size() <= 0) {
            return null;
        }
        return list.get(0);
    }

    @Transactional(readOnly = true)
    public FacilityContactMechState getFirst(Map.Entry<String, Object> keyValue, List<String> orders) {
        List<Map.Entry<String, Object>> filter = new ArrayList<>();
        filter.add(keyValue);
        return getFirst(filter, orders);
    }

    @Transactional(readOnly = true)
    public Iterable<FacilityContactMechState> getByProperty(String propertyName, Object propertyValue, List<String> orders, Integer firstResult, Integer maxResults) {
        Map.Entry<String, Object> keyValue = new AbstractMap.SimpleEntry<>(propertyName, propertyValue);
        List<Map.Entry<String, Object>> filter = new ArrayList<>();
        filter.add(keyValue);
        return get(filter, orders, firstResult, maxResults);
    }

    @Transactional(readOnly = true)
    public long getCount(Iterable<Map.Entry<String, Object>> filter) {
        EntityManager em = getEntityManager();
        CriteriaBuilder cb = em.getCriteriaBuilder();
        CriteriaQuery<Long> cq = cb.createQuery(Long.class);
        Root<AbstractFacilityContactMechState.SimpleFacilityContactMechState> root = cq.from(AbstractFacilityContactMechState.SimpleFacilityContactMechState.class);
        cq.select(cb.count(root));
        if (filter != null) {
            JpaUtils.criteriaAddFilter(cb, cq, root, filter);
        }
        addNotDeletedRestriction(cb, cq, root);
        return em.createQuery(cq).getSingleResult();
    }

    @Transactional(readOnly = true)
    public long getCount(org.dddml.support.criterion.Criterion filter) {
        EntityManager em = getEntityManager();
        CriteriaBuilder cb = em.getCriteriaBuilder();
        CriteriaQuery<Long> cq = cb.createQuery(Long.class);
        Root<AbstractFacilityContactMechState.SimpleFacilityContactMechState> root = cq.from(AbstractFacilityContactMechState.SimpleFacilityContactMechState.class);
        cq.select(cb.count(root));
        if (filter != null) {
            JpaUtils.criteriaAddFilter(cb, cq, root, filter);
        }
        addNotDeletedRestriction(cb, cq, root);
        return em.createQuery(cq).getSingleResult();
    }

    @Transactional(readOnly = true)
    public FacilityContactMechPurposeState getFacilityContactMechPurpose(FacilityContactMechId facilityContactMechId, String contactMechPurposeTypeId) {
        FacilityContactMechPurposeId entityId = new FacilityContactMechPurposeId(facilityContactMechId, contactMechPurposeTypeId);
        return (FacilityContactMechPurposeState) getEntityManager().find(AbstractFacilityContactMechPurposeState.SimpleFacilityContactMechPurposeState.class, entityId);
    }

    @Transactional(readOnly = true)
    public Iterable<FacilityContactMechPurposeState> getFacilityContactMechPurposes(FacilityContactMechId facilityContactMechId, org.dddml.support.criterion.Criterion filter, List<String> orders) {
        EntityManager em = getEntityManager();
        CriteriaBuilder cb = em.getCriteriaBuilder();
        CriteriaQuery<AbstractFacilityContactMechPurposeState.SimpleFacilityContactMechPurposeState> cq = cb.createQuery(AbstractFacilityContactMechPurposeState.SimpleFacilityContactMechPurposeState.class);
        Root<AbstractFacilityContactMechPurposeState.SimpleFacilityContactMechPurposeState> root = cq.from(AbstractFacilityContactMechPurposeState.SimpleFacilityContactMechPurposeState.class);
        cq.select(root);

        Predicate partIdCondition = cb.and(
            cb.equal(root.get("facilityContactMechPurposeId").get("facilityContactMechIdFacilityId"), facilityContactMechId.getFacilityId()),
            cb.equal(root.get("facilityContactMechPurposeId").get("facilityContactMechIdContactMechId"), facilityContactMechId.getContactMechId()),
            cb.equal(root.get("facilityContactMechPurposeId").get("facilityContactMechIdFromDate"), facilityContactMechId.getFromDate())
        );
        cq.where(partIdCondition);

        // Add filter and orders
        JpaUtils.criteriaAddFilterAndOrders(cb, cq, root, filter, orders);

        TypedQuery<AbstractFacilityContactMechPurposeState.SimpleFacilityContactMechPurposeState> query = em.createQuery(cq);
        return query.getResultList().stream().map(FacilityContactMechPurposeState.class::cast).collect(Collectors.toList());
    }

    protected void addNotDeletedRestriction(CriteriaBuilder cb, CriteriaQuery<?> cq, Root<?> root) {
    }

}

