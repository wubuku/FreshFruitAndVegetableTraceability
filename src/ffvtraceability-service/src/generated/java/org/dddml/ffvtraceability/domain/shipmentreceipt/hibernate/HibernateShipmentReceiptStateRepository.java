// <autogenerated>
//   This file was generated by dddappp code generator.
//   Any changes made to this file manually will be lost next time the file is regenerated.
// </autogenerated>

package org.dddml.ffvtraceability.domain.shipmentreceipt.hibernate;

import java.util.*;
import java.time.OffsetDateTime;
import org.dddml.ffvtraceability.domain.partyrole.*;
import org.dddml.ffvtraceability.domain.*;
import jakarta.persistence.EntityManager;
import jakarta.persistence.PersistenceContext;
import org.springframework.stereotype.Repository;
import org.dddml.ffvtraceability.domain.shipmentreceipt.*;
import org.dddml.ffvtraceability.specialization.*;
import org.dddml.ffvtraceability.specialization.hibernate.*;
import org.springframework.transaction.annotation.Transactional;

@Repository("shipmentReceiptStateRepository")
public class HibernateShipmentReceiptStateRepository implements ShipmentReceiptStateRepository {
    @PersistenceContext
    private EntityManager entityManager;

    protected EntityManager getEntityManager() {
        return this.entityManager;
    }

    private static final Set<String> readOnlyPropertyPascalCaseNames = new HashSet<String>(Arrays.asList("ReceiptId", "InventoryItemId", "ProductId", "ShipmentId", "ShipmentItemSeqId", "ShipmentPackageSeqId", "OrderId", "OrderItemSeqId", "ReturnId", "ReturnItemSeqId", "RejectionId", "ReceivedByUserLoginId", "DatetimeReceived", "ItemDescription", "QuantityAccepted", "QuantityRejected", "LotId", "LocationSeqId", "CasesAccepted", "CasesRejected", "ShipmentReceiptRoles", "Version", "CreatedBy", "CreatedAt", "UpdatedBy", "UpdatedAt", "Active", "Deleted"));
    
    private ReadOnlyProxyGenerator readOnlyProxyGenerator;
    
    public ReadOnlyProxyGenerator getReadOnlyProxyGenerator() {
        return readOnlyProxyGenerator;
    }

    public void setReadOnlyProxyGenerator(ReadOnlyProxyGenerator readOnlyProxyGenerator) {
        this.readOnlyProxyGenerator = readOnlyProxyGenerator;
    }

    @Transactional(readOnly = true)
    public ShipmentReceiptState get(String id, boolean nullAllowed) {
        ShipmentReceiptState.SqlShipmentReceiptState state = (ShipmentReceiptState.SqlShipmentReceiptState)getEntityManager().find(AbstractShipmentReceiptState.SimpleShipmentReceiptState.class, id);
        if (!nullAllowed && state == null) {
            state = new AbstractShipmentReceiptState.SimpleShipmentReceiptState();
            state.setReceiptId(id);
        }
        if (getReadOnlyProxyGenerator() != null && state != null) {
            return (ShipmentReceiptState) getReadOnlyProxyGenerator().createProxy(state, new Class[]{ShipmentReceiptState.SqlShipmentReceiptState.class, Saveable.class}, "getStateReadOnly", readOnlyPropertyPascalCaseNames);
        }
        return state;
    }

    public void save(ShipmentReceiptState state) {
        ShipmentReceiptState s = state;
        if (getReadOnlyProxyGenerator() != null) {
            s = (ShipmentReceiptState) getReadOnlyProxyGenerator().getTarget(state);
        }
        if (s.getVersion() == null) {
            entityManager.persist(s);
        } else {
            entityManager.merge(s);
        }

        if (s instanceof Saveable) {
            Saveable saveable = (Saveable) s;
            saveable.save();
        }
        entityManager.flush();
    }

    public void merge(ShipmentReceiptState detached) {
        ShipmentReceiptState persistent = getEntityManager().find(AbstractShipmentReceiptState.SimpleShipmentReceiptState.class, detached.getReceiptId());
        if (persistent != null) {
            merge(persistent, detached);
            entityManager.merge(persistent);
        } else {
            entityManager.persist(detached);
        }
        entityManager.flush();
    }

    private void merge(ShipmentReceiptState persistent, ShipmentReceiptState detached) {
        ((AbstractShipmentReceiptState) persistent).merge(detached);
    }

}

