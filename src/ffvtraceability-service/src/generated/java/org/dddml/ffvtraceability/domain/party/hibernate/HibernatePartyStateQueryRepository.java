// <autogenerated>
//   This file was generated by dddappp code generator.
//   Any changes made to this file manually will be lost next time the file is regenerated.
// </autogenerated>

package org.dddml.ffvtraceability.domain.party.hibernate;

import java.util.*;
import java.time.OffsetDateTime;
import org.dddml.ffvtraceability.domain.*;
import org.hibernate.Session;
import jakarta.persistence.EntityManager;
import jakarta.persistence.criteria.*;
import jakarta.persistence.TypedQuery;
import jakarta.persistence.PersistenceContext;
import org.springframework.stereotype.Repository;
import java.util.stream.Collectors;
import org.dddml.ffvtraceability.domain.party.*;
import org.dddml.ffvtraceability.specialization.*;
import org.dddml.ffvtraceability.specialization.jpa.*;
import org.springframework.transaction.annotation.Transactional;

@Repository
public class HibernatePartyStateQueryRepository implements PartyStateQueryRepository {
    @PersistenceContext
    private EntityManager entityManager;

    protected EntityManager getEntityManager() {
        EntityManager em = this.entityManager;
        String currentTenantId = TenantContext.getTenantId();
        if (currentTenantId == null || currentTenantId.isEmpty()) {
            throw new IllegalStateException("Tenant context not set");
        }
        if (TenantSupport.SUPER_TENANT_ID != null && !TenantSupport.SUPER_TENANT_ID.isEmpty()
            && TenantSupport.SUPER_TENANT_ID.equals(currentTenantId)) {
            return em;
        }
        org.hibernate.Session session = em.unwrap(org.hibernate.Session.class);
        org.hibernate.Filter filter = session.enableFilter("tenantFilter");
        filter.setParameter("tenantId", currentTenantId);
        filter.validate();
        return em;
    }

    private static final Set<String> readOnlyPropertyPascalCaseNames = new HashSet<String>(Arrays.asList("PartyId", "PartyTypeId", "PrimaryRoleTypeId", "ExternalId", "PreferredCurrencyUomId", "Description", "StatusId", "ShortDescription", "Email", "WebSite", "Telephone", "PartyIdentifications", "Salutation", "FirstName", "MiddleName", "LastName", "PersonalTitle", "Nickname", "Gender", "BirthDate", "DeceasedDate", "SocialSecurityNumber", "PassportNumber", "PassportExpireDate", "ExistingCustomer", "OrganizationName", "FamilyName", "Version", "CreatedBy", "CreatedAt", "UpdatedBy", "UpdatedAt"));

    private ReadOnlyProxyGenerator readOnlyProxyGenerator;

    public ReadOnlyProxyGenerator getReadOnlyProxyGenerator() {
        return readOnlyProxyGenerator;
    }

    public void setReadOnlyProxyGenerator(ReadOnlyProxyGenerator readOnlyProxyGenerator) {
        this.readOnlyProxyGenerator = readOnlyProxyGenerator;
    }

    @Transactional(readOnly = true)
    public PartyState get(String id) {
        PartyState state = (PartyState)getEntityManager().find(AbstractPartyState.class, id);
        return state;
    }

    @Transactional(readOnly = true)
    public Iterable<PartyState> getAll(Integer firstResult, Integer maxResults) {
        return getAll(PartyState.class, firstResult, maxResults);
    }
    
    @Transactional(readOnly = true)
    public Iterable<PartyState> get(Iterable<Map.Entry<String, Object>> filter, List<String> orders, Integer firstResult, Integer maxResults) {
        return get(PartyState.class, filter, orders, firstResult, maxResults);
    }

    @Transactional(readOnly = true)
    public Iterable<PartyState> get(org.dddml.support.criterion.Criterion filter, List<String> orders, Integer firstResult, Integer maxResults) {
        return get(PartyState.class, filter, orders, firstResult, maxResults);
    }

    @Transactional(readOnly = true)
    public PartyState getFirst(Iterable<Map.Entry<String, Object>> filter, List<String> orders) {
        return getFirst(PartyState.class, filter, orders);
    }

    @Transactional(readOnly = true)
    public PartyState getFirst(Map.Entry<String, Object> keyValue, List<String> orders) {
        return getFirst(PartyState.class, keyValue, orders);
    }

    @Transactional(readOnly = true)
    public Iterable<PartyState> getByProperty(String propertyName, Object propertyValue, List<String> orders, Integer firstResult, Integer maxResults) {
        return getByProperty(PartyState.class, propertyName, propertyValue, orders, firstResult, maxResults);
    }

    @Transactional(readOnly = true)
    public long getCount(Iterable<Map.Entry<String, Object>> filter) {
        return getCount(PartyState.class, filter);
    }

    @Transactional(readOnly = true)
    public long getCount(org.dddml.support.criterion.Criterion filter) {
        return getCount(PartyState.class, filter);
    }
    // //////////////////////////////////////

    @Transactional(readOnly = true)
    public Iterable<PartyState> getAll(Class<? extends PartyState> stateType, Integer firstResult, Integer maxResults) {
        EntityManager em = getEntityManager();
        CriteriaBuilder cb = em.getCriteriaBuilder();
        CriteriaQuery<AbstractPartyState> cq = cb.createQuery(AbstractPartyState.class);
        Root<AbstractPartyState> root = cq.from(AbstractPartyState.class);
        cq.select(root);
        addNotDeletedRestriction(cb, cq, root);
        TypedQuery<AbstractPartyState> query = em.createQuery(cq);
        JpaUtils.applyPagination(query, firstResult, maxResults);
        return query.getResultList().stream().map(PartyState.class::cast).collect(Collectors.toList());
    }

    @Transactional(readOnly = true)
    public Iterable<PartyState> get(Class<? extends PartyState> stateType, Iterable<Map.Entry<String, Object>> filter, List<String> orders, Integer firstResult, Integer maxResults) {
        EntityManager em = getEntityManager();
        CriteriaBuilder cb = em.getCriteriaBuilder();
        CriteriaQuery<AbstractPartyState> cq = cb.createQuery(AbstractPartyState.class);
        Root<AbstractPartyState> root = cq.from(AbstractPartyState.class);
        cq.select(root);
        JpaUtils.criteriaAddFilterAndOrders(cb, cq, root, filter, orders);
        addNotDeletedRestriction(cb, cq, root);
        TypedQuery<AbstractPartyState> query = em.createQuery(cq);
        JpaUtils.applyPagination(query, firstResult, maxResults);
        return query.getResultList().stream().map(PartyState.class::cast).collect(Collectors.toList());
    }

    @Transactional(readOnly = true)
    public Iterable<PartyState> get(Class<? extends PartyState> stateType, org.dddml.support.criterion.Criterion filter, List<String> orders, Integer firstResult, Integer maxResults) {
        EntityManager em = getEntityManager();
        CriteriaBuilder cb = em.getCriteriaBuilder();
        CriteriaQuery<AbstractPartyState> cq = cb.createQuery(AbstractPartyState.class);
        Root<AbstractPartyState> root = cq.from(AbstractPartyState.class);
        cq.select(root);
        JpaUtils.criteriaAddFilterAndOrders(cb, cq, root, filter, orders);
        addNotDeletedRestriction(cb, cq, root);
        TypedQuery<AbstractPartyState> query = em.createQuery(cq);
        JpaUtils.applyPagination(query, firstResult, maxResults);
        return query.getResultList().stream().map(PartyState.class::cast).collect(Collectors.toList());
    }

    @Transactional(readOnly = true)
    public PartyState getFirst(Class<? extends PartyState> stateType, Iterable<Map.Entry<String, Object>> filter, List<String> orders) {
        List<PartyState> list = (List<PartyState>)get(stateType, filter, orders, 0, 1);
        if (list == null || list.size() <= 0) {
            return null;
        }
        return list.get(0);
    }

    @Transactional(readOnly = true)
    public PartyState getFirst(Class<? extends PartyState> stateType, Map.Entry<String, Object> keyValue, List<String> orders) {
        List<Map.Entry<String, Object>> filter = new ArrayList<>();
        filter.add(keyValue);
        return getFirst(stateType, filter, orders);
    }

    @Transactional(readOnly = true)
    public Iterable<PartyState> getByProperty(Class<? extends PartyState> stateType, String propertyName, Object propertyValue, List<String> orders, Integer firstResult, Integer maxResults) {
        Map.Entry<String, Object> keyValue = new AbstractMap.SimpleEntry<>(propertyName, propertyValue);
        List<Map.Entry<String, Object>> filter = new ArrayList<>();
        filter.add(keyValue);
        return get(filter, orders, firstResult, maxResults);
    }

    @Transactional(readOnly = true)
    public long getCount(Class<? extends PartyState> stateType, Iterable<Map.Entry<String, Object>> filter) {
        EntityManager em = getEntityManager();
        CriteriaBuilder cb = em.getCriteriaBuilder();
        CriteriaQuery<Long> cq = cb.createQuery(Long.class);
        Root<AbstractPartyState> root = cq.from(AbstractPartyState.class);
        cq.select(cb.count(root));
        if (filter != null) {
            JpaUtils.criteriaAddFilter(cb, cq, root, filter);
        }
        addNotDeletedRestriction(cb, cq, root);
        return em.createQuery(cq).getSingleResult();
    }

    @Transactional(readOnly = true)
    public long getCount(Class<? extends PartyState> stateType, org.dddml.support.criterion.Criterion filter) {
        EntityManager em = getEntityManager();
        CriteriaBuilder cb = em.getCriteriaBuilder();
        CriteriaQuery<Long> cq = cb.createQuery(Long.class);
        Root<AbstractPartyState> root = cq.from(AbstractPartyState.class);
        cq.select(cb.count(root));
        if (filter != null) {
            JpaUtils.criteriaAddFilter(cb, cq, root, filter);
        }
        addNotDeletedRestriction(cb, cq, root);
        return em.createQuery(cq).getSingleResult();
    }

    @Transactional(readOnly = true)
    public PartyIdentificationState getPartyIdentification(String partyId, String partyIdentificationTypeId) {
        PartyIdentificationId entityId = new PartyIdentificationId(partyId, partyIdentificationTypeId);
        return (PartyIdentificationState) getEntityManager().find(AbstractPartyIdentificationState.SimplePartyIdentificationState.class, entityId);
    }

    @Transactional(readOnly = true)
    public Iterable<PartyIdentificationState> getPartyIdentifications(String partyId, org.dddml.support.criterion.Criterion filter, List<String> orders) {
        EntityManager em = getEntityManager();
        CriteriaBuilder cb = em.getCriteriaBuilder();
        CriteriaQuery<AbstractPartyIdentificationState.SimplePartyIdentificationState> cq = cb.createQuery(AbstractPartyIdentificationState.SimplePartyIdentificationState.class);
        Root<AbstractPartyIdentificationState.SimplePartyIdentificationState> root = cq.from(AbstractPartyIdentificationState.SimplePartyIdentificationState.class);
        cq.select(root);

        Predicate partIdCondition = cb.and(
            cb.equal(root.get("partyIdentificationId").get("partyId"), partyId)
        );
        cq.where(partIdCondition);

        // Add filter and orders
        JpaUtils.criteriaAddFilterAndOrders(cb, cq, root, filter, orders);

        TypedQuery<AbstractPartyIdentificationState.SimplePartyIdentificationState> query = em.createQuery(cq);
        return query.getResultList().stream().map(PartyIdentificationState.class::cast).collect(Collectors.toList());
    }

    protected void addNotDeletedRestriction(CriteriaBuilder cb, CriteriaQuery<?> cq, Root<?> root) {
    }

}

