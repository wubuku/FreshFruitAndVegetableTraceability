// <autogenerated>
//   This file was generated by dddappp code generator.
//   Any changes made to this file manually will be lost next time the file is regenerated.
// </autogenerated>

package org.dddml.ffvtraceability.domain.attributeset.hibernate;

import java.util.*;
import java.time.OffsetDateTime;
import org.dddml.ffvtraceability.domain.*;
import org.hibernate.Session;
import jakarta.persistence.EntityManager;
import jakarta.persistence.criteria.*;
import jakarta.persistence.TypedQuery;
import jakarta.persistence.PersistenceContext;
import org.springframework.stereotype.Repository;
import java.util.stream.Collectors;
import org.dddml.ffvtraceability.domain.attributeset.*;
import org.dddml.ffvtraceability.specialization.*;
import org.dddml.ffvtraceability.specialization.jpa.*;
import org.springframework.transaction.annotation.Transactional;

@Repository
public class HibernateAttributeSetStateQueryRepository implements AttributeSetStateQueryRepository {
    @PersistenceContext
    private EntityManager entityManager;

    protected EntityManager getEntityManager() {
        EntityManager em = this.entityManager;
        String currentTenantId = TenantContext.getTenantId();
        if (currentTenantId == null || currentTenantId.isEmpty()) {
            throw new IllegalStateException("Tenant context not set");
        }
        if (TenantSupport.SUPER_TENANT_ID != null && !TenantSupport.SUPER_TENANT_ID.isEmpty()
            && TenantSupport.SUPER_TENANT_ID.equals(currentTenantId)) {
            return em;
        }
        org.hibernate.Session session = em.unwrap(org.hibernate.Session.class);
        org.hibernate.Filter filter = session.enableFilter("tenantFilter");
        filter.setParameter("tenantId", currentTenantId);
        filter.validate();
        return em;
    }

    private static final Set<String> readOnlyPropertyPascalCaseNames = new HashSet<String>(Arrays.asList("AttributeSetId", "AttributeSetName", "Description", "AttributeUses", "Version", "CreatedBy", "CreatedAt", "UpdatedBy", "UpdatedAt"));

    private ReadOnlyProxyGenerator readOnlyProxyGenerator;

    public ReadOnlyProxyGenerator getReadOnlyProxyGenerator() {
        return readOnlyProxyGenerator;
    }

    public void setReadOnlyProxyGenerator(ReadOnlyProxyGenerator readOnlyProxyGenerator) {
        this.readOnlyProxyGenerator = readOnlyProxyGenerator;
    }

    @Transactional(readOnly = true)
    public AttributeSetState get(String id) {
        AttributeSetState state = (AttributeSetState)getEntityManager().find(AbstractAttributeSetState.SimpleAttributeSetState.class, id);
        return state;
    }

    @Transactional(readOnly = true)
    public Iterable<AttributeSetState> getAll(Integer firstResult, Integer maxResults) {
        EntityManager em = getEntityManager();
        CriteriaBuilder cb = em.getCriteriaBuilder();
        CriteriaQuery<AbstractAttributeSetState.SimpleAttributeSetState> cq = cb.createQuery(AbstractAttributeSetState.SimpleAttributeSetState.class);
        Root<AbstractAttributeSetState.SimpleAttributeSetState> root = cq.from(AbstractAttributeSetState.SimpleAttributeSetState.class);
        cq.select(root);
        addNotDeletedRestriction(cb, cq, root);
        TypedQuery<AbstractAttributeSetState.SimpleAttributeSetState> query = em.createQuery(cq);
        JpaUtils.applyPagination(query, firstResult, maxResults);
        return query.getResultList().stream().map(AttributeSetState.class::cast).collect(Collectors.toList());
    }

    @Transactional(readOnly = true)
    public Iterable<AttributeSetState> get(Iterable<Map.Entry<String, Object>> filter, List<String> orders, Integer firstResult, Integer maxResults) {
        EntityManager em = getEntityManager();
        CriteriaBuilder cb = em.getCriteriaBuilder();
        CriteriaQuery<AbstractAttributeSetState.SimpleAttributeSetState> cq = cb.createQuery(AbstractAttributeSetState.SimpleAttributeSetState.class);
        Root<AbstractAttributeSetState.SimpleAttributeSetState> root = cq.from(AbstractAttributeSetState.SimpleAttributeSetState.class);
        cq.select(root);
        JpaUtils.criteriaAddFilterAndOrders(cb, cq, root, filter, orders);
        addNotDeletedRestriction(cb, cq, root);
        TypedQuery<AbstractAttributeSetState.SimpleAttributeSetState> query = em.createQuery(cq);
        JpaUtils.applyPagination(query, firstResult, maxResults);
        return query.getResultList().stream().map(AttributeSetState.class::cast).collect(Collectors.toList());
    }

    @Transactional(readOnly = true)
    public Iterable<AttributeSetState> get(org.dddml.support.criterion.Criterion filter, List<String> orders, Integer firstResult, Integer maxResults) {
        EntityManager em = getEntityManager();
        CriteriaBuilder cb = em.getCriteriaBuilder();
        CriteriaQuery<AbstractAttributeSetState.SimpleAttributeSetState> cq = cb.createQuery(AbstractAttributeSetState.SimpleAttributeSetState.class);
        Root<AbstractAttributeSetState.SimpleAttributeSetState> root = cq.from(AbstractAttributeSetState.SimpleAttributeSetState.class);
        cq.select(root);
        JpaUtils.criteriaAddFilterAndOrders(cb, cq, root, filter, orders);
        addNotDeletedRestriction(cb, cq, root);
        TypedQuery<AbstractAttributeSetState.SimpleAttributeSetState> query = em.createQuery(cq);
        JpaUtils.applyPagination(query, firstResult, maxResults);
        return query.getResultList().stream().map(AttributeSetState.class::cast).collect(Collectors.toList());
    }

    @Transactional(readOnly = true)
    public AttributeSetState getFirst(Iterable<Map.Entry<String, Object>> filter, List<String> orders) {
        List<AttributeSetState> list = (List<AttributeSetState>)get(filter, orders, 0, 1);
        if (list == null || list.size() <= 0) {
            return null;
        }
        return list.get(0);
    }

    @Transactional(readOnly = true)
    public AttributeSetState getFirst(Map.Entry<String, Object> keyValue, List<String> orders) {
        List<Map.Entry<String, Object>> filter = new ArrayList<>();
        filter.add(keyValue);
        return getFirst(filter, orders);
    }

    @Transactional(readOnly = true)
    public Iterable<AttributeSetState> getByProperty(String propertyName, Object propertyValue, List<String> orders, Integer firstResult, Integer maxResults) {
        Map.Entry<String, Object> keyValue = new AbstractMap.SimpleEntry<>(propertyName, propertyValue);
        List<Map.Entry<String, Object>> filter = new ArrayList<>();
        filter.add(keyValue);
        return get(filter, orders, firstResult, maxResults);
    }

    @Transactional(readOnly = true)
    public long getCount(Iterable<Map.Entry<String, Object>> filter) {
        EntityManager em = getEntityManager();
        CriteriaBuilder cb = em.getCriteriaBuilder();
        CriteriaQuery<Long> cq = cb.createQuery(Long.class);
        Root<AbstractAttributeSetState.SimpleAttributeSetState> root = cq.from(AbstractAttributeSetState.SimpleAttributeSetState.class);
        cq.select(cb.count(root));
        if (filter != null) {
            JpaUtils.criteriaAddFilter(cb, cq, root, filter);
        }
        addNotDeletedRestriction(cb, cq, root);
        return em.createQuery(cq).getSingleResult();
    }

    @Transactional(readOnly = true)
    public long getCount(org.dddml.support.criterion.Criterion filter) {
        EntityManager em = getEntityManager();
        CriteriaBuilder cb = em.getCriteriaBuilder();
        CriteriaQuery<Long> cq = cb.createQuery(Long.class);
        Root<AbstractAttributeSetState.SimpleAttributeSetState> root = cq.from(AbstractAttributeSetState.SimpleAttributeSetState.class);
        cq.select(cb.count(root));
        if (filter != null) {
            JpaUtils.criteriaAddFilter(cb, cq, root, filter);
        }
        addNotDeletedRestriction(cb, cq, root);
        return em.createQuery(cq).getSingleResult();
    }

    @Transactional(readOnly = true)
    public AttributeUseState getAttributeUse(String attributeSetId, String attributeId) {
        AttributeSetAttributeUseId entityId = new AttributeSetAttributeUseId(attributeSetId, attributeId);
        return (AttributeUseState) getEntityManager().find(AbstractAttributeUseState.SimpleAttributeUseState.class, entityId);
    }

    @Transactional(readOnly = true)
    public Iterable<AttributeUseState> getAttributeUses(String attributeSetId, org.dddml.support.criterion.Criterion filter, List<String> orders) {
        EntityManager em = getEntityManager();
        CriteriaBuilder cb = em.getCriteriaBuilder();
        CriteriaQuery<AbstractAttributeUseState.SimpleAttributeUseState> cq = cb.createQuery(AbstractAttributeUseState.SimpleAttributeUseState.class);
        Root<AbstractAttributeUseState.SimpleAttributeUseState> root = cq.from(AbstractAttributeUseState.SimpleAttributeUseState.class);
        cq.select(root);

        Predicate partIdCondition = cb.and(
            cb.equal(root.get("attributeSetAttributeUseId").get("attributeSetId"), attributeSetId)
        );
        cq.where(partIdCondition);

        // Add filter and orders
        JpaUtils.criteriaAddFilterAndOrders(cb, cq, root, filter, orders);

        TypedQuery<AbstractAttributeUseState.SimpleAttributeUseState> query = em.createQuery(cq);
        return query.getResultList().stream().map(AttributeUseState.class::cast).collect(Collectors.toList());
    }

    protected void addNotDeletedRestriction(CriteriaBuilder cb, CriteriaQuery<?> cq, Root<?> root) {
    }

}

