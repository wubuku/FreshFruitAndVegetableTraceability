// <autogenerated>
//   This file was generated by dddappp code generator.
//   Any changes made to this file manually will be lost next time the file is regenerated.
// </autogenerated>

package org.dddml.ffvtraceability.domain.facilitycontactmech.hibernate;

import java.util.*;
import java.time.OffsetDateTime;
import org.dddml.ffvtraceability.domain.*;
import jakarta.persistence.EntityManager;
import jakarta.persistence.PersistenceContext;
import org.springframework.stereotype.Repository;
import org.dddml.ffvtraceability.domain.facilitycontactmech.*;
import org.dddml.ffvtraceability.specialization.*;
import org.dddml.ffvtraceability.specialization.hibernate.*;
import org.springframework.transaction.annotation.Transactional;

@Repository("facilityContactMechStateRepository")
public class HibernateFacilityContactMechStateRepository implements FacilityContactMechStateRepository {
    @PersistenceContext
    private EntityManager entityManager;

    protected EntityManager getEntityManager() {
        return this.entityManager;
    }

    private static final Set<String> readOnlyPropertyPascalCaseNames = new HashSet<String>(Arrays.asList("FacilityContactMechId", "ThruDate", "Extension", "Comments", "FacilityContactMechPurposes", "Version", "CreatedBy", "CreatedAt", "UpdatedBy", "UpdatedAt"));
    
    private ReadOnlyProxyGenerator readOnlyProxyGenerator;
    
    public ReadOnlyProxyGenerator getReadOnlyProxyGenerator() {
        return readOnlyProxyGenerator;
    }

    public void setReadOnlyProxyGenerator(ReadOnlyProxyGenerator readOnlyProxyGenerator) {
        this.readOnlyProxyGenerator = readOnlyProxyGenerator;
    }

    @Transactional(readOnly = true)
    public FacilityContactMechState get(FacilityContactMechId id, boolean nullAllowed) {
        FacilityContactMechState.SqlFacilityContactMechState state = (FacilityContactMechState.SqlFacilityContactMechState)getEntityManager().find(AbstractFacilityContactMechState.SimpleFacilityContactMechState.class, id);
        if (!nullAllowed && state == null) {
            state = new AbstractFacilityContactMechState.SimpleFacilityContactMechState();
            state.setFacilityContactMechId(id);
        }
        if (getReadOnlyProxyGenerator() != null && state != null) {
            return (FacilityContactMechState) getReadOnlyProxyGenerator().createProxy(state, new Class[]{FacilityContactMechState.SqlFacilityContactMechState.class, Saveable.class}, "getStateReadOnly", readOnlyPropertyPascalCaseNames);
        }
        return state;
    }

    public void save(FacilityContactMechState state) {
        FacilityContactMechState s = state;
        if (getReadOnlyProxyGenerator() != null) {
            s = (FacilityContactMechState) getReadOnlyProxyGenerator().getTarget(state);
        }
        if (s.getVersion() == null) {
            entityManager.persist(s);
        } else {
            entityManager.merge(s);
        }

        if (s instanceof Saveable) {
            Saveable saveable = (Saveable) s;
            saveable.save();
        }
        entityManager.flush();
    }

    public void merge(FacilityContactMechState detached) {
        FacilityContactMechState persistent = getEntityManager().find(AbstractFacilityContactMechState.SimpleFacilityContactMechState.class, detached.getFacilityContactMechId());
        if (persistent != null) {
            merge(persistent, detached);
            entityManager.merge(persistent);
        } else {
            entityManager.persist(detached);
        }
        entityManager.flush();
    }

    private void merge(FacilityContactMechState persistent, FacilityContactMechState detached) {
        ((AbstractFacilityContactMechState) persistent).merge(detached);
    }

}

