// <autogenerated>
//   This file was generated by dddappp code generator.
//   Any changes made to this file manually will be lost next time the file is regenerated.
// </autogenerated>

package org.dddml.ffvtraceability.domain.facilityidentification.hibernate;

import java.util.*;
import java.time.OffsetDateTime;
import org.dddml.ffvtraceability.domain.*;
import jakarta.persistence.EntityManager;
import jakarta.persistence.PersistenceContext;
import org.springframework.stereotype.Repository;
import org.dddml.ffvtraceability.domain.facilityidentification.*;
import org.dddml.ffvtraceability.specialization.*;
import org.dddml.ffvtraceability.specialization.hibernate.*;
import org.springframework.transaction.annotation.Transactional;

@Repository("facilityIdentificationStateRepository")
public class HibernateFacilityIdentificationStateRepository implements FacilityIdentificationStateRepository {
    @PersistenceContext
    private EntityManager entityManager;

    protected EntityManager getEntityManager() {
        return this.entityManager;
    }

    private static final Set<String> readOnlyPropertyPascalCaseNames = new HashSet<String>(Arrays.asList("FacilityIdentificationTypeId", "IdValue", "Version", "CreatedBy", "CreatedAt", "UpdatedBy", "UpdatedAt", "Active", "Deleted"));
    
    private ReadOnlyProxyGenerator readOnlyProxyGenerator;
    
    public ReadOnlyProxyGenerator getReadOnlyProxyGenerator() {
        return readOnlyProxyGenerator;
    }

    public void setReadOnlyProxyGenerator(ReadOnlyProxyGenerator readOnlyProxyGenerator) {
        this.readOnlyProxyGenerator = readOnlyProxyGenerator;
    }

    @Transactional(readOnly = true)
    public FacilityIdentificationState get(String id, boolean nullAllowed) {
        FacilityIdentificationState.SqlFacilityIdentificationState state = (FacilityIdentificationState.SqlFacilityIdentificationState)getEntityManager().find(AbstractFacilityIdentificationState.SimpleFacilityIdentificationState.class, id);
        if (!nullAllowed && state == null) {
            state = new AbstractFacilityIdentificationState.SimpleFacilityIdentificationState();
            state.setFacilityIdentificationTypeId(id);
        }
        if (getReadOnlyProxyGenerator() != null && state != null) {
            return (FacilityIdentificationState) getReadOnlyProxyGenerator().createProxy(state, new Class[]{FacilityIdentificationState.SqlFacilityIdentificationState.class}, "getStateReadOnly", readOnlyPropertyPascalCaseNames);
        }
        return state;
    }

    public void save(FacilityIdentificationState state) {
        FacilityIdentificationState s = state;
        if (getReadOnlyProxyGenerator() != null) {
            s = (FacilityIdentificationState) getReadOnlyProxyGenerator().getTarget(state);
        }
        if (s.getVersion() == null) {
            entityManager.persist(s);
        } else {
            entityManager.merge(s);
        }

        if (s instanceof Saveable) {
            Saveable saveable = (Saveable) s;
            saveable.save();
        }
        entityManager.flush();
    }

    public void merge(FacilityIdentificationState detached) {
        FacilityIdentificationState persistent = getEntityManager().find(AbstractFacilityIdentificationState.SimpleFacilityIdentificationState.class, detached.getFacilityIdentificationTypeId());
        if (persistent != null) {
            merge(persistent, detached);
            entityManager.merge(persistent);
        } else {
            entityManager.persist(detached);
        }
        entityManager.flush();
    }

    private void merge(FacilityIdentificationState persistent, FacilityIdentificationState detached) {
        ((AbstractFacilityIdentificationState) persistent).merge(detached);
    }

}

