// <autogenerated>
//   This file was generated by dddappp code generator.
//   Any changes made to this file manually will be lost next time the file is regenerated.
// </autogenerated>

package org.dddml.ffvtraceability.domain.shipmentreceipt.hibernate;

import java.util.*;
import java.time.OffsetDateTime;
import org.dddml.ffvtraceability.domain.partyrole.*;
import org.dddml.ffvtraceability.domain.order.*;
import org.dddml.ffvtraceability.domain.*;
import org.hibernate.Session;
import jakarta.persistence.EntityManager;
import jakarta.persistence.criteria.*;
import jakarta.persistence.TypedQuery;
import jakarta.persistence.PersistenceContext;
import org.springframework.stereotype.Repository;
import java.util.stream.Collectors;
import org.dddml.ffvtraceability.domain.shipmentreceipt.*;
import org.dddml.ffvtraceability.specialization.*;
import org.dddml.ffvtraceability.specialization.jpa.*;
import org.springframework.transaction.annotation.Transactional;

@Repository
public class HibernateShipmentReceiptStateQueryRepository implements ShipmentReceiptStateQueryRepository {
    @PersistenceContext
    private EntityManager entityManager;

    protected EntityManager getEntityManager() {
        return this.entityManager;
    }

    private static final Set<String> readOnlyPropertyPascalCaseNames = new HashSet<String>(Arrays.asList("ReceiptId", "ProductId", "ShipmentId", "ShipmentItemSeqId", "ShipmentPackageSeqId", "OrderId", "OrderItemSeqId", "ReturnId", "ReturnItemSeqId", "RejectionId", "ReceivedBy", "DatetimeReceived", "ItemDescription", "QuantityAccepted", "QuantityRejected", "LotId", "LocationSeqId", "CasesAccepted", "CasesRejected", "QuantityUnallocated", "OrderAllocations", "ShipmentReceiptRoles", "Version", "CreatedBy", "CreatedAt", "UpdatedBy", "UpdatedAt", "Deleted"));

    private ReadOnlyProxyGenerator readOnlyProxyGenerator;

    public ReadOnlyProxyGenerator getReadOnlyProxyGenerator() {
        return readOnlyProxyGenerator;
    }

    public void setReadOnlyProxyGenerator(ReadOnlyProxyGenerator readOnlyProxyGenerator) {
        this.readOnlyProxyGenerator = readOnlyProxyGenerator;
    }

    @Transactional(readOnly = true)
    public ShipmentReceiptState get(String id) {
        ShipmentReceiptState state = (ShipmentReceiptState)getEntityManager().find(AbstractShipmentReceiptState.SimpleShipmentReceiptState.class, id);
        return state;
    }

    @Transactional(readOnly = true)
    public Iterable<ShipmentReceiptState> getAll(Integer firstResult, Integer maxResults) {
        EntityManager em = getEntityManager();
        CriteriaBuilder cb = em.getCriteriaBuilder();
        CriteriaQuery<AbstractShipmentReceiptState.SimpleShipmentReceiptState> cq = cb.createQuery(AbstractShipmentReceiptState.SimpleShipmentReceiptState.class);
        Root<AbstractShipmentReceiptState.SimpleShipmentReceiptState> root = cq.from(AbstractShipmentReceiptState.SimpleShipmentReceiptState.class);
        cq.select(root);
        addNotDeletedRestriction(cb, cq, root);
        TypedQuery<AbstractShipmentReceiptState.SimpleShipmentReceiptState> query = em.createQuery(cq);
        JpaUtils.applyPagination(query, firstResult, maxResults);
        return query.getResultList().stream().map(ShipmentReceiptState.class::cast).collect(Collectors.toList());
    }

    @Transactional(readOnly = true)
    public Iterable<ShipmentReceiptState> get(Iterable<Map.Entry<String, Object>> filter, List<String> orders, Integer firstResult, Integer maxResults) {
        EntityManager em = getEntityManager();
        CriteriaBuilder cb = em.getCriteriaBuilder();
        CriteriaQuery<AbstractShipmentReceiptState.SimpleShipmentReceiptState> cq = cb.createQuery(AbstractShipmentReceiptState.SimpleShipmentReceiptState.class);
        Root<AbstractShipmentReceiptState.SimpleShipmentReceiptState> root = cq.from(AbstractShipmentReceiptState.SimpleShipmentReceiptState.class);
        cq.select(root);
        JpaUtils.criteriaAddFilterAndOrders(cb, cq, root, filter, orders);
        addNotDeletedRestriction(cb, cq, root);
        TypedQuery<AbstractShipmentReceiptState.SimpleShipmentReceiptState> query = em.createQuery(cq);
        JpaUtils.applyPagination(query, firstResult, maxResults);
        return query.getResultList().stream().map(ShipmentReceiptState.class::cast).collect(Collectors.toList());
    }

    @Transactional(readOnly = true)
    public Iterable<ShipmentReceiptState> get(org.dddml.support.criterion.Criterion filter, List<String> orders, Integer firstResult, Integer maxResults) {
        EntityManager em = getEntityManager();
        CriteriaBuilder cb = em.getCriteriaBuilder();
        CriteriaQuery<AbstractShipmentReceiptState.SimpleShipmentReceiptState> cq = cb.createQuery(AbstractShipmentReceiptState.SimpleShipmentReceiptState.class);
        Root<AbstractShipmentReceiptState.SimpleShipmentReceiptState> root = cq.from(AbstractShipmentReceiptState.SimpleShipmentReceiptState.class);
        cq.select(root);
        JpaUtils.criteriaAddFilterAndOrders(cb, cq, root, filter, orders);
        addNotDeletedRestriction(cb, cq, root);
        TypedQuery<AbstractShipmentReceiptState.SimpleShipmentReceiptState> query = em.createQuery(cq);
        JpaUtils.applyPagination(query, firstResult, maxResults);
        return query.getResultList().stream().map(ShipmentReceiptState.class::cast).collect(Collectors.toList());
    }

    @Transactional(readOnly = true)
    public ShipmentReceiptState getFirst(Iterable<Map.Entry<String, Object>> filter, List<String> orders) {
        List<ShipmentReceiptState> list = (List<ShipmentReceiptState>)get(filter, orders, 0, 1);
        if (list == null || list.size() <= 0) {
            return null;
        }
        return list.get(0);
    }

    @Transactional(readOnly = true)
    public ShipmentReceiptState getFirst(Map.Entry<String, Object> keyValue, List<String> orders) {
        List<Map.Entry<String, Object>> filter = new ArrayList<>();
        filter.add(keyValue);
        return getFirst(filter, orders);
    }

    @Transactional(readOnly = true)
    public Iterable<ShipmentReceiptState> getByProperty(String propertyName, Object propertyValue, List<String> orders, Integer firstResult, Integer maxResults) {
        Map.Entry<String, Object> keyValue = new AbstractMap.SimpleEntry<>(propertyName, propertyValue);
        List<Map.Entry<String, Object>> filter = new ArrayList<>();
        filter.add(keyValue);
        return get(filter, orders, firstResult, maxResults);
    }

    @Transactional(readOnly = true)
    public long getCount(Iterable<Map.Entry<String, Object>> filter) {
        EntityManager em = getEntityManager();
        CriteriaBuilder cb = em.getCriteriaBuilder();
        CriteriaQuery<Long> cq = cb.createQuery(Long.class);
        Root<AbstractShipmentReceiptState.SimpleShipmentReceiptState> root = cq.from(AbstractShipmentReceiptState.SimpleShipmentReceiptState.class);
        cq.select(cb.count(root));
        if (filter != null) {
            JpaUtils.criteriaAddFilter(cb, cq, root, filter);
        }
        addNotDeletedRestriction(cb, cq, root);
        return em.createQuery(cq).getSingleResult();
    }

    @Transactional(readOnly = true)
    public long getCount(org.dddml.support.criterion.Criterion filter) {
        EntityManager em = getEntityManager();
        CriteriaBuilder cb = em.getCriteriaBuilder();
        CriteriaQuery<Long> cq = cb.createQuery(Long.class);
        Root<AbstractShipmentReceiptState.SimpleShipmentReceiptState> root = cq.from(AbstractShipmentReceiptState.SimpleShipmentReceiptState.class);
        cq.select(cb.count(root));
        if (filter != null) {
            JpaUtils.criteriaAddFilter(cb, cq, root, filter);
        }
        addNotDeletedRestriction(cb, cq, root);
        return em.createQuery(cq).getSingleResult();
    }

    @Transactional(readOnly = true)
    public ShipmentReceiptRoleState getShipmentReceiptRole(String shipmentReceiptReceiptId, PartyRoleId partyRoleId) {
        ShipmentReceiptRoleId entityId = new ShipmentReceiptRoleId(shipmentReceiptReceiptId, partyRoleId);
        return (ShipmentReceiptRoleState) getEntityManager().find(AbstractShipmentReceiptRoleState.SimpleShipmentReceiptRoleState.class, entityId);
    }

    @Transactional(readOnly = true)
    public Iterable<ShipmentReceiptRoleState> getShipmentReceiptRoles(String shipmentReceiptReceiptId, org.dddml.support.criterion.Criterion filter, List<String> orders) {
        EntityManager em = getEntityManager();
        CriteriaBuilder cb = em.getCriteriaBuilder();
        CriteriaQuery<AbstractShipmentReceiptRoleState.SimpleShipmentReceiptRoleState> cq = cb.createQuery(AbstractShipmentReceiptRoleState.SimpleShipmentReceiptRoleState.class);
        Root<AbstractShipmentReceiptRoleState.SimpleShipmentReceiptRoleState> root = cq.from(AbstractShipmentReceiptRoleState.SimpleShipmentReceiptRoleState.class);
        cq.select(root);

        Predicate partIdCondition = cb.and(
            cb.equal(root.get("shipmentReceiptRoleId").get("shipmentReceiptReceiptId"), shipmentReceiptReceiptId)
        );
        cq.where(partIdCondition);

        // Add filter and orders
        JpaUtils.criteriaAddFilterAndOrders(cb, cq, root, filter, orders);

        TypedQuery<AbstractShipmentReceiptRoleState.SimpleShipmentReceiptRoleState> query = em.createQuery(cq);
        return query.getResultList().stream().map(ShipmentReceiptRoleState.class::cast).collect(Collectors.toList());
    }

    @Transactional(readOnly = true)
    public ShipmentReceiptOrderAllocationState getShipmentReceiptOrderAllocation(String shipmentReceiptReceiptId, OrderItemId orderItemId) {
        ShipmentReceiptOrderAllocationId entityId = new ShipmentReceiptOrderAllocationId(shipmentReceiptReceiptId, orderItemId);
        return (ShipmentReceiptOrderAllocationState) getEntityManager().find(AbstractShipmentReceiptOrderAllocationState.SimpleShipmentReceiptOrderAllocationState.class, entityId);
    }

    @Transactional(readOnly = true)
    public Iterable<ShipmentReceiptOrderAllocationState> getShipmentReceiptOrderAllocations(String shipmentReceiptReceiptId, org.dddml.support.criterion.Criterion filter, List<String> orders) {
        EntityManager em = getEntityManager();
        CriteriaBuilder cb = em.getCriteriaBuilder();
        CriteriaQuery<AbstractShipmentReceiptOrderAllocationState.SimpleShipmentReceiptOrderAllocationState> cq = cb.createQuery(AbstractShipmentReceiptOrderAllocationState.SimpleShipmentReceiptOrderAllocationState.class);
        Root<AbstractShipmentReceiptOrderAllocationState.SimpleShipmentReceiptOrderAllocationState> root = cq.from(AbstractShipmentReceiptOrderAllocationState.SimpleShipmentReceiptOrderAllocationState.class);
        cq.select(root);

        Predicate partIdCondition = cb.and(
            cb.equal(root.get("shipmentReceiptOrderAllocationId").get("shipmentReceiptReceiptId"), shipmentReceiptReceiptId)
        );
        cq.where(partIdCondition);

        // Add filter and orders
        JpaUtils.criteriaAddFilterAndOrders(cb, cq, root, filter, orders);

        TypedQuery<AbstractShipmentReceiptOrderAllocationState.SimpleShipmentReceiptOrderAllocationState> query = em.createQuery(cq);
        return query.getResultList().stream().map(ShipmentReceiptOrderAllocationState.class::cast).collect(Collectors.toList());
    }

    protected void addNotDeletedRestriction(CriteriaBuilder cb, CriteriaQuery<?> cq, Root<?> root) {
        Predicate isNull = cb.isNull(root.get("deleted"));
        Predicate isFalse = cb.equal(root.get("deleted"), false);
        Predicate notDeleted = cb.or(isNull, isFalse);
        cq.where(cq.getRestriction() == null ? notDeleted : cb.and(cq.getRestriction(), notDeleted));
    }

}

