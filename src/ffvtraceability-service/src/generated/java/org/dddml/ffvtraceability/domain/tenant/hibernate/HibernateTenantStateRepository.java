// <autogenerated>
//   This file was generated by dddappp code generator.
//   Any changes made to this file manually will be lost next time the file is regenerated.
// </autogenerated>

package org.dddml.ffvtraceability.domain.tenant.hibernate;

import java.util.*;
import java.time.OffsetDateTime;
import org.dddml.ffvtraceability.domain.*;
import jakarta.persistence.EntityManager;
import jakarta.persistence.PersistenceContext;
import org.springframework.stereotype.Repository;
import org.dddml.ffvtraceability.domain.tenant.*;
import org.dddml.ffvtraceability.specialization.*;
import org.dddml.ffvtraceability.specialization.hibernate.*;
import org.springframework.transaction.annotation.Transactional;

@Repository("tenantStateRepository")
public class HibernateTenantStateRepository implements TenantStateRepository {
    @PersistenceContext
    private EntityManager entityManager;

    protected EntityManager getEntityManager() {
        return this.entityManager;
    }

    private static final Set<String> readOnlyPropertyPascalCaseNames = new HashSet<String>(Arrays.asList("TenantId", "PartyId", "TimeZoneId", "DateTimeFormat", "Description", "LongDescription", "Version", "CreatedBy", "CreatedAt", "UpdatedBy", "UpdatedAt"));
    
    private ReadOnlyProxyGenerator readOnlyProxyGenerator;
    
    public ReadOnlyProxyGenerator getReadOnlyProxyGenerator() {
        return readOnlyProxyGenerator;
    }

    public void setReadOnlyProxyGenerator(ReadOnlyProxyGenerator readOnlyProxyGenerator) {
        this.readOnlyProxyGenerator = readOnlyProxyGenerator;
    }

    @Transactional(readOnly = true)
    public TenantState get(String id, boolean nullAllowed) {
        TenantState.SqlTenantState state = (TenantState.SqlTenantState)getEntityManager().find(AbstractTenantState.SimpleTenantState.class, id);
        if (!nullAllowed && state == null) {
            state = new AbstractTenantState.SimpleTenantState();
            state.setTenantId(id);
        }
        if (getReadOnlyProxyGenerator() != null && state != null) {
            return (TenantState) getReadOnlyProxyGenerator().createProxy(state, new Class[]{TenantState.SqlTenantState.class}, "getStateReadOnly", readOnlyPropertyPascalCaseNames);
        }
        return state;
    }

    public void save(TenantState state) {
        TenantState s = state;
        if (getReadOnlyProxyGenerator() != null) {
            s = (TenantState) getReadOnlyProxyGenerator().getTarget(state);
        }
        if (s.getVersion() == null) {
            entityManager.persist(s);
        } else {
            entityManager.merge(s);
        }

        if (s instanceof Saveable) {
            Saveable saveable = (Saveable) s;
            saveable.save();
        }
        entityManager.flush();
    }

    public void merge(TenantState detached) {
        TenantState persistent = getEntityManager().find(AbstractTenantState.SimpleTenantState.class, detached.getTenantId());
        if (persistent != null) {
            merge(persistent, detached);
            entityManager.merge(persistent);
        } else {
            entityManager.persist(detached);
        }
        entityManager.flush();
    }

    private void merge(TenantState persistent, TenantState detached) {
        ((AbstractTenantState) persistent).merge(detached);
    }

}

