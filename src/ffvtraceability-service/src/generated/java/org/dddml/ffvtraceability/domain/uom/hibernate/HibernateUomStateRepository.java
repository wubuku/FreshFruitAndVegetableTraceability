// <autogenerated>
//   This file was generated by dddappp code generator.
//   Any changes made to this file manually will be lost next time the file is regenerated.
// </autogenerated>

package org.dddml.ffvtraceability.domain.uom.hibernate;

import java.util.*;
import java.time.OffsetDateTime;
import org.dddml.ffvtraceability.domain.*;
import jakarta.persistence.EntityManager;
import jakarta.persistence.PersistenceContext;
import org.springframework.stereotype.Repository;
import org.dddml.ffvtraceability.domain.uom.*;
import org.dddml.ffvtraceability.specialization.*;
import org.dddml.ffvtraceability.specialization.hibernate.*;
import org.springframework.transaction.annotation.Transactional;

@Repository("uomStateRepository")
public class HibernateUomStateRepository implements UomStateRepository {
    @PersistenceContext
    private EntityManager entityManager;

    protected EntityManager getEntityManager() {
        return this.entityManager;
    }

    private static final Set<String> readOnlyPropertyPascalCaseNames = new HashSet<String>(Arrays.asList("UomId", "UomTypeId", "Abbreviation", "NumericCode", "Gs1AI", "Description", "Active", "Version", "CreatedBy", "CreatedAt", "UpdatedBy", "UpdatedAt"));
    
    private ReadOnlyProxyGenerator readOnlyProxyGenerator;
    
    public ReadOnlyProxyGenerator getReadOnlyProxyGenerator() {
        return readOnlyProxyGenerator;
    }

    public void setReadOnlyProxyGenerator(ReadOnlyProxyGenerator readOnlyProxyGenerator) {
        this.readOnlyProxyGenerator = readOnlyProxyGenerator;
    }

    @Transactional(readOnly = true)
    public UomState get(String id, boolean nullAllowed) {
        UomState.SqlUomState state = (UomState.SqlUomState)getEntityManager().find(AbstractUomState.SimpleUomState.class, id);
        if (!nullAllowed && state == null) {
            state = new AbstractUomState.SimpleUomState();
            state.setUomId(id);
        }
        if (getReadOnlyProxyGenerator() != null && state != null) {
            return (UomState) getReadOnlyProxyGenerator().createProxy(state, new Class[]{UomState.SqlUomState.class}, "getStateReadOnly", readOnlyPropertyPascalCaseNames);
        }
        return state;
    }

    public void save(UomState state) {
        UomState s = state;
        if (getReadOnlyProxyGenerator() != null) {
            s = (UomState) getReadOnlyProxyGenerator().getTarget(state);
        }
        if (s.getVersion() == null) {
            entityManager.persist(s);
        } else {
            entityManager.merge(s);
        }

        if (s instanceof Saveable) {
            Saveable saveable = (Saveable) s;
            saveable.save();
        }
        entityManager.flush();
    }

    public void merge(UomState detached) {
        UomState persistent = getEntityManager().find(AbstractUomState.SimpleUomState.class, detached.getUomId());
        if (persistent != null) {
            merge(persistent, detached);
            entityManager.merge(persistent);
        } else {
            entityManager.persist(detached);
        }
        entityManager.flush();
    }

    private void merge(UomState persistent, UomState detached) {
        ((AbstractUomState) persistent).merge(detached);
    }

}

