// <autogenerated>
//   This file was generated by dddappp code generator.
//   Any changes made to this file manually will be lost next time the file is regenerated.
// </autogenerated>

package org.dddml.ffvtraceability.domain.ordertype.hibernate;

import java.util.*;
import java.time.OffsetDateTime;
import org.dddml.ffvtraceability.domain.*;
import jakarta.persistence.EntityManager;
import jakarta.persistence.PersistenceContext;
import org.springframework.stereotype.Repository;
import org.dddml.ffvtraceability.domain.ordertype.*;
import org.dddml.ffvtraceability.specialization.*;
import org.dddml.ffvtraceability.specialization.hibernate.*;
import org.springframework.transaction.annotation.Transactional;

@Repository("orderTypeStateRepository")
public class HibernateOrderTypeStateRepository implements OrderTypeStateRepository {
    @PersistenceContext
    private EntityManager entityManager;

    protected EntityManager getEntityManager() {
        return this.entityManager;
    }

    private static final Set<String> readOnlyPropertyPascalCaseNames = new HashSet<String>(Arrays.asList("OrderTypeId", "ParentTypeId", "HasTable", "Description", "Version", "CreatedBy", "CreatedAt", "UpdatedBy", "UpdatedAt"));
    
    private ReadOnlyProxyGenerator readOnlyProxyGenerator;
    
    public ReadOnlyProxyGenerator getReadOnlyProxyGenerator() {
        return readOnlyProxyGenerator;
    }

    public void setReadOnlyProxyGenerator(ReadOnlyProxyGenerator readOnlyProxyGenerator) {
        this.readOnlyProxyGenerator = readOnlyProxyGenerator;
    }

    @Transactional(readOnly = true)
    public OrderTypeState get(String id, boolean nullAllowed) {
        OrderTypeState.SqlOrderTypeState state = (OrderTypeState.SqlOrderTypeState)getEntityManager().find(AbstractOrderTypeState.SimpleOrderTypeState.class, id);
        if (!nullAllowed && state == null) {
            state = new AbstractOrderTypeState.SimpleOrderTypeState();
            state.setOrderTypeId(id);
        }
        return state;
    }

    @Transactional
    public void save(OrderTypeState state) {
        OrderTypeState s = state;
        if (s.getVersion() == null) {
            entityManager.persist(s);
        } else {
            entityManager.merge(s);
        }

        if (s instanceof Saveable) {
            Saveable saveable = (Saveable) s;
            saveable.save();
        }
        entityManager.flush();
    }

    public void merge(OrderTypeState detached) {
        OrderTypeState persistent = getEntityManager().find(AbstractOrderTypeState.SimpleOrderTypeState.class, detached.getOrderTypeId());
        if (persistent != null) {
            merge(persistent, detached);
            entityManager.merge(persistent);
        } else {
            entityManager.persist(detached);
        }
        entityManager.flush();
    }

    private void merge(OrderTypeState persistent, OrderTypeState detached) {
        ((AbstractOrderTypeState) persistent).merge(detached);
    }

}

